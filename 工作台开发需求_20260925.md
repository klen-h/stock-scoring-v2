# 一天流程工作台（Workbench）开发需求文档 v1.0

> 2026-09-25 · 交由外部开发实施 · 本文自包含，开发前请通读全文，特别是「铁律」一节

---

## 0. 一句话目标

把系统**已经产出**的「一天时间线」（盘前简报 → 盘中警示/教练卡 → 午间雷达 → 盘后评分/观察池/持仓 → 日报）从散落的 21 个页面，重排成一个**按时间轴组织的桌面优先单页工作台**，回答一个问题：**"现在这个时刻，我该看什么、该做什么"**。

**工作台是巡检/总览，不替代深挖页**（评分榜、个股详情、回测保持独立入口）。

---

## 1. 已拍板的设计决策（不要重新讨论）

| # | 决策 | 说明 |
|---|---|---|
| 1 | **新页面 `/workbench`，设为默认首页** | 路由 `/` 改指向工作台；旧 Dashboard 保留（入口改名"数据中心"） |
| 2 | **桌面优先** | ≥1280px 为主场景；<1024px 退化为纵向折叠时间轴（同一套数据，只换布局） |
| 3 | **横向时间轴 + 点击切换** | 时间轴只切换**主工作区**；右栏常驻不变 |
| 4 | **右栏常驻（最重要）** | 警报流/待办/持仓摘要**全天不变、不被时间轴切走**——已实证教练卡盘中 14:30/13:53/11:10 都会触发 |
| 5 | **回放先做最近 30 天** | 数据层不写死天数；主体用 `ranking_history`（Top50 快照），**不做**精算榜全量回放 |
| 6 | **范围只含当日决策流** | 回测、因子体检、快讯全流、龙虎榜/资金流详情**不进**工作台 |
| 7 | **Phase 1 只读优先** | 教练卡"执行/放弃"回写按钮属 Phase 2（接口已存在，见 §6.3） |

---

## 2. 阶段划分与时间口径（⚠️ 别造第二套定义）

**UI 展示 5 段，后端 phase 只有 3 段**，映射关系如下（后端唯一事实源：
`backend/app/trader_brief.py::current_phase()`，分界 **09:30 / 15:00**）：

| UI 时段 | 时间 | 后端 phase | 核心问题 |
|---|---|---|---|
| ① 盘前 | 09:10–09:30 | premarket | 今天做什么 |
| ② 盘中 | 09:30–11:30 | intraday | 现在执行什么（**执行模式**） |
| ③ 午盘 | 11:30–15:00 | intraday | 同上（瘦身后） |
| ④ 盘后 | 15:00–19:30 | postmarket | 今天结果如何 |
| ⑤ 复盘 | 19:30 之后 | postmarket | 学到什么 |

- **自动跟随**：当前时刻自动选中对应时段，但必须**温和**——阶段切换时若用户正停留在其他时段，**不硬切**，只在时间轴节点上显示提示（如"●已进入盘中"），点击才切换。
- **时间轴节点等宽**，不按真实时间比例（盘前只有 20 分钟、盘中 5.5 小时，按比例会挤成一团）；节点下标注真实时刻。
- 注意：教练确认窗口 09:35、体检卡 10:30/14:45 是**业务时点**，不是阶段边界，别拿来做时间轴分界。

---

## 3. 页面布局

```
┌──────────────────────────────────────────────────────────────┐
│ 顶栏（常驻）：日期: [今天▾] · 市况/温度/指数 · 数据新鲜度🟢     │
├──────────────────────────────────────────────────────────────┤
│ 时间轴（横向导航+徽标）：                                      │
│ ①盘前 ── ②盘中 ── ③午盘 ── ④盘后 ── ⑤复盘                    │
│ 09:10     09:30     11:30     15:00     19:30                 │
│  ⚠️降级    🔴3待办    —         —         —                    │
├───────────────────────────────────────────┬──────────────────┤
│ 主工作区（随阶段切换，懒加载）              │ 常驻右栏（~300px）│
│                                           │ · 待办（教练卡    │
│  各阶段内容见 §4                           │   未决策，含      │
│                                           │   执行/放弃按钮） │
│                                           │ · 持仓摘要        │
│                                           │  （损益/距止损）  │
│                                           │ · 今日雷达摘要    │
│                                           │  （最近 N 条）    │
├───────────────────────────────────────────┴──────────────────┤
│ ▸ 系统状态（数据新鲜度/内存/库体积/任务失败）——默认折叠         │
└──────────────────────────────────────────────────────────────┘
```

**徽标规则**（时间轴节点右上角小圆点/数字）：

| 条件 | 样式 | 数据来源 |
|---|---|---|
| 该时段有 **≥1 条未决策**教练卡 | 红点 + 数字 | `GET /api/coach/alerts` 过滤 |
| 盘前简报 **LLM 降级**（degraded 非空） | 黄点 ⚠️ | `GET /api/system/trader-brief` 的 `degraded` |
| 该时段有 **未读**推送（可选 Phase 2） | 灰点 | 本地 localStorage 已读标记 |
| 无 | 无 | — |

---

## 4. 各阶段主工作区内容

### ① 盘前（09:10–09:30）—— 阅读模式
| 模块 | 数据来源 | 说明 |
|---|---|---|
| 决策简报全文 | `GET /api/system/trader-brief?phase=premarket` | 该关注/该做/该防 + 两融情绪段 |
| 今日待确认计划 | `GET /api/coach/plans` | 开盘确认卡（Phase 2 加确认按钮） |

### ② 盘中（09:30–11:30）—— **执行模式**（大字、少文字、少点击）
| 模块 | 数据来源 | 说明 |
|---|---|---|
| 盘中警示条 | `GET /api/market/overview` | 指数急跌/涨跌比/跌停，秒级滞后 |
| 持仓实时状态 | `GET /api/score/batch/portfolio-radar` | 每票一行：现价/盈亏/距止损 |
| 教练卡（右栏为主，盘中可镜像） | `GET /api/coach/alerts` | 见 §6.3 |

### ③ 午盘（11:30–15:00）—— 瘦身版盘中
同②但默认折叠持仓明细，只保留警示条 + 摘要行 + 右栏。

### ④ 盘后（15:00–19:30）—— 阅读模式
| 模块 | 数据来源 | 说明 |
|---|---|---|
| 评分榜 Top10（摘要） | `GET /api/score/batch/top`（或读日批快照） | 只列 Top10 + "查看完整榜 →" 跳 `/score` |
| 观察池摘要 | `GET /api/score/batch/gate-watch` | 候选数/三绿数/双信号交叉 |
| 今日雷达时间线 | `GET /api/flash/push-log` | 系统今天说过的所有话 |

### ⑤ 复盘（19:30 后）—— 阅读模式
| 模块 | 数据来源 | 说明 |
|---|---|---|
| 日报 | 日报接口（见 `frontend/src/views/DailyReport.vue` 用法） | 全文或摘要 |
| 交易员简报（盘后） | `GET /api/system/trader-brief?phase=postmarket` | |
| 执行一致性 | `GET /api/coach/consistency` | 今日执行率一行 |

**通用规则**：
- 每个模块 = **摘要 + 一键跳转**到现有深挖页；除简报/日报全文外**不放大表格**
- **懒加载**：切到该时段才拉数据（严禁像旧首页一次拉十几个接口）
- 未到时段的节点**置灰**；历史时段**可点开回看**

---

## 5. 常驻右栏（全天不变）

| 模块 | 内容 | 数据来源 | 刷新 |
|---|---|---|---|
| **待办**（教练卡，只显示**未决策**的） | 规则名/标的/摘要 + [执行][放弃+理由] 按钮（Phase 2） | `GET /api/coach/alerts`（返回体已含执行状态，前端过滤 `executed` 为空的） | 60s 轮询 |
| **持仓摘要** | 总盈亏 / 最差一只 / 距止损最近的 | `GET /api/score/batch/portfolio-radar` | 60s |
| **今日雷达摘要** | 最近 5 条系统推送（标题+时间） | `GET /api/flash/push-log` | 120s |
| 小指标（可选） | 近 9 日教练卡数量趋势（数据实证在下降） | `GET /api/coach/alerts` 聚合 | 进页一次 |

**右栏铁则**：
- **回放历史日期时，右栏切换为该日的警报历史**（否则"实时流 vs 历史日"混淆，界面顶部要显眼标注"回放模式·只读"）
- 轮询失败**静默保留上次数据**（不清空、不弹错，只在角落显示"连接中断"小字）
- 教练卡按钮**防误触**：执行/放弃均需二次确认（这是纪律回写数据，错了会污染一致性统计）

---

## 6. 接口清单（⚠️ 全部已存在，Phase 1 零后端改动）

### 6.1 现成接口（已核实，直接用）
| 用途 | 接口 | 备注 |
|---|---|---|
| 决策简报 | `GET /api/system/trader-brief?phase=&refresh=` | 幂等，默认读当日已生成 |
| 教练警报（含执行状态/T+5 结果） | `GET /api/coach/alerts?limit=` | |
| 执行回写 | `POST /api/coach/alerts/{id}/execute` | body `{executed:'yes'|'no', reason?}` |
| 放弃剧本 | `POST /api/coach/plans/{id}/abandon` | body `{reason}` 理由必填 |
| 一致性 | `GET /api/coach/consistency?days=` | |
| 预承诺执行率 | `GET /api/coach/plans/execution-rate` | |
| 持仓雷达 | `GET /api/score/batch/portfolio-radar` | |
| 观察池 | `GET /api/score/batch/gate-watch` | |
| 评分榜 | `GET /api/score/batch/top` | |
| 市场总览（警示条） | `GET /api/market/overview` | |
| 推送日志（今日雷达） | `GET /api/flash/push-log` | |
| 数据新鲜度 | `GET /api/system/status` | |
| 进程内存 | `GET /api/system/memory?types=0` | 底部折叠区 |
| 库体积 | `GET /api/system/db-usage` | 底部折叠区 |

前端 HTTP 封装：`frontend/src/api/index.js`（axios 实例，token 自动注入 `localStorage['auth_token']`）。
**新接口统一加在这个文件里**，命名风格参照既有 `getSystemMemory`。

### 6.2 允许新增的只读接口（仅当 Phase 3 回放需要）
若现有接口不支持"按指定历史日期取数"，允许在 `backend/app/routers/system.py`（或对应 router）新增只读端点，例如：
- `GET /api/workbench/day-index` —— 返回最近 N 天各数据源的有无（哪些天可回放）
- 按日期取 Top50 快照 / 简报 / 日报 / 教练历史
**必须只读、带鉴权（`Depends(get_current_user)`）、失败返回空结构不抛错。**

### 6.3 教练卡数据结构要点
- 教练卡分**两类**（前端区分展示）：**实时类**（09:15 集合竞价起 30s 轮询命中，随时会来）与**定时体检卡**（10:30 / 14:45）
- 推送去重：一天一条规则一只票只推一次（`dedupe_key`=日|规则|标的）
- ⚠️ 教练卡是**纪律提醒**，不是自动交易指令——UI 文案沿用 `coach/monitor.py::POSITION_NOTE` 的表述，不要写"系统已自动卖出"之类的话

---

## 7. 回放（Phase 3）

- 顶栏日期选择器：选历史日期 ⇒ 全页（含右栏）切为**该日只读快照**，顶部横幅"⏪ 回放 2026-09-15 · 只读"
- 数据主体：Top50 快照（`ranking_history`，现有 26 天且持续增长）+ 简报（`trader_briefs`）+ 日报 + 教练历史
- **精算榜（ranking_live）不做历史回放**（14 天滚动保留，属研究场景）
- 数据层**不写死天数**——各数据源可回放深度不一致，日期索引接口（§6.2）负责告知"这天有什么"
- 今天 = 实时模式（可交互）；历史 = 只读

---

## 8. ⚠️ 项目铁律（开发前必读，违反任何一条都会被打回）

| # | 铁律 | 原因 |
|---|---|---|
| 1 | **禁止改动任何评分/信号逻辑** | 项目处于评分权重**冻结期**；工作台只做"编排"，不碰引擎 |
| 2 | **后端 Phase 1 零改动** | 所有接口已存在；新增接口仅限 §6.2 且必须只读+鉴权 |
| 3 | **生产 Python 3.9** | 禁用 PEP 604（`str | None`），用 `Optional[X]` 或 `from __future__ import annotations` |
| 4 | **FastAPI 路由**：没有 `await` 的必须写 `def`，禁止假 async | 阻塞事件循环是全站事故 |
| 5 | **SQL 占位符 `%s`**，双库兼容（PG/SQLite） | `db.fetch/execute` 会自动转换 |
| 6 | **后端日志/错误信息 ASCII**（本地 Windows 是 GBK 控制台，emoji/⇒ 会抛 UnicodeEncodeError） | 已三次踩坑 |
| 7 | **egress 纪律**：不新增整表读、不循环拉远程库 | Supabase 免费档流量有限（详见根目录 `EGRESS.md`） |
| 8 | **前端构建必须通过**：`cd frontend && npm run build` | 验收第一步 |
| 9 | 注释风格：中文、写明"为什么"、带日期（如 `★ 2026-09-25：...`） | 项目惯例 |
| 10 | 组件样式沿用既有 Tailwind 语义类：`bg-card border-border text-muted text-accent`（参见 `ScoreRank.vue`/`Dashboard.vue`） | 视觉一致 |
| 11 | **懒加载 + 失败静默降级**：任何模块加载失败只显示该模块占位"—"，不阻塞整页 | 单用户系统，别用全局报错弹窗 |
| 12 | 提交信息格式：`feat(工作台): xxx` / `fix(工作台): xxx`，正文写清"为什么" | 项目惯例 |

---

## 9. 交付计划（三阶段，每阶段独立可验收）

### Phase 1：骨架 + 右栏（最重要、成本最低）
- 路由 `/workbench` 设为默认首页；顶栏 + 横向时间轴（5 节点、等宽、徽标）
- **右栏常驻**：待办（未决策教练卡，只读展示）+ 持仓摘要 + 今日雷达摘要（60s/120s 轮询）
- 盘前 + 盘后两个阶段视图（其余时段占位"即将上线"）
- 底部折叠系统状态
- ✅ 验收：登录后默认落在工作台；切阶段懒加载；右栏在所有时段可见且不被切走；刷新后停留在当前阶段

### Phase 2：执行模式 + 交互闭环
- 盘中/午盘执行模式（大字持仓行 + 警示条）
- 教练卡 [执行][放弃+理由] 按钮（接 §6.1 两个 POST，带二次确认）
- 温和自动跟随（阶段切换只提示不硬切）
- ✅ 验收：执行/放弃回写后待办列表即时更新；`/coach` 页数据同步

### Phase 3：回放
- 日期选择器 + day-index 只读接口 + 各数据源按日期取数 + 回放模式横幅与右栏切换
- ✅ 验收：选 3 天前日期能看到当日 Top50/简报/日报/教练历史；"今天"恢复交互模式

---

## 10. 明确不做（防蔓延）

- 回测 / 因子体检 / 权重优化 —— 研究工具，保持独立页
- 快讯全流 —— 噪音；只保留"今日雷达摘要"（右栏 5 条）
- 精算榜全量历史回放 —— ranking_live 只有 14 天滚动窗口
- 龙虎榜 / 资金流详情 —— 降级为详情页跳转
- 任何评分参数、推送策略、数据采集的改动 —— 冻结期

---

## 12. 本地开发环境与流量纪律（⚠️ 开发前必读）

**为什么单列一节**：项目数据库是 Supabase **免费档**，历史上曾因本地开发导致**单日近 600MB
流量**。而工作台自带**轮询**（右栏 60s、雷达 120s），本地环境配错会把"看几眼界面"变成持续消耗。

### 12.1 起后端的两种模式（先选一种）

| 模式 | 做法 | 流量 | 适用 |
|---|---|---|---|
| **A. 本地前端 → 线上后端**（UI 开发推荐） | 改 `frontend/vite.config.js` 里 `server.proxy['/api'].target` 为线上后端域名；**不起本地后端** | 本机 0；调试流量走线上实例（它本就在跑） | 工作台 90% 是 UI 编排 ⇒ **首选** |
| **B. 本地前端 → 本地后端** | `python run.py`（proxy 默认就指向 `localhost:8000`） | **取决于 12.2** | 需要改后端 / 断点调试时 |

### 12.2 模式 B 下必须做对，否则流量飙升

1. **先同步数据包**：`python scripts/sync_local.py`
   - 作用：下载 `backend/data/pack/backend-pack.db`，配合 `DATA_SOURCE=local` 让
     K线/指标/`backtest_prices` 读本地 SQLite
   - 加 `--force` = 强制重下（44.9MB 走 **GitHub Pages**，**不消耗 Supabase 流量**）；
     不加则本地包 mtime < 20 小时会跳过
   - ⚠️ **包陈旧 = 灾难**：`pack_source` 判陈旧后会**静默回退查 Supabase**（实测单日近 600MB）。
     启动日志出现包过期 `[WARN]` 就立刻重跑。周一 / 长假后第一个交易日最容易踩。
2. **业务表也应当地化**：`backend/.env` 里把 `DATABASE_URL` **注释掉/留空** ⇒ 业务小表
   （模拟盘 / 自选 / 教练卡 / 持仓）走本地 SQLite，与线上数据分叉、**零 Supabase 流量**
   - ⚠️ 代价：本地没有真实持仓/教练卡 ⇒ 需要**造几条测试数据**才能看到右栏效果
   - ⚠️ 反之，若 `DATABASE_URL` 仍指向线上，**工作台轮询的每一轮都会打线上库**
     （教练卡/持仓/榜单/简报全在业务表里）
3. **开发期放宽轮询**：右栏设计 60s 轮询，开发时可改长（如 300s）或做成可配，避免挂一整天。
4. **别把 `run.py` 挂在后台过夜**：本机 `--reload` 每次改文件即重启，重启即丢进程内缓存
   ⇒ 缓存外的读全部回源。

### 12.3 验收时的流量自检

开发周期结束时，看 Supabase Dashboard 的用量曲线有无异常尖峰 ——
**纯 UI 开发应接近 0 增量**。若出现明显增长，检查 12.2 的两项是否做对。

---

## 13. 参考材料（项目根目录）

- `顶层框架与数据流_20260920.md` —— 系统架构
- `交易日全流程.md` —— 一天各时点的业务时序
- `交易员教练_模拟盘接入说明_20260915.md` —— 教练/执行回写细节
- `EGRESS.md` —— 流量纪律
- `frontend/src/views/ScoreRank.vue` / `Dashboard.vue` —— 组件与样式范本
