# 回测系统详解（撮合语义、退出策略、口径统一与脚本目录）

> 2026-09-20 ｜ 基于 main@82cdc48 + feat/flow5-extreme-inflow 增量（差异处标注）
> 核心代码：`backend/app/backtest/engine.py`（撮合）、`backend/app/backtest/strategies.py`
> （退出策略/信号流/批量价格）、`backend/app/strategies/paper_trading.py`（模拟盘）
> 这份文档回答：回测数字是怎么算出来的、哪些口径必须与生产一致、每个回测脚本干什么。

---

## 0. 回测的三种身份

1. **策略研发**：改规则前先跑离线回测背书（评分曲线/战法准入/退出策略……全部
   如此，判定标准预先写死）；
2. **生产重放**：白名单重算（`_recompute_whitelist`）本质是对 `strategy_results`
   历史信号的回测撮合——白名单是否放行由它决定；
3. **绩效归因**：周报的战法×regime 切片、LLM 信号对账、模拟盘 vs 回测胜率对比。

**口径统一是回测系统的命门**：同一信号在"回测撮合、模拟盘执行、白名单重放"三处
必须算出同样的结果，否则白名单放行的是回测里赚钱、实盘里亏钱的信号。历史上一共
出现过 6 处口径分歧（见 §4），已全部修复或标记。

---

## 1. 撮合引擎语义（`backtest/engine.py: match_signals`）

逐条规则（每条都有代码出处，改前先读）：

| # | 规则 | 语义 |
|---|---|---|
| 1 | T+1 开盘成交 | 信号日 T 盘后产生 → T+1 开盘价 entry_price；当日不检查退出 |
| 2 | 涨停一字买不进 | T+1 开盘=涨停价且全天未开板（low 未低于涨停价）→ **剔除该信号**（记入 skipped_out）；开板则视为可成交 |
| 3 | 涨跌停幅度 | 主板 10%、创业板/科创板 20%、ST 5%、ETF 0（`_limit_pct`，按代码/名称判别） |
| 4 | T+1 制度 | 最早 entry_i+1 检查退出；无可卖日 → 整信号跳过 |
| 5 | 同日止损优先 | 同一天 low≤止损 且 high≥止盈 → **先算止损**（保守） |
| 6 | 跳空按 min(open, stop) | 低开破止损按开盘价成交（更差价，符合实盘） |
| 7 | 止盈按目标价 | high≥target → 按 target 成交（v2 下 target=None 不止盈） |
| 8 | 跌停一字卖不出 | 触发退出日封死跌停 → 顺延到下一可卖日开盘价（可能更差） |
| 9 | 持有到期 | 未触发止损/止盈 → 第 hold 天收盘价离场 |
| 10 | 成本 | 双边：佣金 0.05% + 滑点 0.05% + 印花税 0.1%（卖出）；ETF 免印花 |
| 11 | hold_days | exit_i − entry_i + 1（**K 线根数=交易日**，含成交日） |
| 12 | 输出 | 逐笔 pnl_pct（含成本）、每日收益路径 daily（供净值曲线） |

**成本常量**（`DEFAULT_COSTS`）：
```python
"etf":   {"commission": 0.00025, "slippage": 0.0005, "stamp": 0.0}
"stock": {"commission": 0.0005,  "slippage": 0.0005, "stamp": 0.001}
```

---

## 2. 退出策略 v1 / v2（`backtest/strategies.py`）

| | v1（旧） | **v2（现行，网格扫描定版）** |
|---|---|---|
| 持有期 | 10~20 日（各战法不同） | **3 个交易日** |
| 止损 | 信号自带（默认 −5%） | **介入价 × (1−0.07)**，基准=信号日收盘 |
| 止盈 | 目标价止盈 | **无**（让利润奔跑，到期/止损离场） |
| 切换 | `WARFARE_EXIT_POLICY=v1` | 默认 v2 |

**常量单源**（`backtest/strategies.py:35-36`）：
```python
WARFARE_HOLD_DAYS_V2 = 3
WARFARE_STOP_PCT_V2 = 0.07
```
★ 2026-09-13 审查修复：模拟盘 `_max_hold()`/止损比例、前端战法回测接口、
regime 切片回测全部改为引用这两个常量（此前写死 3/0.93，改了回测模拟盘不自知）。

**apply_exit_policy(signal, base_close)** 是唯一的策略改写入口：回测引擎吃的是
改写后的 signal（hold_days/stop_loss/take_profit），任何新增消费方必须走它。

---

## 3. 回测出口矩阵（谁在什么场景回测什么）

| 出口 | 场景 | 口径 |
|---|---|---|
| 白名单重算 `recommendation._recompute_whitelist` | 每次 get_push_whitelist（落库缓存 6h） | 主力闸门（跟 STRATEGY_MAINFORCE_GATE）+ apply_exit_policy + T+1 撮合；全期+近250日双轨 |
| 模拟盘 `paper_trading` | 实时 | 入场层完整准入 + v2 参数（引用同常量）+ 3 交易日到期 |
| 周报 warfare 回测 `backtest/run.py` | 周五日批 | `_run_warfare`（start 下推 + exit v2） |
| regime 切片 `backtest_warfare_by_regime` | 周报切片/准入矩阵 | 闸门 + exit v2（09-13 对齐） |
| LLM 信号对账 `backtest_llm_signals` | 周报 | tracking_state 已平仓记录（非撮合） |
| 前端战法回测接口 `strategies/backtest.py` | 页面 | **09-13 已接 v2**（此前 v1 口径与白名单打架） |
| 离线脚本（见 §5） | 研发 | 各自文档注明口径 |

---

## 4. 口径统一矩阵（历史分歧清单——防再犯）

| 分歧 | 发现时间 | 处置 |
|---|---|---|
| 模拟盘持有期用自然日（周五成交周一即平） | 09-13 审查 P0-2 | ✅ 改交易日口径 + 未满持有期不平仓 |
| 模拟盘盘后兜底把未到期仓位提前平 | 09-13 审查 P0-2 | ✅ 修复（expire 只在到期日） |
| 模拟盘常量写死 3/0.93 不引用回测常量 | 09-13 审查 P1-8 | ✅ 引用 WARFARE_* 常量 |
| 前端战法回测接口用 v1 口径 | 09-13 审查 P1-8 | ✅ 接 v2 |
| regime 切片回测不套 exit v2/闸门 | 09-13 审查 P1-8 | ✅ 对齐 |
| 白名单重放闸门挂在 exit_policy 上 | 09-13 审查 P1-10 | ✅ 解耦（跟 STRATEGY_MAINFORCE_GATE） |
| 白名单重放 `_load_prices_map` 拉全历史 | 09-13 egress | ✅ start 下推 |
| **低波回测脚本**（strategy_lowvol_admission_test.py） | — | 已与生产同口径（gate 跟运行时开关） |

**新增消费方检查单**（每次写新回测/重放代码过一遍）：
- [ ] T+1 开盘成交？涨停一字剔除？
- [ ] 退出策略走 `apply_exit_policy`？
- [ ] 持有期是交易日根数？
- [ ] 价格加载带 start 下推？
- [ ] 主力闸门跟运行时开关？
- [ ] 成本常量用 DEFAULT_COSTS？

---

## 5. 模拟盘 vs 回测（胜率对比才可信的前提）

**一致点**（09-13 审查后）：T+1 开盘成交、涨停一字剔除、止损优先、v2 参数同源
（3 交易日/−7%）、成本同口径、到期=第 3 个交易日收盘价（盘后兜底按 backtest_prices
日线）。

**已知差异**（设计内，对比时要注明）：
- 模拟盘止损在**盘中实时价**触发（回测按日线 low）；跳空开盘价撮合与回测一致；
- 模拟盘 exit_date 记账日=实际平仓日（比回测口径晚一天，价格对齐）；
- 模拟盘有 G1-G4 风控（同行业≤2/回撤熔断/连亏冷却/日亏停开）与 MIN_POSITION_VALUE
  下限——回测无这些（对比时用"胜率/均收益"不用"总收益"）；
- 模拟盘的池=白名单战法当日信号，回测池=全历史信号。

**对照节奏**：PLAN §2.6——模拟盘跑 1-2 周后做第一次"实测 vs 回测"胜率对比
（09-13 口径统一后数据才可比）。

---

## 6. 离线回测脚本目录（全部本地数据，零 Supabase 交互）

| 脚本 | 问题 | 判定/结论 |
|---|---|---|
| `flow5_curve_backtest.py` | 主力净流入评分曲线单调 vs 倒U | 倒U 胜出 → 已上线 |
| `flow5_pos_backtest.py`（分支） | 倒U要不要加价格位置维度 | **不支持**（低位极端流入仍负超额） |
| `subfactor_ic_backtest.py`（分支） | 技术面/资金面逐子项预测力 | 技术面 5-10 日整体负 IC（反转效应），换手率噪声 |
| `accum_pullback_backtest.py` | 吸筹标签/筹码组合做低吸准入 | 形态标签否决；筹码组合 10 样本待复验 |
| `strategy_lowvol_admission_test.py` | 阴跌市准入矩阵 | 阴跌全禁（与生产同口径） |
| `mainline_crowding_backtest.py` | 拥挤主线否决 | T+5 胜率差 7.6pt → 只减不加 |
| `strategy_mainforce_filter_test.py` | 主力闸门（高位/拉升段） | 已上线 STRATEGY_MAINFORCE_GATE |
| `strategy_exit_sweep.py` | 退出参数网格（h3/h5/h10×止损×止盈） | v2 定版依据 |
| `mainforce_factor_backtest.py` | 主力因子 IC（筹码/资金流） | build_samples 是多个回测的数据基建 |
| `whitelist_review` / `egress_probe` | 白名单判据 / 出口流量归因 | 运维工具 |

数据基建：`research_cache.py`（包优先零回源，日线 49.8 万行 + 资金流 8.4 万行
落本机 sqlite，24h 复用）→ 所有离线回测零 Supabase 交互。

---

## 7. 回测纪律（每条都是踩坑换来的）

1. **判定标准预先写死**：回测前把"支持/不支持"的数字标准写进脚本和文档
   （如 `flow5_pos` 的 ">+0.5% 且 n≥100"），跑完照标准裁决——防事后挑格子。
2. **负结果也要归档**：吸筹形态标签、位置维度都被否决过，这些"不做什么"的
   决定和"做什么"一样值钱。
3. **口径对齐后再比数字**：任何"回测说 X 好"的结论，先过 §4 检查单确认口径。
4. **回测 → 模拟盘 → 生产**三级流水：回测支持的改动先进模拟盘观察，再推生产
   （评分权重二次校准、反转子项都卡在模拟盘验证一步）。
5. **报告归档**：所有回测报告落 `backend/backtest_reports/`（git 跟踪），
   文件名带时间戳，结论回写 PLAN/文档。
