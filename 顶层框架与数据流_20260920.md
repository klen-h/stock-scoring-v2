# 顶层框架与数据流（写给要掌控项目的你）

> 2026-09-20 ｜ 基于 main@4ea36fc（feat/flow5-extreme-inflow 已合并，含 P1 标签 / 因子体检周期化 / shadow 日期修复）
> 配套文档：《评分系统详解》《回测系统详解》（同目录）
> 这份文档回答：系统由哪几块组成、谁在什么时间做什么、数据怎么流、规则改了会传导到哪。

---

## 0. 一句话 + 全景图

**系统做一件事**：每个交易日自动完成"数据采集 → 个股评分 → 战法扫描 → 模拟盘验证 →
白名单筛选 → 企微推送 → 复盘归因"，人只做最后的执行决策。

```
数据源（腾讯行情/东财/金十/龙虎榜/zzshare财报）
        │
        ▼
┌─────────────────────────────────────────────────────┐
│ 数据层：Supabase(主库) + 数据包(backend-pack.db)      │
│   46 张表：行情/评分/信号/模拟盘/报告/状态             │
└─────────────────────────────────────────────────────┘
        │                          │
        ▼                          ▼
┌──────────────────┐      ┌──────────────────────┐
│ Render 常驻服务   │      │ GitHub Actions 日批   │
│ (只读+轻循环)     │      │ (重任务, 19:30+)      │
│ · API 服务        │      │ · 数据回填/快照        │
│ · 盘中盯盘循环    │      │ · 战法扫描/评分快照    │
│ · LLM 叙事        │      │ · 日报/周报/简报       │
└──────────────────┘      └──────────────────────┘
        │                          │
        ▼                          ▼
┌─────────────────────────────────────────────────────┐
│ 决策输出：企微分类推送(风险/教练/简报/告警四群)          │
│           + 前端(Pages) 评分/榜单/模拟盘/日报阅读       │
└─────────────────────────────────────────────────────┘
```

**两个关键认知**（理解一切时序的前提）：
1. **Render 只跑轻循环**：`RENDER_READ_ONLY=1 + ENABLE_HEAVY_JOBS=0` 时，
   scheduler 里所有被 `_heavy()` 包裹的重循环（战法扫描/回填/资金流/快照/日报…）
   一启动就被跳过（`scheduler.py:1644 _heavy`）。Render 只保留 14 个轻循环：
   快讯、持仓跟踪、健康、持仓消息、regime、行情缓存、开盘确认、模拟盘成交/跟踪、
   教练、复盘叙事、午间雷达、日报 loop（兜底）、盘前简报。
2. **重任务的唯一执行方是 Actions 日批**：cron-job.org 北京 18:00 触发
   `kline-data.yml`（K线包）→ 19:00 触发 `backend-pack.yml`（K线+指标+主力包）
   → 包完成后 `workflow_run` 接棒 `daily-batch.yml`（约 19:30）跑 21 个任务
   （顺序见 §3）。

---

## 1. 全天时序表（工作日，北京时间）

| 时间 | 执行方 | 做什么 | 产出/写哪张表 |
|---|---|---|---|
| 03:20-04:20 | Render* | 财务数据同步 | stock_finance_zz |
| 04:30-23:59 | Render* | zzshare 财报同步 | stock_finance |
| 07:00-08:00 | Render* | 财经日历刷新 | flash_calendar |
| 08:55-13:00 | Render* | 宏观面板（us10y/油价/黄金） | macro 面板缓存 |
| 09:10-11:30 | Render | **盘前决策简报**（LLM, tier=fast + 影子对比） | trader_briefs |
| 09:30/15:00 | — | 开盘/收盘 | |
| 09:35-11:20 | Render | **开盘确认**（白名单战法买点确认推送） | — |
| 09:35 起 | Render | **模拟盘成交**（pending→holding，写 coach_plans 剧本） | paper_positions |
| 盘中 30s | Render | **教练规则求值**（9 条规则，止损/持有3日推企微风险群） | coach_alerts |
| 盘中 5min | Render | 持仓跟踪（止损/止盈实时价） | paper_positions |
| 盘中 120s | Render | 全市场行情缓存刷新（腾讯） | 内存 _cache |
| 11:35-13:00 | Render | 午间雷达（LLM） | — |
| 15:05-15:35 | Render* | 收盘行情快照（写库） | market_snapshot |
| 15:10-16:40 | Render* | 板块快照 | sector_daily |
| 15:35-23:59 | Render* | 矛盾扫描（指数/宽度背离） | contradictions |
| 15:40 起 | Render | **regime 重算**（幂等）+ **coach 收盘回写**（heavy 规则全量） | market_regime_history / coach_alerts |
| 16:10-23:59 | Render* | 回测价格回填（150只/日配额） | backtest_prices |
| 17:00-23:59 | Render* | 主力资金流回填 | mainflow_history |
| 17:30-23:59 | Render* | **主力行为状态**（吸筹/出货标签） | mainforce_state |
| 17:45-23:59 | Render* | 龙虎榜回填 | lhb_history |
| 18:00-19:00 | Render* | 评分快照（连续上榜统计） | ranking_history |
| **18:00** | cron-job.org→Actions | **K线数据包**生成+发布 Pages | data/kline/* |
| 19:00-21:00 | Render* | 主线日报 + 消息分快照 | industry_mainline / news_history |
| **19:00** | cron-job.org→Actions | **backend-pack**（K线+指标+主力） | data/kline/backend-pack.db.gz 等 |
| **19:30+** | Actions | **日批 21 任务**（见 §3） | 全部核心表 |
| 19:30-23:59 | Render(兜底) | 日报 loop（日批已写则跳过逻辑见下） | daily_reports |
| 周五 | Actions | 周度回测报告（战法/regime 切片） | backtest_reports |

`*` = 只读模式下被 `_heavy` 关闭，唯一执行方是 Actions 日批对应任务。
**Render 与日批真正的双写交集只有 3 处，全部有幂等保护**：
regime（同日第二次跳过）、daily_report（Render 兜底先跑会被日批新鲜版覆盖，
自愈冗余设计）、trader_brief（盘前 premarket / 盘后 postmarket 按 (date,phase) 分库）。

---

## 2. Actions 日批 21 任务（顺序即依赖关系）

`scripts/daily_batch.py DEFAULT_ORDER`（~19:30 开始，先校验数据包新鲜度 + 刷共享行情）：

```
backfill(回测价格回填) → market_regime(市场状态判定★) → mainflow(资金流)
→ market_snapshot(收盘快照) → calendar → mainforce_state(主力行为)
→ sector_snapshot → strategy_scan(战法扫描★) → contradiction_scan(矛盾扫描)
→ contradiction_report → score_snapshot → mainline(主线) → news_snapshot(消息分)
→ rank_live(全量精算榜★) → shadow_rank(旁路衰减榜) → lhb → zz_finance
→ weekly_report(周五) → subfactor_ic(因子体检·月度) → daily_report(日报)
→ trader_brief(盘后简报★)
```

★ = 决策链关键任务。任何任务失败会汇总推送**告警群**，不阻断后续任务。

---

## 3. 决策主链路（信号从哪来到哪去）

```
①数据就绪(19:30 日批)
   → ②市场状态判定 market_regime（offensive/neutral/neutral_bearish/defensive）
      ↓ 决定：评分权重 / 战法准入 / trade_gate / 主力乘数闸门
   → ③战法扫描（6 战法，扫描层只判 base 类型攒样本）
      ↓ 入场层完整判定：阴跌闸门+高波白名单+主力闸门+风险钩子
   → ④模拟盘入池 → 次日 9:35 成交 → coach 写入场剧本
   → ⑤白名单重放（全期+近250日双轨，expectancy 判据）→ 推送白名单
   → ⑥盘后推送（风险群：战法信号；简报群：日报/周报）
   → ⑦教练循环（30s 求值：止损/持有3日推风险、教练群）
   → ⑧复盘（三段 LLM 叙事 + 矛盾扫描 + 模拟盘 T+5 回填）
```

**准入分档**（最容易混淆的设计）：同一个战法信号有两层判定——
- **扫描层**（调度/手动 scan，`for_scan=True`）：只判 base 类型（进攻→趋势类、
  震荡→震荡类、防御→禁），**阴跌和高波白名单不拦截** → 信号照常落库攒样本；
- **入场/推送层**（模拟盘入池 `paper_trading`、推送白名单 `get_push_whitelist`）：
  完整判定（阴跌市全禁、高波白名单、主力闸门、风险钩子暂停）。
- 目的：坏市况下不推送不交易，但样本不断流，未来才能复盘"坏市况里哪种战法其实行"。

---

## 4. 核心数据表（谁写谁读）

| 表 | 写入方 | 主要消费方 |
|---|---|---|
| market_snapshot | 日批 market_snapshot | 浮筹/恢复行情缓存/池口径 |
| backtest_prices | 日批 backfill（+Render 兜底回填） | 回测引擎/regime 判定/评分 K线 |
| market_regime_history | 日批 market_regime（Render 15:40 幂等前置） | 评分权重/战法准入/闸门/coach |
| mainforce_state | 日批 mainforce_state | 评分第5因子/主力闸门/前端主力标注 |
| strategy_results | 日批/Render strategy_scan | 白名单重放/回测 |
| ranking_live | 日批 rank_live | 前端服务端榜单 |
| ranking_history | Render 评分快照 | 消息分回测/权重优化/榜单对照 |
| paper_positions/account | Render 模拟盘 | 教练/统计/白名单校验 |
| coach_plans/alerts | Render coach | 教练推送/执行一致性 |
| trader_briefs | 日批+Render 盘前 | 简报阅读/推送 |
| llm_usage_daily/by_provider | 每次 LLM 调用 | 日熔断预算/免费额度对账 |
| llm_shadow_log | 影子模式（对比期） | 新旧模型质量对比 |
| schedule_state | 所有 mark_schedule_done | 跨进程幂等 |

---

## 5. 目录地图（改哪去哪找）

```
backend/app/
├── scoring/engine.py          # 评分引擎核心（五维+子项锚点+信号）★最常改
├── scoring/live_ranking.py    # 全量精算榜
├── scoring/kline_cache.py     # K线缓存（pack 优先）
├── routers/scoring.py         # 评分 API（实时榜/单股/_calc_technical_fast）
├── strategies/
│   ├── market_regime.py       # regime 判定封装 + 战法准入（阴跌闸门/高波白名单）
│   ├── paper_trading.py       # 模拟盘（入池/成交/跟踪/统计/风控G1-G4）
│   ├── recommendation.py      # 推送白名单（双轨重放+半衰期）
│   └── base.py                # 战法基类与注册
├── backtest/
│   ├── engine.py              # 撮合引擎（match_signals）
│   ├── market_regime.py       # regime 判定引擎+细化（两日确认）
│   ├── strategies.py          # 退出策略 v1/v2 + 信号流 + 批量价格加载
│   └── run.py                 # 回测报告入口
├── mainforce/                 # 主力行为（state/flow/overlay/gate/chips/phases）
├── contradictions/            # 矛盾扫描（scanner/risk_hook）
├── coach/                     # 交易员教练（rules/monitor/audit）
├── flash/                     # 快讯/调度器(scheduler)/LLM/企微(notify)
├── routers/                   # API 路由
└── trader_brief.py            # 决策简报
scripts/
├── daily_batch.py             # 日批入口（19 任务）
├── flow5_curve_backtest.py    # flow5 曲线回测
├── flow5_pos_backtest.py      # flow5×位置分层回测（分支）
├── subfactor_ic_backtest.py   # 子指标因子体检（分支）
└── ...各专项回测脚本
frontend/src/
├── utils/scoringEngine.js     # 前端评分引擎（与后端同曲线★改锚点必须同步）
├── utils/klineDB.js           # 本地 IndexedDB（数据包存储）
└── composables/useFrontendScoring.js  # 本地计算编排
```

---

## 6. 配置开关总表（关键项，完整版见《全项目审查》§4）

| 开关 | 默认 | 作用 |
|---|---|---|
| RENDER_READ_ONLY / ENABLE_HEAVY_JOBS | Render=1/0 | 只读模式：重循环全关，重任务归日批 |
| DATA_SOURCE | db（默认） | 读数据包（零 Supabase 流量）；**Render 须显式配 pack**，否则默认 db 回源 Supabase 吃 egress |
| STRATEGY_GRIND_GATE | on | 阴跌市（neutral+MA向下）战法入场全禁 |
| STRATEGY_WIDE_SCAN | on | 扫描层只判 base 类型（攒样本） |
| MIDDAY_REGIME_GATE | 1 | 午盘 LLM 信号仅进攻市放行 |
| CONTRADICTION_RISK_HOOK | 1（已配） | severe 次日新仓风险×0.5+趋势战法暂停 |
| NB_TURNOVER_CAP(_PCT/_SCORE) | 1/8/40 | 阴跌市高换手资金面封顶 |
| MAINFORCE_MODE | 无默认值 | 出货嫌疑 ×0.85 闸门（读端现算）；**须显式配 auto**（代码侧无法代改，Render 控制台） |
| WARFARE_EXIT_POLICY | v2 | 退出策略（3交易日/-7%/无目标价） |
| WHITELIST_CRITERION | expectancy | 白名单判据（均收益>0.3%+PF≥1.2） |
| WHITELIST_ROLLING(_DAYS) | 1/250 | 白名单双轨滚动窗口 |
| COACH_ENABLED | on | 教练循环 |
| WECHAT_HOOK_{RISK/COACH/BRIEF/ALERT} | — | 分类群机器人（无 IP 限制，生产分类靠它） |
| LLM_FREE_*（BASE_URL/KEY/MODEL/SHADOW） | — | LLM provider 链；SHADOW=1 影子对比期 |

---

## 7. 治理铁律（每一条都有血泪史）

1. **先回测再上线**：改评分曲线/战法规则必须离线回测背书（判定标准预先写死，
   防止事后挑格子——见 `评分倒U改进` 文档 P2 的实践）。
2. **带开关可回滚**：所有新逻辑挂 env 开关，默认关闭或默认旧行为
   （STRATEGY_GRIND_GATE/NB_TURNOVER_CAP/LLM_FREE_SHADOW…）。
3. **口径单源**：同一语义只允许一处实现。退出策略常量在 `backtest/strategies.py`，
   评分锚点在 `engine.py`/`scoringEngine.js` **必须逐点同步**（改一处必须改另一处）。
4. **时区唯一源**：一律 `rules.beijing_now()`（Actions 是 UTC，混用会静默错位）。
5. **幂等**：同日重跑不产生双份（schedule_state 跨进程标记 + upsert + P0-1 regime 幂等）。
6. **egress 纪律**：读大表必须带 LIMIT/列裁剪/start 下推；高频读走缓存+sync_meta
   版本门控；研究脚本走 research_cache（包优先零回源）。目标 <167MB/天。
7. **降级不丢通知**：企微 notify 解析链（分类群→应用→主群）与 LLM provider 链
   （免费站→主力站）都是"任一级失败自动降级"，绝不因单一通道故障丢输出。
8. **影子先行**：换模型/换策略先影子对比（llm_shadow_log / 模拟盘 A/B），
   数据说话后再切。

## 8. 部署与分支

- **push main → Render 自动部署**（env 变更也会触发）；日批由 cron-job.org 定时触发。
- **本地验证**：`python -c "import app.main"`（冒烟）+ `backend/test_*.py` 桩测试。
- **环境变量三处同步**：本地 `.env`（gitignored）/ Render 控制台 / GitHub secrets
  （Actions 用的变量）。模板见 `backend/.env.example`。
- `feat/flow5-extreme-inflow` 已合并入 main（P1 主力标签语义区分 + 评分体检报告 + 因子体检周期化）。

## 9. 已知问题与观察项（详见各报告）

- 《全项目审查_逻辑冲突与优化建议_20260913.md》：P2 遗留项清单
- 《评分系统体检_子指标IC与优化建议_20260920.md》：技术面子项 5-10 日负 IC
  （反转效应），建议 1/2 待模拟盘验证
- open_confirmation 绕过盘后过滤链（审查 P1-9，未修）
- 生产 price_pos 覆盖缺口（001368 案例，筹码计算路径）
