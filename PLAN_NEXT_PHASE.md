# 下一阶段计划（2026-09-05 夜间整理）

> 承接：PLAN_MAINFORCE（已上线）、PLAN_CROWDING_FACTOR（已关闭，被主力筹码因子替代）。
> 本文档是接下来 2-4 周的执行路线图，按优先级排序。

---

## P0 · 本周内

### 1. MAINFORCE_MODE=auto 观察期（已具备条件，等下一个交易日）
- 测试环境 `.env` 加 `MAINFORCE_MODE=auto`，观察 ≥1 周（必须覆盖一次大跌段）
- 观察项：出货嫌疑股被降权后 Top10 的实际表现 vs 原排序（mainforce_state 表有留痕可对照）
- 通过标准：崩盘段 Top10 回撤收窄 ≥1pt，且上涨段不明显踏空 → 再定默认值

### 2. 战法体系复健（动态白名单已上线，当前全军覆没）
动态白名单实测（547 信号，T+1 撮合 + 涨停一字剔除）：龙回头 45.1%/175、
单阳不破 48.6%/218、均线粘合 47.4%/114、均线回踩 47.1%/34 —— **无一达 55%**。
三个改造方向（可并行验证）：
- **主力过滤**：只对「吸筹区/洗盘段」个股出信号（mainforce_state 已有标签，
  `signal_persistence` 落库时 join 过滤），预期能剔除一批"形态像但主力不在"的假信号
- ** regime 准入收紧**：高波动震荡段继续全禁（现有 ADMISSION_MATRIX 口径不变）
- **退出改造**：当前战法止盈/止损参数多为拍脑袋，用 engine 的逐笔数据做参数扫描
  （hold 3/5/10、止损 5%/7%/10% 网格），样本 500+ 够扫

### 3. 消息分验证定版（已固化 scripts/news_score_validation.py）
- 初步 T+2 IC +0.202（n=161，3 截面日）——方向为正
- 积累至 ≥20 个快照日（约 4 周）重跑；若合并 IC 稳定 ≥0.10 → 设计纳入综合分
  （第 6 维度或基本面加权项，届时单独评审）

## P1 · 两周内

### 4. 龙虎榜席位数据（主力行为的"实名制"补强）
- 新浪/东财龙虎榜接口已探明（东财封禁 24-48h 后可用）：机构席位净买入、
  游资席位（量化识别知名营业部）单/连续上榜
- 落表 `lhb_history`，与 mainflow 交叉：龙虎榜净买 + 筹码低位 = 强吸筹确认
- 个股矛盾扫描扩展：龙虎榜机构买入 vs 后续主力净流出的持续性验证

### 5. 模拟盘卖出侧现实化
- 买入侧涨停一字守卫已上线；卖出侧跌停无法平仓的顺延逻辑（对齐 engine.match_signals
  的 `_sellable` 口径）落到 `paper_trading` 的平仓路径

### 6. 个股层矛盾扫描：推荐 vs 主力行为的自我冲突检测
- 已有 index_vs_mainflow（指数 vs 全池主力），下一步个股级：
  「综合分 ≥65 买入信号」∩「出货嫌疑」→ 自动降级提醒 + 企微标注
  —— 这是系统对用户说真话的关键一环（评分说买、主力说出货时，必须告知）

## P2 · 本月内

### 7. GitHub Actions 指标刷新迁移（CONTEXT 已知问题 #3）
- 每日指标/K线缓存刷新从 Render 0.1 CPU 迁到 Actions 定时任务，Render 只做查表
- mainforce_state 计算（11 分钟 CPU 密集）是第一个迁移候选

### 8. Watchlist/组合页评分口径统一（CONTEXT 已知问题 #2）
- 自选/组合仍走后端 score_single；统一到前端引擎（与排行榜同源）

### 9. backtest_prices 池扩展：544 → 3000 只
- 现在全池因子验证只覆盖 544 只（评分池）；扩展到全市场可让主力因子在
  更宽的截面上验证（含小票/次新，检验因子是否只在大票生效）
- GitHub Actions 每日 16:00 的 kline pack 已含全量 → 从 IndexedDB pack 反灌即可

### 10. LLM 复盘提效（CONTEXT 已知问题 #1）
- DeepSeek-R1 600s 超时：换 DeepSeek-V3（快）做复盘、R1 只做周度深度复盘

## 观察清单（不投入开发，只积累数据等阈值）

| 项 | 触发动作 | 当前状态 |
|---|---|---|
| 消息分 IC ≥0.10（20 快照日） | 设计纳入综合分 | +0.202/3 日 |
| 任一战法重达 55%/30样本 | 自动恢复推送（已动态） | 全部 <50% |
| 主力因子 offensive 段样本 ≥500 | 验证上涨段方向翻转假设 | 段内样本 ~330 |
| 龙虎榜 × mainflow 重叠 ≥30 例 | 吸筹确认器上线 | 未开工 |
| 矛盾扫描积累 ≥60 条 | 兑现率统计（resolved 字段） | 7 条 |

## 已关闭 / 已替代
- ~~拥挤度乘数（PLAN_CROWDING_FACTOR 阶段 2）~~ → mainforce 出货乘数覆盖（残差 IC +0.04）
- ~~硬编码推送白名单~~ → 动态胜率白名单（recommendation.get_push_whitelist）
