# Python 缓存
__pycache__/
*.pyc

# 打包产物（本地/Actions 生成，不入库）
#   data/kline/           前端包、后端包、行情快照、失败黑名单等产物
#   backend/data/         pack_source 下载落盘的数据包（Render 运行时目录）
data/
backend/data/

# zzshare：通过 pip 安装（backend/requirements.txt），本地目录只是残留的子模块引用
#   此前它在 git 索引中是 gitlink（160000）但 .gitmodules 缺失 → Actions 里
#   `git submodule foreach` 报 "No url found for submodule path 'zzshare'" (exit 128)，
#   干扰发布步骤。已从索引移除（git rm --cached zzshare），这里忽略本地目录。
zzshare/

# SSH 私钥：deploy.pem 是腾讯云轻量服务器的登录密钥，绝不能进仓库
#   （2026-09-09：该文件就放在项目根目录，且未被忽略，一次 git add -A 即泄露）
*.pem
*.ppk
*.key
deploy.pem

# 环境凭证：backend/.env 含 DATABASE_URL / JWT_SECRET / GITHUB_TOKEN 等，
#   绝不入库（2026-09-09 腾讯云迁移：服务器上手工创建，模板见 .env.example）。
#   此前仓库里没有显式规则，只靠"从没 add 过"兜底，风险太大。
.env
.env.local
.env.*.local
!.env.example
