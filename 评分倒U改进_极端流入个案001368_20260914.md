# 评分倒U改进：极端流入强势股误伤个案（001368）与后续开发项

> 立项：2026-09-14（用户报告个案 → 生产 API 实测诊断）
> 状态：**待开发**（三项改进，P1 可立即做，P2 需回测背书，P3 为决策分支）
> 关联：`backend/backtest_reports/flow5_curve_20260913_0220.md`（倒U曲线的原始回测背书）、
> `全项目审查_逻辑冲突与优化建议_20260913.md`、`股票逻辑全景_20260912.md` §3.3
> 项目铁律提醒：**改评分曲线必须先回测再上线**（倒U本身就是这样上的），本文件的 P2 也不例外。

---

## 1. 案例背景

用户报告：001368（通达创智）走势强势，但 ① 上不了评分排行榜；② 详情/列表里"主力净流入"
标签是**红色**（减分项），怀疑方向搞反了；③ 质疑评分系统为什么忽略强势股。

## 2. 生产实测数据（2026-09-14 凌晨，/api/score/001368 + /api/score/batch/top）

| 项 | 数值 |
|---|---|
| 综合分 / 信号 | **60.2 / 观望** |
| 实时榜 Top100 入榜线 | **64.8**（第 50 名 66.7、榜首 76.1、总池 1548）→ 差 4.6 分不进榜 |
| 技术面 | 68.8（RSI 80 强势、MA 72、MACD 70——技术面并不差） |
| 资金面 | **53.7** ← 拖分主力 |
| ├ 量价/动量/换手/成交额 | 65 / 70.9 / 65 / 57（都正常） |
| └ **主力净流入子项** | **0 分**（flow5_amt = **+42.97%**，五日主力净流入占成交额 43%，极强） |
| 基本面/成长 | 58.4 / 54.9（平庸，非主因） |
| factors_down | `['主力净流入']` ← 红色标签来源 |
| 主力标注 | phase=**markup（拉升段）**、flow5_amt=+42.97、**price_pos=None**（筹码位置没算出）、signal=None |

诊断结论：数据方向**没有搞反**（+42.97% 确实是巨额流入），红色与不进榜都指向同一个根因——
**倒U评分曲线把 >+20% 的极端流入一律打 0 分**。

## 3. 根因分析（三个层次）

### 3.1 评分层：倒U曲线没有"价格位置"维度（主因）

2026-09-13 上线的倒U曲线（`engine._score_flow5` / 前端 `scoreFlow5`）：
`-10→0 / -6→15 / -3→30 / 0→55 / +2→80 / +5→55 / +10→30 / +20→5 / >20→0`。

- 设计假设：短期**极端**主力净流入 = 拉高出货/接力风险（"散户陷阱"），回测背书：
  5993 样本/9 截面，极端流入子集 5 日去超额 -3.99%→倒U后 -0.87%，整体去超额 IC
  从 -0.126 翻正 +0.062（`flow5_curve_20260913_0220.md`）。
- **但曲线是一维的**：`flow5` 单变量 → 分数，不看 `price_pos`（价格位置）。当时的备选
  方案②（"高位惩罚 overlay"：原始曲线 + flow5>+5 & price_pos>0.75 才 ×0.85）保留了
  "低位极端流入不惩罚"的区分，最终采用的方案③（全面替换倒U）把它抹掉了。
- 结果：**低位刚启动 + 极端流入的强势主升浪**（001368：RSI 80、主力 markup 段）与
  **高位出货前的极端流入**被同一把尺子打到 0。回测在平均意义上支持倒U，但个体误伤
  存在，001368 就是样本。

### 3.2 数据层：price_pos 覆盖缺口（P2 的前置障碍）

001368 的 `price_pos = None`——主力筹码指标（chip_metrics）对它没算出位置。
可能原因：小盘/次新/换手结构导致筹码分布计算失败或数据缺失。
**若 price_pos 覆盖率低，P2 的"低位不降分"就没有可靠的分层变量**——回测前必须先统计
5993 样本（或全池）的 price_pos 覆盖率；覆盖率不足则考虑代理变量：
距 250 日高点（mainline crowding 用过 `距250日高点<5%`）、近 20 日涨幅（`ret20>30%`）。

### 3.3 展示层：减分标签不区分语义（用户困惑的直接来源）

factors_down 的红绿判定只看**子项分值**（低→红色减分项），与流入方向无关。于是：
- 主力**流出**（flow5 为负，倒U低分）→ 红色"主力净流入" ✓ 语义正确
- 主力**极端流入**（flow5>+20，倒U刻意打 0）→ 同款红色"主力净流入" ✗ 信息丢失
  ——用户看到"强势股 + 主力净流入红标"，第一反应就是"搞反了"。
列表页只有标签没有数值，"方向正确但被降分"这层信息完全不可见。

---

## 4. 待开发项

### P1 · 展示层：减分标签区分语义【成本 ~30 分钟，建议尽快】

**目标**：`flow5 > +5` 时的红色标签从"主力净流入"改为 **"主力极端流入（散户陷阱降分）"**，
与真流出区分；`flow5 ≤ +5` 的低分维持"主力净流入"（那时红=真的在流出）。

**实现位置（两处，语义必须一致）**：
1. 后端 `app/scoring/engine.py` `_score_capital`：flow5 > 5 且 `mf_score` 低时，
   details 的 key 由 `"主力净流入"` 改为 `"主力极端流入(散户陷阱降分)"`（factors 列表
   从 details key 生成，改名即生效；权重/归一化逻辑用分值不用 key，改名安全）。
   注意 `details` 契约：前端按 `分值` 字段渲染，key 改名不影响。
2. 前端 `frontend/src/composables/useFrontendScoring.js` `_extract_factors`（约 695 行
   一带）：判定"主力净流入"为减分项时，若 `stockInfo.flow5_amt > 5` → tag 文案用
   "主力极端流入（散户陷阱降分）"。

**验收**：001368 详情页与列表页标签显示新文案；正常流出股（如任意 flow5<-3 的票）
标签仍是"主力净流入"；`score_regression_check.py` / `verify_pack_score_parity.mjs`
不因 key 改名报警（如有涉及需同步）。

### P2 · 模型层：倒U曲线加价格位置维度【需回测背书，半天～一天】

**目标**：验证"低位极端流入"与"高位极端流入"的后续收益是否显著分层；若分层成立，
给倒U加位置维度（低位极端流入不降分或轻降），修复 001368 类误伤。

**回测设计**（扩展现有 `scripts/flow5_curve_backtest.py`）：
1. 样本：沿用 5993 条/9 截面（2026-06-29~08-24）；price_pos 从主力 chip 补挂。
2. **前置统计**：price_pos 覆盖率；若 <80% → 增加代理变量组
   （距250日高点、ret20），两组各跑一遍。
3. 分层矩阵：`flow5 ∈ {>+20, +10~+20, +5~+10, 0~+5, <0}` ×
   `price_pos ∈ {>0.75, 0.5~0.75, <0.5}`，每格输出 5 日/10 日去超额收益、胜率、n。
4. 判定标准（预先定死，防止事后挑格子）：
   - 若 `flow5>+10 & price_pos<0.5` 格的去超额收益 **> +0.5%** 且 n≥100：
     低位极端流入不惩罚（该区间分数抬回 ≥55），其余维持倒U → 改曲线；
   - 若分层不显著（格子间差 <0.5%）：**维持现状**（P1 足矣），结论归档。
5. 上线：按项目纪律带开关（如 `FLOW5_POS_WEIGHTED=1`）+ 前后端同步 + parity 校验。

**红线**：不回测不改曲线；`股票逻辑全景_20260912.md` §3.3 同步更新；前后端曲线
逐点一致（本轮 flow5 改造已建立"改一处必须同步另一处"的约定）。

### P3 · 决策分支（P2 回测出来后走其中一条）

- 回测支持分层 → 上位置加权曲线，001368 类个股恢复应得分；
- 回测不支持 → **维持现状**（体系主动放弃"强势+极端流入"型，接受误伤换取整体
  IC），P1 的标签区分让用户看得懂为什么——这本身就是教练式"可解释性"的一部分。

---

## 5. 复现/诊断命令（备查）

```bash
# 单股评分（含 factors_up/down 与主力标注）
curl -s "https://stock-scoring-v2.onrender.com/api/score/001368"
# 榜单入榜线
curl -s "https://stock-scoring-v2.onrender.com/api/score/batch/top?limit=100"
# 倒U原始回测
python scripts/flow5_curve_backtest.py
```

## 6. 备忘

- 001368 的 `price_pos=None` 同时意味着：前端 `applyOverextensionGate`（高位透支
  降档，price_pos>0.9）对它也不生效——筹码覆盖率问题影响的不止本项（见 3.2）。
- 本案例与"主力净流入标签红色"同类困惑的历史背景：倒U上线（09-13）前，主力净流入
  是单调加分项（流入越多分越高），老用户对红色标签的预期是"流出"——新旧语义切换
  没有做过用户侧说明，P1 落地时建议在 `股票逻辑全景` §3.3 补一句标签语义。
