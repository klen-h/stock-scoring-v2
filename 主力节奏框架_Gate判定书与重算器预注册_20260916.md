# 主力节奏框架 · Gate 判定书与重算器预注册

> 日期：2026-09-16　样本：mainflow_history 127 交易日（2026-03-16 ~ 09-15）× 沪深300
> 依据：`主力节奏框架_开发计划_v1.0.md` §4 + 2026-09-16 评审修订
> 性质：**预注册裁决记录**——判定标准先于结果落盘，结果只填空，不反向调整标准。
> 复现：`python scripts/rhythm_profile.py`（全模块，seed=42，θ=1.0σ）

---

## 0. 预注册声明（角色定义）

**宽度重算器的角色 = 换数据源重问同一个问题，不是翻案。**

如果重算器被视为「救样本」，设计会不自觉偏向 favorable 的选择；本判定书将它的成功与失败标准在跑之前写死（§4），设计与 127 日画像完全同构。**Gate B 的 DOWNGRADE 不是事故，是画像的第一个真产出。**

---

## 1. Gate A（节拍存在性）：NO-GO

**裁决实验**（null bootstrap ×1000，重采样 E 保边际分布摧毁时序结构 → 模拟 C → 同参数 zigzag）：

| 量 | 实际 | 白噪声 95% 带 | p |
|---|---|---|---|
| 流入 episode 中位长度 | 4 日 | [2, 7] | 0.473 |
| 流出 episode 中位长度 | 3 日 | — | 0.829 |
| 流入 episode 数 | 10 | 均值 10.6 | — |

**判定：NO-GO——episode 切分与随机切割不可区分。**

B0（Q=5.4 < 11.07，ACF(1)=-0.009）已预警；null 模拟证实：E 近白噪声 → C_t 近随机游走 → zigzag 的"形式达标"（n≥5、中位≥3 日、θ 敏感性平滑）是 null 的预言而非结构证据。

**后果**：基于 C_t zigzag 的 episode 节拍（开发计划 §4.A 主口径）**不成立**。若宽度序列重验节拍，须用全新的状态×方向数据，不得复用本切分。

## 2. Gate B（信息独立性）：进攻侧无信号

| 检验 | 结果 | 判定 |
|---|---|---|
| B2 R_t ~ E_{t-1}+R_{t-1} | E 每+100亿 → 次日 2.09bp（t=0.286） | 零信息 |
| B2b 分组对照 | 差 0.123%（t=0.53） | 零信息 |
| B3 影子 E_t ~ R_{t-1} | 指数+1% → 次日 E **-27.3亿**（t=-2.25） | 非追涨，是涨后兑现流出 |
| 对照组（流入确认日 d1 后） | 5/10/20 日 **-0.8%/-1.5%/-2.2%**（差 -73/-151/-236bp） | **确认流入 = 短期顶部选择器** |

**判定：进攻侧跟随（跟着流入加仓）在日尺度资金流上无证据、对照组方向为负。** R_{t-1} 系数 -0.169（t=-2.33）：本市场短期反转——确认型指标天然滞后于已实现涨跌，确认后均值回归。

## 3. Gate C（滞后经济性）：以对照组为准

滞后成本占比 33% 的形式 PASS 是**虚的**（多数 episode 剩余收益为负，占比分母失真）。以对照组为准：**确认后无肉，进攻侧仅剩"不追"纪律价值，无仓位带价值。**

## 4. 防守侧：价格状态的别名（127 日口径，功效不足注记）

**裁决回归**（y=fwd20 ~ 流出确认哑变量 + 近20日指数收益 + 20日波动 + 20日新低哑变量，Newey-West，n=87）：

| 哑变量 | 控制 | 后系数 | t |
|---|---|---|---|
| 流出确认 | 前 | -1.94%（20日均值 vs 无条件） | — |
| 流出确认 | 后 | **-0.69%** | **-0.67** |

**判定（按预注册判据）：不显著 → 防守层是价格状态的别名。** 系统直接用价格规则（20日新低/波动/近20日跌幅），资金流时间序列用途归零；**横截面用途（flow5 IC、accum/distribution 标签）不受影响。**

**注记**：n=87、哑变量仅 9 个 1，功效极低——"不显著"≠"无信息"，本判定为** provisional**，宽度序列扩样后同规格重估（§5 预注册）。但 1:20 打样的教训已固化：**控制价格状态后表观效应塌缩 2/3，宽度重算器的控制变量是生死项。**

---

## 5. 宽度重算器预注册（P0.D，跑之前落盘）

### 5.1 角色与数据事实

- 角色：**换数据源（127 日资金流 → 三年价量代理）重问同一个问题**（市场级节奏对指数的前瞻信息），非翻案。
- **判据依赖调查（overlay.py L88-99）**：
  - `distribution` = 高位高获利 ∧（资金流出 ∨ **纯价量弱口径**）→ 三年可重算为**价量代理**
  - `accum` = 低位筹码密集 ∧ **资金流入（无退化路径）**→ 三年只能重算为「低位筹码密集」纯价量代理
  - ∴ 三年窗口上重算的是**价量代理标签（代理的代理）**，与原标签信息不同源；**它的失败不反证原标签**，成功也不能直接迁移结论。
- **重算器自身验收**（先于主检验，2026-09-16 修订——原始一致率有基数率 bug：稀有标签下被「双方都无标签」格点主导，垃圾代理也能自动 ~81% 一致）：
  - 按标签**条件化**报数：「无标签」格不进分母；每个标签报 **sensitivity**（原标签股中代理也标出的比例）与 **precision**（代理标出股中原标签比例），或 Cohen's κ（机会校正）
  - **accum 的理论上限先算**：accum 代理丢了「资金流入」条件，其一致率天花板 = P(资金流入 | 低位筹码密集)，该量在 127 日窗口即可算（不碰三年样本，不违预注册）。若天花板本身 < 阈值 → 阈值定错，会以工程失败错杀科学命题，须先改阈值而非归档
  - 通过标准（sensitivity 口径）：dist ≥70%、accum ≥ max(50%, 其理论上限 × 0.7)

### 5.2 主口径（防多重比较，只此一个）

```
W_t = accum 代理占比 − distribution 代理占比（净宽度，市场级日序列）
稳健性口径（不进判定，只报不依）：单占比、W 的 5 日变化率
```

**半边分解（预注册，非第二次通过的机会）**：无论主检验成败，报 `accum-only` 与 `dist-only` 两个分量，用途**仅限「解释信号来自哪半边」**。鉴于 -7.5pt vs +1.1pt 的不对称先验，dist-only 分量很可能才是信息所在；不预注册这个分解，主检验失败时将无法区分「市场无节奏」与「accum 半边稀释了信号」。

### 5.3 null 与样本切分

- null：**block bootstrap（块长 20 日）**——保留 W 与收益各自的自相关，摧毁交叉信息
- 样本切分：三年窗口**前 2/3 探索、后 1/3 确认**，两段各自独立报数；**分位数在各自段内定**（非全样本分位，后者是轻微前视）

### 5.4 成功标准（全部满足才算成功）

控制价格状态（近20日指数收益 + 20日波动 + 20日新低，即 §4 同款控制变量——**生死项**）后：

1. W 分位 top-bottom 组未来 20 日收益差 **≥ +100bp**（参照系：127 日对照组量级 236bp；比这小的效应在 n≈730、自相关折损后与噪声不可分）
2. 前段与后段**同向**
3. block bootstrap **p < 0.05**（单侧——方向已在假设里；块长 20 日下可行偏移仅 ~36 个，p 分辨率有限，提前知道）

**推断对象写死**：控制后价差 = 回归中 W 分位哑变量的系数差（fwd20 ~ W 分位哑变量 + 控制变量，Newey-West 滞后 ≥20）；null 用该回归残差的 block bootstrap。

### 5.5 实现细节（跑数前锁死，预注册的完整性就在这五条）

1. **一致率的比较对象**：用当前 overlay.py 在 127 日窗口**重算**原标签（非历史存档），保证两边只差「资金流输入」一个变量——否则代码版本漂移污染检验；
2. **筹码燃烧期**：chips.py 依赖历史收敛，三年窗口开头一段标签不可信 → 报出有效样本起点（镜像 C_t 的 30 日燃烧期设计）；
3. **两段 regime 构成**只做描述性记录，不做条件化重跑（那是另一条岔路）；
4. 分位数、推断对象、null 均按 §5.3/§5.4 执行。

### 5.6 终局三档（工程失败 ≠ 科学证伪）

| 结局 | 档案记录 | 实际操作 |
|---|---|---|
| 一致率不过（仪器坏） | 重算器归档，时间序列半边标记「三年尺度暂不可检验，维持 127 日证据状态」 | 同样不做时间序列座基，但保留数据源升级后重开的权利 |
| 主检验失败（仪器有效） | **真 KILL** | 座基收缩为「横截面选股 + 价格状态防守」，时间序列半边从北极星删除 |
| 主检验通过 | 时间序列半边复活，进入状态机 v2 设计（§1 已规定不复用 C_t 切分） | 仍须先过信号日志前瞻期，不直接上仓位 |

---

## 6. 净产出（两条半，无论 Gate 终局如何，立即可用）

127 日画像的净结论，不依赖任何进一步检验，**已落地进教练预警**：

1. **市场短期反转**（R_{t-1} = -0.169，t=-2.33，**已过控制**）——昨日大涨后不加仓、昨日大跌后不割肉；
   - ★ 2026-09-16 修订：原「确认流入=短期顶部选择器（-236bp）」是**未控制**数字。对流入确认哑变量跑同款控制回归（近20日收益+波动+新低）后，-2.2% 塌缩至 **-1.08%（t=-1.14）不显著** → 与反转是同一现象的两次表述，**并入本条**，不再单列。
2. **B3 微观注脚**（保留为机理，非独立纪律）——涨的那天就是轧差口径「主力」在净卖的那天（指数+1% → 次日 E -27 亿）。

## 7. 本周状态

| 事项 | 状态 |
|---|---|
| Gate A null 模拟 | ✅ NO-GO |
| Gate 判定书落盘 | ✅ 本文档（2026-09-16 二审修订） |
| 防守侧状态匹配回归 | ✅ 别名（provisional，§4 注记） |
| 宽度重算器（预注册先行） | 预注册 ✅ §5，实现待做 |
| 09-17 FOMC 附 B 手动规则 | 判定落库照旧，动作冻结「观察不加仓」（第 5 样本） |
| 净产出进教练预警 | ✅ 已落地（反转纪律 + B3 注脚，#1 已并入反转） |

---

## 8. 明日开局清单（2026-09-17，已预注册的执行起点）

1. **步骤 0（半天）**：探 `chips.py` 计算成本 + `pack` 数据源三年覆盖 → 写代理标签重算器 → 跑 127 日重叠期一致率验收（§5.1 条件化 sensitivity/precision 口径，非原始一致率）
2. 一致率通过（§5.1 sensitivity 口径）→ 步骤 1 主检验（W_t + 控制变量 + block bootstrap + 段内分位，2~3 天）
3. 一致率不过 → 按 §5.6 终局「工程失败」归档（标记「暂不可检验」，非 KILL）
4. 主检验失败 → §5.6「真 KILL」；通过 → 状态机 v2 + 信号日志前瞻期
5. 下午 FOMC 附 B 手动规则照旧：信号照常判定落库，**动作冻结为「观察，不加仓」**（判定书 §5.6 优先级 > 附 B 动作列）

---

**一句话收尾**：画像没有失败这回事——它告诉你，你原本想建的「跟随流入的进攻座基」应该换成「不追涨的防守纪律 + 三年宽度的最后一次时间序列验证」。
