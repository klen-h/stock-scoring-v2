# 交易日全流程 · 页面与接口时间轴

> 一个 A 股交易日内，本系统"自动任务 + 页面 + 接口 + 数据"的完整流转。
> 所有时间为北京时间。标注 ★ = 本地模式（前端本地计算）下不发生后端调用。

---

## 〇、一天时间轴总览

```
03:20 ─ 财报同步(东财)                    │ 盘前
04:30 ┄ (周一) zzshare 财报扩展同步       │
07:00 ─ 财经日历抓取                      │
08:55 ─ 宏观快照锁定                      │
09:10 ─ 盘前复盘(LLM)                     │
09:25 ─ 集合竞价结束 → 用户打开页面        │
09:30 ─ 开盘 ────────────────────────────│ 盘中
09:35 ─ 开盘确认(模拟盘买入/放弃)          │
09:40 ─ 行情缓存每2-3min刷新(评分用)      │
        快讯监控每5min / 持仓负面消息每10min │
        盘中风险警示每30min(指数急跌/涨跌比/跌停) │
        前端页面轮询实时数据               │
11:30 ─ 上午收盘                          │
11:35 ─ 午间雷达(盘中L2快扫→企微预警)      │ 午间
13:00 ─ 下午开盘 ─────────────────────────│ 盘中
14:57 ─ 尾盘                              │
15:00 ─ 收盘                              │
15:05 ─ 行情收盘快照(全市场)              │ 盘后
15:10 ─ 板块快照                          │
15:30 ─ K线缓存刷新                       │
15:35 ─ 矛盾扫描(全量L1/L2/L3) + 验证闭环  │
15:40 ─ 回测价格回填 / 战法扫描 / 市场状态判定 / 回测预热 │
16:00 ─ 指标缓存刷新 / (周五)周度回测报告  │
16:00┄ GitHub Actions 数据包生成(另一runner)│
16:10 ─ 增量回填补漏(信号股/滞后的)       │
17:00 ─ 主力资金流回填(新浪)              │
17:30 ─ 主力行为状态计算(全池筹码/阶段)    │
17:45 ─ 龙虎榜同步                        │
18:00 ─ 评分快照(ranking_history)         │
19:15 ─ 主线日报                          │
19:20 ─ 消息分快照(news_history)          │
19:30 ─ 每日日报(硬数据+LLM解读, 企微可选) │
03:20 ┄ (次日) 财报同步循环                │
```

---

## 一、盘前（03:20 – 09:25）

| 时间 | 自动任务 | 动作 | 落表 |
|---|---|---|---|
| 03:20-04:20 | finance_loop | 东财季度财报同步 | stock_finance |
| 04:30起 | zz_finance_sync_loop（仅周一） | zzshare 现金流/资产负债/扣非全池同步 | stock_finance_zz |
| 07:00-08:00 | calendar_loop | 抓取今日+明日财经日历（金十） | flash_state/日历接口缓存 |
| 08:55-13:00 | macro_daily_loop | 宏观快照锁定（美债/美元/油价/金价+方向分），**当日不再变** | macro_daily/macro_history |
| 09:10-11:30 | review_loop(premarket) | 盘前复盘：隔夜外围+财经日历+宏观 → LLM 出盘前预案 | flash_reviews |
| 全天循环 | flash_loop（每5min） | 7×24 快讯抓取与诊断 | flash_news/analyses |

**用户页面**：此时段打开首页 → 看宏观方向/市场环境（读 macro_daily 锁定值 +
实时指数）；日报页看昨日日报。

**接口**：`GET /api/macro/panel`（宏观面板）、`GET /api/flash/...`（快讯）、
`GET /api/calendar`（日历）——全是轻查询。

---

## 二、开盘与盘中（09:25 – 11:30）

### 09:25-09:30 用户打开页面

| 页面 | 调用接口 | 说明 |
|---|---|---|
| 全部页面 | `POST /api/auth/login`（首次） | token 进 localStorage |
| 首页 `/` | macro 面板 + 指数行情 + `/api/score/weights` | 市场环境分 + 生效权重 |
| 评分排行 `/score`（后端模式） | `GET /api/score/batch/top`（★重） | 两阶段：简化分排序→TopN 精算 |
| 评分排行（★本地模式） | **零后端评分调用**：IndexedDB 指标包 `_series` + 腾讯实时快照 + `GET /api/stock/finance/batch` + `GET /api/score/weights` | 仓内 Worker 精算；仅有财报/权重两个轻调用 |
| 详情页 `/stock/:code`（★本地模式） | `GET /api/stock/realtime/:code`（轻）+ `GET /api/stock/finance/:code`（轻）+ `/support-resistance` + `/rsi-signals` + 消息接口 | **评分/K线全本地**：不调 `/api/score/:code` 与 `/api/stock/kline`（Render OOM 触发点） |
| 详情页（本地不可用兜底） | `GET /api/score/:code`（★重）+ `GET /api/stock/kline/:code`（★重）+ `GET /api/stock/technical` | 回退路径 |

### 09:30-11:30 盘中自动任务

| 时间 | 任务 | 动作 |
|---|---|---|
| 09:35-11:20 | open_confirmation_loop | 模拟盘开盘确认：实时开盘价 vs 参考价 → 五档指引（放弃/全仓/半仓/观望），量比验证防诱多；涨停一字买不进直接取消 |
| 09:40-11:30 | stock_cache_refresh_loop（每2-3min） | 全量股票实时行情刷新 → tencent._cache（评分/宽度/涨跌比的实时底座） |
| 09:40-11:30 | intraday_alert_loop（每30min） | **盘中风险警示**：上证急跌≤-1.5% / 涨跌比<0.25 / 跌停≥30 家 → 企微（每类每日一次） |
| 全时段 | flash_loop（每5min） | 快讯抓取；持仓负面消息 news_alert_loop 每 10 分钟 |
| 11:35-13:00 | midday_radar_loop | **午间雷达**：指数vs宽度 + 板块叙事vs资金（东财盘中实时）→ severe/obvious 才推企微【先知雷达·午间预警】 |

**盘中用户页面行为**：
- 评分排行/详情页：交易时段自动轮询实时数据（非交易时段停止轮询——项目规范）
- **评分排行页内警示条**：12s 轮询 `/api/market/overview`（单请求），指数急跌
  ≤-1.5% / 涨跌比<0.25 / 跌停≥30 → 页面顶部红黄横幅（秒级滞后，企微 30min 级的页内前置）
- **自选/组合页快速轮询**：持仓+自选 12s 批量刷价（腾讯单请求），盈亏实时感
- 详情弹窗（战法页）：介入价/止损/目标为扫描时点计算，快照陈旧时前端显示黄色提示
- 快讯监控页 `/monitor`：`GET /api/flash/...` 实时快讯流；页面通知开关 →
  浏览器系统通知（新诊断/信号入场出场）
- 盘中收到的企微：开盘确认结果、午间预警、盘中风险警示、持仓负面消息
  （需 `WECHAT_BUSINESS_ALERTS=1`，Render 环境变量）

**盘中拿不到的（设计如此，数据源决定）**：两融（T+1 早上）、情绪温度计（T+1 上午）、
龙虎榜（T 日盘后 17:45）、主力资金流（T 日盘后 17:00）、北向（已停止披露）。

---

## 三、午间（11:30 – 13:00）

| 时间 | 任务 | 动作 |
|---|---|---|
| 11:32-13:00 | review_loop(lunchbreak) | 午间复盘：上午盘面小结 → LLM |
| 11:35-13:00 | midday_radar_loop | 午间雷达（见上） |

**用户**：午休翻日报/快讯；数据层无新输入（两融/情绪若深市未出会延续昨日完整口径）。

---

## 四、下午盘中（13:00 – 15:00）

与上午相同：行情缓存刷新、盘中风险警示、快讯轮询、页面轮询。
（午间雷达当日已完成，不再触发。）

---

## 五、收盘与盘后（15:00 – 23:59）—— 当天的"真正产出期"

| 时间 | 任务 | 动作 | 落表/产出 |
|---|---|---|---|
| 15:03-23:59 | review_loop(postmarket) | 盘后复盘（LLM 深度）：矛盾验证结果注入 prompt | flash_reviews |
| 15:05-15:35 | market_snapshot_loop | 全市场收盘快照（3,200+ 只） | market_snapshot |
| 15:10-16:40 | sector_snapshot_loop | 板块快照 | sector_daily |
| 15:30 | kline_cache_refresh_loop | K线缓存刷新（两轮重试+成功率告警） | kline_cache |
| 15:35-23:59 | contradiction_scan_loop | **矛盾全量扫描（L1/L2/L3）**+ **验证闭环**（T+1 行情验证昨日预判） | contradictions |
| 15:40 | strategy_scan_loop | **战法全量扫描**：10 战法 × 质量池（市值≥50亿）→ K线新鲜度守卫 → 主力过滤闸门 → 动态白名单（实时胜率≥55%/30样本）→ 企微推送（含建议仓位+环境传导链）→ 模拟盘入池 | strategy_results |
| 15:40 | regime_cache_loop | 市场状态判定（沪深300: MA/ADX/ATR）→ 动态权重 | market_regime_history |
| 15:40-23:59 | backtest_prices_refresh_loop | 指数/ETF/个股日线增量回填（腾讯源+就绪校验+缺口告警） | backtest_prices |
| 16:00 | indicator_cache_refresh_loop | 预计算指标刷新（_series 60 天） | indicator_cache |
| 16:00（周五） | backtest_report_loop | 周度回测报告（战法×regime 分层） | backtest_reports |
| 16:05-23:59 | backtest_preheat_loop | 三策略全量回测预热（ENABLE_HEAVY_JOBS 开关管控） | strategy_backtest/缓存 |
| 17:00-23:59 | mainflow_refresh_loop | **主力资金流全池回填**（新浪，逐股限速） | mainflow_history |
| 17:30-23:59 | mainforce_state_refresh_loop | **主力行为状态全池计算**（筹码+阶段+组合信号） | mainforce_state |
| 17:45-23:59 | lhb_refresh_loop | **龙虎榜同步**（日榜+池内席位明细） | lhb_history |
| 18:00-19:00 | score_snapshot_loop | 评分快照（当日全池分数，因子回测原料） | ranking_history |
| 19:15-21:00 | mainline_loop | 主线日报（行业主线识别） | industry_mainline |
| 19:20-21:00 | news_history_loop | 消息分快照（新闻情绪 vs 收益验证原料） | news_history |
| 19:30-23:59 | daily_report_loop | **每日日报**：硬数据（宏观/结构/两融情绪/ETF/主力/龙虎榜/持仓矛盾影响）+ LLM 解读 → 企微可选 | daily_reports |

### 上游：GitHub Actions 数据包生成（独立 runner，16:00 定时+手动）

工作流 `kline-data.yml`（timeout 120min，实测 ~70min）：
1. 冒烟导入（调度器/路由破坏 10s 内失败）
2. 全市场实时行情**拉取一次落盘共享**（11999 只，前后端两包复用，杜绝双拉）
3. 前端包：质量池 3000 只 × 150 天 → `kline-pack-latest.json.gz`（~30min）
4. 后端包：**质量减法池 ~750 只**（剔科创创业/ST/亏损/<50亿）× 750 根 + 预计算指标
   + 指数/ETF → `backend-pack.db.gz` + `indicators-pack.json.gz`（~35min）
5. 发 GitHub Pages → 消费方：前端"更新数据"按钮（双包）+ Render `DATA_SOURCE=pack`（backend-pack）+ 本地 `sync_local.py`
| （每月1号） | industry_map_loop | 行业映射重建 | industry_map |

**盘后用户页面**：
- 日报页 `/report` → `GET /api/daily-report`（当日硬数据+LLM 解读）
- 矛盾页 `/contradictions` → `GET /api/contradictions`（当日矛盾+验证徽章）、
  `GET /api/contradictions/report`、`POST /scan`（手动重扫）、`POST /validate`
- 战法页 `/strategies` → `GET /api/strategies/scan`（当日命中）、市场状态
  `GET /api/strategies/market/regime`
- 回测中心 `/backtest` → `GET /api/backtest`（或前端本地回测，零请求）
- 模拟盘 `/paper` → 持仓/平仓/风控事件
- 龙虎榜相关：撤退休 context（17:45 后数据可用）

---

## 六、页面 × 接口速查（前端调用的完整清单）

| 页面 | 主要接口 | 重接口？ |
|---|---|---|
| 登录 `/login` | `POST /api/auth/login` `POST /api/auth/register` | 轻 |
| 首页 `/` | `/api/macro/*`、指数行情、`/api/score/weights` | 轻 |
| 评分排行 `/score` | 后端模式 `/api/score/batch/top`＋`/batch/bottom`＋`/score/stats`；★本地模式仅财报 batch + 权重 + 腾讯实时 | 后端模式重 / ★本地零重 |
| 详情页 `/stock/:code` | ★本地：realtime+finance+support-resistance+rsi+消息；兜底：`/api/score/:code`＋`/api/stock/kline`＋`/technical` | 兜底路径重 |
| 自选 `/watchlist` | `/api/watchlist/*` + 批量评分 | 中 |
| 组合 `/portfolio` | `/api/portfolio/*` | 轻 |
| 交易计划 `/trade-plans` | `/api/trade-plans/*` | 轻 |
| 模拟盘 `/paper` | `/api/paper/*`（账户/入池/平仓/风控） | 轻 |
| 战法 `/strategies` | `/api/strategies/scan`、`/market/regime`、`/{战法}/detail/{code}`（K线实时+介入价陈旧标注） | 轻（读当日落库） |
| 回测 `/backtest` | `/api/backtest`（重）／★前端本地回测（零请求） | 后端重 / ★本地零 |
| 快讯 `/monitor` | `/api/flash/*` | 轻 |
| 日历 `/calendar` | `/api/calendar` | 轻 |
| 板块 `/sector` | `/api/sector/*` | 轻 |
| 主线 `/mainline` | `/api/mainline/*` | 轻 |
| 资金流向 `/capital` | `/api/capital/*`、`/api/stock/flow` | 轻 |
| 日报 `/report` | `/api/daily-report` | 轻（读落库） |
| 矛盾 `/contradictions` | `/api/contradictions`＋`/summary`＋`/report`＋`/scan`＋`/validate` | 轻（扫描时重） |
| 系统绩效 `/performance` | `GET /api/system/performance`（三轨：模拟盘/排行榜/回放；启动预热+1h缓存） | 轻（首访冷路径 ~33s，前端超时 120s） |
| （首页卡片） | `GET /api/system/status`（数据新鲜度仪表盘：11 类数据源最后更新） | 轻 |
| 数据更新 | GitHub Pages：`kline-pack-latest.json.gz`＋`indicators-pack.json.gz` | 零后端（Pages 免费流量） |

**★ 本地模式验收口径**：交易日盘中，评分排行 + 详情页的日志里不出现
`/api/score/batch/top`、`/api/score/:code`、`/api/stock/kline`、`/api/stock/technical`。

---

## 七、企微推送一览（一天可能收到什么）

| 时间 | 推送 | 条件 |
|---|---|---|
| 09:35-11:20 | 模拟盘开盘确认（买入/放弃/半仓+理由） | 有 pending 信号时 |
| 盘中（≤每30min） | 先知雷达·盘中风险警示（指数急跌/涨跌比/跌停） | 触发极端阈值，每类每日一次 |
| 盘中（≤每30min） | 🔴 全市场熔断级（跌停≥100） | 模拟盘 pending 买入全部取消 |
| 11:35-13:00 | 先知雷达·午间预警（宽度/板块/北向弃用后为 2 维度） | severe/obvious 才推 |
| 盘中每10min | 持仓负面消息 | 持仓股出负面快讯 |
| 15:40 后 | 战法买入信号（含目标/止损/建议仓位/v2 离场提示） | 动态白名单战法 + 高/中置信度 |
| 19:30 后 | 每日日报全文 | `DAILY_REPORT_PUSH=1` 时（默认只落库） |
| 任意 | 任务失败告警 | 调度器 `_notify_failure` |

---

## 八、数据新鲜度对照（盘中能不能用到）

| 数据 | 可用时点 | 盘中时效 |
|---|---|---|
| 实时行情/宽度/涨跌停 | 盘中实时（2-3min 刷新） | ✅ |
| 开盘量比/高低开 | 09:35 实时 | ✅ |
| 快讯 | 5 分钟 | ✅ |
| 北向 | ❌ 已停止披露 | — |
| 两融余额/买入额 | T+1 早上（深市更晚） | 昨日口径 |
| 情绪温度计 | T+1 上午 ~10:33 | 昨日口径 |
| 龙虎榜 | T 日 17:45 | 隔日口径 |
| 主力资金流 | T 日 17:00 | 隔日口径 |
| 财报（净现比/商誉） | 季度（周更同步） | 结构性口径 |
| 情绪温度计子项-两融市值比 | 依赖两融 | 昨日口径 |
