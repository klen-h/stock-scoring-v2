# 主力节奏跟随框架 · 开发计划 v1.0

> 日期：2026-09-16　状态：开发计划（能直接开工）
> 关系：**北极星**（`主力节奏跟随框架_20260916.md`）管「信什么、可靠度分层」；**本文档**管「怎么把所信的东西组合成可执行、可证伪的机制」
> 一句话：北极星是认知论，本文档是机制与验证设计——**北极星缺验证，本计划补；本计划缺地基，北极星给。**

---

## 0. 三份文档的分工（避免再写第四份）

| 文档 | 回答什么 | 状态 |
|---|---|---|
| 北极星 `主力节奏跟随框架_20260916.md` | 什么信息可用、可靠度多少、什么永远不用 | 认知论，v1.0 |
| **本计划** | 可用信息怎么组合成拍子、怎么发现自己在错 | **本文档** |
| 事件剧本 `PLAN_EVENT_PLAYBOOK_20260915.md` | 事件窗口的备案与加严（本框架的一个实例） | 吸收其触发器 |

**接口契约**：三层信息模型（北极星）+ Gate 判定结果（本计划）+ 参数表（校准来源非空）三者共同构成"座基"，缺一不可。

---

## 1. 地基问题（必须先点破）：横截面 ≠ 时间序列

**这是整个框架最重要的一个区分，也上一版北极星最大的漏洞。**

北极星 §1.1 引用的所有 ★★★ 背书，**全是横截面证据**：

| 证据 | 它证明的 | 类型 |
|---|---|---|
| flow5 IC +0.039/+0.062 | 「流入多的**个股**相对表现好」 | 横截面（选股排序） |
| distribution -7.5pt / accum +1.1pt | 「打标签的**个股**后续跑输/跑赢」 | 横截面（个股标签） |

**它们都不证明「市场级流入期该重仓、流出期该轻仓」——这才是"跟节奏"座基的核心命题，且它是时间序列命题。**

一个因子完全可以 IC 为正而择时能力为零（系统性下跌日里，选股排序救不了仓位）。

**因此「跟节奏」必须显式拆成两个命题：**

- **命题 A（横截面跟随）**：已有证据（flow5、accum/distribution），归**选股**，不属于座基；
- **命题 B（时间序列跟随）**：市场级流入/流出期 → 仓位姿态，**当前证据 = 4 次 FOMC + 半年 mainflow = 约等于零**。

**Phase 0 的全部检验围绕命题 B 设计。** 命题 B 证不成立，座基就降级为"防守型节奏层"（§2），而非撤销——两者不冲突。

---

## 2. 不对称现实：防守优先

北极星自己的数据在说一件事，被"跟对节奏事半功倍"的乐观叙事盖住了：

- **识别出货灵**（distribution -7.5pt），**识别吸筹不灵**（accum +1.1pt）；
- 4 次 FOMC：**流入是脉冲**（D+1 回流 3/4，D+2 全部转流出），**流出是趋势**（阴跌市惯性）。

**结论：座基的第一形态很可能是「防守型」——识别并回避确认的流出期（证据强），而非跟随吸筹（证据弱）。**

> 进攻侧跟随的仓位带，**挂到 Gate B 通过之后**才允许存在；Gate B 不通过，座基只保留"识别流出期、不接飞刀"这一件事——它单独就有价值，且对滞后最不敏感。

---

## 3. 信息模型：三层 + 相位状态机（补齐中间档）

### 3.1 三层信息（北极星定，本文档沿用）

```
状态层 ★★★  accum/distribution 标签（有回测背书）→ 可决策
方向层 ★★☆  flow5 / 北向 / 两融 持续趋势（IC 正但单日噪声大）→ 看连续趋势
时点层 ★☆☆  单日资金流、盘中实时（样本少）→ 仅形态参考，永不进决策链
```

**纪律**：第一层只收敛为已实现的 `accum`/`distribution` 两标签；「拉升」「洗盘」显式列为**不实现（叙事词汇）**——它们是意图词，不是数据事实。

### 3.2 相位状态机 = 「状态 × 方向」的组合编码器

北极星三层里，「顺流/逆流/观望」太粗（管不了分几步进、每步多重），「时点」已判死刑。中间缺的正是 **episode 尺度的相位序列 S0–S4**——它只用状态+方向层的慢变量，**不违反时点死刑**。

| 相位 | 名称 | 进入条件（用状态×方向定义） | 姿态 |
|---|---|---|---|
| S0 | 无节奏/观望 | 默认；或 30 日无合格 episode | 空仓/最低仓 |
| S1 | 流出确认 | distribution 宽度上升 **或** 市场级 flow5 下行 | 禁加仓（只减不加） |
| S2 | 流出衰减/企稳观察 | 连续 k 日不创新低 **且** 收盘不创 20 日新低 | 维持不加 |
| S3 | 回流确认 | accum 宽度上升 **且** 持续 N 日（N 由 Phase 0 校准） | 跟随仓位带 |
| S4 | 流入停滞 | 回撤 ≥ δ·θ 或指数滞涨 | 减仓 |

**"宽度"定义**（横截面资产的时间序列化，缺口 4 的钥匙）：

- `accum 宽度` = 当日 `signal='accum'` 的股票数 / 全池股票数；
- `flow5 市场分位` = 当日全池 flow5 的均值 / 其在历史分布中的分位。

这两个量把个股级信息聚合成**市场级时间序列**，命题 B 的回测样本就不再受 `mainflow_history` 半年限制。

---

## 4. Phase 0：画像与 Gate（GO/NO-GO 的唯一来源）

> 原则：**先复盘，后规则**。产出：`scripts/rhythm_profile.py` + 四张表 + Gate 判定书。

### 4.A episode 切分与统计

```
输入: 市场级净流入序列（mainflow_history 全池 SUM），市场级收益（沪深300）
1. 剔除内生性: 回归 F_t ~ R_t（winsorize 1%/99% + 稳健回归双跑；-1009 亿这类
   单点会绑架 β，必须双口径对照）
2. E_t = 残差（超额流）；C_t = 累计超额流
3. 对 C_t 做 zigzag，反转阈值 θ
4. 每段统计: 长度 / 幅度 / 期间指数路径 / 结束后 5·10·20 日指数收益
5. θ 敏感性: θ ∈ {0.5, 1, 1.5, 2}·σ_E，强制输出敏感性表
```

**验收点**：流入 episode 中位长度 ≥ 3 日且中位幅度 ≥ θ 下限；若结构对 θ 不稳健（换档即面目全非）→ 判定为无结构。

### 4.B 内生性检验（裁决命题 B）

| 检验 | 回归 | 回答什么 |
|---|---|---|
| B0 | E_t 自相关 | E 是白噪声吗？（是 → episode 为随机切割，**直接 KILL**） |
| B1 | F_t ~ R_t | 流对价的基准弹性 β（剥离用） |
| B2 | **R_t ~ E_{t-1} + R_{t-1}** | **昨天的超额流对今天有信息吗？（命题 B 核心）** |
| B3 | E_t ~ R_{t-1} | 流只是追涨吗？（显著为正 → 影子证据） |

统计口径：Newey-West 标准误；**报效应量与方向一致性，不赌 p 值**（n≈120 功效有限）。

### 4.C 滞后成本与可跟性（裁决命题 B 的经济性）

对每个流入 episode，用 v1.0 状态机**前向模拟**求识别日 d1，输出三张表：

1. **单 episode 明细**：d0、d1、滞后天数、滞后成本、剩余收益、占比；
2. **识别日后收益 vs 无条件分布**（对照组！）：d1→d1+5/10/20 指数收益 vs 同期无条件分位——无差异 = 时机信号无价值；
3. **对称检验（防守侧）**：S1（流出确认日）之后收益分布——跟随流入与回避流出是两个独立命题。

### 4.D 宽度序列（新解锁，Phase 0 第一课）

**目的**：用三年个股数据回测市场级节奏，把命题 B 的样本从半年扩到三年。

**第一步（先做，决定可行性）**：确认 `mainforce_state` 的 accum/distribution 标签能否用历史 `backtest_prices` + pack K 线**重算到三年**。筹码结构（price_pos/winner_ratio/concentration）由 K 线算，理论上可回溯；flow5 宽度受 mainflow_history 半年限制。**这一步的产出是一张"数据可得性清单"，不是假设。**

**第二步**：若可重算 → 构造 accum 宽度、distribution 宽度、flow5 市场分位三条市场级序列，对沪深300 做前瞻检验（宽度今日值 → 未来 5/10/20 日指数收益）。

**这一步是本计划能否把命题 B 从"4 次 FOMC 零证据"升级为"三年可回测"的关键。**

### 4.5 Gate 判定书（显式落盘，含数字依据）

| Gate | 通过 | 降级 | 放弃 |
|---|---|---|---|
| **A 节拍存在性** | episode 结构稳健 | 挂起（空状态为主，扩样后重估） | — |
| **B 信息独立性** | 流入/流出双侧方向一致 | 仅流出侧显著 → **防守型节奏层**（只回避，不跟随） | 双侧皆无 → 降为动量过滤器或撤 |
| **C 滞后经济性** | 中位占比 <50% | 50~70% → 仅回避用途 | >70% → 撤 |

**降级不是失败**：防守型节奏层（识别流出期、不接飞刀）单独就有建仓价值。

---

## 5. Phase 1：相位状态机落地（Gate = GO 或 DOWNGRADE 后）

### 5.1 设计原则（继承北极星红线）

1. **盘后可算**：决策路径禁止 import 任何实时接口；
2. **描述数据，不描述意图**：状态名只写数据事实（「累计超额流出中」），禁止「洗盘」「诱多」；
3. **宁慢勿错**：aggregate 流是公募/保险/量化/游资混合拍，阈值偏保守；
4. 空状态（S0）是一等公民；每个相位有失效条件，无永久状态；
5. fail 语义分方向：**加仓侧 fail-closed（数据缺失禁止加仓），持仓侧 fail-open（维持现状+提醒）**。

### 5.2 参数表（校准来源非空，否则不许进生产）

| 参数 | 初始值 | 校准来源 |
|---|---|---|
| θ | — | P0.A 敏感性表 |
| k | — | episode 中位长度 P25~P40 |
| δ | 0.5 | P0.A |
| N（accum 宽度持续日） | — | P0.D 宽度序列 |
| 新低窗口 | 20 日 | 沿用剧本 20 日口径（已定） |
| 仓位带 | — | P0.C 滞后成本 × 教练风控参数 |

### 5.3 防前视：生产口径 vs 画像口径

β 估计：画像用全样本（仅描述），**生产必须用截至 t-1 的滚动窗口（如 60 日）**。回放验收分开报告——画像口径的美妙数字不算数。

### 5.4 机制验收（明确：不是收益验收）

半年数据**验证不了策略收益，只能验证机制**。验收物：

1. 状态时间轴图（逐日相位 + C_t + 指数叠加），空状态必须出现过；
2. 转移日志：每次转换附触发它的具体数字（审计链）；
3. **死代码检查**：每条规则历史触发 ≥1 次，否则进死代码清单——「连续 2 日净流入」4/4 不触发的教训制度化；
4. 确定性回放：读冻结快照重跑一致。

### 5.5 信号日志（前瞻验证的唯一干净途径）

```
每条信号落库: 日期 | 规则版本 | 相位 | 触发数字 | 动作
事后回填: 后 5/10/20 日指数路径 | episode 实际结束日 | 跟随误差 | 滞后成本
```

季度末汇总。**小样本下，回测说不了的话让前向记录去说。**

### 5.6 规则版本化

任何参数修改 → `RULES_CHANGELOG.md`（日期、旧值、新值、依据）。回测报告标注所用规则版本；改规则后重跑的结果不得与旧版混排比较。

---

## 6. Phase 2：与事件剧本融合

### 6.1 触发器替换（正式关闭剧本遗留矛盾）

剧本「连续 2 日净流入」在 4 次历史中 0 触发，**废弃**。替换为：

```
✅ 相位 = S3（回流确认，参数版本 vX，生产口径）
❌ 单日流入不算（P0.A 已证日尺度负自相关）
```

### 6.2 事件窗口加严

D0~D+2 内 S3 确认加严（k → k+2 或 θ → 1.5θ，P0 校准后定）。**L3 对 L2 只能收紧、不能放松。**

### 6.3 备案文案改版

数字注入源改为：rhythm 模块当前相位 + Phase 0 历史统计。LLM 红线不变（只渲染措辞、禁数字）。fail-open 不变。

---

## 7. Phase 3：执行层联动

### 7.1 东财盘中预警前置检验（20 个交易日）

每日记录东财收盘时点值 vs 新浪定稿 → 符号一致率、幅度偏差。**一致率 ≥80%** 才允许东财盘中值做「预警」（仍不可决策）；否则盘中预警下线。

### 7.2 冲突裁决表（regime 归位）

| 冲突 | 裁决 |
|---|---|
| 教练熔断 vs S3 加仓 | 熔断赢（风险层 > 时机层） |
| **regime 天花板 30% vs S3 姿态带 50%** | **取 min（regime 是宏观拍，节奏是微观拍）** |
| 事件窗口加严 vs 相位已确认 | 取更严 |
| 数据缺失 vs 任何加仓 | 禁止加仓 |
| S3 时段 vs 买什么 | 组合分/错杀名单排序照旧（内容维归 L4，不是节奏维） |

**裁决顺序**：教练熔断 > regime 天花板 > 事件窗口加严 > 相位姿态 > 内容选择。**风险最高，内容最低。**

---

## 8. 实施排期

| 阶段 | 内容 | 工作量 | 依赖 |
|---|---|---|---|
| P0.D 数据可得性 | mainforce_state 能否重算三年 | 0.5 天 | 无 |
| P0.A~C | episode 切分 + 内生性 + 滞后成本 | 1 周 | P0.D |
| P0.D 宽度序列 | 三年市场级节奏回测 | 1 周 | P0.D 数据 |
| **Gate 评审** | 判定书落盘 | 0.5 天 | P0 |
| P1 | 状态机 + 回放 + 信号日志 | 1 周 | Gate = GO/DOWNGRADE |
| P2 | 剧本触发器替换 + 备案改版 | 2~3 天 | P1 |
| P3 | 东财一致性检验 + 联动 | 持续 | P1 |

**本周动作**：
1. 北极星修订（纯文本，半天）——见 §9；
2. **09-17 照旧**：附 B 手动规则原样执行，结果按格式进信号日志（成为 Phase 0 第 5 个样本）；
3. **第一课 = P0.D 数据可得性**：确认 accum/distribution 能否重算三年。

---

## 9. 验收标准（操作化）

1. 术语即代码：文案/规则中的术语全部可在术语表找到公式；
2. 确定性回放：读冻结快照重跑一致；
3. 参数表校准来源列非空（禁止"30 次"这类无来源阈值——北极星 §7.3 的"≥30 次"已改为"Phase 0 校准"）；
4. **决策路径依赖闭包静态检查**（非文本 grep）：决策模块的 import 依赖不含实时接口，或定义信号生成函数白名单；
5. 死代码检查通过（每条规则历史触发 ≥1 次）；
6. 空状态在状态时间轴图中出现过；
7. Gate 判定书显式落盘（GO/DOWNGRADE/KILL + 数字依据）；
8. 所有输出含 n 与数据窗口；
9. LLM 红线继承（数字由代码注入）；
10. 信号日志 schema 落地，首条记录 = 09-17 事件手动规则。

---

## 10. 风险与 Kill 标准

| 风险 | 概率 | 应对 |
|---|---|---|
| Gate B/C 失败：流是价格的影子 | 不低 | 降级路径预定义（§4.5），框架不绑架结论 |
| 命题 B 与命题 A 混淆（横截面当时间序列） | 高 | §1 显式拆题，Phase 0 只检验命题 B |
| n≈120 过拟合 | 高 | 方向性标注 + 版本化 + 前瞻日志 + 扩样重估 |
| 单点异常值扭曲回归（-1009 亿） | 中 | winsorize + 稳健回归双跑 |
| mainforce_state 标签无法重算三年 | 中 | P0.D 先确认；不可行则宽度序列只覆盖半年，命题 B 回测缩水 |
| 叙事寄生（洗盘/诱多） | 中 | 命名红线 + 第一层只保留 accum/distribution |
| 前视偏差（β 用未来数据） | 高 | §5.3 双口径强制分离 |
| 规则漂移（看完结果改规则） | 高 | changelog + 版本标注 |

---

## 附 A：术语表（定义即代码）

| 术语 | 定义 |
|---|---|
| 净流入 F_t | `mainflow_history` 当日全池主力净流入（亿） |
| 日收益 R_t | 沪深300 当日涨跌幅 |
| 超额流 E_t | F_t 对 R_t 回归残差 |
| 累计超额流 C_t | ΣE_s（zigzag 递归，前 30 日燃烧期） |
| episode | C_t 的 zigzag 单向段 |
| accum 宽度 | signal='accum' 股票数 / 全池数 |
| distribution 宽度 | signal='distribution' 股票数 / 全池数 |
| flow5 市场分位 | 全池 flow5 均值的历史分位 |
| 识别日 d1 | 检测规则首次确认「流入 episode」的交易日（前向口径） |
| 企稳 | 连续 k 日收盘 ≥ 滚动 20 日最低收盘价 |
| 可跟性 | 中位滞后成本占比 <50% 且识别滞后中位 ≤3 日 |

---

## 附 B：09-17 FOMC 临时手动规则（框架完成前照旧执行）

Phase 0 不可能在 09-17 前完成，本次事件按以下**未验证的人工规则**处置（本身即 P0.C 要检验的规则形态）：

```
触发: D+1~D+3 累计净流入 > 0 且 每日收盘 ≥ D0 最低收盘价
未触发: 观察延长至 D+5，同一口径
判定: 09-18 / 09-21 / 09-22 盘后手动查 mainflow_history 累计值，新浪定稿为准
动作: ★ 冻结为「观察，不加仓」（2026-09-16 Gate B 进攻侧 DOWNGRADE 已废止跟随加仓，
     判定书优先级高于本附则）。信号照常判定、照常落库——第 5 个样本的价值在数据，
     不在任何一笔交易。
```

**4 次历史触发率 1/4**（仅 07-30 触发）：可能是选择性（好事）也可能是噪声（坏事），n=4 无法区分。**诚实结论：本次大概率「观察 + 不动」，这本身符合「宁慢勿错」。** 结果按格式进信号日志，成为第 5 个样本。

---

**收尾一句话**：这份计划的价值不在"现在是对的"，而在**每一步都安排好了怎么发现自己错**——拆题（横截面/时间序列）、Gate 裁决、对照组、死代码检查、版本化、信号日志。北极星管"信什么"，本计划管"怎么验证所信"，两边合并浇筑，座基才成立。
