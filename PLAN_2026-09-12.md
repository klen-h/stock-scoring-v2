# 主计划（2026-09-12 版）· 数据面收口 → 脱离 Supabase → 决策工作流收尾

> **本文件是仓库里唯一的现行计划。**
> 历史 plan 原文（11 份）全部归档在 `PLAN_ARCHIVE_2026-09-12.md`；代码注释里引用的旧文件名，内容在归档里按目录查找。
>
> 承接来源：`PLAN_NEXT_PHASE.md`（09-11 版）的 P0/P1/P2 + 10 份专题 plan 的未完成项，已按主题**去重归并**（同一件事只出现一次，并标注来源）。
> 定位：功能层基本完备，脆弱点集中在**数据面**（包新鲜度 / 覆盖 / 配额）与**迁移闭环**（脱离 Supabase）。

---

## 0. 旧计划的去向（收拢结论）

| 原计划 | 处置 |
|---|---|
| `PLAN_NEXT_PHASE.md`（09-11） | 全部拆解进本文 P0/P1/P2 |
| `PLAN_CROWDING_FACTOR.md` | **已关闭**：残差化后 IC +0.041≈0，改由主力筹码因子 / `mainforce_overlay ×0.85` 承担 |
| `PLAN_MAINFORCE.md` | 阶段 1-2 已落地（H1/H5/H5'/H6 成立）；阶段 3 并入 §2 |
| `PLAN_PACK_MIGRATION.md` | 主体完成（Phase 0/2 验收、日批已迁 Actions）；收尾项并入 §1 |
| `PLAN_REGIME_BEARISH.md` | 主体落地（防御化权重被数据否决）；阶段 1.3/1.4 与阶段 2 并入 §3 |
| `PLAN_ZZSHARE_INTEGRATION.md` | 阶段 0/1 落地；验收门槛与阶段 2 并入 §2 |
| `PLAN_PAPER_TRADING.md` / `PLAN_PAPER_RISK.md` | **代码已实现**（文件里「规划中，未实施」已过时）；后续校验并入 §6 |
| `PLAN_TRADER_WORKFLOW.md` | Phase 1 大部落地；收尾与 Phase 2/3 并入 §4 |
| `先知雷达_功能Plan_v1.0.md` | 多数已覆盖；依赖数据积累的 4 项并入 §5 |
| `复盘Plan_2026-09-03.md` | 当日作业已完成（有时效性）；唯一跨期项（行业地缘抗性）并入 §3 |

---

## 1. 当前状态快照（09-12 周六）

**已上线/已修（09-10 ~ 09-12）**

| 项 | 内容 | 状态 |
|---|---|---|
| 数据包新鲜度 | CDN 击穿 + 重试只置标志位；`_parse_pack_date` 只认 `%Y-%m-%d`（实际 `YYYYMMDD`）→ 常驻进程挂旧包 | `1ddfacf` + `8340a87`（日历漏洞修复，**待周一验证**） |
| 包覆盖 | 池口径改 `market_snapshot`（战法池同口径 2052 只），缺 650 只回源问题终结 | `1ddfacf` **待周一验证** |
| 前端 K 线 | 末根补齐重试 + 详情页包新鲜度门 | `041d552` **待周一验证** |
| 榜单 | 日批全量精算 → `ranking_live` 落库，后端当日直读 | `9baeef3` **待周一验证** |
| 主力分桶 / regime | none 桶恒空修复；日批新增 `market_regime` | `b1b5a1a` / `6fd1d7b` **待周一验证** |
| 消息分快照 / 周报 | 迁入日批（`news_snapshot` / `weekly_report`），报告落库 | ✅ 周报已上线（09-11 23:49，83 秒）；消息分已手工补 09-11 |
| 财经日历 / 浏览器镜像 | 日历迁日批；镜像退役 + `app/data_files.py` 水位核对 | ✅ `848a0d5` |
| LLM 空响应 | 空响应纳入重试 + 兜底模型 + 失败原因透传（含 `finish_reason`/思考字数） | 代码已推，**Render 部署后看 `last_error`** |
| Supabase egress | 归因 → **四步治理**：① 包覆盖 2052 只不再回源（`1ddfacf`）② 数据集版本门控 `sync_meta`（数据没变不重拉、变了立刻可见）+ 研究脚本本机缓存 `research_cache`（包优先零回源）③ 大结果集列裁剪 + 进程缓存（tracking / market_snapshot / macro / 浮筹）④ 通知与诊断轮询 `since` 下推 | `c43a163` / `8340a87`，**待 09-14/15 实测对账** |
| 时间口径 | 全链路统一北京时间（唯一时间源 `rules.beijing_now_iso()` / `to_beijing()`），顺带修掉通知管道 naive/aware 混比静默吞事件（15/20） | ✅ `8340a87` 已提交推送 |
| 决策简报自动化 | 盘后进日批（`trader_brief` 任务：幂等 + 企微推送）+ 盘前 Render 轻量 loop（9:10-11:30 窗口，属"只读模式保留的 LLM 叙事类"）；非交易日跳过、推送走业务开关 | 2026-09-12 新增，**待工作日首跑**（周一 9:1x 盘前 / 日批盘后） |
| 战法推送白名单 | 复核得：**白名单当前算出空集 → 战法推送自 09-05 动态化后一直静默**。6 战法胜率天花板 ~50%（与自家闸门验证"胜率 48.1→50.3% 但均收益 +0.07→+0.44%、盈亏比 1.05→1.36"一致），而门槛是"胜率≥55%" → 判据与体系特性矛盾。已把判据做成可配置 + 统计补「均收益/盈亏比/中位」；期望判据（均收益 >0.3% 含成本、盈亏比 ≥1.2）下仅`龙回头`达标（136 样本 / 胜率 48.5% / 均 +0.46% / PF 1.39） | 2026-09-12 复核完成，**默认仍不推（零行为变化）；是否切 `WHITELIST_CRITERION=expectancy` 待你拍板** |
| **Supabase 换号迁移** | 旧账号项目配额耗尽（8/20-9/20 周期用 16.24GB）→ 402。决策：**换新 Supabase 账号**（新配额 5GB/月；治理后常态 2-3GB/月，够用）。迁移由另一路 pg_dump/pg_restore 完成；我做了**逐表验收**：46 表全恢复，关键表数据全部截止 **09-11（最新交易日）**，大 JSON 两表本轮也过了（`kline_cache` 2,220 / `indicator_cache` 1,144，上一轮断线失败的两张）；用户资产类（users/paper/portfolio/watchlist/plans）全在；线上冒烟 ✓（榜单 200，000807 71 分"买入"）。总大小 106MB vs 旧 198MB = 旧库三周膨胀，非缺数据 | 2026-09-12 **迁移完整**；Render/.env 本就指向新项目（零配置切换）。**待周一最终验收**：日批写库 + 简报首跑 + egress 观察 |

**运行形态**：日批在 Actions（私有仓库 cron 停用，改 cron-job.org 触发）· 后端 `DATA_SOURCE=pack` + `RENDER_READ_ONLY=1` + `ENABLE_HEAVY_JOBS=0` · 冷备库腾讯云 `stock-postgres`（PG17，内网 only，每日 03:00 备份留 14 份）。

> ✅ **09-12 那批改动已提交并推送**：`c43a163`（egress 步骤 ②③：版本门控 + 研究缓存）、`8340a87`（egress ②③④ + 时区统一 + 包新鲜度日历修复）、`a8570c6`（本计划收拢）。当前 `HEAD = origin/main = a8570c6`，工作区干净。

---

## 2. 遗留项总表（按主题归并，标注来源）

### 2.1 数据面 / 迁移
- [ ] `backtest_prices` 回填配额：每日 150 只 → 594 只缺口需 4 天，按「距评分池远近」排优先级（09-11 P1）
- [ ] `backtest_prices` 池扩展 544 → 3000 只（用 kline pack 反灌）（09-11 P2）
- [ ] `mainflow_history` 整表读根治（8.7MB/次）：让数据包携带资金流序列后从 pack 读 —— **需先评估读取层正确性**（09-11 观察项 #14）
- [ ] `indicator-refresh.yml` 退役 + 旧 JSON 版 `backend-pack` 打包逻辑清理（PACK_MIGRATION Phase 4）
- [ ] Supabase 密码重置三处同步（GitHub secret / Render env / 本地 `.env`）（PACK_MIGRATION 第十一节）
- [ ] 手册 v3.1 补记第八章（数据包 / 迁移）（PACK_MIGRATION Phase 4）

### 2.2 主力 / 因子
- [ ] `MAINFORCE_MODE=auto` 观察期结论（≥1 周且覆盖一次大跌段）（MAINFORCE 阶段 3 / 09-11 P2）
- [ ] 买侧组合（吸筹区）接入低吸类战法池：单阳不破 / 均线回踩 的准入过滤（MAINFORCE 阶段 3）
- [ ] 龙虎榜席位数据（游资/机构识别）——数据层已入库，**席位口径待排期**（MAINFORCE 阶段 3）
- [ ] 矛盾扫描 × 主力行为交叉验证：矛盾信号日 ∩ 出货嫌疑股 的重叠度（MAINFORCE 阶段 3）
- [x] ~~**战法白名单口径复核**~~ —— ✅ 2026-09-12 完成（结论见 §1 状态快照）：**真问题是判据错了**（体系胜率天花板 ~50%，却用 ≥55% 门槛 → 白名单恒空、推送静默）；已加期望型判据（可切换）+ 复核脚本 `scripts/strategy_whitelist_review.py`；**待你决定是否启用**（启用后仅推「龙回头」）
- [ ] zzshare 阶段 1 验收门槛（决定是否并入质量维度）：现金流因子分桶单调或 IC>0.1；与既有质量分相关性<0.7；覆盖率≥80%（ZZSHARE §阶段 1）
- [ ] zzshare 阶段 2：净现比子指标（改 `_score_quality`，需回测背书）· 涨停原因/连板高度（战法复盘 + regime 情绪）· 行业分位估值回测 · 龙虎榜异动上下文（ZZSHARE §阶段 2）
- [ ] zzshare 0.4.11 无资金流接口（`stock_moneyflow`/`market_mf`）→ 升级包后复查（ZZSHARE 遗留）

### 2.3 市场状态（regime）
- [ ] 状态切换平滑：hysteresis（连续 3 日多数决）+ `neutral ↔ neutral_bearish` 冷却期（≥2 交易日）（REGIME_BEARISH 阶段 1.4）
- [ ] 前端可见「当前状态 + 生效权重」（REGIME_BEARISH 执行清单）
- [ ] 行业「地缘抗性」白名单调节项：行业层系数 0.95~1.05，不新增维度（REGIME_BEARISH 阶段 1.3 / 复盘Plan 唯一跨期项）
- [ ] 阶段 2（需新数据源）：股东户数筹码集中度 · 经营/自由现金流 · 海外收入占比 · 超跌偏离度（属"改指标"，需回测）（REGIME_BEARISH 阶段 2）
- [ ] 删除 KDJ / 新增超跌偏离度 → 推迟到阶段 1 完成后验证（REGIME_BEARISH）

### 2.4 决策工作流
- [x] ~~**简报自动化 + 企微定时推送**（盘前 9:10 / 盘后 19:35）~~ —— ✅ 2026-09-12 已实现（日批 `trader_brief` + Render 盘前 loop，见 §1 状态快照）→ TRADER_WORKFLOW Phase 1 收尾完成
- [ ] Phase 2：评分/战法/主线候选**一键转交易计划** · 持仓风险聚合日报矩阵 · 计划命中提醒（经 track_loop）· 模拟盘绩效归因（按入场理由分组）
- [ ] Phase 3：决策点验证体系（矛盾预警/主线跟随/透支降档/消息降档各自胜率）· 不达标自动降级反哺 · 交易员流程 K 看板
- [ ] **榜单单一事实源评审**：前端本地榜 vs 服务端全量精算榜 差异须 ≤1 分，或在 UI 标注口径（09-11 P1#2）
- [ ] Watchlist / 组合页评分口径统一到前端引擎（09-11 P2）

### 2.5 先知雷达 / 矛盾
- [ ] 个股层矛盾扫描：`评分 ≥65（买入）` ∩ `出货嫌疑` → 自动降级提醒 + 企微标注（09-11 P2）
- [ ] 矛盾样本 ≥60 条后：信号生成器评估（矛盾 → 可执行信号的胜率验证）（先知雷达 §后续待办）
- [ ] 验证闭环 ≥30 条后：分类型准确率调阈值（先知雷达 §后续待办）
- [ ] L3 持久性跟踪：同一公司连续 N 期现金失血 → 升级预警（先知雷达 §后续待办）
- [ ] 双模型路由（等 Qwen3-32B 接入渠道明确）（先知雷达 §后续待办 / 09-11 P2）

### 2.6 模拟盘
- [ ] **关键校验点**：跑 1-2 周后，用同批信号对比「模拟盘实测胜率 vs 回测引擎胜率」（PAPER_TRADING）
- [ ] `paper_risk_events` 攒一个月后按实际触发频率回调阈值：G1 同行业≤2 只 · G2 回撤≥5% 熔断 · G3 连亏 3 笔冷却 1 日 · G4 日亏≥2% 停开（PAPER_RISK 开放问题 2）
- [ ] 波动率仓位上线后组合收益不可直接比 → 对比验证以**胜率**为主（PAPER_RISK 开放问题 3）

---

## 3. P0 · 今天 ~ 周一开盘前（09-12 ~ 09-13）

1. ✅ **提交 09-12 那批改动** —— 已完成并推送（`c43a163` / `8340a87` / `a8570c6`）。
2. ✅ **补 GitHub secrets** —— 已完成（`LLM_BASE_URL` / `LLM_MODEL`）。Render 侧亦已就绪：`/api/flash/status` 显示 `model=deepseek-ai/DeepSeek-R1`、`last_error=null`。
3. **周一（09-14）日批验收 5 个数字**（照抄日志即可判健康，这批修复 09-11 时还没进仓库）：
```
① backend-pack  → 「战法池口径覆盖: 2052/2052（缺口 0）」
② kline-data    → 「末根补齐第 1 轮: N 只停在 …」→ 残留趋 0
③ daily-batch   → 「[2/15] 市场状态: 2026-09-14 … 权重={...}」
④ daily-batch   → 「[12/15] 全量精算榜: 300 行落库」+「消息分每日快照: N 只」
⑤ 无 [WAF] / 无「判过期走实时拉取」/ 无「数据包日期 != 今天」
```
4. **前端 4 项验证**：002452 上榜 · 000567 资金面 5 因子 · 分桶 `mainforce.none` 有样本 · 详情页 K 线末根 = 最近交易日。
5. **决策简报首跑验证**（2026-09-12 新增功能）：周一 9:1x 应收到「🧭 交易员决策简报（盘前）」企微推送；日批日志应有 `trader_brief` 一行（`postmarket N 字…已推送企微`）——两边都是幂等的，同日重复跑不会重复推。

---

## 4. P1 · 本周（09-14 ~ 09-18）

1. **egress 实测对账**：看官方 `Observability → Database API → API Egress`，重点验证**周一的柱子不再高于周二~周五**（`kline_cache` 单只回退的指纹）；若仍超 167MB/天，按 §2.1 的 `mainflow` 从 pack 读继续压。
2. **决策简报自动化**（§2.4 第一条）：✅ 2026-09-12 完成（日批盘后 + Render 盘前）→ 本周只需观察首周推送质量（是否太长/是否漏项）。
3. **战法白名单口径复核**（§2.2）：✅ 2026-09-12 完成（判据可配置 + 复核脚本；默认零行为变化）→ 你拍板是否切 `WHITELIST_CRITERION=expectancy`。
4. **`backtest_prices` 回填配额**排优先级（§2.1）。
5. **榜单单一事实源评审**（§2.4）：先出一份差异对照（本地榜 vs 服务端榜，Top100 几分之差），再决定"直接展示服务端榜"还是"UI 标注口径"。

---

## 5. P1 · 本周末（迁移闭环）—— ✅ **已被「换新 Supabase 账号」方案取代（2026-09-12）**

> 原计划（买域名 → 命名隧道 → DB 切腾讯云）是为"脱离 Supabase"设计的。9/12 配额 402 后实际走了
> **换新 Supabase 账号**（新配额 5GB/月，治理后常态 2-3GB/月够用）→ **域名/隧道不再必需**。
> 下面步骤保留作参考；腾讯云冷备库（`stock-postgres`）继续作为**灾备**保留，其每日 03:00 备份职责不变。
> 若新账号再遇配额问题，重启本节即可（域名是唯一前置）。

1. **命名隧道替换快速隧道**：cloudflared named tunnel → `https://api.<域名>` 固定地址（快速隧道重启即换域名，绝不能当生产）
2. **DB 切到服务器本机 Postgres**：重跑 migrate 同步 → 改 `DATABASE_URL` → 重启 → 逐表行数比对
3. **解决"外部写入方访问 DB"**（三选一，**α 推荐**）：
   - α：API + 日批都搬到腾讯云（服务器自己写自己的库）；Actions 只留**不碰库**的 `kline-data`，`backend-pack` 改造为不读库或一并搬走
   - β：`cloudflared access tcp` 暴露 `db.<域名>:5432`（服务令牌）给 Actions/Render —— 改动小，链路多一跳
   - γ：Postgres 开公网 + IP 白名单 —— **不推荐**（GitHub/Render 出口 IP 动态）
4. **前端切换**：`VITE_API_BASE_URL=https://api.<域名>/api` → push → deploy-preview 重建 → 硬刷新验证
5. **下线与归档**：Render 观察 1-2 天后停用；Supabase 保留只读 1 周后归档；**密码三处同步**（§2.1）
6. **备份外迁**：`deploy/backups/*.dump` 定期 scp/OSS 出一份（防单机故障）

**验收**：`api.<域名>/api/health` 200 · 前端全页面可用（登录/排行/详情/日报/模拟盘）· 日批写库成功 · Supabase 流量归零。

---

## 6. P2 · 本月（数据积累 / 验证型，急不来）

- 个股层矛盾扫描自动降级（§2.5，需 `≥60` 条样本）
- 消息分验证定版：IC ≥0.10（需 20 个快照日；现 +0.202 / 3 日）
- `MAINFORCE_MODE=auto` 定默认值（需覆盖一次大跌段）
- zzshare 阶段 1 验收 → 决定是否并入质量维度（§2.2）
- 矛盾 × 主力行为交叉验证（§2.2）
- 模拟盘胜率对比校验（§2.6，需 1-2 周平仓数据）
- L3 持续失血跟踪 / 验证闭环调阈值（§2.5）
- regime 状态切换平滑 + 前端展示生效权重（§2.3）
- 行业地缘抗性白名单调节项（§2.3）
- 决策工作流 Phase 2/3（§2.4）
- LLM 复盘提效：R1 → V3 分档（R1 只做周度深度复盘）
- `backtest_prices` 扩到全市场（§2.1）
- 手册补记 + 退役 workflow 清理（§2.1）

---

## 7. 观察清单（不投开发，等阈值）

| 项 | 阈值 | 当前 |
|---|---|---|
| Supabase 日 egress | <167MB/天（免费档 5GB/月） | 09-10 实测 458MB/天 → 09-12 治理后**投影 70~95MB/天**（未计 ② 版本门控 / 研究缓存，实际应更低） |
| 消息分 IC | ≥0.10（20 快照日） | +0.202 / 3 日 |
| 主力因子 offensive 段样本 | ≥500 | ~330 |
| 矛盾扫描兑现率统计 | ≥60 条 | 4 条 |
| 主力分桶（吸筹/出货） | n ≥20 | accum 26 / distribution 10 |
| 全量精算榜 vs 前端本地榜 | 差异 ≤1 分 | 待首跑对比 |
| 模拟盘实测 vs 回测胜率 | 偏差可解释 | 待 1-2 周数据 |

---

## 8. 风险与回滚

| 场景 | 回滚 / 兜底 |
|---|---|
| Supabase 402 / 配额 | **已发生并处置（09-12）**：换新账号项目迁移完成；治理后常态 2-3GB/月 < 5GB。兜底：腾讯云冷备库（每日 03:00 备份）随时可切；前端「本地计算」可顶核心评分 |
| Actions 包失败 | 读侧 `_is_stale()` 自动回退 DB（不会静默用两三天前的数据算分） |
| API 不可用 / 隧道失效 | 前端开「本地计算」（包在 Pages，零 API） |
| 迁移数据不一致 | 迁移脚本自带源/目标行数比对；Supabase 原库不删，只读保留 1 周 |
| 新进程缓存读到旧值 | 三类缓存（tracking / market_snapshot / macro_history）都是"写入即失效 + 短 TTL"，TTL 均短于数据变化周期 |
| 时间口径异常 | 统一走 `rules.beijing_now_iso()` / `to_beijing()`；历史无偏移值按 UTC 解释，前后端口径一致 |
