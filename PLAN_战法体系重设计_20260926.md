# 战法体系重设计 v1.0（融合版）

> 2026-09-26 v1.0。触发：白名单实测为空（有信号战法全负期望），且修复滚动窗口后确认
> **即使窗口生效白名单仍为空** ⇒ 问题不在门槛、不在窗口，在体系结构。
> v1.0 变更：融合《战法体系重构_周期框架设计_20260926》的可用资产（仓位梯度/灰度上线/
> 三底展示/季节隐喻 UI），并吸收三轮交叉评审的全部修正（见附录 A 收敛表）。
> 定位：框架设计文档（不是实施清单）。**每一项都需回测背书后才能上线**
> （项目铁律：无回测不上线，C1 解禁闸门是唯一例外且已有回测依据）。

---

## 0. 摘要（一段话版）

现有体系的根本缺陷是：**10 个注册战法只有"形态"维度，没有"状态"维度** —— 每个战法都声称
自己适合某类市场（"震荡市首选"），但没有任何机制**强制**它只在那种市场里出信号；白名单
又是全局的，不区分状态 ⇒ 阴跌市里"震荡市战法"照样发信号、照样进评估，于是 2026-08~09
的阴跌段把所有战法的期望都打成负数。**新框架的核心只有一句话：准入单位从"战法"改为
"战法×状态"的格子**。阴跌格子默认为空（**默认空仓**；"熊市结构性行情是否值得开通道"
是第 1 步回测的待检验问题，不预设结论），拐点期用情绪先行指标做确认加速（feature-flag，
默认关），进攻期补一条趋势 sleeve（B 族，1 个入场 × 2 种出场）。其余资产（regime、评分、
闸门、纪律）全部保留。**季节（春夏秋冬）只是四态引擎的派生视图，不是新状态层。**

---

## 一、现状审视：实测证据

### 1.1 数据（2026-09-26 实测，strategy_results 全量重放，T+1 开盘撮合）

注册战法 10 个、历史有信号 8 个（老鸭头 5 日、仙人指路 1 日、早晨之星 0 信号）。
以下 6 个有足量样本：

| 战法 | n | 胜率 | 均收益 | PF | 半衰期（后段 vs 前段胜率） |
|---|---|---|---|---|---|
| single_yang_unbroken 单阳不破 | 445 | 41.3% | **-0.73%** | 0.65 | 25.6% ← 57.2% ⚠️崩塌 |
| ma_pullback 均线回踩 | 46 | 28.3% | **-1.31%** | 0.54 | 13.0% ← 43.5% ⚠️崩塌 |
| ma_convergence_breakout 均线粘合突破 | 395 | 27.8% | **-1.48%** | 0.32 | 16.7% ← 39.1% ⚠️崩塌 |
| dragon_turnaround 龙回头 | 159 | 44.7% | **+0.06%** | 1.04 | 接近打平，未达门槛 |
| limit_up_boomerang 涨停回马枪 | 3 | 33.3% | -1.86% | 0.29 | 样本不足 |
| advance2retreat1 进二退一 | 1 | 0% | -3.64% | 0 | 样本不足 |

判据已是 expectancy（均收益>0.3% 且 PF≥1.2，比胜率 55% 宽）⇒ **全部不达标**。
半衰期崩塌集中在 2026-08~09（阴跌段）——**同一个战法，前半段（震荡/回暖）40-57%，后半段（阴跌）13-26%**。

⚠️ 注意：上表是**全局**数字。`ma_pullback`/`ma_convergence_breakout` 全局样本大且期望深负，
大概率是形态本身失效而非状态错配 —— 它们的最终裁决在第 1 步分状态回测（死法见 7.4 淘汰规则）。

### 1.2 四个结构性缺陷（按因果排序）

| # | 缺陷 | 证据 |
|---|---|---|
| **D1** | **战法与市场状态无强制配对**。`strategies/market_regime.py` 有 TRENDING/OSCILLATING 分组，但只是"推荐启用列表"，**没有任何一处在出信号时检查当前状态** | 单阳不破自称"震荡市首选"，neutral_bearish 下照样出信号、照样进白名单评估 |
| **D2** | **白名单是全局的，不分状态**。一个战法在 offensive 赚、在 defensive 亏，全局平均可能恰好过线（被旧战绩撑住），也可能恰好不过线（现在的局面） | 09-13 加双轨想修"旧战绩撑住"，但全期轨只增不减 ⇒ 又造出"永久否决"；两者是同一个错误的两面：**都 refused 把样本按状态切开** |
| **D3** | **形态标签在阴跌市系统性失真**。"横盘蓄势/均线粘合/缩量回踩"这类形态，在下跌中继里与底部形态**肉眼不可分** | 09-13 筹码回测：accumulation 形态标签胜率 43.4% vs 基线 41.7%（无效）；而"筹码+净流入"组合 +1.26pt（有效）⇒ **位置与筹码比形态可靠** |
| **D4** | **反馈机制静默失效**。滚动窗口 250 日 > 历史 23 个信号日 ⇒ 从未生效且不报警（09-26 已修：显式告警） | 半衰期崩塌 09-13 就该暴露的问题，拖到 09-26 才被看见 |

**一句话根因：D1 是病，D2/D4 是止痛药（且过期了），D3 是选材错误。**

---

## 二、A股历史拐点规律：可用的与不可用的

### 2.1 不可用（必须明示放弃的）

| 妄想 | 为什么放弃 |
|---|---|
| **预测转熊拐点** | 转熊由政策/事件/流动性驱动，本质不可预测。所有"逃顶"叙事都是事后拟合 |
| **抄 V 形底第一波** | 924 式脉冲等不到回调确认。**接受它**——低吸定位的固有成本 |
| **情绪反转预测** | **本项目实测**（743 交易日）：冰点后次日 -0.042%（49%），亢奋后次日 +0.170%（58%）⇒ **情绪是延续不是反转**，不能做反向指标 |

### 2.2 可用（有数据、可回测的规律）

| 规律 | 证据 | 对应资产 |
|---|---|---|
| **R1 情绪先于指数**：反转确认时，涨停家数/连板高度/赚钱效应的修复**早于** MA 金叉 3-10 个交易日 | 2019-01、2024-02、2024-10 三次底部 | `market_emotion_daily` + zzshare 连板梯队 |
| **R2 熊市主体是阴跌+短反弹**：次级折返多、持续短、高度递减 | 2018 全年 6 次反弹全败于 MA60 | regime hysteresis + neutral_bearish |
| **R3 熊市里的结构性行情**：总量熊 ≠ 个股全熊 | 2013 创业板、2021 周期、2023 微盘 | 行业主线 Top5 + sector_daily（**是否开通道待第 1 步验证**） |
| **R4 趋势市回调买入正期望，阴跌市负期望** | 本项目半衰期数据即证据 | `backtest_warfare_by_regime` 分层回测 |
| **R5 反转初期的脉冲不可追但可搭车**：反转后主线板块的**第一次回调**是全周期最佳低吸点 | 2019-03、2024-10 中旬 | 龙回头/均线回踩形态现成，缺"状态标签" |
| **R6 顶部要主动收**：涨幅 >20% 的持仓主动止盈、止损收紧 | 2021-02、2024-10-08 宽度背离在前 | 矛盾雷达宽度项 + 秋态纪律（§6.6） |

### 2.3 从规律到框架的推论

- R4 ⇒ **D1 必须修**：同一个形态，期望的正负由状态决定，不由形态决定。
- R1 ⇒ 拐点处理有先行指标，不必干等 MA 金叉（但须过实验门槛才有决策权，见 6.0）。
- R2 ⇒ "阴跌市默认空仓"不是保守，是该状态下的最优策略。
- R3 ⇒ 空仓期不是没事干：主线监测是阴跌市的核心工作（为反转备弹药）。
- R5 ⇒ 趋势 sleeve 不必追第一根大阳，等第一次回调——与低吸体系天然兼容。
- R6 ⇒ 转熊协议之外还需要"收获"协议（秋态子标签）。

---

## 三、设计原则（六条，每条从一个教训导出）

| # | 原则 | 来源教训 |
|---|---|---|
| **P1** | **没有全天候战法。准入单位是"战法×状态"的格子，不是战法** | D1/D2 |
| **P2** | **不猜拐点，只做右侧确认；确认要快（情绪先行，须实验背书），确认前仓位必须低** | 2.1 + R1 |
| **P3** | **形态只当触发器，不当过滤器。位置、筹码、量能才是过滤器** | D3 |
| **P4** | **负期望格子必须静默（不推不扫），且静默必须可见（告警落库）** | D4 |
| **P5** | **每个战法必须声明"它赚的是什么钱"（alpha 来源），不能归因的钱不收** | 审视 A2 |
| **P6** | **风格暴露诚实披露：放弃 V 形底第一波与打板，接受牛市初期跑输；不确定 ≠ 仓位** | 审视文档 + 过渡态设计（证据才配得上仓位）|

---

## 四、框架总览：三层 + 一矩阵 + 季节派生视图

```
┌─────────────────────────────────────────────────────────────┐
│ 状态层（基底，不改）：regime 四态 + 情绪温度（overlay，实验项） │
│   offensive / neutral / neutral_bearish / defensive           │
│   × 情绪温度：冰点 / 分歧 / 亢奋（market_emotion_daily）       │
│   × 顶部背离子标签（秋态，§6.6）                               │
├─────────────────────────────────────────────────────────────┤
│ 季节派生视图（纯函数，可测试）：                                │
│   夏 = offensive        秋 = offensive + 顶部背离子标签        │
│   春/惊蛰 = neutral + 情绪先行阳性                             │
│   冬 = neutral_bearish / defensive                            │
│   ⚠️ 单一事实源是四态引擎；季节只是 UI/话术层，无决策权         │
├─────────────────────────────────────────────────────────────┤
│ 战法层（重构核心）：战法×状态条件准入矩阵（第五节）              │
│   A 低吸整理族（现有 8 个有信号战法全部归此）                   │
│   B 趋势回调族（1 个入场 × 2 种出场，吃 R5）                    │
│   C 防守族（= 空仓 + 主线监测）                                │
├─────────────────────────────────────────────────────────────┤
│ 执行层（全部保留）：评分排序 / 买入闸门 / 仓位分档 / 止损 v2     │
│   / 持有期 / 模拟盘 A/B ｜ 仓位上限=阶梯定价（§7）              │
└─────────────────────────────────────────────────────────────┘
```

**与现状最大的不同不在零件，在连接方式**：状态层不再只是"评分权重调节器"，而是
**战法准入的第一道闸**（矩阵行选择）。**与周期框架设计的最大不同**：季节不是新状态层，
是派生视图 ⇒ 四态引擎一行不改、回退天然成立、无双头指挥。

---

## 五、核心：战法×状态条件准入矩阵

### 5.1 矩阵（v0 假设，逐格回测后填充/推翻）

| 战法（族） | alpha 来源（P5） | offensive | neutral | neutral_bearish | defensive |
|---|---|---|---|---|---|
| 单阳不破（A） | 震荡市多头蓄势后的突破惯性 | △ | ? | **禁** | **禁** |
| 均线回踩（A） | 上升趋势中回调至均线的支撑惯性——**offensive 态下即"趋势中继"载体** | ? | ? | **禁** | **禁** |
| 均线粘合突破（A） | 长期横盘压缩后的波动率释放 | **禁**（趋势市粘合多为趋势末端） | ? | **禁** | **禁** |
| 龙回头（A/B） | 强势股回调后的趋势惯性延续——**offensive 态下即"趋势中继"载体** | ? | ? | **禁** | **禁** |
| 涨停回马枪（A） | 涨停股回调至启动位的博弈修复 | ? | ? | **禁** | **禁** |
| 老鸭头/进二退一/仙人指路（A） | 各自形态惯性 | ? | ? | **禁** | **禁** |
| **主线首次回调（B，新）** | 反转确认后主线板块第一次集体回调（R5）。**1 个入场 × 2 种出场**：v0 = v2 固定止损+固定目标（脉冲收割语义）；v1 实验 = ATR 跟踪止损（慢牛持有语义，未验证过，单独跑）。出场模式按"宽度是否持续扩张"选择 | **主战场**（窗口只开一次） | — | — | — |
| 空仓 + 主线监测（C） | 阴跌市默认最优：不亏 + 为反转备弹药 | — | — | **默认** | **默认** |

**标注规则**：`?` = 待第 1 步分状态回测填充；`△` = 待定；**样本 <30 的格子按 `insufficient`
处理 = 禁**（保守）。**禁 = 不扫描不评估不展示信号**（不是"评估后不达标"）。

⚠️ **A 族在 offensive 的格子即"趋势中继"的载体**（均线回踩/龙回头在 offensive 的语义
就是趋势跟随的回调入场）⇒ **不另建追涨族**。若第 1 步验证"A 族 @ offensive"为负，
那时再议追涨 sleeve——不用未验证的方案补可能不存在的缺口。

### 5.2 为什么"禁"比"评估后不达标"好

① 签了计划单的用户照样会手动跟（信号在页面上）；② 样本污染全局统计（D2）；
③ 计算资源白花。矩阵化后：被禁战法**不扫描**，页面显示
"当前状态禁用低吸族（阴跌市历史期望为负，空仓待反转）"——**把"为什么不推"写在明处**（P4）。

### 5.3 格子级评估（替代现有双轨白名单）

```
白名单从"战法列表"改为 { (strategy, regime): pass/fail/insufficient } 字典
每个格子独立：
  · 样本 ≥ 30（沿用）
  · expectancy：均收益 > 0.3% 且 PF ≥ 1.2（沿用；新战法同一标准，不降标）
  · 滚动窗 = 该格子近 N 个有信号日（N 在第 1 步按各格子样本量定）
  · 全期轨**降级为参考项**（不再一票否决——修掉"永久否决"结构缺陷）
推送时只查"当前状态"那一列；状态切换当日格子快照落库（审计）。
```

---

## 六、拐点协议（右侧确认，不预测）

### 6.0 总开关：情绪维度的实验地位

`EMOTION_ACCELERATOR = off`（默认关）。6.1/6.3 中的情绪条件**在实验通过前只是展示项，
不进决策链**。实验 = 第 2 步"情绪分位 × A 族胜率"交互分析；通过则开 flag，不通过则
情绪维度永久降级为展示。风险不对称性（6.1 指向早卖=便宜、6.3 指向只观察=零成本）只降低
风险，**不豁免验证**。

### 6.1 转熊确认（offensive → defensive，快通道）

- 触发：MA20 下穿 MA60 或 高波动+趋势向下（现有 regime 逻辑）+ hysteresis 3 日
- **加速条件（实验项，flag 关）**：情绪三联败——涨停家数 <20 且 昨涨停溢价 <0 且
  连板断层/塌陷 ⇒ 不等 3 日，当日降半仓，次日确认后按 defensive 处理
- 理由：R2（熊市反弹短且弱），左侧每一天都在付学费

### 6.2 阴跌生存（neutral_bearish / defensive 稳态 = "冬"）

- 战法：禁 A 族，默认空仓（C 行）
- **结构性通道问题**：R3 表明熊市有结构性行情，但**是否为 A 族开"行业强"通道，
  是第 1 步的可检验问题**（按信号所在行业 20 日动量分组看期望），数据说话前不开通道
- 唯一日常工作：**主线监测**——行业主线 Top5 + 板块 20 日动量排序，产出
  "若反转，买什么"候选池（写进明日准备，不产生交易动作）

### 6.3 反弹识别（→ neutral = "春/惊蛰"，两层 + 阶梯）

- **层 1（情绪先行，实验项）**：涨停家数连续 3 日 >40 且 昨涨停溢价转正 且 连板高度 ≥3
  ⇒ 标签升级"反弹观察"（不解禁，只预扫 A 族信号入观察池，**仓位 0%**）
- **层 1.5**：层 1 阳性 + 宽度（涨跌家数比）连续 3 日 >1 ⇒ 解禁观察仓（**≤2 成**）
- **层 2（结构确认）**：宽度连续 5 日 >1 + 指数收复 MA20 ⇒ 解禁 A 族（**≤4 成**）
- **证伪**：观察期内再触 6.1 情绪三联败 ⇒ 打回 + 冷却 5 个交易日
- 过渡态语义 = **neutral + 情绪先行阳性**（不是独立状态）；四态引擎的默认态就是 neutral
  （L226-229），**"无状态"不可能发生**，无需额外兜底

### 6.4 反转确认（neutral → offensive = "夏"，B 族接管）

- 触发：现有 offensive 判定 + hysteresis
- **B 族窗口**：进入 offensive 后**第一个**"主线板块集体回调 ≥5%"的交易日（R5），
  **窗口只开一次**（第二次回调按普通 neutral 处理）
- 仓位：可至满配（分档限制仍由 position_sizing 管）

### 6.5 状态机转换图（汇总）

```
                 6.1 快通道(情绪三联败,实验项)
   offensive ────────────────────────► defensive
      ▲                                  │
      │ 6.4 趋势确认+B族首次回调窗口       │ R2 阴跌稳态("冬")
      │                                  ▼
   neutral ◄──── 6.3 层2(宽度+MA20) ───── neutral_bearish
      ▲    ▲                                │        ▲
      │    └─ 6.3 层1/1.5(情绪先行,阶梯) ───┘        │
      │          （证伪⇒打回+冷却5日）                 │
      └─ 6.1（普通转熊，3日hysteresis）───────────────┘
```

### 6.6 秋态（顶部背离子标签 = "秋"，收获协议）

offensive 态下的**子标签**（不新增状态）：宽度背离（指数 5 日新高但上涨占比 <0.50）持续 3 日。

生效后的纪律（R6）：
- 禁止新仓；`近 20 日涨幅 >20%` 的持仓提示主动止盈半仓（教练卡提醒，不强制）
- 止损从 ATR×2 收紧到 ATR×1（若 B 族 v1 已启用跟踪止损）
- 秋态持续 10 日或切冬态 ⇒ 3 日内降至空仓
- 拥挤度代理（v0 展示级）：Top3 行业成交额占全市场比重的分位数（`sector_daily` 可算）；
  两融历史**不存在**，三底校验中的"资金底"长期只能展示，不进回测

---

## 七、仓位：阶梯定价（替代"状态→仓位"一步跳）

仓位上限 = f(**已满足的确认条件数**)，不是状态名的一一映射——因为状态判定本身有假信号率
（冬→春假信号 40% 是可容忍的：假春天成本 ≈ 20%×0.4%×~5 笔 ≈ 本金 -0.4%，有亏损暂停纪律兜底；
真春天收益是 B 族整段利润。**真假比 >1:8 即划算**）。

| 确认阶梯 | 仓位上限 |
|---|---|
| 状态 = 冬（neutral_bearish/defensive） | **0%** |
| 春·层1（情绪先行阳性，实验项） | 0%（只观察） |
| 春·层1.5（+宽度 3 日） | ≤20%（单笔风险减半 0.4%） |
| 春·层2（+收复 MA20，5 日） | ≤40% |
| 夏（offensive） | 满配（单笔 0.8%，分档管） |
| 秋（顶部背离子标签） | 只减不加；10 日 → 0% |

"三底完成度 ■□□"（政策底/市场底/资金底）作为**展示项**进前端（数据补齐前市场底/
资金底只能展示不能回测）。

---

## 八、灰度上线（吸收自周期框架）

| 阶段 | 内容 | 开关 |
|---|---|---|
| W1 | 季节派生视图 + 矩阵**影子模式**（只记录不拦截） | `MATRIX_MODE=shadow` |
| W2 | 矩阵进入扫描门控（被禁战法不扫描） | `MATRIX_MODE=gate` |
| W3 | 格子化白名单替代全局白名单 | `WHITELIST_V2=1` |
| W4 | B 族上线（回测→模拟盘 A/B 之后） | `TRACK_B=1` |
| 回退 | 任何阶段 `off` = 完全回到现有行为。**四态引擎全程一行不改**（季节是派生层）⇒ 回退目标永远存在 | `SEASON_MODE=off` |

---

## 九、现有资产迁移清单

### 9.1 全部保留（不动）

regime 四态+hysteresis（**状态层基底，永不改**）｜评分体系+动态权重｜买入闸门（主力/解禁）｜
止损纪律 v2/持有期/仓位分档｜模拟盘 A/B｜market_emotion_daily/zzshare 连板梯队｜
滚动窗口告警（09-26 已修）｜矛盾雷达（全季节预警，秋态宽度背离复用）。

### 9.2 改造

| 资产 | 改造 |
|---|---|
| `strategies/market_regime.py` 的 TRENDING/OSCILLATING 二分 | **废弃**（D1 病灶），由 5.1 矩阵取代 |
| `recommendation.py` 双轨白名单 | 改为 5.3 格子字典；全期轨降级为参考 |
| 战法注册表 | 加 `alpha_source`（P5）+ `allowed_regimes`（初值 = 5.1） |
| 日批 `strategy_scan` | 扫描前查矩阵列，被禁战法跳过 |
| `paper_trading` | 仓位上限按 §7 阶梯注入 |

### 9.3 新增（每项过 9.4 门槛）

| 新增 | 优先级 | 依赖 |
|---|---|---|
| 分状态回测完整化 | **P0**（一切前提） | 无（数据已有） |
| 情绪×战法交互实验 | **P0**（决定情绪维度去留） | 无 |
| B 族"主线首次回调" | P1 | 板块动量 + 分状态回测 |
| 拐点协议编码（6.1-6.6） | P1 | 两项 P0 结论 |
| 秋态拥挤度代理 | P2 | sector_daily |
| 季节派生视图（前端"冬春夏秋"） | P2 | 矩阵上线后 |

### 9.4 验收门槛（无回测不上线）

1. **矩阵每个 `?`**：分状态重放撮合（口径同现状），样本 ≥30 才允许下结论；
   不足 = `insufficient` = 禁（保守）。
2. **显式淘汰规则**：某战法**全部可运行格子**中，样本 ≥30 的格子**均负期望** ⇒ 直接淘汰，
   不进矩阵（`ma_pullback` 46 样本大概率以 insufficient 形式出局，结局相同，死法不同——均诚实记录）。
3. **情绪维度**：第 2 步实验通过才开 `EMOTION_ACCELERATOR`；不通过 = 永久展示项。
4. **B 族**：历史 offensive 段"主线首次回调"事件回测 + ≥1 个月模拟盘；**验收标准与现有
   白名单同一（PF≥1.2），不降标**。ATR 跟踪止损（v1 出场）**单独**实验——A股 T+1 与跳空
   穿透两个折价因素未验证前，v0 出场 = v2 固定止损+固定目标。
5. **拐点协议整体**：以现有数据覆盖的阴跌段+回暖段做事件回放，对照"恒定状态（无协议）"。

---

## 十、实施顺序（依赖驱动）

```
第 1 步  分状态回测完整化            ← 承载 5 个待决问题：
           ① A 族各格子期望（填矩阵 ?）
           ② 龙回头@offensive（趋势中继载体是否成立）
           ③ 行业动量分组（结构性通道开不开）
           ④ MA20 斜率协变量（neutral 拆不拆的先决数据）
           ⑤ ma_pullback / ma_convergence_breakout 的死法（淘汰 or insufficient）
第 2 步  情绪×战法交互实验            ← 决定情绪维度去留（6.0 flag 开关依据）
第 3 步  矩阵落地 + strategy_scan 状态门控（W1 影子 → W2 门控）
第 4 步  白名单格子化（W3）
第 5 步  拐点协议编码（6.1-6.6）+ 仓位阶梯注入 paper_trading
第 6 步  B 族（回测 → 模拟盘 A/B → W4 上线）+ 季节派生视图前端
```

**第 1-2 步纯研究、零生产风险，且即使后面全不做，结论本身就能回答
"现有 8 个战法到底哪些值得留"。** 五态升级（若未来需要）为**条件触发**：
MA20 斜率协变量显著 且 各格子填充率 >70% 时才评估拆分，不排日历。

---

## 十一、明确放弃什么（P6，诚实披露）

| 放弃 | 代价 | 为什么认 |
|---|---|---|
| V 形反转第一波 | 924 式行情跑输 1-2 周 | 低吸定位固有成本；追第一根大阳 = 换负期望体系 |
| 打板/追涨停 | 涨停生态 +1.83%/日 体量 | 一字买不进（实测 749 子集），不可捕获 |
| 阴跌市的 A 族交易 | 熊市"无聊" | R4 实测该状态 A 族负期望，空仓即最优（结构性通道待验证，见 6.2） |
| 全天候圣杯 | — | P1：不存在；接受体系有空窗期 |
| 2015-2024 全周期回测 | 五态验证计划 | **数据不存在**（backtest_prices 仅约 3 年且非全市场）；可验证范围 2023-2026，约 1-2 个周期 |

---

## 十二、与两份源文档的关系

- 本文 v1.0 = `PLAN_战法体系重设计_20260926.md`（v0，执行内核）+ `战法体系重构_周期框架
  设计_20260926.md`（终点草图）的融合。**吸收自后者**：仓位梯度（改造为 §7 阶梯定价）、
  灰度上线（§八）、三底展示（§7 展示项）、季节隐喻（§四派生视图）、秋态收获协议（§6.6）、
  Ⅲ 型政策脉冲的结构性位置（B 族出场模式 2 选 1）。
- **未吸收**（数据/样本预算不允许）：五态独立状态层、8 个新策略（春雷/惊蛰/融冰/乘势/接力/
  补涨——右侧轨 3 个与"A 族 @ offensive"功能重复，其余待 B 族跑通后再议）、2015-2024 验证计划。
- `PLAN_2026-09-12.md` §2.2 白名单口径讨论：双轨制保留但全期轨降级为参考。
- `项目专业审视_不足与优化建议_20260920.md` A2/A3：A2 → 第 2 步实验 + 6.3；A3 → B 族 + 6.4。
- `PLAN_REGIME_BEARISH.md`"防御化权重被回测否决"**继续有效**：矩阵不动评分权重，只动战法
  准入——两者正交。**仓位阶梯（§7）与被否决的"防御化权重"不同物**：前者是仓位上限，
  后者是评分权重；若未来对阶梯有疑，须独立回测，不得援引旧结论直接否决。

---

## 附录 A：三轮交叉评审收敛表（2026-09-26）

| 议题 | 结论 |
|---|---|
| 空仓 vs 结构性行情 | 不开通道不写死；第 1 步按行业动量分组验证 |
| 情绪维度 | `EMOTION_ACCELERATOR=off` 默认关；6.1/6.3 标实验项 |
| B 族范围 | 1 个入场（主线首次回调）× 2 种出场；不建追涨族 |
| neutral 拆分 | 不拆；MA20 斜率作协变量记录，显著再议 |
| 淘汰规则 | 全部可运行格子中样本 ≥30 者均负期望 ⇒ 出局（7.4-2） |
| 融合映射 | 五季 = (regime × 情绪 × 顶背) 派生纯函数；四态引擎永不改 |
| 过渡态 | neutral + 情绪先行；引擎默认态已是 neutral，无状态漏洞不存在 |
| 仓位 | 阶梯定价 = f(确认条件数)；假信号阈值从损益不对称性推导，不倒推 |
| 状态层升级 | 条件触发（协变量显著+格子填充率>70%），不排日历 |
| 秋态拥挤度 | sector_daily 成交集中度代理，v0 展示级；两融历史缺，资金底长期展示 |
| 验收标准 | 新战法与现有白名单同一标准（PF≥1.2），不降标 |
