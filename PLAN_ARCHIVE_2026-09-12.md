# Plan 归档（截至 2026-09-12）

> 本文件把 2026-09-12 之前在仓库根目录散落的 11 份 plan **原文**合并归档（逐字保留，未删改）。
> **现行计划只有一份**：`PLAN_2026-09-12.md`。
> 代码注释里若引用旧文件名（如 `PLAN_MAINFORCE.md`、`PLAN_NEXT_PHASE.md`），内容就在本文对应章节，按下面的目录查找即可。

## 目录与处置

| # | 原文件 | 大小 | 最后修改 | 处置 |
|---|---|---|---|---|
| 1 | `PLAN_NEXT_PHASE.md` | 12.8 KB | 2026-09-12 00:18 | **并入主计划**：P0/P1/P2 全部拆解进 `PLAN_2026-09-12.md` |
| 2 | `PLAN_CROWDING_FACTOR.md` | 13.5 KB | 2026-09-05 03:10 | **已关闭**：残差化后 IC +0.041≈0，由主力筹码因子 / mainforce_overlay ×0.85 替代 |
| 3 | `PLAN_MAINFORCE.md` | 9.1 KB | 2026-09-05 02:26 | **阶段 1-2 已落地**；阶段 3 遗留项并入主计划 |
| 4 | `PLAN_PACK_MIGRATION.md` | 8.7 KB | 2026-09-06 12:32 | **主体已完成**（Phase 0/2 验收通过，日批已迁 Actions）；收尾项并入主计划 |
| 5 | `PLAN_REGIME_BEARISH.md` | 13.2 KB | 2026-09-03 02:31 | **主体已落地**（防御化权重被数据否决）；阶段 1.3/1.4 与阶段 2 遗留并入主计划 |
| 6 | `PLAN_ZZSHARE_INTEGRATION.md` | 5.5 KB | 2026-09-03 21:00 | **阶段 0/1 已落地**；阶段 1 验收门槛与阶段 2 并入主计划 |
| 7 | `PLAN_PAPER_TRADING.md` | 7.1 KB | 2026-08-31 20:48 | **代码已实现**（文档状态行写「规划中」已过时）；胜率对比校验并入主计划 |
| 8 | `PLAN_PAPER_RISK.md` | 5.9 KB | 2026-09-02 01:01 | **代码已实现**；阈值回调（risk_events 攒一个月）并入主计划 |
| 9 | `PLAN_TRADER_WORKFLOW.md` | 3.6 KB | 2026-09-10 01:56 | **Phase 1 大部落地**；企微定时推送与 Phase 2/3 并入主计划 |
| 10 | `先知雷达_功能Plan_v1.0.md` | 23.1 KB | 2026-09-06 04:04 | **多数已覆盖**；依赖数据积累的 4 项待办并入主计划 |
| 11 | `复盘Plan_2026-09-03.md` | 9.0 KB | 2026-09-03 23:26 | **当日作业已完成**（操作清单有时效性）；其中唯一跨期项（行业地缘抗性白名单）已并入主计划 |

## 说明

- 「处置」列给出每份计划在本次收拢中的去向：**已关闭 / 已落地 / 遗留项并入主计划**。
- 遗留项已按主题去重归类写进 `PLAN_2026-09-12.md`（同一件事只出现一次，并标注来源计划）。
- 原文里的 commit 号、日期、阈值等一律保持原样；与当前代码不一致之处（例如某计划写「规划中，未实施」但代码已实现），以主计划的状态判定为准。


---

## 1. 下一阶段路线图（2026-09-11 版，数据面收口 + 脱离 Supabase）

> 原文件：`PLAN_NEXT_PHASE.md`（12.8 KB，最后修改 2026-09-12 00:18）  
> 处置：**并入主计划**：P0/P1/P2 全部拆解进 `PLAN_2026-09-12.md`

<!-- ===== 以下为 PLAN_NEXT_PHASE.md 原文（逐字保留，未做改写）===== -->

# 下一阶段计划（2026-09-11 起）· 数据面收口 + 脱离 Supabase

> 承接：本文件 09-05 版（战法复健 / 龙虎榜 / 模拟盘卖出侧等已落地，未完成项归入本文 P2）。
> 定位：**功能层已基本完备，脆弱点全在"数据面"**——包新鲜度、包覆盖缺口、榜单口径、DB 配额。
> 本阶段目标：把这两天暴露的五类数据面问题一次性收口，并把数据库从 Supabase 迁到自有服务器。

---

## 背景：09-10 / 09-11 两天暴露的问题（已修，等验证）

| # | 问题 | 根因 | 修复 | 状态 |
|---|---|---|---|---|
| 1 | 日批拿到 09-09 旧包 | CDN `max-age=600` + Pages 部署空窗；重试循环只置 `_ready_checked`，`_db_fresh()` 看 mtime 判"刚下过=新鲜"→ 30 分钟一次都没重下 | 下载带唯一查询串击穿缓存；新增 `redownload()`，重试改为强制重下 | `1ddfacf` 待验证 |
| 2 | Render 挂着旧包供旧 K 线 | 常驻进程 `_ready_checked` 短路后永不复查；且 `_parse_pack_date` 只认 `%Y-%m-%d`（实际是 `YYYYMMDD`）→ 陈旧判定形同虚设 | 新增 30 分钟周期自检 `_maybe_refresh()`；日期双格式解析 | `1ddfacf` 待验证 |
| 3 | 战法扫描回源撞 WAF + Supabase 流量 456MB/天 | 包只覆盖 1431 只，战法池需 2052 只 → 缺 650 只每天回源（`backtest_prices` 单只全历史 1100 万行/21 天、`kline_cache` 30 万次查询） | 池口径改为 `market_snapshot`（战法池同口径 2052 只）；后端包超时 150→180 分钟 | `1ddfacf` 待验证 |
| 4 | 前端包 739/1534 只少最后一根 K 线（本地评分与后端差 3.8 分） | 腾讯复权序列收盘后**逐只**补齐，18:00 拉时近半还没更新；后端包 20:30 拉就齐 | 打包脚本新增"末根补齐重试"；前端 job 超时 60→90 分钟 | `041d552` 待验证 |
| 5 | 详情页 K 线停在旧日期 | 详情页"本地优先"不校验新鲜度 → 旧 IndexedDB 压过后端新数据 | 新增包新鲜度门（落后即回退后端） | `041d552` 已生效 |
| 6 | 本地榜有、后端榜没有（002452） | 后端榜单两阶段：简化分（动量+换手+PE）取前 100 当候选池 → 基本面型股票（简化 352 名/精算 69.8）永不入池 | 日批全量精算 → `ranking_live` 落库；后端当日直接读、盘中并入候选池 | `9baeef3` 待首跑 |
| 7 | 主力分桶恒为空 | ① `if mf_key != "none"` 让 none 桶永不累加 ② 850 行 `mainforce_signal` 全 NULL（前端保存快照路径漏写标签） | 两处修复 + 回填 36 行（accum 26/distribution 10） | `b1b5a1a` 已生效 |
| 8 | 市场状态权重停在 09-08 | `regime_cache_loop` 是 Render 15:40 循环（只读模式被关），而日批任务清单漏了它 | 日批新增 `market_regime` 任务（backfill 之后） | `6fd1d7b` 待首跑 |

### 09-11 夜间追加（用户当日巡检暴露）

| # | 问题 | 根因 | 修复 | 状态 |
|---|---|---|---|---|
| 9 | 消息分快照落后 3 天（停在 09-08） | `news_history_loop` 在 `_heavy()` 里 → 只读模式同样被关；日批漏配 | 日批新增 `news_snapshot` 任务（排在 score_snapshot 后） | `e8f45f2`，**已手工补跑 09-11（54 只）** |
| 10 | 回测中心最新报告停在 09-05 | `backtest_report_loop` 同样在 `_heavy()`；且报告只写文件，**Render 文件系统是临时的**（部署即清空） | 日批新增 `weekly_report`（周五）+ 报告正文落库 `backtest_reports`，前端库→文件兜底；追加「本周无报任意工作日自愈 / `--tasks weekly_report` 点名即跑」；`regime_review` 逐股远程查询改批量加载 | ✅ **2026-09-11 23:49 已上线**（Actions run #9，83 秒完成，前端回测中心最新一份 = `backtest_report_weekly_20260911_154945.md`） |
| 11 | 决策简报 AI 暂不可用（空响应） | `call_llm` 收到 HTTP 200 但 content 为空时**直接 return ""**（注释宣称重试实则没有）；推理模型思考与答案共用 `max_tokens` | 空响应纳入重试 + 2 次起切 `LLM_MODEL_FALLBACK` + `finish_reason=length` 时 max_tokens 翻倍（上限 32768）+ 失败原因透传到简报与 `/api/flash/status` | `e8f45f2`/`60d00ba`，本地实测真实简报提示词正常（Render 待部署后看 `last_error`） |
| 12 | Supabase egress 364→458MB/天（免费档 167MB/天） | 大结果集读：`SELECT * backtest_prices WHERE code=$1`（18037 次/1112 万行）、`mainflow_history` 整表（99 次/724 万行）、`flash_news` 整行（6534 次/187 万行） | 列裁剪（去掉 id/code/name）+ `load_prices` 30min 进程缓存 + `load_flow_map`/`load_flow` 30min 缓存 + `load_raw_items` 120s 缓存（写入即失效）+ id 查询收窄到近 3 天 | `e8f45f2`；今晚起叠加"包覆盖 2052 只不再回源" |
| 13 | 09-12 复查 egress 仍 458MB | ① 09-11 含**一次性迁移 pg_dump 174MB**（01:25-01:40 执行）→ 常规约 284MB；② **`_count_entries()` 为取 `len()` 全文读 50 条诊断(2.3KB/条)+300 条快讯**，而前端每个开着的标签页**每 5 分钟**调 `/api/flash/backup` → `flash_analyses` 749 次/天≈40MB + `flash_news` 311 次/天≈19MB | `_count_entries` 改 `COUNT(*)`（LEAST 对齐原 LIMIT 上限，保证镜像判据不变）；快讯去重无新条目直接跳过；`save_raw_data` 收窄 | `b29ee00`/`dc39df3`；**下一验证点=09-14 周一完整日批** |
| 14 | mainflow_history 整表读（8.7MB/次，~36MB/天） | `load_flow_map()` 全表读：日批 `mainforce_state` 1 次/天 + Render `gate_states_for_signals`（已 30min 缓存） | 彻底解法=让数据包携带资金流序列后从 pack 读（需改主力行为读取层，评估正确性后再做） | 观察项 |
| 15 | 「浏览器镜像」空转 | 它保护的 9 个文件**全部已迁库**（flash_state/flash_analyses/flash_reviews/tracking_state/macro_history/etf_close/schedule_state），文件只剩空壳；**而真正还在文件里的 4 项（财经日历/LLM 用量/K线缓存/数据包）它从来没覆盖过** | 前端轮询退役；新增 `app/data_files.py`（清单+DB 水位+启动/每 6h 核对+关键项企微告警，每天最多一次）+ `GET /api/system/runtime-files`；`/flash/backup`、`/flash/restore` 降级为手动工具 | ✅ `848a0d5` |
| 16 | 第 5 个「只读模式受害者」：财经日历停在 09-04 | `calendar_loop` 在 `_heavy()` 里被关，日批又漏配 → 扫描发现（新预警模块直接把「更新于 09-04」报了出来） | 日批新增 `calendar` 任务（实测 130 条/1 秒；失败保留旧缓存不抛异常） | ✅ `848a0d5` |

---

## P0 · 今天 ~ 明早（应急 + 验收）

### 1. Supabase 配额决策（**今天 09-11 宽限期最后一天**）
- **现状**：宽限期内读写正常；**阶段 1 冷备库已完成**——腾讯云 `stock-postgres`（PG17，内网 only）已含全量数据，逐表行数一致（backtest_prices 478,795 / mainflow 81,735 / mainforce_state 4,459 / kline_cache 2,220 / ranking_history 850 …），每日 03:00 自动备份（保留 14 份）。
- **已定**：暂不买域名 → 前端继续吃 Supabase，接受 402 一来切本地模式。
- **今天要做的判断**：明早看 Supabase 流量面板
  - 若流量降到配额内（免费档 ~167MB/天）→ 维持现状，等域名
  - 若仍 400MB+ → 二选一：① Supabase Pro $25 保命 ② 立即上域名走完整迁移（P1）
- **应急动作（0 风险，随时可做）**：
  1. 前端排行榜页开「本地计算」开关（零 API，包在 Pages）→ 核心评分可用
  2. 启用冷备库：重跑 `bash deploy/migrate_supabase.sh` 同步 → `DATABASE_URL` 指向 `postgres:5432` → `docker compose restart backend`

### 2. 今晚 Actions 验收（5 个数字，日志里逐条对）
```
① backend-pack  → 「战法池口径覆盖: 2052/2052（缺口 0，目标 0）」      ← 缺 659 只归零
② kline-data    → 「末根补齐第 1 轮: N 只停在 20260910 之前」→ 残留应趋 0
③ daily-batch   → 「[2/15] 市场状态: 2026-09-11 neutral 权重={...}」  ← regime 追到当日
④ daily-batch   → 「[12/15] 全量精算榜: 300 行落库（date=…, 池=1561, 榜首 …）」
⑤ daily-batch   → 日志里 `[WAF] K线请求被拦截` / `判过期走实时拉取` 应大幅减少；
                  且不再出现「数据包日期 != 今天」
```

### 3. 前端/接口验证（明早）
| 检查点 | 期望 |
|---|---|
| 排行榜（API 模式） | 002452 出现在榜上；`/api/score/batch/top` 的 `total=1561` |
| 详情页 000567 | 资金面出现第 5 因子「主力净流入」（等 Render 部署到最新 commit） |
| BucketStats | `mainforce.none` 有样本（814 行基数）；accum 26 / distribution 10（n<20 灰显） |
| 详情页 K 线 | 末根 = 09-10/09-11（前端包补齐 + 新鲜度门生效） |

### 4. 两项配置补齐（需你操作，各 1 分钟）
- **GitHub secrets**：`LLM_BASE_URL=https://api.siliconflow.cn/v1`、`LLM_MODEL=deepseek-ai/DeepSeek-R1`
  （现为空 → 矛盾报告/日报的 AI 解读全部失败：`Invalid URL '/chat/completions'`）
- **Render**：Events 确认部署到最新 commit（`6fd1d7b`/`190c988`），没有就 Manual Deploy

---

## P1 · 本周末（迁移闭环：彻底脱离 Supabase）

**唯一前置**：买一个域名（¥30-60/年）→ NS 指到 Cloudflare。
（国内地域 80/443 需 ICP 备案走不通；命名隧道是**出站**方案，不需要备案。）

1. **命名隧道替换快速隧道**：cloudflared named tunnel → `https://api.<域名>` 固定地址（快速隧道重启即换域名，绝不能当生产）
2. **DB 切到服务器本机 Postgres**：重跑 migrate 同步 → 改 `DATABASE_URL` → 重启 → 验证
3. **解决"外部写入方访问 DB"**（关键设计决策，三选一）：
   - **方案 α（推荐，最干净）**：API + 日批都搬到腾讯云（服务器自己写自己的库）；Actions 只留**不碰库**的 `kline-data`；`backend-pack` 改造为不读库或一并搬走
   - **方案 β**：`cloudflared access tcp` 暴露 `db.<域名>:5432`（服务令牌）给 Actions/Render（改动小，链路多一跳）
   - **方案 γ**：Postgres 开公网 + IP 白名单（**不推荐**：GitHub/Render 出口 IP 动态）
4. **前端切换**：`VITE_API_BASE_URL=https://api.<域名>/api` → push → deploy-preview 重建 → 硬刷新验证
5. **下线与归档**：Render 观察 1-2 天后停用；Supabase 保留只读 1 周后归档
6. **备份外迁**：`deploy/backups/*.dump` 定期 scp/OSS 出一份（防单机故障）

**验收**：`api.<域名>/api/health` 200 · 前端全页面可用（登录/排行/详情/日报/模拟盘）· 日批写库成功 · Supabase 流量归零。

---

## P1 · 本周（收口功能缺口）

1. **决策简报自动化**：日批加 `trader_brief`（postmarket）+ 企微推送 —— 现在只有"打开页面才生成"，盘前 9:10 / 盘后 19:35 推送是 PLAN_TRADER_WORKFLOW 的 Phase 1 收尾项
2. **榜单单一事实源评审**：前端「本地模式」与服务端「全量精算榜」仍可能有几分差异 → 要么本地模式直接展示服务端榜，要么在 UI 上标注口径（避免又被当成 bug）
3. **`backtest_prices` 读放大治理**：即便包齐了，`SELECT * FROM backtest_prices WHERE code=$1`（单只全历史）仍是最大 egress 源 → 加列裁剪（只要 date/close）+ 进程内 LRU
4. **回填配额**：backtest_prices 每日 150 只 → 594 只缺口要 4 天，按"距评分池远近"排优先级

---

## P2 · 本月（承接 09-05 版未完成项）

- 个股层矛盾扫描：`评分 ≥65 买入` ∩ `出货嫌疑` → 自动降级提醒 + 企微标注
- 消息分验证定版：IC ≥0.10（需 20 个快照日，现 +0.202/3 日）
- MAINFORCE_MODE=auto 观察期结论（需覆盖一次大跌段）
- Watchlist / 组合页评分口径统一到前端引擎
- backtest_prices 池扩展 544 → 3000 只（用 kline pack 反灌）
- LLM 复盘提效：R1 → V3 分档（R1 只做周度深度复盘）

---

## 观察清单（不投开发，等阈值）

| 项 | 阈值 | 当前 |
|---|---|---|
| 消息分 IC | ≥0.10（20 快照日） | +0.202 / 3 日 |
| 主力因子 offensive 段 | 样本 ≥500 | ~330 |
| 矛盾扫描 | ≥60 条兑现率统计 | 4 条 |
| 主力分桶（吸筹/出货） | n ≥20 | accum 26 / distribution 10 |
| 全量精算榜 vs 前端本地榜 | 差异 ≤1 分 | 待首跑对比 |
| Supabase 流量 | <167MB/天（免费档） | 456MB/天（09-10） |

---

## 风险与回滚

| 场景 | 回滚 / 兜底 |
|---|---|
| Supabase 402 / 配额 | 冷备库已就绪：migrate 同步 → `DATABASE_URL` 切换 → restart；代码层无改动，改回 Supabase 串即回滚 |
| Actions 包失败 | 读侧 `_is_stale()` 自动回退 DB（不会静默用两三天前的数据算分） |
| API 不可用 / 隧道失效 | 前端开「本地计算」（包在 Pages，零 API） |
| 迁移数据不一致 | 迁移脚本自带源/目标行数比对；Supabase 原库不删，只读保留 1 周 |


---

## 2. 拥挤度因子（方案 B：regime 闸门 + 乘数）

> 原文件：`PLAN_CROWDING_FACTOR.md`（13.5 KB，最后修改 2026-09-05 03:10）  
> 处置：**已关闭**：残差化后 IC +0.041≈0，由主力筹码因子 / mainforce_overlay ×0.85 替代

<!-- ===== 以下为 PLAN_CROWDING_FACTOR.md 原文（逐字保留，未做改写）===== -->

# 拥挤度惩罚因子：验证与上线计划（PLAN_CROWDING_FACTOR）

> 触发：2026-09-03。大跌段因子诊断显示「高质量/高成长 ≠ 抗跌」，资金面是唯一稳定正预测力。
> 目标：以**价格代理的拥挤度**（前期涨幅/距高点）识别「机构拥挤、无承接盘」风险，在震荡/震荡偏空段惩罚拥挤、保护超跌低吸，**不新增常设权重维度**。
> 依赖：DB（Supabase）已恢复可用（2026-09-03 实测可写）。价格历史：`backtest_prices` / `kline_cache`。
> 创建：2026-09-03

---

## 一、诊断结论与待固化证据

大跌段（9/2）因子相关性观察（来自临时分析，**数字需在阶段 0.1 固化脚本后正式落库**）：

| 维度 | 观察相关 | 解读 |
|---|---|---|
| 技术 | ≈ -0.064 | 震荡市动量失效、假突破多（与既有回测「技术面预测力≈0」一致） |
| 基本面 | ≈ +0.077 | 弱正，弱于资金面 |
| 资金 | ≈ +0.094 | **唯一稳定正预测力** → 震荡市是资金博弈市 |
| 成长 | 大跌段高分组更惨（≈ -2.33% vs 大盘 ≈ -1.14%） | 高成长=高估值=长久期，贴现率敏感 |
| 质量 | 大跌段同跌 | 高质量=机构重仓白马=拥挤交易，缺承接盘 |

**核心假设（待验证）**：
- H1：在 neutral / neutral_bearish / defensive 段，**拥挤度与未来 2/5 日收益负相关**（越拥挤越该回避）
- H2：拥挤度惩罚只在这些段有效；**offensive 段拥挤=动量，惩罚方向相反**（此为否决"方案 A 常设维度"的关键）
- H3：「高质量 × 低拥挤」抗跌显著优于「高质量 × 高拥挤」→ 质量因子需拥挤度过滤（交互效应）

⚠️ 相关性数字混入了大跌段市场 beta。正式分析需**残差化/分组去 beta**（沪深300 回归残差或按市值-行业分桶后组内中性化），否则会高估"跌多是因子问题"。

---

## 二、为什么不做"新增第六维度/大调权"（否决方案 A）

用户方案 A 将权重改为 24/21/23/14/9/10（另有 1% 未配平）会带来 3 个问题：
1. **常设惩罚在进攻段方向相反**：offensive 段的收益来源就是动量/强者恒强，拥挤惩罚会把排行首尾颠倒 → 必须 regime 条件触发，等价于"不是第六维，是第五维的调节项"。
2. 权重重构污染回测基线：现 27/23/23/17/11 是 350 条已验证快照对照出的，重配无法隔离"是拥挤因子生效还是权重漂移生效"。
3. 拥挤度本质是**风险调节**，不是 alpha 维度——按乘数实现，回测对照只需"开关两态"，干净。

**采用方案 B（乘数）但加 regime 闸门**，且实验期不改线上排行（见阶段 2）。

---

## 三、拥挤度度量设计（纯函数，可离线回测）

新增 `backend/app/backtest/crowding.py`（评分引擎与离线回测共用，避免两套实现）：

```python
def crowding_metrics(bars: list) -> dict | None:
    """bars: [{date, open, high, low, close}, ...] 升序，至少 21 根。"""
    if not bars or len(bars) < 21:
        return None
    last = bars[-1]["close"]
    def px(n):  # 第 n 个交易日前收盘（含今天）
        return bars[-n]["close"] if len(bars) >= n else bars[0]["close"]
    ret_5  = (last / px(5)  - 1) * 100
    ret_20 = (last / px(21) - 1) * 100
    ret_60 = (last / px(61) - 1) * 100 if len(bars) >= 61 else None
    high_250 = max(b["high"] for b in bars[-250:]) if len(bars) >= 21 else max(b["high"] for b in bars)
    return {"ret_5": round(ret_5, 2), "ret_20": round(ret_20, 2),
            "ret_60": round(ret_60, 2) if ret_60 is not None else None,
            "dist_from_high": round((high_250 - last) / high_250 * 100, 2)}

def crowding_score(m: dict) -> float:
    """0~100，越低=越拥挤。仅价格，不依赖财报/行业映射。"""
    s = 100.0
    r20 = m.get("ret_20") or 0
    # 近 20 日涨幅：>30 → -50；>20 → -40；>10 → -25
    if r20 > 30: s -= 50
    elif r20 > 20: s -= 40
    elif r20 > 10: s -= 25
    r60 = m.get("ret_60")
    if r60 is not None and r60 > 50: s -= 20
    if (m.get("dist_from_high") or 100) < 5: s -= 15
    return max(0.0, min(100.0, s))
```

**与用户原稿的 3 处差异（有意为之，需在评审时解释）**：
1. **去掉"超跌加分"分支**：用户版 `ret_20d < -10 → +20`。超跌反弹捕捉已由技术面/资金面承担，拥挤度里再加分=双计数且会反向干扰"惩罚高位"的单调性。若超跌加分有价值，单独成指标回测。
2. **补 ret_5**：连涨后刚松动（近 5 日动能衰竭但 20 日仍高）是更早的拥挤瓦解信号，供交互检验用，不进基础 score。
3. **dist_from_high 用高 250 日窗口截断**：`bars[-250:]` 防止上市不足 250 日时误用全历史起点。

**惩罚乘数（先保守，[0.85, 1.0]）**：

```python
def crowding_multiplier(crowd_score: float) -> float:
    if crowd_score < 30:  return 0.85
    if crowd_score < 50:  return 0.95
    if crowd_score < 70:  return 1.00
    return 1.00   # 上限不加分（超跌奖励交给技术面）
```

不变量：乘数**只降不升**，`final = raw_total × mult`，mult ∈ [0.85, 1.0]。

---

## 四、阶段 0：立即执行（不依赖回测，纯离线）

### 0.1 固化因子分析脚本（消除"临时数字漂移"）
新建 `scripts/factor_analysis.py`，从 `ranking_history` 已验证快照（复用 `load_verified_snapshots` 口径）输出正式报告到 `backend/backtest_reports/factor_YYYYMMDD.md`：
- 每快照日 → 未来 2/5 交易日收益（用 `_load_prices_map` 定长口径，**不是**"到今天"）
- 分维度 IC（Spearman，维度分 vs 未来收益），输出：整体 / 按 regime 分段 / **沪深300 beta 残差化后**三种口径对照
- 大跌段（9/2）单独切片复核用户引用的数字

### 0.2 拥挤度回顾性对照（先于阶段 1，马上能出结果）
- 对 8/20~9/2 每日快照记录（`ranking_history` 含 dimensions_json + price），回填 `crowding_score/mult`（价格取自 `backtest_prices`，快照日当日前 60 根）
- 输出 3 张表：
  - A. 原 total Top10 的持有 2/5 日胜率/收益（对照组，即现 64.3%/+1.42% 基线口径）
  - B. `total × mult` 重新 Top10 后的胜率/收益
  - C. 分桶矩阵：`{质量高/低} × {拥挤高/低}` 的未来收益（交互效应初判）
- **验收**：C 表若「高质量×低拥挤 > 高质量×高拥挤」成立 → 阶段 1 全量推进；若 B 表无改善 → 停下，先调度量参数再复测

### 0.4 阶段 0 初版结果（2026-09-03 已执行）

工具：`scripts/factor_analysis.py`（450 条维度快照，8/20~9/2 每日 Top50；报告在 `backend/backtest_reports/factor_*.md`）

| # | 发现 | 数据 | 含义 |
|---|---|---|---|
| 1 | **拥挤分在震荡偏空段对 2 日收益显著正 IC** | 持2日去超额 +0.268(n=100)，早段 -0.004 | H1 初步成立，但**仅偏空段** → 支持「regime 条件启用」而非常设 |
| 2 | 资金面"强预测"主要是市场 beta | 原始 0.080 → 去超额 0.006；早段 0.036→0.010 | 用户口径「资金 +0.094 唯一最强」**未复现**；去 beta 后资金面近归零 |
| 3 | 震荡偏空段最强的两个：基本面 / 拥挤分 | 基本面 0.357/0.232(去超额)｜拥挤分 0.257/0.268(去超额) | 与既有回测「基本面最强 +0.109」一致 |
| 4 | total 在偏空段预测力≈0 | -0.046 ~ -0.067 | 印证「高分≠抗跌」 |
| 5 | A/B 惩罚几乎不改排序 | Top10 完全相同；Top20 仅持2日胜率 +0.7pt/均 +0.07pt | **高分池"咬合面"极窄**：450 条仅 7 条触发惩罚 |
| 6 | 拥挤分桶单调（方向支持） | 持2日 高拥挤 -6.85%(n=2)/中 -4.29%(n=5)/低 -0.04%(n=343) | 方向对但高拥挤样本 n≤7，仅作方向参考 |
| 7 | 质量×拥挤交互样本不足 | 质量高×低拥挤 n=2（-5.25%）不可信 | C 交互**暂不可下结论**，需 9/4/9/9 补样本 |

**结构性诊断（为何惩罚触发这么少）**：
- `ranking_history` 每日固定 Top50 高分股 = **多维度均衡**，20 日暴涨 >10% 的"拥挤股"很少能同时拿高分进池；
- 8/20~9/2 已含大跌段，拥挤股价格自然回落 → 拥挤度量自我消除，观察滞后；
- `backtest_prices` 仅 544 只（评分池），**无法重建 7 月大涨段全市场截面**补历史高拥挤样本 → 高拥挤样本只能靠 9/4 起自然累积。

**对设计的修正（进 9/4 正式回测前需定）**：
- D1 作用面：total 乘数对"总排序 Top10/20"影响预计持续很小。备选作用域：① 只作 `neutral_bearish` 排名层过滤（一票否决拥挤分<30）；② 作用于更大候选池（Top50 内重排）；③ 改在 `technical` 维度内部作拥挤子项。9/4 A/B 对比 Top10/20/50 三档即可回答 D1。
- D2 拥挤度量观察时点：用"快照日往前 20 日涨幅"在下跌段天然偏小 → 需额外报告"9/1 大跌前一日各股拥挤分"，检验**瓦解前**识别能力（预检样本虽少但方向关键）。

---

## 五、阶段 1：P0 正式回测（**9/4 周五 16:30 收盘后**，勿用 9/5/9/6 周末）

| 任务 | 时间 | 说明 |
|---|---|---|
| 固定 T+2/T+5 回测（9/2 大跌快照满期复核） | **9/4 盘后**（T+2 满）；**9/9 盘后**（T+5 满，9/2 起第 5 交易日） | 候选排序：现权重 total / ×crowd_mult / defensive / 原排名不加权 |
| 五维度相关性重跑（去 beta） | 9/4 盘后 | 阶段 0.1 脚本全量跑 |
| 质量×拥挤度交互检验 | 9/4 盘后初版 / 9/9 定版 | 分 4 组比持有 5 日均收益 + 胜率 |

方法统一沿用上次 450 条快照口径：每日 Top10 模拟选股，比胜率 + 平均收益（两持有期口径一致才采纳）。

**验收门槛**（不达标不固化）：
- B 组（×crowd）在 **neutral / neutral_bearish** 段 5 日口径同时 ≥ 对照 A：胜率 +2pt 或 均收益 +0.3pt
- 且 offensive 段（若有样本）B 不劣于 A（否则证明需 regime 闸门，收益减半保留）
- 交互矩阵「高质量×低拥挤」显著（t 或样本量下均值）优于「高质量×高拥挤」

---

## 六、阶段 2：验证通过后上线（regime 条件乘数，默认关）

改动范围（若上线）：
1. `app/backtest/crowding.py`：`crowding_score/multiplier`（阶段 0.2 已实现并测过）
2. `app/scoring/engine.py` `score_stock/score_batch`：在维度归一化得到 `total` 后应用乘数
   - 仅当 `regime ∈ {neutral, neutral_bearish, defensive}` 时 `total_final = total × mult`
   - `offensive` / regime 未知 / 价格不足 21 根 → 恒 1.0（不降分）
   - 乘数**不参与** `weighted_score` 明细（保持 sum(weighted)==raw total 自洽）；`total_final` 只用于排序输出
   - 上线开关：`CROWDING_MODE=off|auto` 环境变量，默认 `off`，先在测试 env `auto` 观察
3. `ranking_history` 快照：同时落 `total_score`（原）+ `crowd_score`/`crowd_mult` 列（新加），保证线上生效后可随时用原始分回滚对照
4. 信号判定（signal/signal_level 阈值）**仍用 raw total**，只排序用 ×mult —— 避免同一股票因拥挤被降级成"卖出"的语义混乱（先评审再定）

**回退**：`CROWDING_MODE=off` 即回滚，无迁移成本。观察期 ≥ 1 周（覆盖 1 次大跌段）再决定是否放宽 mult 到 [0.8,1.0] 或纳入 offensive。

---

## 七、风险与不变量

| 风险 | 应对 |
|---|---|
| 拥挤度与现有技术动量同源，削弱 offensive 段收益 | regime 闸门 + 默认 off；回测 offensive 段必须不劣化 |
| 上市 <60 根次新 / 停牌缺口导致 ret_60 失真 | len 检查降级（None 即不算该项），价格 <21 根返回 None→mult=1 |
| 双计数（技术面已含动量/RSI） | mult 只降不升、幅度保守；回测对照 A/B 现净增量 |
| 历史快照只有 Top10/Top50，无全市场截面 | 交互检验在下行/拥挤段样本小 → 结论标注样本量，不夸大 |
| 9/2 快照持有期未满 | 已排 9/4(T+2) / 9/9(T+5) 节点 |
| 乘数改变排序但推荐/推送白名单不变（两套逻辑） | 上线文档写明影响面仅排行/快照验证，战法推送不受影响 |

**不变量**：不改维度权重、不加常设维度、不动 `_score_*` 指标；所有改动先在离线/脚本层出报告，看数据再动线上。

---

## 八、执行清单

- [x] 0.1 `scripts/factor_analysis.py` + 正式相关性报告（含残差化口径，2026-09-03）
- [x] 0.2 `app/backtest/crowding.py`（metrics/score/multiplier）+ 回顾性 A/B/C 三表（2026-09-03，结论见 0.4）
- [x] 0.4 D1 决策（2026-09-05 定版，**见下方 D1 结论**）
- [ ] ~~P0 9/4 T+2 回测 + 阶段 2 上线~~ → **由 PLAN_MAINFORCE 主力筹码因子替代，本计划关闭**

---

## 九、D1 结论（2026-09-05，全池截面 2,314 样本定版）：拥挤度被主力筹码因子覆盖，计划关闭

同一批截面（4 日 × 583 股）同时计算 `crowding_score`（价格代理）与
`price_pos`（筹码位置，PLAN_MAINFORCE）的 5 日前瞻 IC：

| 指标 | 数值 |
|---|---|
| IC(crowding_score, fwd5) | +0.123 |
| IC(price_pos, fwd5) | **-0.343**（强度 3 倍） |
| corr(crowding, price_pos) | -0.281（相关但不同源） |
| **crowding 对 price_pos 残差化后 IC** | **+0.041 ≈ 0** |

**结论**：拥挤度想要捕捉的「机构拥挤、高位无承接」信息，筹码位置因子以
3 倍强度完整覆盖；残差化后拥挤度几乎无增量。**不再上线 CROWDING_MODE
乘数**，高位风险统一由 `mainforce_overlay` 的出货嫌疑乘数（×0.85）承担
——它同时携带了主力资金流与阶段信息，比纯价格代理更完整。

`app/backtest/crowding.py` 保留（纯函数、被 factor_analysis 引用），不再接入线上。
本计划由 **PLAN_MAINFORCE.md** 接棒。


---

## 3. 主力思维体系（筹码位置 / 资金流 / 阶段标签）

> 原文件：`PLAN_MAINFORCE.md`（9.1 KB，最后修改 2026-09-05 02:26）  
> 处置：**阶段 1-2 已落地**；阶段 3 遗留项并入主计划

<!-- ===== 以下为 PLAN_MAINFORCE.md 原文（逐字保留，未做改写）===== -->

# 主力行为因子：假设 → 验证 → 上线（PLAN_MAINFORCE）

> 触发：2026-09-05。回测数据验证阶段 + 因子诊断结论「综合分 IC≈0，散户因子（技术/估值/成长/质量）解释力弱」。
> 立场：跳出散户思维（这只股票好不好），换主力思维（筹码在谁手里、主力走到哪一步）。
> 创建：2026-09-05 ｜ 状态：阶段 1 数据回填 + 全池截面验证中

---

## 一、为什么是"主力思维"（诊断结论）

已有回测证据（`backtest_reports/factor_20260903_2030.md`）：

| 证据 | 数字 | 散户思维的失败点 |
|---|---|---|
| 综合分 total 对未来 2/5 日 IC | 0.018 / 0.032（≈0） | 五维加权选出的"好股票"不涨 |
| 资金面去 beta 后 IC | 0.080 → 0.006 | 现资金面只是量价/换手，不是主力行为 |
| 基本面是唯一稳定因子 | 0.117 / 0.232（偏空段） | 但只回答"公司好不好"，不回答"主力做没做" |
| 拥挤分在震荡偏空段 IC | 0.268 | 方向对了，但 Top50 高分池咬合面窄、触发少 |

**核心判断**：评分体系在回答"什么是好公司/好形态"，没有一条指标在回答
"**主力现在在做什么**"。主力资金的进出才是 A 股个股 mid-term 行情的直接原因——
吸筹 → 洗盘 → 拉升 → 出货，散户亏损的根源就是在"出货段"接了主力换手的筹码。

## 二、大胆假设（可证伪）

| # | 假设 | 验证方法 | 通过标准 |
|---|---|---|---|
| H1 | **获利盘/筹码位置**：低位低获利盘（price_pos 低）未来收益为正，高位高获利盘为负 | 全池截面 IC + 五分位单调性 | 持5日 IC 绝对值 ≥0.03 且分桶单调 |
| H2 | **筹码集中度**：集中度低（90% 成本区间窄）= 主力控盘，未来收益为正 | 同上 | 同上 |
| H3 | **主力资金流**：5 日主力净流入占成交额高 → 未来收益为正（吸筹跟踪） | 同上 | 同上 |
| H4 | **主力阶段标签**：吸筹段 > 盘整段平均；出货段/下跌段 < 平均 | 分阶段未来收益表 | 吸筹-出货收益差 ≥1pt/5日 |
| H5 | **组合信号**：「低位筹码密集 × 主力净流入」显著跑赢全池平均 | 命中组 vs 其余 | 差 ≥1pt/5日 且胜率 ≥55% |
| H6 | **撮合现实化**：涨停一字买不进的信号剔除后，战法回测胜率更真实（尤其龙回头 13.2% 那类） | engine.py 前后对照 | 剔除数占比报告，不设门槛 |

## 三、度量设计（已实现，`backend/app/mainforce/`）

### chips.py 筹码分布（CYQ 轮动模型，纯 OHLCV + 流通股本）
- 每根K线成交量摊入 [low, high] 价格网格（150 档）；每日按**真实换手率**（量/流通股本，快照流通市值反推）衰减老筹码
- 输出：`winner_ratio` 获利盘、`cost_bias` 现价/平均成本溢价、`concentration` 筹码集中度（P95-P5)/(P95+P5)、`price_pos` 现价在 90% 筹码区间位置
- 无流通股本降级：量/MA120量 × 2% 基准换手

### flow.py 主力资金流（`mainflow_history` 表，120 日）
- 主源**新浪** MoneyFlow（r0_net+r1_net = 特大+大单净流入 ≈ 东财"主力"口径；占比 = 净流入/四档成交额合计）
- 东财 fflow/daykline 兜底（2026-09-05 实测连续 ~600 请求后被封 IP，勿再高频打东财）
- 流通股本：market_snapshot（腾讯 15:10 快照 float_cap/price 反推，19.4B 股与东财逐股核对一致）

### phases.py 主力阶段（量价规则，阈值集中一处便于回调）
拉升 / 出货 / 洗盘 / 吸筹 / 下跌 / 盘整 六标签，证据强度从上往下判。

## 四、阶段 1：全池截面验证（`scripts/mainforce_factor_backtest.py`）

- **全池**截面（非 Top50 高分池——解决拥挤因子"咬合面窄"的结构性问题）：
  每 5 个交易日一个截面 × 全部可交易股票 ≈ 24 截面 × 500+ 只 ≈ **9000+ 样本**
- 口径与 factor_analysis.py 一致：定长行号前瞻收益（T+5/T+10）、原始 + 去市场超额双口径、Spearman IC、五分位单调性
- 报告：`backend/backtest_reports/mainforce_YYYYMMDD_HHMM.md`

## 五、阶段 2：上线（验证通过才做，带开关）

1. `scoring/engine.py`：资金面子项增加「主力行为」——筹码位置/主力净流入强度/阶段标签（权重从资金面内部拆出，不动五维权重）
2. 环境开关 `MAINFORCE_MODE=off|auto`（默认 off，先观察）
3. `ranking_history` 快照同步落主力因子值，保证可回滚对照
4. 战法准入：吸筹段标的单独成池，喂给低吸类战法（单阳不破/均线回踩）

## 六、风险与不变量

| 风险 | 应对 |
|---|---|
| 新浪与东财主力口径不同（r0 阈值差异） | 全数据集单一来源（新浪），内部一致即可回测；上线后固定新浪源 |
| 流通股本是"当前快照反推"，历史增发/解禁有误差 | 近 120 日窗口内误差小；筹码指标按相对分位使用，弱化绝对误差 |
| 数据源风控 | 新浪礼貌限速 0.25s/股；东财已封禁（24-48h），冷却期不打 |
| 换手率代理衰减参数 | 主路径用真实换手（流通股本），代理仅降级备胎 |
| **不变量** | 不动五维权重；不达验收门槛不上线；所有改动先离线报告 |

## 七、阶段 1 验收结论（2026-09-05，全池 20 截面 × 10,744 样本）

> 报告：`backend/backtest_reports/mainforce_20260905_0124.md`（真实换手口径 + IC 分日稳定性）

| # | 假设 | 结论 | 数据 |
|---|---|---|---|
| H1 | 获利盘/筹码位置预测收益 | ✅ **成立（全项目最强单因子）** | price_pos 10日去超额 IC **-0.236**；五分位完美单调：Q1（底部）+1.88%/胜率63% vs Q5（顶部）-1.81%/42% |
| H2 | 筹码集中度=控盘 | ⚠️ 方向成立但弱 | 10日去超额 IC -0.04，Q5（最发散）-0.77% vs Q1-Q3 约 +0.6~0.8% |
| H3 | 追主力净流入 | ❌ **反向指标** | 去超额 IC -0.05~-0.13；**倒U结构**：温和净流入 Q2-Q3 最优（+1.1~1.3%），极端流入 Q5 最差（-0.6%）——"跟着大单买"是散户陷阱 |
| H4 | 阶段标签 | ⚠️ **风控侧成立，买侧 regime 依赖** | 出货段 10日 **-13.8%**/胜率8.3%（n=48）；拉升段震荡市 -6.4%（追高陷阱）；吸筹段 10日 +1.52% 但去超额 -0.95 |
| H5 | 低位密集×主力流入组合 | ✅ 成立 | 10日 **+1.08pt**、胜率 66.9%（n=1086） |
| H5' | **高位高获利×主力流出组合（卖出）** | ✅✅ **最强发现（regime 稳健）** | 5日 **-4.3pt**、10日 **-7.5pt**（n=440，胜率 37.5%/39.5%） |
| H6 | 撮合现实化 | ✅ 已落地 | 547 笔战法信号剔 1 笔（假想亏 -8.3%）；涨停潮时段价值更大 |

**IC 分日稳定性的关键发现（决定架构）**：筹码位置因子 IC 在 6/29-7/13（下跌段）
-0.3~-0.5，7/17-8/14（上涨段）**翻正** +0.1~+0.2，8/18 后（大跌启动）又转负
——与拥挤度因子完全同款的 **regime 依赖**。因此主力因子**不做常设维度**，
做「regime 闸门 + 风控乘数 + 展示标签」。

## 八、已落地（阶段 2，全部带开关/留痕）

1. **日批状态表** `mainforce_state`（`app/mainforce/state.py`）：
   全池 1,173 只筹码+阶段+组合信号，11 分钟算完，调度器每日 17:30 自动刷新
2. **资金流日更** `mainflow_history`（新浪主源 0.25s/股，调度器每日 17:00）
3. **排行榜接入**（`routers/scoring.py` score_top）：
   - 默认 `MAINFORCE_MODE=off`：只挂 `mainforce` 标签字段（不动分、不重排）
   - `MAINFORCE_MODE=auto`：出货嫌疑 ×0.85 重排（仅 regime ∈ 闸门，读端现算）
   - 信号判定仍用 raw total；`mainforce_state` 表本身即每日留痕（可回滚对照）
4. **前端** ScoreRank.vue Top 榜新增"主力"列（出货红/吸筹绿，悬浮显示
   阶段+筹码位置+获利盘+5日主力净流入）
5. **撮合现实化** engine.py：涨停一字买不进剔除（skipped_out 收集）+ 跌停顺延

**战法白名单预警（顺带发现）**：单阳不破样本扩至 218 笔后胜率 48.6%
（原白名单依据 56 样本 60.7% ≥55% 已不再成立），建议复核推送白名单口径。

## 九、下一步（阶段 3）

- [ ] MAINFORCE_MODE=auto 在测试环境观察 ≥1 周（覆盖一次大跌段）再定默认值
- [ ] 买侧组合（吸筹区）接入低吸类战法池（单阳不破/均线回踩的准入过滤）
- [ ] 龙虎榜席位数据（游资/机构识别）——新浪/东财接口已探明，待排期
- [ ] 矛盾扫描 × 主力行为交叉验证：矛盾信号日 + 出货嫌疑股的重叠度

## 十、执行清单

- [x] chips.py 筹码引擎 + 冒烟（000017：获利盘 99.7%/拉升段，量级合理）
- [x] phases.py 阶段识别（向量化预计算，544 只秒级）
- [x] flow.py：新浪主源 + 东财兜底 + 快照股本反推（东财 2026-09-05 被封 24-48h，勿高频）
- [x] mainflow_history 全量回填 543/544 × 120 日（65,136 行）
- [x] engine.py 撮合现实化（涨停一字买不进剔除 + 跌停顺延）
- [x] scripts/mainforce_factor_backtest.py 全池截面验证（10,744 样本）
- [x] H1~H6 验收结论（本文件第七节）
- [x] 阶段 2 上线：state 日批 + 调度器接入 + score_top 标注/乘数 + 前端主力列


---

## 4. 数据包迁移（Actions 产包 / Render 免 OOM / Supabase 瘦身）

> 原文件：`PLAN_PACK_MIGRATION.md`（8.7 KB，最后修改 2026-09-06 12:32）  
> 处置：**主体已完成**（Phase 0/2 验收通过，日批已迁 Actions）；收尾项并入主计划

<!-- ===== 以下为 PLAN_PACK_MIGRATION.md 原文（逐字保留，未做改写）===== -->

﻿# 数据包迁移计划（backend-pack / Render 瘦身 / 前端本地化）

生成：2026-09-06 ｜ **死线：2026-10-03**（Supabase 组织超额 → 项目受限）

---

## 一、背景与三个问题

1. **Supabase egress 超额**：本周期 10.6GB / 免费 5GB（212%），大头是
   backtest_prices(71MB) / kline_cache(19MB) / indicator_cache(9MB) 被反复读；
2. **Render 免费实例被详情页打挂**：0.1CPU/512MB 上，`/api/score/000567` 等
   并发重请求 → 502/503 → 浏览器误报 CORS（已复现实锤，9/5）；
3. **JSON 数据包是内存炸弹**：45MB JSON → Python dict 常驻 150-200MB，
   512MB 实例直接 OOM（已实测）。

根因共识：**8/25-9/5 功能浪潮让每个新功能都成为「每日读库的永久订阅」**，只叠加不替换。

---

## 二、目标架构（终态）

- **GitHub Actions**（每日 16:00，重活全在这里）产出三个包，全部发 GitHub Pages（免费）：
  - `backend-pack.db.gz` —— Render 启动下载，sqlite3 按需查（常驻约 0）
  - `indicators-pack.json.gz` —— 浏览器 Worker 读 _series 评分（500 天口径）
  - `kline-pack-latest.json.gz` —— 浏览器图表/本地回测（现状不变，已支持增量）
- **Supabase**（瘦身后）：只剩业务小表 <10MB，egress 目标 <0.5GB/周期
- **Render**（瘦身后）：只剩快讯监控、模拟盘撮合、实时行情、health
  —— 不再算分、不再读肥表，OOM 消失
- **浏览器**（两个用户）：本地评分/详情/回测展示，零后端重请求

---

## 三、Phase 0：已完成（2026-09-05/06）

- [x] generate_backend_pack.py：SQLite 打包（klines/indicators/codes/meta 四表）
      + indicators-pack.json.gz（前端指标小包）+ 双完整性护栏（exit 5/6）
- [x] pack_source.py：DATA_SOURCE 三档（db/pack/local）读取层，
      SQLite 按需查询（常驻约 0）+ 30h 新鲜度 + 陈旧护栏（_is_stale）+ 缺包优雅回退
- [x] 读取层接线 4 处：kline_cache / indicator_cache / backtest/data / mainforce/state
      （全部「pack 未命中回退 DB」，接口签名不变）
- [x] main.py lifespan：启动后台预下载（非阻塞，失败降级 DB）
- [x] overlay.py：主力筹码计算加 260 根窗口（CPU 护栏，解详情页 OOM 的 CPU 层）
- [x] refresh_kline_cache：两轮重试 + 失败退避 + 成功率告警（方案 A）
- [x] CI Python 3.9 兼容修复（PEP 604 注解，7 文件）
- [x] BACKTEST_PREHEAT_WINDOW 死循环笔误修复 + ENABLE_HEAVY_JOBS 开关

---

## 四、Phase 1：上线验证（本周，P0）

> 目标：Render 安全切到 pack 模式，详情页不再 502。

- [ ] push 全部改动，确认 ci.yml（Python 3.9）通过
- [ ] 确认 Secrets：DATABASE_URL（重置后的新密码）+ GH_PAGES_TOKEN
- [ ] Actions 手动触发 kline-data 工作流，验收三件事：
  - [ ] 日志出现「SQLite: ... indicators=1394」（修复验证）
  - [ ] 日志出现「bars=... 约750」（深度验证）
  - [ ] Pages 上 backend-pack.db.gz Last-Modified 为最新
- [ ] Render 环境变量：DATA_SOURCE=pack + ENABLE_HEAVY_JOBS=0 → Restart
  - [ ] 日志出现「[main] 后端数据包就绪: 日期，N 只」
  - [ ] 详情页连开 5 次，无 502（对照 9/5 复现基线）
- [ ] 本地：python scripts/sync_local.py + .env 加 DATA_SOURCE=local
- [ ] 回测口径验证：同一只股票，pack 价格 vs DB backtest_prices
      收盘价对比（qfq vs 东财，除权日允许差异，其余应一致）

**验收标准**：Render Logs 无 OOM/502；Supabase 日 egress <100MB。
**回滚**：删 DATA_SOURCE 变量 → 回 DB 模式（1 分钟）。

---

## 五、Phase 2：前端消费 pack（✅ 2026-09-06 完成，用户已验收）

> 目标：两个用户切「本地计算」后，Render 的重接口调用频次大降。

- [x] IndexedDB 新增 indicators store（DB v2 无损升级）；下载 indicators-pack.json.gz
      （ensureIndicatorsPack 与 K 线包日期对齐自动拉取；localStorage
      'kline_data_base_url' 可覆盖包地址，本地验收/私有部署用）
- [x] 评分优先读 _series（与后端 indicator_cache 同一引擎预计算的事实源），
      缺失/盘中不新鲜时回退 150 根现算（seriesIsFresh 与 appendTodayBar 互为反证）
- [x] 个股详情页 K 线/指标叠加/评分全本地（loadLocalKline/computeLocalScore），
      消除 /api/stock/kline 与 /api/score/{code} 并发爆发（OOM 触发点）；
      本地不可用自动回退后端接口
- [x] 评分口径对齐验证：**同一份 _series 两端打分，25 只市值前排股票 diff 全部
      = 0.000**（scripts/verify_pack_score_parity.py + pack_score_parity.mjs）
- [x] 附带修复：DecompressionStream 在部分内嵌浏览器构造器存在但流读取必抛
      （"Failed to fetch"）→ klineDB 解压加 pako 纯 JS 降级

**验收标准**：本地模式下，详情页/排行榜零后端重接口调用。✅ 用户已验收。
**回滚**：本地模式是用户开关（localStorage score_use_frontend_mode），关掉即回后端。

---

## 六、Phase 3：日批迁 Actions（10/3 前，P1）

> 目标：数据积累不断档，且不完全依赖 Render 存活。

- [ ] 评分快照迁 Actions：Actions 拉腾讯行情 + 用 pack 指标算分 →
      写 ranking_history（写入免费）；Render 侧保留为兜底（pack 模式下跳过）
- [ ] 每日日报迁 Actions：LLM_API_KEY 入 Secrets；读 pack/小表生成日报
- [ ] mainforce_state 评估：已走 pack 数据源，Render 保留（轻）
- [ ] 战法扫描：主数据走腾讯直连，Render 保留（中负载，观察）

**验收标准**：Actions 失败有企微告警；连续失败时 Render 兜底任务自动顶上。

---

## 七、Phase 4：观察与收尾（10/3 后一周）

- [ ] Supabase egress 曲线 <5GB（目标 <1GB）→ 确认限制解除
- [ ] Render Metrics：Memory 曲线平稳、无 restart
- [ ] 清理：JSON 版 backend-pack 逻辑删除、indicator-refresh.yml
      （pack 稳定后其 DB 写入不再被消费）
- [ ] Supabase 密码重置收尾：确认 Render / 本地 / GitHub Secrets 三处已同步新密码
- [ ] 手册（Agent知识库 v3.1）补记：第八章遗留项状态更新

---

## 八、风险与对策

| 风险 | 概率 | 对策 |
|---|---|---|
| qfq（腾讯）vs 东财口径漂移，回测结果变化 | 中 | Phase 1 对比验证；差异大则 backtest 读取保留 DB 源 |
| Actions 失败 → pack 陈旧 → 评分用旧指标 | 中 | _is_stale 护栏已回退 DB；失败推企微；快照门 18:45 强制执行 |
| Render 512MB 仍然 OOM | 低 | Phase 2 完成后只剩轻负载；仍崩则升级 Render starter（$7/月） |
| GitHub Actions cron 延迟/丢失 | 高（已知特性） | 双 cron + 幂等 + _is_stale 回退 + 手动触发兜底 |
| Supabase 10/3 照常限制 | 中 | Phase 1 完成即脱离肥表依赖；真被限制也只是小表，可快速迁新项目 |

---

## 九、明确不做（本期）

- **增量 delta 包**（后端）：Pages 流量免费、全量 12-20MB 可接受；
  触发条件：本地同步频繁且 74 秒不可接受，或包涨到 50MB+
- **模拟盘 / 快讯监控前端化**：有状态 + 实时，必须留常驻服务
- **后端彻底轻量化重构**：等 Phase 1-3 稳定后自然水到渠成

---

## 十、后端包减法（2026-09-06，Actions 2h 超时对策）

现象：kline-data 工作流 timeout 120min——前端包（3000只×150天）30min 完成，
后端包（1394只×1100日历天）1.5h+ 未完成（腾讯限流下重试/退避吃时长）。

对策（generate_backend_pack.py 已实现）：
- **质量减法**：剔除科创创业（688/689/300/301/302）、ST/亏损（PE≤0）、
  总市值<50亿（与战法扫描门槛对齐）——这些股票在评分/排行/战法全链路
  本就不会被消费；模拟全市场筛选率 53%（3230→1715），包池 1394→约750
- **市值降序拉取**：指数+ETF 在最前，个股按市值降序——即使超时中断，
  质量池已完整落包
- 前端包（3000 只）保持不变：本地详情页图表要覆盖全市场
- 消费影响评估：mainforce_state 覆盖收窄至质量池（与排行资格一致）；
  688/300 个股 Render 兜底走 DB（读取层已回退），前端本地图表走前端包

预计后端包耗时降至 ~35min，全工作流 ~70min < 120min。下次 Actions 运行验证。

---

## 十一、当前待办（给下次会话/明天开盘前）

- [ ] push（含 3.9 兼容修复 + SQLite 版脚本）
- [ ] 配 DATABASE_URL secret（若未配）
- [ ] 手动跑 kline-data 工作流 → 验证新包（indicators ≈1394、bars ≈750/只）
- [ ] 验证通过后：Render 切 DATA_SOURCE=pack
- [ ] 周一开盘观察：详情页是否还 502、Render 日志有无 OOM
- [ ] Supabase 密码重置 + 三处同步（若尚未完成）


---

## 5. 震荡偏空档（NEUTRAL_BEARISH）+ 因子质量改善

> 原文件：`PLAN_REGIME_BEARISH.md`（13.2 KB，最后修改 2026-09-03 02:31）  
> 处置：**主体已落地**（防御化权重被数据否决）；阶段 1.3/1.4 与阶段 2 遗留并入主计划

<!-- ===== 以下为 PLAN_REGIME_BEARISH.md 原文（逐字保留，未做改写）===== -->

# 震荡偏空状态判定 + 评分维度调整计划

> 触发：2026-09-03 大跌（沪指 -0.97%、创业板 -2.39%、3900 只下跌、缩量 2465 亿）
> 现状：系统判定 `neutral`（震荡档 27/23/23/17/11），但行情实为「震荡下行」
> 约束：**Supabase 当前不可达**（pooler TCP 不通），DB 相关改动需等恢复后验证
> 创建：2026-09-03

---

## ⚡ 回测结果（2026-09-03，已执行）—— 修订核心结论

450 条维度明细快照（8/20~9/2 共 9 个交易日），每日 Top10，快照日收盘买入：

| 候选权重 | 持有2日 胜率/均收益 | 持有5日 胜率/均收益 |
|---|---|---|
| **A 现震荡档 27/23/23/17/11** | **54.3% / +0.54%** | **62.5% / +1.76%** |
| B 震荡偏空提案 18/18/25/12/27 | 50.0% / +0.20% | 62.5% / +1.05% |
| C 用户建议 15/20/25/10/30 | 50.0% / +0.35% | 62.5% / +1.17% |
| D 防御档 12/15/25/13/35 | 47.1% / -0.06% | 60.0% / +0.77% |
| 原排名 Top10（不加权） | 48.6% / -0.43% | 50.0% / +0.55% |

**结论（两个持有期口径一致）**：
1. **现震荡档全面最优** → ❌ "立即切防御/震荡偏空权重"**被数据否决**，权重保持不变
2. 加权评分有效：A 档 Top10（+1.76%）显著优于原排名（+0.55%）
3. ⚠️ 样本仍偏少（5日口径 40 条）且以震荡期为主；**9/2 大跌期的快照持有期未满**，防御化权重在"真下跌期"是否更优，需 9/4 后补充回测再定
4. 修订后的正确应对：**改善状态判定（加市场宽度 → NEUTRAL_BEARISH 仅作状态标签与仓位警示）+ 改善因子质量（行业内分位数）**，不动维度权重

## 一、结论先行

| 项 | 结论 |
|---|---|
| 用户判断"不是震荡市、是震荡偏空" | ✅ 采纳（新增 `NEUTRAL_BEARISH` 子状态） |
| "立即切 Defensive 防御档" | ❌ 否决（defensive=熊市主跌浪配置，ADX 15.87 是弱趋势，套用会失去超跌反弹捕捉力） |
| 质量提权（11% → ~27%） | ✅ 采纳（震荡偏空核心是"不踩雷"） |
| 基本面提权至 25% | ✅ 采纳 |
| 成长降权（17% → ~12%） | ✅ 采纳（紧缩环境成长承压） |
| 行业内分位数打分（PE/PB/负债率/毛利率） | ✅ 采纳（映射已 100%，engine.py 注释里的技术债该还了） |
| 缩量环境下成交额因子中性化 | ✅ 采纳（用户洞察：系统性因素混入个股 alpha） |
| 北向资金行业流向 | ❌ 不可得（仅总量且盘中为 0；持股明细接口从未实现）→ 改用 `/sector/industry-flow` |
| 海外收入占比 / 原材料依赖（地缘抗性维度） | ❌ 不可得（财报附注未解析）→ 改用行业白名单调节项 |
| 筹码集中度（股东户数） | ⚠️ 需新建数据源 → 阶段 2 |
| 经营现金流 / 自由现金流 | ⚠️ 需扩展财报接口 → 阶段 2 |
| 跌停家数占比进个股技术维度 | ❌ 逻辑错位（市场层指标，所有个股同值，只平移分不改变排序）→ 放 regime 层 |
| 删除 KDJ / 新增超跌偏离度（改指标） | ⚠️ 推迟（违反"只改权重不改指标"方法论，且 DB 挂了无法回测）→ 阶段 1 后验证 |

---

## 二、关键事实核查（已验证，勿重复怀疑）

| # | 事实 | 证据 |
|---|---|---|
| 1 | **市场宽度数据系统里已有**：涨跌家数、涨停/跌停家数、平均涨跌幅、市场温度 | `app/routers/market.py` L150-216（`_cache["stocks"]` + `market_temperature()`） |
| 2 | 现有 regime 判定**不含宽度**，只吃沪深300 的 MA/ADX/ATR | `app/backtest/market_regime.py` `detect_market_regime()` |
| 3 | 北向只有总量，盘中常为 0，行业明细未实现 | `app/eastmoney.py` L373；`routers/capital.py` L67-70 返回空数组 |
| 4 | fundamental 无现金流 / 海外收入 / 股东户数字段 | 全库搜索 `cash_flow`/`overseas`/`holder_num` 无结果 |
| 5 | industry-flow（东财行业主力净流入）**已有**，可替代北向行业流向 | `routers/sector.py` L47 |
| 6 | `DATABASE_URL` 不设置即自动走 SQLite | `app/database.py` L43 `_use_postgres = DATABASE_URL.startswith("postgresql://")` |

---

## 三、阶段 0：不依赖 DB，今晚可做（代码层）

### 0.1 新增 `NEUTRAL_BEARISH` 状态判定 —— `app/backtest/market_regime.py`

**判据**（在现有三档之外插入，优先级在 DEFENSIVE 之前）：

```
NEUTRAL_BEARISH（震荡偏空）=
    ADX < 20                                    # 无强趋势（排除熊市主跌浪）
    AND ma_trend == down                        # MA20 < MA60
    AND ( 涨跌比 < 0.4                          # 宽度恶化：跌家数 >> 涨家数
          OR 近 2 日沪深300 累计跌幅 < 0          # 连续阴跌
          OR (跌停家数 >= 20 AND 涨停家数 < 跌停家数) )   # 情绪冰点
```

**权重**（介于 neutral 与 defensive 之间，偏防御但保留反弹捕捉力）：

| 维度 | neutral | **neutral_bearish** | defensive | 调整理由 |
|---|---|---|---|---|
| 技术 | 27 | **18** | 12 | 降权但有保留：震荡下行中假突破多，但超跌反弹仍需技术面识别 |
| 资金 | 23 | **18** | 15 | 缩量环境下量价失真，降权 |
| 基本面 | 23 | **25** | 25 | 防御市重估值安全边际 |
| 成长 | 17 | **12** | 13 | 紧缩环境成长估值承压最大 |
| 质量 | 11 | **27** | 35 | ★ 核心变化：震荡偏空的首要任务是"不踩雷" |

**宽度数据接入方式**（关键设计）：
- 宽度来自 `_cache["stocks"]`（**内存，仅盘中有**）→ 盘后判定会取不到
- 方案：新增 `market_breadth_daily` 表（DB 恢复后落库），盘后 15:05 快照一次当日 up/down/limit_up/limit_down/avg_change
- 降级：无宽度数据时，用沪深300 当日涨跌 + 近 3 日累计涨跌近似（保证盘后也能判定）

### 0.2 资金面"成交额"因子缩量中性化 —— `app/scoring/engine.py`

**问题**：`_score_capital` 里"成交额强度"占 25%，缩量市会让**所有股票一起扣分**——这是市场系统性因素，不该计入个股 alpha。

**改法**：把"成交额绝对值"改为"**成交额 / 全市场近 20 日均额**"的相对值（即成交额分位），使因子衡量"该股相对市场的活跃度"而非"市场整体水量"。
- 全市场均额从 `market.py` 的 `stats.total_amount` 取，缓存 60s（已有）
- 无数据时降级为原逻辑

### 0.3 行业内分位数打分（代码先写，DB 恢复后启用）—— `app/scoring/engine.py`

`engine.py` 头部注释明确写了这笔技术债：
> 资产负债率、毛利率的阈值是绝对阈值，但行业差异极大……等个股→行业映射表建成后，应改为"行业内相对排名"打分。

**改动范围**：
| 指标 | 现状 | 改为 |
|---|---|---|
| PE | 绝对阈值 | 行业内分位（越低分越高） |
| PB | 绝对阈值 | 行业内分位 |
| 资产负债率 | 绝对阈值 | 行业内分位（银行 92% 不再被误伤） |
| 毛利率 | 绝对阈值 | 行业内分位（白酒 90% 不再被误伤） |

**实现**：新增 `_industry_percentile(code, metric, value)`，从 `stock_industry` 取同行业股票，从 `stock_finance` 取同行业该指标分布，算分位。
**性能**：分位数分布按行业缓存（49 个行业 × 指标，每日刷新一次），不要每只股票现算。

---

## 四、阶段 1：DB 恢复后立即执行

### 1.1 回测验证新档权重（必做，不可跳过）
用已有 **350 条已验证快照**（2026-08-20~08-31）跑"只改权重"对照：
- 对照组：`neutral` 27/23/23/17/11（已知 64.3% / +1.42%）
- 候选 1：`neutral_bearish` 18/18/25/12/27
- 候选 2：用户建议 15/20/25/10/30
- 方法：每日 Top10 模拟选股，比胜率 + 平均收益
- **验收**：新档需优于 neutral 档才固化，否则调整权重重测

### 1.2 补齐数据 + 复核
- 补完回填残余（当时 53 只滞后 + ETF `sh588000`）
- 重新判定 regime（此时应有 09-03 数据，验证是否正确识别为 `neutral_bearish`）

### 1.3 行业"地缘抗性"白名单调节项
用映射表（100% 覆盖）标出**低海外暴露行业**作为震荡偏空期的加分项：
- 受益：军工、电网设备、公用事业（供水供气/电力）、内需消费（食品/商业百货）、建筑建材
- 受损：半导体/消费电子（海外收入高）、航运/石油（油价双向）、有色（全球定价）
- 实现：作为**行业层调节系数**（如 0.95~1.05），不新增维度、不破坏权重和为 1

### 1.4 状态切换的平滑处理
新增状态后要防"状态抖动"：
- 复用现有 hysteresis（连续 3 日多数决），`neutral ↔ neutral_bearish` 之间额外加**冷却期**（如切换后至少维持 2 个交易日）
- 状态切换时记 `market_regime_history`，前端可见

---

## 五、阶段 2：中期（需新建数据源，非紧急）

| 项 | 数据源 | 成本 | 优先级 |
|---|---|---|---|
| 筹码集中度（股东户数） | 东财股东户数接口（季度） | 中（新表 + 抓取 + 低频更新） | 中 |
| 经营现金流/净利润、自由现金流 | 东财财报现金流量表（扩展 fundamental） | 中高（财表结构改动 + 历史回填） | 高（防暴雷价值大） |
| 海外收入占比 | 财报附注解析 | 高 | 低（白名单已能近似） |
| 超跌偏离度（价格 vs MA60 偏离） | 已有 K 线 | 低 | 中（但属"改指标"，需回测） |

---

## 六、Supabase 不可用的应急决策

**现状**：pooler `aws-0-ap-northeast-1.pooler.supabase.com:6543` TCP 不通（本地网络正常，百度/东财均通）→ Supabase 侧故障，需用户登控制台排查（项目暂停/配额/区域故障）。

**能否切本地 SQLite**：能（`DATABASE_URL` 不设即走 `backend/data/app.db`）。

**但不建议切**，理由：
| 功能 | 是否依赖历史数据 | 切 SQLite 后 |
|---|---|---|
| 实时评分 / 战法扫描 / 快讯 | 否（主要靠外部行情源） | ✅ 可用 |
| 评分排行历史 / 评分验证 | 是（`ranking_history`） | ❌ 归零 |
| **行业主线分析** | 是（`ranking_history` + `stock_industry`） | ❌ 归零 |
| 回测 / 市场状态判定 | 是（`backtest_prices` 沪深300 历史） | ❌ 归零（MA60/ADX 需长序列） |

→ **结论**：等待 Supabase 恢复优先。若长时间不恢复且急需实盘，再切 SQLite 应急（仅用实时评分/战法扫描，历史类功能挂"数据积累中"提示）。

---

## 七点五、二轮修正（2026-09-03，评审反馈落地）

| 反馈 | 落地情况 |
|---|---|
| 极端缩量警示（regime 层，不改打分） | ✅ `_market_shrink_ratio()`：沪深300 当日额/20日均额（close×volume 代理），detail 输出 `volume_ratio_20d`，<0.7 打警示日志。实测 09-02 为 0.88（不极端） |
| 金融负债率细分（银行/证券/保险分开） | ✅ 按名称关键词二级识别（`_fin_sub_industry`，含"人寿"→保险），指标分布进子桶：银行 114 只（90~94%，方差极小）→ **固定 75**；券商/保险 → 子行业分位；保险样本 <10 自动降级 70。实测中信证券 1.0→40.5（分位压缩到 [40,85]：高杠杆在紧缩期是风险应扣分，但极端分位过狠） |
| 外围恐慌触发器（三选二） | ✅ `_external_panic()`：日经<-2% / 布伦特>+2%且黄金<-1%（滞胀信号） / 美债10Y单日+5bp，数据源 `get_macro_panel()`（含 `derived.us10y_bp_change`，异常时不触发）。作为 neutral_bearish 判据之一（保持 neutral+MA down 前提） |
| `REGIME_OVERRIDE` 环境变量 | ❌ **未实现**（此前提议 B 方案后 DB 恢复、判定已正常工作，止血需求消失）。如需手动覆盖可后续加，当前判定链已足够 |
| 9/5 非农紧急回测 + "外部冲击档" | 📋 列入 9/5 收盘后任务：用大跌期快照补充回测，若防御化权重显著翻盘，再评估仅在"外围恐慌触发"时启用的外部冲击档（技术15/资金15/基本面25/成长10/质量35） |



- [x] `NEUTRAL_BEARISH` 状态可被判定（✅ 09-02 已验证命中：降级判据"近2日累计下跌"；主服务内走宽度判据）
- [x] 新档权重回测（✅ 已执行：**回测否决防御化权重**，保持震荡档；见顶部回测结果）
- [x] 行业内分位数启用后，银行股负债率不再被误伤（✅ 实测：常熟银行 28→65 中性分、茅台毛利率 97.5、五粮液负债率恢复 85；金融行业负债率走中性特判而非分位——分位被"金融混银行/券商/保险"证伪）
- [x] ~~缩量日成交额因子中性化~~（✅ 核查后**不需要**：`_score_amount` 本就是自相对指标——今日 vs 自身10日均额，缩量市 ratio≈1 全体中性，不存在系统性惩罚）
- [ ] 状态切换 hysteresis + 冷却（阶段 1.4，待做）
- [ ] 前端可见当前状态与生效权重（待做）
- [ ] PE/PB 行业分位在主服务生效确认（依赖行情缓存加载，独立脚本中为空属预期）

---

## 八、风险与回退

| 风险 | 应对 |
|---|---|
| 新档权重回测不过 | 保留 neutral 档，neutral_bearish 仅作展示不生效 |
| 行业内分位数样本不足（某行业 <10 只） | 样本不足时降级为绝对阈值，不做分位 |
| 宽度数据盘后缺失 | 降级用沪深300 涨跌近似（0.1 已设计） |
| 状态频繁切换 | hysteresis + 2 日冷却（1.4） |
| 改指标类改动（阶段 2）引入回归 | 一律先回测，禁止直接上线 |


---

## 6. zzshare 低频补充数据源（财报/现金流）

> 原文件：`PLAN_ZZSHARE_INTEGRATION.md`（5.5 KB，最后修改 2026-09-03 21:00）  
> 处置：**阶段 0/1 已落地**；阶段 1 验收门槛与阶段 2 并入主计划

<!-- ===== 以下为 PLAN_ZZSHARE_INTEGRATION.md 原文（逐字保留，未做改写）===== -->

# zzshare 数据源接入评估与整合计划（PLAN_ZZSHARE_INTEGRATION）

> 触发：2026-09-03。README 声称 40+ 接口（行情/涨停/龙虎榜/情绪/板块/财务）。
> 实测（zzshare 0.4.11，匿名）：**财务 5 表 + 涨停复盘 + 板块/情绪/龙虎榜可用；资金流/实时接口（stock_moneyflow/rt_k/market_mf）此版本实际缺失**。
> 原则：zzshare 只作**低频补充数据源**（匿名限流/慢、10s 超时自动重试），不替代现有腾讯/东财主链路。
> 创建：2026-09-03

---

## 一、现有项目缺口 × zzshare 能力对照

| # | 现有项目缺口（对应历史 plan） | zzshare 能力 | 接口 | 优先级 |
|---|---|---|---|---|
| 1 | 质量维度只有 ROE/负债率/毛利率 3 指标；无**现金流质量/扣非/商誉**（PLAN_REGIME_BEARISH 阶段2「经营现金流」） | ✅ 完整现金流量表/资产负债表/指标表 | `finance_cash_flow` / `finance_balance` / `finance_indicator` | **P0（十分需要）** |
| 2 | 基本面估值 PE/PB 仅实时值，无**历史日频估值序列**（行业分位/估值因子回测缺数据） | ✅ 日频估值表（PE/PB/PS/PCF/市值/换手）2005 至今 | `finance_valuation` + `finance_pit`（无前视） | **P1** |
| 3 | 涨停只统计家数（market breadth），无**连板高度/涨停原因/梯队**；战法"涨停回马枪"等缺题材归因 | ✅ 涨停复盘 | `uplimit_stocks` / `uplimit_hot` / `review_uplimit_reason` / `uplimit_trend` | **P1** |
| 4 | 板块分析数据只有东财行业映射；缺**题材热度人气榜**交叉验证主线 | ✅ 同花顺热度 Top / 板块排名 | `ths_hot_top` / `plates_rank` / `market_plate_stocks` | P2 |
| 5 | regime/情绪只有宽度自算 | ✅ 情绪指标/涨跌分布 | `market_sentiment` / `updown_distribution` / `sentiment_trend` | P2 |
| 6 | 龙虎榜/异动（持仓撤退提醒上下文） | ✅ 龙虎榜/异动 | `lhb_list` / `movement_alerts` | P2 |
| 7 | 交易日历依赖本地推断 | ✅ 交易日历 | `trade_days` | 已接入方向（低） |
| — | **资金面无真实资金流**（北向空壳） | ❌ **0.4.11 无此接口**（README 超前） | `stock_moneyflow`/`market_mf` | 暂不可用，升级包后复查 |

**P0 判断依据**：质量权重在 defensive 档 35% / neutral 档 11%+；而暴雷（商誉减值/现金流断裂）恰好是"绿盘高分仍在榜"的隐藏雷。加净现比/扣非后质量维度才接近"不踩雷"本意，且**不改变权重的增量（维度内加子指标）需要回测验证** → 先入库 + 因子分析，再决定并入打分。

---

## 二、阶段 0（已做）：接口可用性实测

- `trade_days` / `finance_latest(indicator|valuation)` 匿名可用（有 10s 超时自动重试，慢）；
- `finance_latest.indicator` 返回 18 列：roe/gross_profit_margin/net_profit_margin/扣非/营收环比/净利同比…；
- `finance_latest.valuation` 返回 12 列：pe/pb/ps/pcf_ratio/market_cap/trade_date/turnover_ratio…（pcf 为负=现金流失血）；
- `stock_moneyflow`/`rt_k`/`market_mf` **AttributeError 不存在**（pyi/README 声明超前于实现）→ 不接；
- 匿名限流+慢，token 可通过环境变量 `ZZSHARE_TOKEN`（官网免费）提升频率。

---

## 三、阶段 1（本次已实现代码）：现金流/财报增量入库 + 因子分析脚本

改动文件（低风险、不触碰评分主链）：
1. `backend/app/zzshare_client.py`：DataApi 懒加载封装（token 从 env `ZZSHARE_TOKEN`，缺省匿名）；
2. `backend/app/zzshare_finance.py`：表 `stock_finance_zz`（code 主键）初始化 + 批量同步最新 indicator/balance/cash_flow → 落库；
3. `scripts/zz_finance_sync.py`：CLI 同步（`--limit` 小样本验证 / 默认全池分批），只写不读评分；
4. `scripts/zz_factor_analysis.py`：从库算「净现比 OCF/净利润」「毛利率/净利率/扣非占比」「商誉/净资产」「有息负债率」等质量扩展因子，复用 ranking_history 快照验证（参考 factor_analysis.py 方法论）→ 出 md 报告。

**验收门槛**（决定是否并入质量维度）：
- 现金流类因子在大跌段（9/1~9/2 快照）对未来 2/5 日收益有区分（分桶单调或 IC>0.1）；
- 与既有质量分组合后相关性 < 0.7（不是重复测量）；
- 覆盖率 ≥ 80%（财报齐全股票）。

**明确不做**：本期不改 engine 质量子指标/权重/排行（等因子报告再定，防无回测上线）。

---

## 四、阶段 2（后续，先出报告再决定）
- 质量维度加「净现比」等子指标（改 `_score_quality`，需 9/4/9/9 回测背书）；
- 涨停原因/连板高度 → 战法复盘增强（涨停回马枪题材归因）与 regime 情绪参考；
- 行业分位估值回测（用 valuation 历史序列把 PE/PB 绝对阈值升级为行业内分位——PLAN_REGIME_BEARISH 阶段 1.3 的待办数据）；
- 龙虎榜/异动 → 撤退提醒上下文。

---

## 五、风险与边界

| 风险 | 应对 |
|---|---|
| zzshare 接口不稳定（10s 超时/限流） | 只作低频补充；同步放 23:00 后（BACKTEST 回填窗口外）；失败重试+跳过不阻塞主流程 |
| 匿名 token 频率低 | env `ZZSHARE_TOKEN` 可选提升；默认每只股票低频 |
| 财报口径与现有 finance.py（东财）不一致 | stock_finance_zz 独立存储；入库时保留 pubDate 可追溯 |
| 改打分导致排行漂移 | 阶段 1 不碰 engine；并入前必须有因子报告 + 回测 |
| 第三方库升级后接口名变化 | adapter 单点隔离，升级只动 zzshare_client.py |


---

## 7. 模拟盘自动交易（信号→成交→跟踪→白名单反哺）

> 原文件：`PLAN_PAPER_TRADING.md`（7.1 KB，最后修改 2026-08-31 20:48）  
> 处置：**代码已实现**（文档状态行写「规划中」已过时）；胜率对比校验并入主计划

<!-- ===== 以下为 PLAN_PAPER_TRADING.md 原文（逐字保留，未做改写）===== -->

# 模拟盘/纸面交易模块实施计划

> 创建时间：2026-08-31
> 状态：规划中，未实施

## 设计目标

把盘后扫描的战法信号自动转为模拟持仓 → 次日开盘确认价成交 → 盘中/盘后自动跟踪止损止盈 → 平仓后回填真实胜率，反哺 `PUSH_STRATEGY_WHITELIST` 白名单刷新（`recommendation.py` 注释已预留此意图）。

## 核心设计原则

- **撮合口径与回测引擎完全同源**：复用 `backtest/engine.match_signals` 的 T+1 开盘成交、`low<=stop` 止损（按 `min(open,stop)` 出）、`high>=tp` 止盈、含双边成本的逻辑，保证模拟盘胜率与回测胜率可比。
- **默认全自动 + 可手动**：白名单战法信号自动入池；Strategies.vue 信号行也提供手动"转模拟持仓"按钮。
- **与真实持仓体系隔离**：不复用 `user_portfolio`/localStorage 体系，独立建表走后端持久化。

---

## Phase 1：数据层（建表 + 信号入池）

**新建表 `paper_positions`**（追加到 `schema.sql`，参照 `etf_signals` 生命周期结构）：

| 字段 | 说明 |
|---|---|
| id, code, name, strategy_name | 基础信息 |
| signal_date, confirmation_json | 信号来源（复用 `strategy_results` 的信号字段） |
| entry_price | 参考介入价（信号价） |
| fill_price, fill_date | **实际成交价/日**（次日开盘价） |
| stop_loss, target_price | 止损/止盈位 |
| shares, cost | 模拟股数（按 position_pct + 固定虚拟本金计算） |
| status | `pending → holding → closed / cancelled` |
| exit_price, exit_date, exit_reason | 平仓信息（stop_loss / take_profit / manual / expire / fill_rejected） |
| pnl_pct, pnl_amount, is_win | 平仓后计算（含双边成本，与 `match_signals` 相同参数） |
| fill_note | 五档指引 + 量比验证结果快照 |

约束：`UNIQUE(strategy_name, code, signal_date)` 保证幂等。

**新建表 `paper_account`**：虚拟总本金、已实现盈亏、当前占用仓位（单账户，无 user 维度，与现有单机架构一致）。

**入池逻辑（新文件 `backend/app/strategies/paper_trading.py`）：**
- `auto_ingest_signals()`：盘后扫描推送完成后调用，把白名单战法（`get_push_whitelist()`）+ 准入（`is_strategy_admitted`）+ 高/中置信度的当日信号写入 `paper_positions(status=pending)`，与企微推送同一批信号，幂等。
- 防重复持仓：同一 code 已有 holding 状态仓位则跳过。
- 池容量上限（如 20 个 pending），按 confidence 排序截断。

## Phase 2：成交确认（次日开盘）

**挂进现有 `open_confirmation_loop`（09:35-11:20 窗口）**，或新增 `paper_fill_loop` 同窗口执行：
- 复用 `get_stocks_batch` 拉实时行情 + `_calc_vol_ratios` 量比。
- 复用 `build_open_confirmation` 五档指引：
  - **放弃档**（高开>3% 或已破止损）→ `status=cancelled`，`exit_reason=fill_rejected`（也算信号质量数据）。
  - **可成交档**（正常/低吸/回踩）→ 按**实时开盘价** `fill_price` 成交 → `status=holding`，`fill_note` 存量比验证结果（高开缩量防诱多时降级为半仓）。
- 幂等：`is_schedule_done('paper_fill')`，错过窗口可补跑（用当日开盘价从 `backtest_prices` 兜底）。
- 成交后推企微摘要（复用 `push_strategy_signals` 管道）。

## Phase 3：持仓跟踪（止损/止盈）

**新增 `paper_track_loop`**，照 `track_loop` 模式：
- 盘中每 5 分钟拉持仓股实时行情：
  - `price <= stop_loss` → 止损平仓
  - `price >= target_price` → 止盈平仓
  - 可选增强：浮盈 >5% 后止损上移至成本价（移动止损，与回测口径需对齐确认）
- 盘后兜底：`backtest_prices_refresh_loop`（15:40）跑完后用日线 `low/high` 校验一遍，防止盘中循环漏判（对齐回测"日内先触发止损"规则）。
- 平仓时计算 `pnl_pct`（含成本参数，与 `match_signals` 相同）、`is_win`，写 `exit_*` 字段，推企微平仓通知。
- 超期强平：持有 N 日（如 20 日）未触发则按收盘价离场（对齐回测持有到期口径）。

## Phase 4：胜率回填

- 新增 `paper_stats()`：复用 `_strategy_stat` 口径（trades / win_rate / avg_pnl_pct / profit_factor / avg_hold_days），按 `strategy_name` 分组已平仓交易。
- **白名单自动刷新**：样本 ≥30 且胜率 ≥55% 的战法自动进入推送白名单，跌破则移除——实现 `recommendation.py` 注释中预留的意图，`PUSH_STRATEGY_WHITELIST` 改为"人工初始名单 ∪ 模拟盘自动名单"。
- 回写 `STRATEGY_STATS`（推荐话术中的历史胜率背书改用模拟盘实测值，标注数据来源）。
- 每日盘后把统计快照写入 `paper_stats_history`（可选表），观察胜率漂移。

## Phase 5：API + 前端

**后端新路由 `backend/app/routers/paper.py`**（前缀 `/api/paper`）：
- `GET /positions?status=`、`POST /positions/manual`（手动转仓）、`POST /positions/{id}/close`（手动平仓）、`DELETE /positions/{id}`（pending 取消）
- `GET /stats`（分战法胜率统计）、`GET /account`（账户总览）
- 注册进 `main.py`

**前端：**
- 新视图 `PaperTrading.vue`（或并入 Portfolio.vue 加 Tab）：pending（待确认）/ holding（持仓中，实时盈亏）/ closed（平仓记录）/ 统计面板（分战法胜率 vs 回测胜率对比）。
- `Strategies.vue` 信号行加"转模拟持仓"按钮（挂现有 addWatch/addPlan 旁）。
- `api/index.js` 增加对应封装。

## Phase 6：调度注册

`scheduler.start()` 新增两个 loop（每个 1 行）：
- `paper_fill_loop`（09:35-11:20）—— 或并入 `open_confirmation_loop`
- `paper_track_loop`（盘中每 5 分钟 + 盘后兜底一次）

---

## 实施顺序与工作量

| 阶段 | 内容 | 依赖 |
|---|---|---|
| 1 | 建表 + 入池逻辑 + 手动转仓 API | 无 |
| 2 | 开盘成交确认 loop | P1 |
| 3 | 止损止盈跟踪 loop | P2 |
| 4 | 统计回填 + 白名单自动刷新 | 有平仓数据后（约 2-4 周积累） |
| 5 | 前端页面 | P2 后即可开发 |

## 关键校验点

跑 1-2 周后，用同批信号对比"模拟盘实测胜率 vs 回测引擎胜率"，两者应高度接近（差异来自盘中实时触发 vs 日线回放的粒度差），这本身就是对整个回测体系的验证。

## 可复用的现有代码钩子

| 复用点 | 位置 |
|---|---|
| 表结构范本（生命周期字段） | `schema.sql` 的 `etf_signals` 表 |
| 信号来源 | `strategy_results.results_json`（15:40 `strategy_scan_loop` 落库） |
| 准入/白名单过滤 | `market_regime.is_strategy_admitted` + `recommendation.get_push_whitelist` |
| 五档指引 + 量比 | `recommendation.build_open_confirmation` + `scheduler._calc_vol_ratios` |
| 撮合口径 | `backtest/engine.match_signals`（T+1 开盘、止损止盈、含成本） |
| 统计口径 | `backtest/strategies._strategy_stat`（win_rate/avg_pnl_pct/profit_factor） |
| 调度模式 | `track_loop`（盘中每 5 分钟）+ `schedule_state` 幂等 |
| 企微推送 | `wechat.push_strategy_signals`（不受业务开关限制） |
| 前端按钮模式 | `Strategies.vue` 信号行的 addWatch/addPlan |


---

## 8. 模拟盘组合级风控（开仓 Gate + 波动率仓位）

> 原文件：`PLAN_PAPER_RISK.md`（5.9 KB，最后修改 2026-09-02 01:01）  
> 处置：**代码已实现**；阈值回调（risk_events 攒一个月）并入主计划

<!-- ===== 以下为 PLAN_PAPER_RISK.md 原文（逐字保留，未做改写）===== -->

# 模拟盘组合风控实施计划

> 创建时间：2026-09-02
> 状态：规划中，未实施
> 前置：`PLAN_PAPER_TRADING.md` Phase 1-6 已上线（成交确认 / T+1 跟踪 / 统计回填）

## 设计目标

模拟盘当前只有**逐仓止损止盈 + 资金约束**，缺组合级风控。不补上的后果：

1. **行业集中**：同一战法同一天选出的股票高度同板块（如全是银行），实盘不会满仓 5 只同行业 → 模拟盘胜率**系统性高估实盘**
2. **无熔断**：战法进入失效期时无限开新仓，失去对"连亏期"的真实检验
3. **等额仓位失真**：固定 5 万/仓不看个股波动，高波动股的风险敞口被低估

**核心原则：风控只做在"开仓 gate + 账户状态"层，不改已成交笔的撮合与 pnl 口径**
——胜率按笔统计与回测仍可比；风控改变的是"开不开这一笔"，不是"这笔怎么算"。

---

## 规则总表

### A. 开仓 Gate（`fill_pending_positions` 成交分支，与现有资金检查并列）

| # | 规则 | 阈值（可配常量） | 动作 | exit_reason |
|---|---|---|---|---|
| G1 | 同行业持仓上限 | 同 `main_industry` ≤ **2** 只 | 放弃 | `industry_limit` |
| G2 | 组合回撤熔断 | 账户净值自峰值回撤 ≥ **5%** | 冻结开仓（净值修复或手动解锁） | `risk_freeze` |
| G3 | 连亏冷却 | 连续 **3** 笔止损平仓 | 冷却 **1 个交易日** | `cooldown` |
| G4 | 日亏损限额 | 当日平仓亏损合计 ≥ **2%** 本金 | 当日不再开新仓 | `daily_stop` |

已有且不重复实现：资金检查（`no_capital`）、单战法上限 8 笔（`strategy_limit`）、
入池时止损异常过滤、T+1 不可卖。

### B. 波动率仓位（替代固定 5 万等分）

```
每股风险 = entry_price - stop_loss          （信号自带止损，天然可用）
单笔风险额 = 本金 × 0.8% = 8,000 元          （对齐 tracker.RISK_CONFIG 的 1.5%，因 20 仓更保守）
股数 = 单笔风险额 / 每股风险
市值钳位 = [20,000, 80,000]                  （防极端：低价股巨量 / 高价股 1 手都买不起）
止损缺失兜底：每股风险 = 2 × ATR14（get_kline 日线计算，参考 backtest/market_regime._atr）
```

与回测口径差异：回测等权 `position_ratio`，模拟盘波动率仓位 → **胜率按笔统计不受影响**；
组合收益曲线口径不同，对比验证时注明（胜率对比为主，收益对比为辅）。

### C. 半仓（防诱多）逻辑保留

`half` 档 = 股数减半（风险额同减半），与现有一致。

---

## 数据层变更

**`paper_account` 幂等加列**（ALTER TABLE IF NOT EXISTS 风格，参照 ranking_history 的迁移）：

| 字段 | 说明 |
|---|---|
| peak_equity REAL | 账户净值峰值（回撤基准），初始 = initial_capital |
| cooldown_until TEXT | 连亏冷却截止日（YYYY-MM-DD，空=无） |
| risk_frozen INTEGER | 回撤熔断标志 0/1 |

**净值口径**：`equity = initial_capital + realized_pnl + Σ(持仓浮盈)`；
`track_positions` 与每次平仓时刷新 `peak_equity = max(peak, equity)`。

**新表 `paper_risk_events`（审计 + 企微推送底稿）**：

| 字段 | 说明 |
|---|---|
| id, time | 自增 / 事件时间 |
| event_type | industry_limit / risk_freeze / cooldown_start / daily_stop / unfreeze |
| code | 相关个股（可空） |
| message | 人话描述（推企微用同一文案） |

---

## Phase 划分与工作量

| Phase | 内容 | 依赖 |
|---|---|---|
| 1 | 数据层加列 + risk_events 表 + 净值/峰值维护（track 与平仓两处挂钩） | 无 |
| 2 | G1-G4 开仓 gate + 冷却/熔断状态机（cooldown 到期由 `paper_track_loop` 检查解除） | P1 |
| 3 | 波动率仓位（替代固定单仓；需 `get_kline` 拉 ATR） | P1 |
| 4 | `/api/paper/risk`（风控状态总览）+ 前端模拟盘页"风控状态"卡片 + 企微事件推送 | P2 |

## 可复用代码钩子

| 钩子 | 位置 |
|---|---|
| 个股→行业映射 | `sector_industry.get_stock_industry(code)` → `main_industry` |
| ATR 参考实现 | `backtest/market_regime._atr()`（14 日，纯 numpy） |
| 行情批量 | `tencent.get_stocks_batch` |
| 风控参数范本 | `signals/tracker.RISK_CONFIG`（实盘信号跟踪已在用同一套思路） |
| gate 模式 | `paper_trading` 现有资金/战法上限检查（照抄结构） |
| 事件推送 | `flash/wechat.push_markdown_batched(force=True)` |
| 冷却到期检查 | `paper_track_loop`（每 5 分钟已在跑） |

---

## 验收标准

1. 场景构造验证全过：同行业第 3 只被拒 / 连亏 3 笔次日 cooldown / 回撤触发冻结 / 当日亏损达限停开
2. 风控事件 100% 落 `paper_risk_events` + 同步企微
3. `paper_stats` 样本可标注"风控启用日期"，**对比验证（模拟盘 vs 回测）时区分风控前后样本**
4. 已成交笔的 pnl 计算零改动（不改 `_close_position` 的结算公式）

---

## 阈值取值依据与开放问题

| 阈值 | 取值 | 依据 |
|---|---|---|
| 单笔风险 0.8% | 8,000 元 | tracker 用 1.5%，模拟盘 20 仓满仓更保守取半 |
| 回撤熔断 5% | 5 万 | tracker `max_total_risk=6%` 同源略紧 |
| 同行业 2 只 | — | 组合分散常规；等 risk_events 攒数据后回调 |
| 连亏 3 笔冷却 1 日 | — | 战法失效期的最小可观测窗口 |

**开放问题**：
1. 行业映射每月重建一次，新股/未覆盖股行业缺失 → 建议**缺失视为独立行业不限制**，但记 risk_events 留痕（宁可漏杀不可错杀开仓机会，模拟盘目的是攒样本）
2. 阈值初期为专家经验值，`paper_risk_events` 攒一个月后用实际触发频率回调
3. 波动率仓位上线后，与回测等权口径的组合收益不可直接比 → 对比验证以胜率为主

## 建议落地顺序

**本周**：Phase 1 + 2（gate 是核心价值，半天工作量）
**下周**：Phase 3 波动率仓位 + Phase 4 可见化
**持续**：risk_events 攒 2-4 周 → 与平仓样本一起做"模拟盘 vs 回测"首次正式对比


---

## 9. 交易员决策工作流（决策简报）

> 原文件：`PLAN_TRADER_WORKFLOW.md`（3.6 KB，最后修改 2026-09-10 01:56）  
> 处置：**Phase 1 大部落地**；企微定时推送与 Phase 2/3 并入主计划

<!-- ===== 以下为 PLAN_TRADER_WORKFLOW.md 原文（逐字保留，未做改写）===== -->

# PLAN_TRADER_WORKFLOW.md — 交易员工作流整合

> 2026-09-10 立项。核心思想：项目已按交易员的一天天然长出了时间线任务体系
> （15:05 快照 → 16:05 主线 → 17:00 资金流 → 17:30 主力 → 18:00 评分 → 19:30 日报…），
> 但功能散落各页，用户要开 6 个页面拼凑"今天的情况"。
> 本 Plan 把系统从「数据仪表盘」升级为「决策工作流」。

## 锚定验收标准（防范围蔓延）

**任何时刻打开系统，3 秒内知道"现在该看什么、该做什么"。**
每个新功能必须回答：它服务哪个时点、哪个决策。

## 现状盘点（数据与任务层覆盖率 ~85%）

| 时段 | 交易员在想什么 | 已有 | 缺口 |
|---|---|---|---|
| 盘前 8:00-9:15 | 昨晚发生什么？今天防什么？ | 盘前复盘(LLM)、宏观锁定 8:55、财经日历、矛盾扫描 | **缺"今日关注清单"串联** |
| 开盘 9:30 | 候选今天买不买？什么价？ | 9:35 开盘买点确认、模拟盘撮合 | 缺与 trade_plans/watchlist 联动 |
| 盘中 | 持仓有事吗？主线动了吗？ | 快讯 5min、持仓负面 10min、午间雷达 | 缺持仓异动聚合 |
| 盘后 15:00-16:30 | 今天谁在动？谁在出货？ | 收盘快照、板块快照、矛盾扫描、战法扫描、主线 16:05 | ✅ |
| 收盘后 17:00-19:30 | 重新评分、明日候选 | 资金流 17:00、主力 17:30、评分 18:00、日报 19:30（LLM） | ✅ 全在 Actions |
| 验证闭环 | 策略到底赚不赚钱？ | BucketStats（评分/主力/信号）、模拟盘绩效、战法白名单 | ✅ 09-09 起含主线/吸筹出货 |

## Phase 1：交易员决策简报（AI）⬅ 当前

**最高性价比**：管道全是现成的（LLM + 数据任务），只换"决策视角"的聚合与 prompt。

- [ ] `collect_trader_brief(phase)` 数据聚合：按 phase（premarket/intraday/postmarket）
      聚合矛盾命中、主线榜/切换、主力异常（大幅流出/吸筹）、持仓风险（持仓股的消息/
      主力/矛盾命中）、候选信号（Top50/战法）、今日日历
- [ ] LLM 决策简报 prompt：三段式「该关注 / 该做 / 该防」+ 引用数据出处
- [ ] 落表 `trader_briefs(date, phase, content_json)` + API `GET /api/report/trader-brief`
- [ ] 前端：日报页顶部加决策简报区块（按当前时间自动选 phase）
- [ ] 企微推送（盘前 9:10 / 盘后 19:35 各一次，复用 push_markdown_batched）

## Phase 2：信号 → 计划的自动化闭环

- [ ] 评分/战法/主线候选 → 一键转 `user_trade_plans`（预填买价/止损/目标）
- [ ] 持仓风险聚合日报：持仓股 ×（主力流出/消息负面/矛盾命中/技术破位）矩阵
- [ ] trade_plans 命中提醒（触达止损/目标价经 track_loop 推送）
- [ ] 模拟盘绩效归因：按入场理由（战法/信号等级/主线归属）分组统计

## Phase 3：决策质量回测

- [ ] 决策点验证体系（BucketStats 同款）：每个决策维度单独验证胜率
      ——矛盾预警准确率 / 主线跟随超额 / 透支降档有效性 / 消息降档有效性
- [ ] 反哺机制：验证不达标的自动化决策（降档/闸门）自动降级或调参
- [ ] 交易员流程本身的 K 看板：简报命中率 / 计划执行率 / 模拟盘 vs 基准

## 原则

1. **先验证再依赖**：任何自动化决策（降档/闸门/推送）都要有 BucketStats 式的
   历史验证，不达标的自动降级
2. **数据不出现有架构**：决策简报只聚合已有表，不为简报新建采集任务
3. **AI 出观点，数据给出处**：LLM 简报的每条判断必须引用具体数据（股票/数值），
   防幻觉


---

## 10. 先知雷达（L1/L2/L3 矛盾扫描）v1.0→v1.1

> 原文件：`先知雷达_功能Plan_v1.0.md`（23.1 KB，最后修改 2026-09-06 04:04）  
> 处置：**多数已覆盖**；依赖数据积累的 4 项待办并入主计划

<!-- ===== 以下为 先知雷达_功能Plan_v1.0.md 原文（逐字保留，未做改写）===== -->

================================================================================
【功能Plan】先知雷达（Prophet Radar）—— 矛盾洞察与超前预判引擎
版本：v1.1（2026-09-06 执行状态更新）
日期：2026-09-06
状态：部分落地（见下方执行对照）
================================================================================

〇、执行对照（2026-09-06 评审结论：与本项目建设大幅重叠，缺口已补齐）
================================================================================

本 Plan 由另一会话独立撰写，未包含项目已有建设。逐项对照：

| Plan 内容 | 项目现状 | 结论 |
|---|---|---|
| L1 扫描（财经日历预期差） | ✅ 已有 scan_calendar_surprise（surprise_score 口径一致） | 已覆盖 |
| L2 扫描（六维背离） | ✅ 已有 5 个 L2 扫描器（指数/板块/量价/北向/主力资金流） | 已覆盖（散户融资/分析师一致预期数据源缺失，暂缓） |
| 状态判定器 | ✅ 已有 market_regime（MA60+ADX+ATR+宽度+滞后缓冲+neutral_bearish） | 已覆盖 |
| 评分集成（矛盾/拥挤惩罚） | ✅ 已有 mainforce overlay（regime 闸门乘数，回测验证后上线） | 已覆盖且更严谨（先回测再上线） |
| 拥挤度因子（近20日涨幅） | ✅ 已上线并被更强的主力筹码因子替代（残差 IC +0.04） | 已关闭 |
| 行业内分位数打分 | ✅ 2026-09-03 落地 _industry_pct_score | 已覆盖 |
| 双模型路由（Qwen3/R1） | 现有 DeepSeek 单链路 | 暂缓（收益不明确） |
| 信号生成器 | 战法信号体系 + 动态白名单已运行 | 暂缓（矛盾样本 ≥60 条后评估，见观察清单） |

【本次新增落地（2026-09-06）】——Plan 中真正缺失的三块：

1. ✅ **L3 财报断层扫描**（contradictions/l3_scanner.py）
   - 净现比 = 经营现金流/扣非净利润，**现金失血型**（OCF<0）报警
   - 数据：stock_finance_zz 全池 1,991 只（zzshare，周一 04:30 自动周更）
   - 工程细节：非银金融排除（OCF 含客户资金变动，华林证券 -46 的教训）、
     报告期季节性阈值缩放（半年报×0.5）、|ratio|>10 异常剔除
   - 首扫结果：624 只盈利公司中现金失血 110 只（中报季典型水平）
2. ✅ **矛盾验证闭环（知识库雏形）**（contradictions/validator.py + store 验证字段）
   - 方向规则：指数/宽度/量价/北向类 → bearish；主力资金流按标题判向
   - 验证口径：矛盾日 T+1 上证涨跌（最保守的方向对错检验）
   - 回写 contradictions 五个验证列；准确率统计（整体+分类型）入报告
     "预判验证"章节 + /api/contradictions/validation-stats；前端验证徽章
   - 调度：contradiction_scan_loop 每日扫描后自动验证
3. ✅ **持仓顾问（日报版）**（daily_report._holdings_radar）
   - 主力行为信号 × L3 断层 × 用户持仓交叉；浮盈/浮亏差异化建议
   - 首扫命中 3 只持仓（出货嫌疑 → 浮盈"反弹减仓"/浮亏"观望不补仓"）

【后续待办（原 Plan 保留，依赖数据积累）】
- [ ] 矛盾样本 ≥60 条后：信号生成器评估（矛盾→可执行信号的胜率验证）
- [ ] 验证闭环积累 ≥30 条后：分类型准确率调阈值
- [ ] L3 持久性跟踪：同一公司连续 N 期现金失血 → 升级预警
- [ ] 双模型路由（等 Qwen3-32B 接入渠道明确）

一、功能定位
================================================================================

【一句话定义】
先知雷达是项目的"认知层"核心模块，通过L1/L2/L3三层矛盾扫描，在价格反应
之前识别市场的预期差、行为背离与信息断层，输出超前信号、状态判定与持仓
决策，将系统从"跟随市场"升级为"预判市场"

【与现有系统的关系】
  现有系统架构：
    数据层  ->  macro_panel / sector_flow
    评分层  ->  scoring/engine.py
    LLM层   ->  diagnosis / review
    执行层  ->  模拟盘 / 用户持仓
                  |
         【新增】先知雷达（认知层）
                  |
    矛盾扫描 -> 状态判定 -> LLM推理 -> 信号生成 -> 持仓顾问

【核心价值】
  1. 时间优势：L1领先1-3天，L2领先3-7天，L3领先7-30天
  2. 风险过滤：通过矛盾识别，过滤假突破、诱多、拥挤陷阱
  3. 决策闭环：从"数据->评分->信号->持仓->风控"全链路打通
  4. 认知沉淀：每次矛盾验证结果回写知识库，持续进化

================================================================================
二、架构设计
================================================================================

【模块拆分】

先知雷达 = 5大子模块 + 1个知识库

  矛盾扫描器(Scanner) -> 状态判定器(Regime) -> LLM推理层(Reasoner)
                                                  |
  知识库(Knowledge) <- 信号生成器(Signal) <- 持仓顾问(Advisor)

【数据流】

  开盘前(9:00)          盘中(9:30-15:00)          收盘后(15:30)
      |                       |                         |
      v                       v                         v
  矛盾扫描(L1/L2/L3)     矛盾监控(实时触发)         矛盾验证(昨日预判)
      |                       |                         |
      v                       v                         v
  状态判定               状态维持或切换              状态更新写入历史
      |                       |                         |
      v                       v                         v
  LLM推理(生成预案)      LLM即时分析                LLM复盘验证逻辑
      |                       |                         |
      v                       v                         v
  信号生成+持仓建议      信号刷新+风险警示           信号归档+知识沉淀

================================================================================
三、五大子模块详细设计
================================================================================

【3.1】矛盾扫描器（Contradiction Scanner）

职责：每日三次扫描（开盘前、午间、收盘后），识别L1/L2/L3矛盾

扫描任务调度：
  09:00  pre_market_scan()   -> 扫描隔夜事件、财经日历、外围市场
  12:00  mid_market_scan()   -> 扫描上午盘面异常、板块背离
  15:30  post_market_scan()  -> 扫描全天矛盾、验证昨日预判
  21:30  us_market_scan()    -> 扫描美股、美联储讲话、数据发布

【L1扫描逻辑】

输入：财经日历事件 + CME FedWatch + 个股财报
输出：L1矛盾列表（按surprise_score排序）

代码框架：
```python
class L1Scanner:
    def scan(self, events):
        contradictions = []
        for event in events:
            if not event.has_expectation:
                continue
            surprise = (event.actual - event.expected) / abs(event.expected) * 10
            if abs(surprise) > config.l1_surprise_threshold:
                contradictions.append(L1Contradiction(
                    event=event.name,
                    expected=event.expected,
                    actual=event.actual,
                    surprise_score=surprise,
                    correction_direction="up" if surprise > 0 else "down",
                    confidence="高" if abs(surprise) > 5 else "中",
                    time_window="1-3日",
                ))
        return sorted(contradictions, key=lambda x: abs(x.surprise_score), reverse=True)
```

【L2扫描逻辑】

六维背离检测：

| 维度 | 叙事层数据 | 行为层数据 | 背离判定 |
|------|-----------|-----------|----------|
| 散户 | sentiment_index | margin_balance_change | 情绪↑但融资↓ |
| 机构 | analyst_consensus | northbound_flow | 推荐买入但流出 |
| 政策 | policy_statement | actual_liquidity | 宽松表态但缩量 |
| 板块 | narrative_clusters | sector_flow | 利好叙事但资金流出 |
| 指数 | index_price | market_breadth | 指数红但宽度差 |
| 量价 | price_trend | volume_trend + MACD | 价升量减 |

代码框架：
```python
class L2Scanner:
    def scan(self):
        checks = [
            self._check_retail_divergence(),
            self._check_institutional_divergence(),
            self._check_policy_divergence(),
            self._check_sector_divergence(),
            self._check_index_divergence(),
            self._check_price_volume_divergence(),
        ]
        contradictions = [c for c in checks if c.is_divergence]
        for c in contradictions:
            c.persistence = self._check_persistence(c.dimension, days=2)
            if c.severity == "严重" and c.persistence >= 2:
                c.trigger_alert = True
        return contradictions
```

【L3扫描逻辑】

财报季/重大事件后触发：

```python
class L3Scanner:
    def scan_financial_reports(self, stocks):
        gaps = []
        for code in stocks:
            report = self._get_latest_report(code)
            ocf_ni_ratio = report.operating_cashflow / report.net_profit
            if ocf_ni_ratio < config.l3_ocf_ni_ratio_threshold:
                gaps.append(L3Gap(
                    dimension="财报断层",
                    public_info=f"净利润{report.net_profit}亿",
                    verified_info=f"OCF/NI={ocf_ni_ratio:.2f}",
                    gap_size="大" if ocf_ni_ratio < 0.3 else "中",
                ))
        return gaps
```

【3.2】状态判定器（Regime Detector）

职责：基于ADX/MA/宽度/外围指数，判定市场状态，支持hysteresis缓冲

```python
class RegimeDetector:
    def detect(self, panel, history):
        adx = panel.get("adx", 0)
        ma_trend = self._ma_trend(panel)
        breadth_ratio = self._breadth_ratio(panel)
        foreign_panic = self._foreign_panic_index(panel)

        if adx > config.adx_trend_threshold:
            regime = "offensive" if ma_trend == "up" else "defensive"
        else:
            if ma_trend == "down" and (breadth_ratio < config.breadth_panic or foreign_panic):
                regime = "neutral_bearish"
            else:
                regime = "neutral"

        regime = self._apply_hysteresis(regime, history)

        return MarketRegime(
            name=regime,
            weights=config.regime_weights[regime],
            adx=adx,
            breadth_ratio=breadth_ratio,
            foreign_panic=foreign_panic,
        )
```

【3.3】LLM推理层（Prophet Reasoner）

职责：将矛盾扫描结果输入LLM，生成超前预判和叙事分析

【双模型路由】

| 场景 | 模型 | 温度 | JSON模式 | 理由 |
|------|------|------|----------|------|
| 矛盾扫描结果解析 | Qwen3-32B | 0.1 | 是 | 快、准、JSON稳 |
| 盘前/盘后复盘 | DeepSeek-R1 | 0.3 | 否 | 推理深、可解释 |
| 紧急矛盾（L3） | DeepSeek-R1 | 0.2 | 否 | 需要深度因果链 |

【LLM输入模板核心规则】

你是"先知雷达"的核心推理引擎。你的任务不是解释已发生的价格变动，
而是在价格变动之前，通过识别矛盾来预判方向。

核心原则：
1. 矛盾越大，后续修正烈度越大
2. 矛盾越早识别，超额收益越丰厚
3. 三层矛盾可以联动升级（L1->L2->L3）
4. 行为层（资金实际动向）比叙事层（口头表态）更真实

输出要求：
- 必须基于输入数据推理，禁止编造
- 置信度<60%时标注"低置信度"，建议降级为"观望"
- 禁止在reasoning中使用"通常/历史上/一般"等模糊词
- 必须给出具体的传导链：矛盾->宏观变量->行业->资产->操作

【3.4】信号生成器（Signal Generator）

职责：将LLM推理结果转化为可执行的交易信号

```python
class SignalGenerator:
    def generate(self, contradictions, regime, llm_output):
        signals = []
        for c in contradictions:
            if c.layer == "L1" and c.confidence == "高":
                signals.append(self._from_l1(c, regime))
            elif c.layer == "L2" and c.severity == "严重":
                signals.append(self._from_l2(c, regime))
            elif c.layer == "L3" and c.gap_size == "大":
                signals.append(self._from_l3(c, regime))

        signals.extend(self._from_llm(llm_output))
        signals = [s for s in signals if self._validate(s, regime)]
        return signals[:5]

    def _validate(self, signal, regime):
        # 震荡偏空禁止长期做多
        if regime.name == "neutral_bearish" and signal.direction == "long":
            if signal.timeframe not in ["日内", "波段(3-5日)"]:
                return False
        # 拥挤度过滤
        if signal.crowding_score < 30:
            signal.confidence = "低"
            signal.position_sizing = "轻仓试探"
        # D状态合规
        if signal.d_state_compliance_violation:
            return False
        return True
```

【信号Schema】

```json
{
  "signals": [
    {
      "etfName": "黄金ETF",
      "direction": "long",
      "timeframe": "波段(3-5日)",
      "confidence": "中",
      "entry_condition": "回调至4450附近且未跌破4400",
      "invalidation": "跌破4400或9月CPI超预期反弹",
      "support": 4400,
      "resistance": 4600,
      "position_sizing": "轻仓试探",
      "reasoning": "央行购金结构性支撑+加息概率已部分定价",
      "contradiction_source": "L1_预期差_沃勒鸽派",
      "regime_alignment": "neutral_bearish_允许",
      "crowding_score": 45
    }
  ]
}
```

【3.5】持仓顾问（Portfolio Advisor）

职责：基于矛盾洞察和信号，对用户持仓给出具体操作建议

```python
class PortfolioAdvisor:
    def advise(self, portfolio, contradictions, signals):
        advices = []
        for holding in portfolio:
            advice = HoldingAdvice(
                code=holding.code,
                name=holding.name,
                cost=holding.cost,
                current_price=holding.current_price,
                pnl_pct=holding.pnl_pct
            )

            # 矛盾影响评估
            relevant = self._find_relevant(holding, contradictions)
            if relevant:
                advice.contradiction_impact = self._assess_impact(holding, relevant)

            # 定价程度判断
            advice.pricing_degree = self._judge_pricing(holding, signals)

            # 具体操作建议
            advice.action = self._recommend_action(holding, advice, regime)
            advice.stop_loss = self._calculate_stop_loss(holding)
            advice.take_profit = self._calculate_take_profit(holding)

            advices.append(advice)
        return advices

    def _recommend_action(self, holding, advice, regime):
        # 磨底期纪律
        if regime.name in ["neutral_bearish", "defensive"]:
            if holding.pnl_pct < -5:
                return "持有观望，不补仓"
            elif holding.pnl_pct > 3:
                return "利用反弹减仓"

        # 拥挤度纪律
        if advice.crowding_score < 30 and holding.pnl_pct > 0:
            return "拥挤度高，止盈减仓"

        # 矛盾驱动
        if advice.contradiction_impact == "利空" and holding.pnl_pct > 0:
            return "矛盾利空，减仓避险"

        return "持有观察"
```

持仓建议输出格式示例：

```
⚡ 对您的持仓影响

【000567 海德股份】成本6.976，现价6.76，浮亏-3.09%
- 相关矛盾：L1_非农大超预期(+16.2万)->紧缩预期强化
- 定价程度：已部分定价（昨日评分Top1但已跌3%）
- 建议：持有观望。非银金融有防御属性，但大盘系统性风险未解除
- 止损位：6.30（-10%）

【000617 中油资本】成本7.505，现价7.19，浮亏-4.20%
- 相关矛盾：L2_油价涨但油气板块资金流出（拉高出货）
- 定价程度：未完全定价（反身性校验失败）
- 建议：若反弹至7.30+，减仓避险
- 止损位：6.80（-10%）
```

================================================================================
四、知识库设计（Knowledge Base）
================================================================================

职责：沉淀每次矛盾的识别、验证、修正结果，持续进化系统认知

数据模型：

```python
@dataclass
class ContradictionRecord:
    id: str
    timestamp: datetime
    layer: str  # L1/L2/L3
    description: str
    identified_by: str

    # 预判
    predicted_direction: str
    predicted_magnitude: str
    predicted_time_window: str
    confidence: str

    # 验证
    validated: bool = False
    validation_timestamp: Optional[datetime] = None
    actual_direction: Optional[str] = None
    actual_magnitude: Optional[str] = None
    validation_result: Optional[str] = None  # 成功/失败/部分成功
    failure_reason: Optional[str] = None
    model_adjustment: Optional[str] = None
```

学习闭环：
  识别矛盾 -> 生成预判 -> 市场验证 -> 记录结果 -> 模型调优
      ^_______________________________________________|

================================================================================
五、与现有系统的集成接口
================================================================================

【5.1】与评分系统的集成

```python
# scoring/engine.py 中新增
from prophet import ProphetRadar

class ScoringEngine:
    def __init__(self):
        self.prophet = ProphetRadar()

    def score(self, stock):
        base_score = self._calculate_base_score(stock)

        # 先知雷达增强
        contradiction_penalty = self.prophet.get_contradiction_penalty(stock)
        crowding_penalty = self.prophet.get_crowding_penalty(stock)
        regime_multiplier = self.prophet.get_regime_multiplier(stock)

        final_score = (base_score + contradiction_penalty + crowding_penalty) * regime_multiplier

        return Score(
            value=final_score,
            regime=self.prophet.current_regime.name,
            contradictions=self.prophet.active_contradictions,
        )
```

【5.2】与LLM诊断流的集成

在 llm/prompt.py 中新增"先知雷达技能"：

步骤1：L1扫描（预期差）
  - 检查今日是否有重要数据发布
  - 对比"市场预期"与"实际值"
  - 若|surprise_score|>3，标记为高预期差事件

步骤2：L2扫描（行为背离）
  - 检查六维背离：散户/机构/政策/板块/指数/量价
  - 若发现严重背离且持续2日，触发"主力逻辑切换"警报

步骤3：L3扫描（信息断层）
  - 财报季检查：经营现金流/净利润比率
  - 地缘事件检查：官方声明 vs 物理数据

步骤4：矛盾联动分析
  - L1+L2同时出现 = 高概率修正
  - L1+L2+L3同时出现 = 极高概率剧烈修正

步骤5：生成超前预判
  - 基于矛盾，预判未来1-3天的价格方向
  - 给出具体的传导链
  - 标注置信度和失效条件

【5.3】与复盘流的集成

```python
# review.py 中新增矛盾验证

def run_review_llm(...):
    # 原有复盘逻辑...

    # 新增：矛盾验证
    prophet = ProphetRadar()
    yesterday_contradictions = prophet.get_yesterday_contradictions()
    validation_results = prophet.validate(yesterday_contradictions, today_panel)

    # 将验证结果注入复盘prompt
    review_prompt += format_validation_results(validation_results)
    review_prompt += format_new_contradictions(prophet.scan())

    # 继续原有复盘逻辑...
```

================================================================================
六、实施路线图
================================================================================

【Phase 1：基础框架（2周）】

Week 1:
  [ ] 搭建矛盾扫描器基础框架（L1/L2/L3类定义）
  [ ] 实现L1扫描（财经日历+CME FedWatch）
  [ ] 实现状态判定器（neutral_bearish判定逻辑）
  [ ] 集成到现有评分系统（环境变量切换）

Week 2:
  [ ] 实现L2扫描（六维背离检测）
  [ ] 实现信号生成器（基础Schema）
  [ ] 实现持仓顾问（基础建议逻辑）
  [ ] 编写LLM Prompt（先知雷达技能）
  [ ] 单元测试（用9/1-9/5数据验证）

【Phase 2：LLM集成（2周）】

Week 3:
  [ ] 实现双模型路由（诊断Qwen3-32B / 复盘DeepSeek-R1）
  [ ] 集成矛盾扫描结果到诊断流Prompt
  [ ] 集成矛盾验证到复盘流Prompt
  [ ] 实现知识库（SQLite/JSON存储）

Week 4:
  [ ] 实现L3扫描（财报质量检测）
  [ ] 实现拥挤度因子（近20日涨幅计算）
  [ ] 实现行业内分位数打分（映射表集成）
  [ ] 端到端测试（完整交易日模拟）

【Phase 3：优化迭代（2周）】

Week 5:
  [ ] 回测验证（用8/20-9/5数据验证矛盾识别准确率）
  [ ] 调优L1/L2/L3阈值（基于回测结果）
  [ ] 优化LLM Prompt（减少幻觉、提高JSON稳定性）
  [ ] 用户测试（内部试用）

Week 6:
  [ ] 修复Bug
  [ ] 性能优化（扫描速度<1秒）
  [ ] 文档完善
  [ ] 正式上线

================================================================================
七、预期效果与KPI
================================================================================

【量化指标】

| 指标 | 当前基线 | 目标 | 验证方式 |
|------|----------|------|----------|
| 矛盾识别准确率 | — | L1>70%, L2>60%, L3>50% | 回测验证 |
| 信号胜率 | 62.5%（震荡档） | +10pp | 固定持有期回测 |
| 信号盈亏比 | — | >1.5 | 模拟盘统计 |
| 回撤控制 | — | 最大回撤<15% | 模拟盘统计 |
| 响应速度 | — | 扫描<1秒，LLM<10秒 | 性能测试 |

【定性效果】

1. 从"跟随市场"到"预判市场"：在非农数据、沃什讲话等事件前，提前1-3天识别矛盾
2. 从"解释已发生"到"洞察将发生"：日报不再是事后总结，而是超前预案
3. 从"单一维度"到"三层验证"：通过L1+L2+L3联动，提高决策置信度
4. 从"静态权重"到"动态适应"：根据矛盾类型和市场状态，自动调整评分权重

================================================================================
八、风险与对策
================================================================================

| 风险 | 概率 | 影响 | 对策 |
|------|------|------|------|
| LLM幻觉导致错误信号 | 中 | 高 | 数据真实性铁律+JSON校验+置信度过滤 |
| 矛盾识别过度敏感（噪音） | 中 | 中 | 阈值调优+hysteresis缓冲+人工审核初期 |
| 计算资源不足（双模型） | 低 | 中 | Qwen3-32B成本低，R1仅用于复盘 |
| 数据延迟（regime判定滞后） | 中 | 高 | 环境变量覆盖+实时回填机制 |
| 用户不理解矛盾理论 | 低 | 低 | 日报增加"矛盾解读"小节，渐进式教育 |

================================================================================
九、命名与品牌
================================================================================

【功能名称】先知雷达（Prophet Radar）

【命名理由】
- "先知"：体现"超前预判"的核心价值
- "雷达"：体现"扫描-识别-预警"的工作方式
- 英文Prophet：取"预言者"之意

【内部代号】CPE（Contradiction Prophet Engine）

【用户可见名称】
- 日报中："📡 先知雷达扫描"
- 前端："先知雷达 | 今日识别X个矛盾"
- 信号标签："[先知] 基于L2行为背离生成"

================================================================================
【文档结束】
================================================================================


---

## 11. 2026-09-03 交易日复盘与 9/4 预案

> 原文件：`复盘Plan_2026-09-03.md`（9.0 KB，最后修改 2026-09-03 23:26）  
> 处置：**当日作业已完成**（操作清单有时效性）；其中唯一跨期项（行业地缘抗性白名单）已并入主计划

<!-- ===== 以下为 复盘Plan_2026-09-03.md 原文（逐字保留，未做改写）===== -->

# 2026年9月3日 A股深度复盘Plan
**版本**: v2.1 | **创建时间**: 2026-09-03 20:32

---

## 背景
- **A股历史定位**: 周期9第三阶段(2024.09-2026)，4000点黄金震荡区间，以时间换空间
- **今日市场状态**: 指数微红(沪指+0.02%)但超3500只下跌，典型的'权重股护盘+小票失血'
- **外围环境**: 日韩暴跌(-4%/-3%)、油价$90+、10Y美债4.75%+、美伊冲突升级

---

## 第一部分：A股本质理解

理解A股的历史脉络，才能做好当日复盘


### 制度试验史
1990-1996额度审批制(各省分配上市名额，服务国企脱困)→2005股改全流通→2019注册制→2024新质生产力。A股本质是'政策驱动的融资市场'，不是'价值投资市场'

**对今日启示**: 今日保险/券商上涨=政策护盘信号，不是基本面改善。半导体大跌=注册制扩容后估值中枢下移的延续

### 供需平衡演化史
IPO扩容→存量资金稀释→估值中枢下移。注册制后5000+上市公司，'仙股化'开始。2024-2026年政策托底但增量资金有限

**对今日启示**: 今日缩量1.78万亿(较昨日再缩)=增量资金枯竭，存量博弈下'涨指数不涨个股'是必然结果

### 人性博弈史
七亏二平一赚。散户牛市末期追高、熊市底部割肉。机构同质化抱团→踩踏。信息传播加速→泡沫发酵更快(2024年9-10月21天暴涨37%是最短牛市)

**对今日启示**: 今日3500只下跌=散户恐慌割肉+机构调仓踩踏。培育钻石/海运上涨=游资抱团小票，与基本面无关

### 范式跃迁史
散户短线→机构化→量化+AI驱动。分红驱动从'圈钱'转向'回报'，但转变缓慢且痛苦

**对今日启示**: 今日领涨的保险/券商有分红支撑(负熵资产)，领跌的半导体多数零分红(耗散资产)。A股正在从'炒估值'向'攒股复投'范式艰难转型

---

## 第二部分：今日市场解构

用项目数据对9月3日做充分复盘

### 2.1 指数层面
- **表面**: 沪指+0.02%，深成指+0.10%，创业板+0.01%——'三大指数全红'
- **实质**: 超3500只下跌，涨跌比≈1:3.5。指数红是权重股(保险/券商)护盘的假象
- **历史映射**: 类似2015年股灾期间'国家队护盘'——指数稳住，个股失血。这不是底部，是'磨底'

### 2.2 板块层面

**领涨板块**:

- **培育钻石**: 题材炒作(小市值、机构少、筹码轻) | 持续性: 低，游资脉冲 | 海外关联: 极低

- **海运/保险/券商**: 政策护盘+高股息防御 | 持续性: 中，但券商是'渣男板块'，高开即巅峰 | 海外关联: 低(保险有利率敏感性)

- **贵金属**: 地缘避险(美伊冲突) | 持续性: 中，取决于冲突是否升级 | 海外关联: 高(跟随国际金价)

**领跌板块**:

- **半导体/通信设备**: 外围科技股估值重估(英伟达跌4.57%+沃什鹰派+实际利率↑) | 历史映射: 类似2021年茅指数瓦解→2024年赛道股崩盘→2026年AI硬件踩踏，机构抱团→同质化→踩踏的循环 | 教训: N/A

- **农业**: 上周涨幅超6%后的获利回吐(补跌) | 历史映射: N/A | 教训: 即使是'防御板块'，涨高了也会跌

- **文化传媒/互联网/餐饮旅游**: 消费复苏预期落空+外资流出(中概股-2.19%) | 历史映射: N/A | 教训: N/A

### 2.3 资金面层面
- **成交额**: 1.78万亿，较昨日再缩量(连续两日缩量)
- **解读**: 缩量下跌=买盘枯竭，比放量杀跌更危险。说明大资金在观望，不是恐慌出逃

### 2.4 外围传导层面
- **日韩股市**: 韩国-3.99%，日经-2.85%→A股低开
- **油价**: 布伦特$90.49，WTI $85.76→通胀预期↑→科技股承压
- **美债**: 10Y 4.75%+，30Y 5.3%+→全球贴现率↑→成长股杀估值
- **美伊冲突**: 霍尔木兹海峡风险→地缘溢价→军工脉冲(但A股军工联动弱)

---

## 第三部分：项目数据驱动的复盘框架

用你的系统数据做结构化复盘

### 3.1 市场状态判定
- **当前判定**: neutral(震荡档)
- **问题**: 8/28数据滞后，未反映9/1-9/3大跌
- **建议判定**: neutral_bearish(震荡偏空)

**判定依据**:
- ADX<20(无强趋势)
- MA20<MA60(趋势向下)
- 涨跌比<0.4(宽度恶化)
- 连续2日缩量下跌(买盘枯竭)
- 日韩暴跌+油价暴涨(外围恐慌)

### 3.2 五维度评分复盘
- **当前权重**: 技术27/资金23/基本面23/成长17/质量11
- **问题**: 昨日回测显示'成长/质量'在大跌段不仅无防守，反而跌更多(成长高分组-2.33%，质量高分组-2.02%)
- **原因**: 高成长/高质量=前期涨多的白马=机构拥挤=大跌段被抛售

### 3.3 拥挤度因子验证
- **假设**: 高成长+低拥挤的股票今日抗跌

### 3.4 行业地缘抗性验证
- **白名单行业**: 军, 工, 、, 电, 网, 、, 公, 用, 事, 业, 、, 内, 需, 消, 费, 、, 建, 筑, 建, 材

---

## 第四部分：历史周期对照

将今日行情映射到A股35年历史

### 4.1 最相似的历史阶段
- **候选1**: 周期6(2008-2012)四万亿后的磨底期
- **候选2**: 周期7(2015-2016)去杠杆后的磨底期
- **候选3**: 周期9第二阶段(2021-2024)赛道股崩盘

### 4.2 历史铁律对今日的启示
- **牛短熊长**: 当前处于周期9的磨底期，不是牛市起点。磨底期平均6-12个月
- **杠杆铁律**: 外围紧缩=全球流动性收紧=变相去杠杆。科技股对利率最敏感，首当其冲
- **分红负熵**: 今日领涨的保险/券商有分红支撑，领跌的半导体多数零分红。'无分红=耗散资产'
- **拥挤踩踏**: 半导体大跌=前期AI抱团(2024-2025)瓦解的延续。机构同质化→踩踏，与2021年茅指数瓦解同理

---

## 第五部分：明日(9月4日)预案

基于今日复盘，制定明日策略

### 5.1 关键观察指标

| 指标 | 时间 | 阈值 | 操作 |
|------|------|------|------|
| A50期指开盘 | 9:15 | 低开>1%=情绪恶化，高开=意外强势 | 若低开>1%，临时将REGIME_OVERRIDE设为neutral_bearish |

| 指标 | 时间 | 阈值 | 操作 |
|------|------|------|------|
| 涨跌家数比 | 9:25集合竞价 | <0.3=情绪冰点 | 冰点时质量权重临时+5%，技术权重-5% |

| 指标 | 时间 | 阈值 | 操作 |
|------|------|------|------|
| 跌停家数 | 盘中持续 | >50家=恐慌 | 触发 regime 层警示：'极端缩量+跌停放大，技术面信号可信度下降' |

| 指标 | 时间 | 阈值 | 操作 |
|------|------|------|------|
| 油价 | 盘中 | 布伦特>95=滞胀恐慌 | 若油价冲95，全面防御，不追任何板块 |

| 指标 | 时间 | 阈值 | 操作 |
|------|------|------|------|
| 今晚美股 | 21:30-4:00 | 道指-2%=全球风险规避 | 若美股大跌，明早A股低开概率极高，提前减仓 |

### 5.2 持仓操作建议

**中国船舶(600150)**:
- 成本: 37.40
- 当前逻辑: 军工/造船长期受益霍尔木兹海峡风险，但短期受大盘情绪拖累
- 建议: 不补仓。若明日军工脉冲(如海峡封锁传闻)，利用反弹减仓而非加仓。参考周期6/7磨底期，每次反弹都是减仓窗口
- 止损: 若跌破35(约-6%)，考虑止损

**中油资本(000617)**:
- 成本: 7.505
- 当前逻辑: 油价涨但油气板块高开低走(反身性校验失败)，市场认为油价脉冲不可持续
- 建议: 若明日有脉冲上涨(油价站稳90)，减仓避险。7.505成本接近当前价，解套机会大于中国船舶
- 止损: 若跌破7.0(约-7%)，止损

### 5.3 系统操作清单
- [P0] 检查market_regime_history，确认最新记录日期
- [P0] 若记录滞后，启用REGIME_OVERRIDE=neutral_bearish
- [P1] 从ranking_history提取今日Top10，验证实际表现
- [P1] 计算拥挤度因子(近20日涨幅)与今日收益的相关性
- [P1] 从sector_flow验证白名单行业资金流向
- [P2] 9/5非农前，每日固定持有期回测(T+3/T+5)

---

## 第六部分：长期认知建设

基于A股历史，建立正确的投资认知

### 6.1 A股不是美股
- A股是'政策市+资金市'，不是'基本面定价市'
- 牛短熊长是铁律，脉冲牛市12-18个月，磨底熊市24-48个月
- '七亏二平一赚'不是诅咒，是结构——散户在错误的时间做错误的事
- 机构同质化抱团→踩踏，与散户追涨杀跌本质相同

### 6.2 当前周期位置
- 周期9第三阶段(2024.09-2026)：4000点黄金震荡区间
- 政策托底但增量资金有限，'以时间换空间'
- 高股息蓝筹(保险/银行/电力) vs 科技成长(半导体/AI) 两极分化
- 当前处于'磨底期'，不是'牛市起点'

### 6.3 对量化系统的启示
- 资金面(+0.094)是唯一正预测力——A股震荡市本质是资金博弈
- 成长/质量因子在紧缩环境下反向——需要'拥挤度'过滤
- 行业地缘抗性(白名单)在震荡偏空期有效——需要行业调节系数
- 状态判定(NEUTRAL_BEARISH)比权重调整更重要——先判断'是什么市'，再决定'怎么打'

---

**核心结论**: 当前不是'抄底'时机，是'磨底'阶段——指数微红是权重股护盘假象，3500只下跌才是真相。参照周期6(2008-2012)和周期7(2015-2016)，磨底期平均6-12个月，期间每次反弹都是减仓窗口。

