# 评分系统详解（个股 0-100 分是怎么算出来的）

> 2026-09-20 ｜ 基于 main@4ea36fc（feat/flow5-extreme-inflow 已合并）
> 核心代码：`backend/app/scoring/engine.py`（约 1800 行）｜ 前端镜像：`frontend/src/utils/scoringEngine.js`
> 这份文档回答：一只股票的分数从哪来、每个子项怎么打分、信号怎么定、改规则要动哪里。

---

## 0. 定位与设计哲学

评分系统把一只股票压缩成 **0-100 综合分 + 五档信号**（强烈买入/买入/观望/卖出/强烈卖出），
用途是**排序与初筛**（排行榜、买入候选），不是精确择时。

三条设计原则：
1. **五维加权**：技术/资金/基本面/成长/质量，权重随市场状态动态切换；
2. **子项锚点化**：每个子项是一条明确的打分曲线（阈值表），不搞黑盒模型——
   代价是锚点=专家经验，需要用回测持续检验（见 §7 因子体检）；
3. **数据缺失降级**：某维度数据缺失时**跳过并归一化**（不记 0 分），缺失太多宁给中性。

**三条计算路径**（同一套锚点，必须逐点一致）：

| 路径 | 执行方 | 数据 | 输出 |
|---|---|---|---|
| 日批精算榜 | Actions rank_live（19:30+） | 当日收盘 pack + 指标预载 | ranking_live 落库 |
| API 实时榜 | Render /api/score/batch/top | 实时行情缓存 + pack | 3 分钟缓存，前端默认榜 |
| 前端本地计算 | 浏览器 scoringEngine.js | 本地 IndexedDB 数据包 + 实时行情 | 本地榜（离线可用） |

一致性目标：本地榜与服务端榜差异 ≤1 分。已同步项：flow5 倒U、nb 换手钳制、
权重/regime 注入（`/api/score/weights` 返回 weights + regime_state）。
已知差异源：本机用实时行情 vs 服务端收盘数据；本机 roughScore 粗排两阶段近似。

---

## 1. 数据准备：指标行（tech_data）

引擎不接收原始 K 线，接收**指标行数组**（`_calc_technical_fast` 从 K 线向量化计算，
每行字段）：

```
date, open, high, low, close, volume,
ma5, ma10, ma20, ma60, dif, dea, macd(柱), rsi,
k, d, j, boll_upper, boll_mid, boll_lower
```

生产链路：K线 → `indicator_cache.compute_latest_indicators`（含 `_series` 近 80 行）
→ 引擎取 `latest=tech_data[-1]`、`prev=tech_data[-2]`。
**数据不足 30 行 → 技术面给中性 50 分**。

---

## 2. 五维权重（动态切换）

| 维度 | 默认静态 | 进攻 | 震荡(含nb) | 防御 |
|---|---|---|---|---|
| 技术面 | 0.32* | 0.50 | 0.27 | 0.12 |
| 资金面 | 0.20* | 0.25 | 0.23 | 0.15 |
| 基本面 | 0.18* | 0.08 | 0.23 | 0.25 |
| 成长 | 0.18* | 0.12 | 0.17 | 0.13 |
| 质量 | 0.12* | 0.05 | 0.11 | 0.35 |

`*` 默认静态权重**前后端完全一致**（后端 `engine.py W_*` 常量 = 前端
`scoringEngine.js DEFAULT_WEIGHTS` = 0.32/0.20/0.18/0.18/0.12）；生产中 regime
全程接管（引擎启动即按当日 regime set_weights，静态值只在 regime 缺失时兜底）。
- **切换机制**：日批/Render 判定 regime → `engine.set_weights(regime=...)`（判重键
  date+state）；前端从 `/api/score/weights` 拉取注入（同响应含 regime_state，
  驱动 nb 换手钳制）。
- **neutral_bearish**：权重=震荡（09-03 回测否决了防御化），但 regime_score=-20、
  触发换手钳制与战法闸门。
- **验证状态**：震荡档已回测背书（350 快照，64.3% vs 60.0%）；进攻/防御档
  **尚无样本验证**（regime_review 中两类样本为 0）——等积累。

---

## 3. 子项锚点全表（★=体检结论）

### 3.1 技术面（子项权重 25/25/20/15/15）

**MA趋势（25）**：基线 50，多空对称加减。
| 条件 | 分值变化 |
|---|---|
| price > MA5 / < MA5 | +5 / −5 |
| price > MA20 / < MA20 | +5 / −5 |
| 多头排列 MA5>MA10>MA20 | +8 |
| 部分多头 MA5>MA10 | +4 |
| 完美空头 | −8 |
| 部分空头 | −4 |
| price > MA60 | +4 |
| price 距 MA60 3% 以内 | +2 |
| MA5 金叉 MA10（昨日≤今日>） | +3 |
| 死叉 | −3 |

**MACD动量（25）**：基线 50。DIF>0 ±5；红柱 +4（较昨日放大再 +3）/ 绿柱 −4；
DIF>DEA ±4；金叉 +5 / 死叉 −5；连续 5 日红柱 +4（3 日 +2）。

**RSI强弱（20）——趋势跟随口径**（★旧版均值回归口径已废弃）：
| RSI | 分值 |
|---|---|
| ≥80 | 80（极强，防追顶略降） |
| 70~80 | **90**（最佳） |
| 60~70 | 78 |
| 50~60 | 62 |
| 40~50 | 50 |
| 30~40 | 38 |
| 20~30 | 22 |
| <20 | 15 |

**KDJ（15）**：基线 50。K>D ±3；金叉 +5 / 死叉 −5；K、D 同>80 +3 / 同<20 −3；
J>100 +2 / J<0 −2。

**布林带（15）**：位置 pos=(price−下轨)/带宽。pos>0.8 +6；>0.6 +4；>0.4 +1；
>0.2 −3；>0 −5；跌破下轨 −6。

> ★ 因子体检（2026-09-20，`subfactor_ic_20260920_0103.md`）：**以上 5 子项在
> 5-10 日持有窗全部负 IC**（RSI −0.218/MA −0.192/布林 −0.179/MACD −0.140），
> 强市截面不衰减 → 短持有窗上是截面内反转效应。处置：技术面定位改写+权重
> 二次校准已在建议清单（需模拟盘验证），**禁止直接翻曲线**。

### 3.2 资金面（子项权重 30/25/20/25/20，5 因子时总权重 120 归一化）

**量价配合（30）**：最近 5 日上涨日量 vs 下跌日量——放量上涨/缩量下跌加分，
量价背离减分。

**涨跌动量（25）**：基线 50。今日涨幅 min(涨幅×2, +10)；近 5 日/20 日涨跌幅
分档加减（如 20 日 <−5% −4）。

**换手率（20）**：| 1~3% → 90（最佳）| 3~5% → 80 | 5~8% → 65 | 8~15% → 45 | >15% → 低 |。
★ 体检：5 日 IC≈0、10 日 +0.075（全场唯一正 IC）→ 重校准候选。
**nb 钳制**：regime=neutral_bearish 且换手>8% → 资金面**总分**封顶 40（不改子项）。

**成交额（25）**：基线 50。今日额/10 日均额 >2 +12、>1.5 +8、>1 +3、<0.5 −8；
近 5 日均额/前 5 日均额 >1.3 +8、>1.1 +4、<0.7 −8。

**主力净流入（20）——倒U曲线（09-13 上线，前后端同曲线）**：
| flow5(%) | 分值 |
|---|---|
| ≤−10 | 0 |
| −10~−6 | 0→15 |
| −6~−3 | 15→30 |
| −3~0 | 30→55 |
| 0~+2 | 55→**80（峰值）** |
| +2~+5 | 80→55 |
| +5~+10 | 55→30 |
| +10~+20 | 30→5 |
| >+20 | 0 |

- 设计：温和流入最优，极端流入=散户陷阱刻意降分（回测：整体去超额 IC
  −0.126→+0.062）。
- **位置维度已回测否决**（`flow5_pos_20260920_0044.md`：低位极端流入 5 日去超额
  −0.301%，不满足预注册标准）→ 不加位置维度。
- **标签语义**（分支 P1）：flow5>+5 时 details key 改为
  `主力极端流入(散户陷阱降分)`，与真流出区分。
- ★ 体检：倒U后全样本 IC +0.004（中性，改造前单调曲线 −0.126）——改善成立。

### 3.3 基本面（PE 30 / PB 20 / 市值规模 25 / 振幅 25）

**PE**：PE≤0 → 40 分（亏损，路由层会过滤出买入榜）。**行业内分位优先**
（行业内相对便宜即高分），分位样本不足降级绝对曲线，且按市值分两支：
- 中小盘（<200 亿，按成长股）：PE<15→90；<30→80；**<60→70（成长溢价不重罚）**；
  <100→50；≥100→30。
- 大盘/价值股（≥200 亿）：更严格（<10→95、<15→85、<25→75、<40→…）。

**PB**：行业分位/绝对曲线（结构同 PE）。
**市值规模**：≤0→50；<20 亿→75；20~50 亿→**85（最佳）**；50~200→70；
200~1000→55；≥1000→40。
**振幅**：波动率适中加分、过高减分。

### 3.4 成长（营收/净利各半，缺失一半按另一半折算）

增长曲线（yoy%）：
≥150→92 ｜ 100~150→85~92 ｜ 50~100→75~85 ｜ 20~50→62~75 ｜ 0~20→50~62 ｜
−20~0→38~50 ｜ −50~−20→22~38 ｜ <−50→max(8, 22+yoy×0.3)

### 3.5 质量（ROE / 资产负债率 / 毛利率）

- **ROE 曲线**：≥25→95；15~25→80~95；8~15→…（线性插值）。
- **资产负债率**：非金融用绝对曲线（<20→95…）；**金融行业细分**（09-03）：
  银行固定偏高好感分（负债=存款是商业模式）、券商/保险用**子行业分位**压缩到
  [40,85]、多元金融中性。
- **毛利率**：**行业内分位优先**（白酒 90% 不再被绝对阈值误伤），样本不足降级
  绝对曲线。

---

## 4. 加权、归一化与信号

- **维度加权**：`综合分 = Σ(维度分 × 维度权重)`，权重和=1（缺失维度跳过后重新
  归一化——缺一半数据不等于另一半得 0）。
- **资金面特例**：5 因子时子权重和=120 → 先归一化回 100 再乘维度权重。
- **信号阈值**（`_derive_signal`）：

| 综合分 | 信号 | level |
|---|---|---|
| ≥80（且无维度 <20） | 强烈买入 | +2 |
| ≥65 | 买入 | +1 |
| 45~65 | 观望 | 0 |
| ≤35 | 卖出 | −1 |
| ≤20 | 强烈卖出 | −2 |

- **暴雷否决**：任一维度分 <20 → 即使总分 ≥80 也只给"买入"。

---

## 5. 榜单层面的附加修正层（不改变子项，改信号/展示/排序）

| 修正 | 触发 | 动作 | 位置 |
|---|---|---|---|
| 主力行为标注 | mainforce_state | distribution 信号 ×0.85 乘数**仅标签**（MAINFORCE_MODE=auto + regime 闸门；方案 A：排序用 raw total） | routers/scoring |
| 消息面强负面降档 | news_score ≤ −2 | 买入→观望（前端） | useFrontendScoring |
| 高位透支降档 | price_pos > 0.9 | 买入→观望（前端） | useFrontendScoring |
| 拥挤主线 | 行业主线 crowded | confluence 不 +1（只减不加） | mainforce/confluence |
| nb 换手钳制 | nb 市 + 换手>8% | 资金面封顶 40 | engine |
| 亏损股过滤 | PE≤0 且 EXCLUDE_LOSS_MAKING | 不进买入榜（回避榜不过滤） | routers/scoring |
| 池质量门槛 | 流通市值 ≥50 亿、价格 ≥3 元 | 进不了候选池 | routers/scoring |

---

## 6. 001368 案例（把以上规则串起来）

通达创智：flow5=+42.97% → 倒U给主力净流入子项 **0 分**（>+20 区间）→ 资金面
53.7 → 综合分 60.2（观望），低于实时榜 Top100 线 64.8 → 不在榜。红色标签是
"分值低→减分项"判定，不是方向反了。分支已把标签改为
"主力极端流入(散户陷阱降分)"消除歧义。完整分析见
《评分倒U改进_极端流入个案001368_20260914.md》。

---

## 7. 因子体检现状与修改规范

**体检结论**（2026-09-20，`subfactor_ic_20260920_0103.md`，7486 样本）：
技术面/动量类 8 子项在 5-10 日窗**整体负 IC**（RSI −0.218、MA −0.192…），
强市截面不衰减——短持有窗上技术面是反向指标（截面内反转效应）。换手率
5 日 IC≈0（唯一 10 日正 IC +0.075）。倒U后主力净流入中性。**处置建议见
《评分系统体检_子指标IC与优化建议_20260920.md》**（技术面定位改写/权重二次
校准/换手率重校准，均需模拟盘验证后上线）。

**修改规范（铁律）**：
1. 改任何锚点 = 改曲线 → 必须先离线回测背书（判定标准预先写死）；
2. 后端 `engine.py` 与前端 `scoringEngine.js` **逐点同步**（同文件内有对照注释）；
3. 同步后跑 `scripts/score_regression_check.py` / `verify_pack_score_parity.mjs`；
4. 更新《股票逻辑全景》对应小节 + 本文档锚点表；
5. 带 env 开关上线，默认旧行为。
