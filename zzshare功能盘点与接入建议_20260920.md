# zzshare 功能盘点、接入建议与数据源选型（2026-09-20）

> 实测环境：pip zzshare 0.4.11 + 仓库根 vendored 副本（本轮真实探测）
> 关联：《项目专业审视_不足与优化建议_20260920.md》§A2/C2（alpha 因子与风险事件缺口）
> 现有封装：`backend/app/zzshare_client.py`（单点隔离，设计良好）｜ 日批任务：`zz_finance`（16/19）

---

## 0. 先说结论

1. zzshare 的差异化价值**不在行情**（自有腾讯/东财模块已覆盖），而在四块自有模块
   没有的数据：**涨停复盘生态（含连板梯队/涨停原因）、情绪指标、题材热度、
   龙虎榜席位 + PIT 财务**——正好对上《项目专业审视》§A2/C2 列出的 alpha 缺口。
2. 当前项目**只用了它的财务接口**（9 个里的 5 张表 → stock_finance_zz 快照表），
   59 个 API 的利用率约 15%——大量高价值接口闲置。
3. **PLAN 遗留项可关闭**：`stock_moneyflow`/`market_mf` 在 0.4.11 中路径已注册、
   但快捷方法未生成（实测 AttributeError），可用底层 `api.query(path, params)`
   直调或升级 zzshare 解决。
4. **发现一个导入陷阱**：仓库根的 `zzshare/` vendored 副本（与 pip 包仅换行符差异）
   会遮蔽 pip 包——从仓库根目录跑 Python 时 `from zzshare.client import DataApi`
   直接 ModuleNotFoundError。建议处理（见 §4.1）。

---

## 1. API 全景（59 个接口，按对项目的价值分组）

### T0 · 情绪周期套件（9 个）——补专业审视 §A2 最大缺口

| 接口 | 内容 | 项目用途 |
|---|---|---|
| `sentiment_timing` | **情绪择时** | 与本项目 regime/宽度信号做互补校验（实测可通，匿名返回空，需 token） |
| `sentiment_trend` / `sentiment_trend_range` | 情绪趋势序列（可回填历史） | **历史情绪分位序列** → 情绪分位 × 战法胜率交互回测（专业审视 A2 待验证项的数据源） |
| `market_sentiment` / `market_hot_sentiment` | 市场情绪/热度 | 盘前简报情绪段增强 |
| `market_style` | 市场风格（大小盘/成长价值） | regime 的第二维（风格维）候选 |
| `uplimit_trend` / `updown_distribution` | 涨停趋势/涨跌分布 | 替换本项目用 OHLC 近似的涨停家数（官方口径含 20cm/ST/非一字） |

> 本轮实测：`sentiment_timing`/`market_style` 可通但匿名返回 0 行——**需领
> ZZSHARE_TOKEN 后复测**（免费获取）；工作日调用才有当日数据。

### T1 · 涨停复盘生态（10 个）——战法归因 + 连板高度（PLAN zzshare 阶段 2 正名）

| 接口 | 内容 | 项目用途 |
|---|---|---|
| `review_uplimit_hot_step` | **连板梯队**（实测 ✓ 14 行） | 空间板/龙头识别 → 情绪周期因子（连板高度是 A 股短线核心状态量） |
| `uplimit_stocks` | 每日涨停清单 | 涨停回马枪/龙回头战法的信号源增强 |
| `stock_uplimit_reason` / `stock_uplimit_reason_history` | **个股涨停原因**（含历史） | 战法信号归因（"为什么涨"）+ 题材持续性过滤 |
| `review_uplimit_reason(_open)` | 全市场涨停原因分布 | 题材热度归因 |
| `uplimit_hot` | 涨停热点板块+连板梯队 | 同上 |
| `uplimit_market_value` / `updown_distribution` | 涨停市值分布/涨跌家数 | 情绪结构 |

### T2 · 龙虎榜深化（4 个）——解锁 MAINFORCE 阶段 3 的"席位口径"

| 接口 | 内容 | 项目用途 |
|---|---|---|
| `lhb_detail` | 榜单明细（买卖席位） | 席位数据入库 |
| `lhb_trader_history` | **营业部/游资历史战绩**（实测 ✓ 5 行） | 顶级游资跟踪、席位胜率画像 |
| `lhb_list` / `lhb_stock_history` | 榜单列表/个股上榜历史 | 现有 lhb_history 的增强 |

### T3 · 财务深化（7 个）——解锁基本面因子体检 + 防前视

| 接口 | 内容 | 项目用途 |
|---|---|---|
| **`finance_pit`** | **Point-in-time 财务（防前视）** | 评分系统基本面因子的历史截面重建——解锁《评分系统体检》§1.4"基本面/成长/质量从未体检"的数据缺口 |
| `finance_valuation` | 历史**估值序列**（PE/PB 分位的历史截面） | 同上：PE/PB 因子的历史回测数据源 |
| `finance_indicator` | 扩展财务指标（含净现比等） | zzshare 阶段 2 的"净现比子指标"（改 _score_quality） |
| `finance_latest/stock/range` | 现用（finance_latest 已接） | — |

### T4 · 题材与热度（6 个）【P3，锦上添花】

`topic_table_list/detail/stocks`（题材成分与K线）、`ths_hot_top` / `stock_ths_hot`
（同花顺热股榜=散户关注度）、`market_plate_popular_reason`（板块上涨原因）。
用途：过热风险标签（热度极端=接力风险）、题材成分映射。

### T5 · 其他（`movement_alerts` 盘中异动、`trade_days` 交易日历、`rt_k`/`stk_mins`
实时与分钟线、`ai_report_*` AI 报告）——按需单点接入，当前不排期。

---

## 2. 现状用量与接入成本

- **已用**：`finance_latest`（5 张财务表）→ `stock_finance_zz` 快照表，日批任务
  `zz_finance`（第 16/19 位），`zzshare_client.py` 单点封装（token 懒加载、
  ts_code 转换、超时重试在上层 SDK）。
- **接入成本**：新接口走同一封装加方法即可；历史回填类（情绪趋势序列）建议
  进 `research_cache` 模式（本地 sqlite 缓存 + 24h 复用），与既有回测框架无缝。
- **限流**：匿名可用但慢、部分接口 0 行；`ZZSHARE_TOKEN` 官网免费领取可提额
  （本地 .env 已留位）。

---

## 3. 接入路线图建议（对齐《项目专业审视》路线图）

| 优先级 | 事项 | 对应缺口 |
|---|---|---|
| ① | 领 ZZSHARE_TOKEN → 复测 T0/T1 全部接口（工作日数据） | 数据可用性确认 |
| ② | `sentiment_trend_range` 历史回填 → **情绪分位 × 低吸战法胜率**交互回测（专业审视 A2 的待验证项） | 情绪周期因子 |
| ③ | `review_uplimit_hot_step` + `stock_uplimit_reason` 接入战法复盘 | 连板高度/信号归因 |
| ④ | `lhb_trader_history` → 席位口径（MAINFORCE 阶段 3 解锁） | 龙虎榜深化 |
| ⑤ | `finance_pit` + `finance_valuation` 回填 → 基本面因子历史截面体检 | 体检数据缺口 |
| ⑥ | `stock_moneyflow` 用 `api.query()` 直调验证 → 关闭 PLAN 遗留 | 遗留项 |

---

## 4. 注意事项与坑

1. **导入陷阱（本轮发现）**：仓库根的 `zzshare/` 目录（vendored 源码副本，与 pip
   包仅换行符差异，但外层 `__init__.py` 为空）会遮蔽 pip 包——从仓库根目录跑
   Python 时 `from zzshare.client import DataApi` 直接 ModuleNotFoundError。
   **建议**：删除或改名为 `vendor_reference/zzshare-src/`（涉及删除你的文件，
   需你确认；requirements 已 pin `zzshare>=0.4.11`，删副本无风险）。
2. **快捷方法缺失**：`stock_moneyflow`/`market_mf` 在 SHORTCUTS 映射表里注册了
   路径但未生成方法（0.4.11 实测 AttributeError）→ 用 `api.query("路径", params)`
   直调，或升级 zzshare 后复查。
3. **匿名限流与数据范围**：部分接口匿名返回 0 行；README 声明行情 2005 年起、
   其他数据"以接口提供方为准"——**接入前先用 token 复测数据范围**。
4. **接口稳定性**：zzshare 是免费第三方 SDK（api.zizizaizai.com），接口路径可能
   变更——上层必须继续走 `zzshare_client.py` 单点封装（现有设计正确），新接口
   先小样本实测再回填。

---

## 5. 更好的库推荐（诚实评估，结论是"不要堆库"）

| 库 | 覆盖 | 稳定性 | 成本 | 评估 |
|---|---|---|---|---|
| **zzshare**（现用） | 涨停复盘/情绪/题材/龙虎榜席位/PIT 财务（差异化） | 中（免费 SDK，接口路径偶变） | 免费/token | **继续做差异化主力**——它的涨停生态与情绪套件是自有模块和主流库里都稀缺的 |
| **tushare pro** | 财务/每日指标/退市名单/历史成分，质量高、有 PIT | 高 | ~200 元/年起（积分制） | **建议作为付费备选**：唯一能修复"幸存者偏差"（退市股名单+历史成分）的便捷来源；接 `zzshare_client.py` 同款封装即可 |
| **akshare** | 最广（数千接口） | **低**（爬虫接口频繁失效） | 免费 | 仅按需单点引入兜底，不建议整体依赖（维护成本高于收益） |
| **baostock** | 日线/复权因子/退市股，稳定 | 高但 T+1 更新、接口少 | 免费 | OHLC 备份源候选（当前 pack 链路已稳，暂不需要） |
| efinance/qstock/adata | 与现有 eastmoney/tencent 模块重叠 | 中 | 免费 | **不推荐重复引入** |

**选型原则**（比选哪个库更重要）：
1. 数据源越多维护越重——**每个新源必须走 `zzshare_client.py` 式单点封装**，
   且先 30 天影子验证（数据范围/字段稳定/延迟实测）再转正；
2. 同一数据两个源时，固定**主源 + 校验源**角色（如 mainflow 主源东财、
   zzshare stock_moneyflow 做交叉校验），避免双写冲突；
3. 财务类数据优先选 **PIT 口径**（finance_pit / tushare），防前视是回测可信度的
   底线。

---

## 6. 复现命令

```bash
cd backend && python - << 'EOF'
from app.zzshare_client import get_api
api = get_api()
print(api.review_uplimit_hot_step(date1="2026-09-18"))   # 连板梯队（已验证）
print(api.lhb_trader_history(trader_name="量化基金"))      # 席位历史（已验证）
print(api.sentiment_timing())                             # 需 token（匿名 0 行）
print(api.query("stock/moneyflow", {"ts_code": "000001.SZ"}))  # 底层直调
EOF
```
