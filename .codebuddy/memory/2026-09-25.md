# 2026-09-25

## 📄 工作台开发需求文档定稿（交外部模型开发）

**用户决策**：同意 `/workbench` 设默认首页 + 回放先做 30 天；要求先出**开发需求文档**
交给其他模型开发，并问"还有什么要调整和优化的"。

**产出**：`工作台开发需求_20260925.md`（项目根目录，遵循项目"中文命名+日期"文档惯例）。
**核心设计考量 —— 文档必须自包含**（其他模型没有我们的对话上下文）：
- 已拍板的 7 项决策写死（默认首页/桌面优先/横向+右栏常驻/回放30天/只含当日流/Phase1只读优先）
- **UI 5 段 vs 后端 phase 3 段的映射表**（分界 09:30/15:00 复用 `current_phase()`，别造第二套；
  09:35 教练确认窗、10:30/14:45 体检卡是业务时点不是阶段边界）
- **接口清单 14 个全部已核实存在**（Phase 1 零后端改动）；允许新增的只读接口限定在 day-index 等
- **徽标规则表**（未决策教练卡=红点+数字 / 简报降级=黄点）
- **回放规则**：选历史日期全页含右栏切只读 + 横幅标注；数据层不写死天数
- **12 条项目铁律**（冻结期禁碰评分 / 3.9 禁 PEP604 / 假 async / %s 双库 / ASCII 日志 / egress /
  Tailwind 语义类 / 懒加载失败静默 / 注释带日期 / commit 格式）—— 这些是其他模型不知道的项目纪律
- 三阶段交付各带可验收标准；§10"明确不做"防蔓延

**我在文档中主动补的优化点**（用户没提但必要）：
1. 温和自动跟随（不硬切，只提示）
2. 教练卡按钮二次确认（执行回写会污染一致性统计）
3. 右栏轮询失败静默保留上次数据
4. 回放模式下右栏也切历史（防实时/历史混淆）
5. 性能预算：切阶段才懒加载（旧首页教训）
