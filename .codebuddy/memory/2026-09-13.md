# 2026-09-13

## 🔍 排行榜 Edge 轨道只显示 9 个快照日（08-17~09-01）的根因（**数据覆盖缺口，待修**）
- 现象：`/api/system/performance` 排行榜 Top10（每日等权 T+5）窗口止于 09-01、仅 9 日。
- **ranking_history 本身完整**：08-17~09-11 共 **17 个快照日** ×50 行 = 850 行全齐（含价格），每日 Top10 价格全 >0。仅 08-18/19/21 三天快照缺失（快照任务早期不稳，非近期问题）。
- **真根因 = 个股 `backtest_prices` 回填覆盖不全**：回填任务只覆盖 ETF 池 + sh000300 + **战法涉及个股**，评分股不回填。实证：09-04 上榜的 10 只里 8 只日线停在 09-07~09-10（仅 000685/000933 两只到 09-11 完整）→ 09-02 起的快照日 T+5 凑不满 6 根 K 线（performance.py L99 `len(b)==fwd+1` 过滤）或每日凑不满 5 只 → 全部缺席。09-01 能显示纯粹因其上榜股恰好多只近期被战法回填过。
- **影响**：排行榜 edge 轨道把 09-02 起的样本全丢了——而这正是"近期衰减"最需要观察的窗口（09-12 周报"后 30% 胜率 17.7%"同期）。
- **修法（未实施）**：① 日批/回填任务把当日 ranking_history 上榜股（50 只）纳入 backtest_prices 回填（低成本保未来）② 一次性补历史：08-17 以来全部上榜 distinct code（约百余只）批量回填到最新 → 立即解锁 09-02~09-04 的 T+5。③ 顺带：performance.py `_ranking_track` 每股单独查库（10×N 天 = 上百次查询），可改批量 IN 查询。
- 网络注意：本机 DNS 间歇故障已解决——用户把网卡 DNS 手动改为 223.5.5.5/119.29.29.29（截图确认设置正确），flushdns 后解析 0.03s 正常。

## ✅ 排行榜 T+5 缺口修复（09-13，**未提交**；补历史后台运行中）
- **根因修正**：`_collect_strategy_codes` 其实已含近 30 天评分股（backfill_history.py L95-101）——不是"不在池里"，是**每晚 150 只配额排队 + 09-12 换库打断**：实测全池 643 只个股仅 144 只同步到 09-11，499 只滞后（141 只停 09-07）。数据源拉取完全正常（000567/000807 增量拉取返回全 5 根实证）。
- **①回填任务改造**（backfill_history.py）：新增 `_collect_rank_codes(days=7)`——近 7 天上榜股从 ranking_history 取 distinct code，在 pending 排序中**插队最优先**（`key=(上榜0/普通1, 滞后日期)`），保证排行榜 T+5 窗口内数据必然就绪；`backfill_daily(quota=DAILY_STOCK_QUOTA)` 参数化，`quota=None` 不限量；stats 新增 `rank_priority`。默认行为不变（scheduler/daily_batch 调用无参）。missing 检测段遍历全量 pending，不受影响。
- **②补历史**：`backfill_lagging()` + CLI `--lagging`（幂等可重跑），09-13 本地后台执行中（499 只 × 1s 限速 ≈ 10 分钟）。跑完后排行榜轨道应显示 08-17~09-04（17 个快照日 - 缺失的 08-18/19/21 = 14 天；09-02/03/04 的 T+5 = 09-09/10/11 均已满足）。
- ⚠️ 验证注意：`/api/system/performance` 有 **1h 进程缓存**（`_perf_cache`），补完后要等缓存过期或重启 Render 才能看到窗口变化。
- LIKE 查询注意：psycopg2 参数化 SQL 里 LIKE 的 `%` 要写 `%%`（调试时踩过）。

## ✅ 补历史完成 + 排行榜轨道闭环验证（09-13）
- 后台补历史结果：`stock_pending=480` 全部处理、`stock_rows=15466` 写入、**`stock_missing=[]`**（全部同步到基准 09-11）、`rank_priority=116`（近 7 天上榜股插队生效）。注：stats["rows"]=7 是历史设计——个股行数只记在 `stock_rows` 不计入 `rows`，勿误读为"只补了 7 行"。
- **排行榜轨道恢复**：9 天（08-17~09-01）→ **12 天（08-17~09-04）**，09-02/03/04 三个快照日解锁 ✓。cum_return **-1.14%** vs 沪深300 **-3.76%**（跑赢 +2.62pp）。
- **观察点**：近 5 个快照日（08-28~09-04）4 天 mean 为负（-1.63/-2.14/-1.50/-2.42），仅 09-02 +1.94——评分榜在普跌市也在亏（虽跑赢基准），与 09-12 周报"后 30% 胜率 17.7%/近期衰减"互为印证。
- 缺失的 08-18/19/21 三天是**快照本身缺失**（非本次修复范围）；09-07 起的 T+5 会随周一后的交易日推进自然解锁。

## 📋 评审《外部研判与功能需求简报_20260913.md》§5（6 题：2 通过 / 4 需修改 / 0 驳回）
- **Q1 数据核验（通过，个别需手动核实）**：联网复核——8月CPI同比3.4%✓、核心同比3.7%（简报漏掉的更关键边际）、非农+16.2万✓、Warsh 5/22上任✓、特朗普施压✓、布油破100✓、非农后加息概率60%/9-2 66%✓。**无法精确复核需用户手动核实**：CPI后85-88%FedWatch、Kalshi57%/Polymarket49%、PPI5.4%、10Y"一度4.91-5.0%"（搜到9/10是4.85%）、联邦基金3.50-3.75%、胡塞攻占穆哈港事件。三情景55/30/10/<5=主观先验不可证伪，"年内1次55%"偏高（非农强+核心CPI3.7%+PPI+油价→"2次+"可能被低估），建议改用点阵图年内次数的离散场景。
- **Q2 历史复核（需修改）**：方向对（首次加息≠牛市终结、市场跑在Fed前），但"回测12/24月收益分布"无量化、只定性；供给型样本(1994/2004/2021-22)缺位——而本次最像2022还是1994正是置信度分叉点；A股两段主因是自身因素（简报已自认），只可作"天花板"不可作"幅度预测"。
- **Q3 规则冲突（需修改，红线）**：§2.3 五触发器里 4 条与现有信号重复——加仓③=regime offensive判定、减仓③=margin_sentiment/sentiment_vs_margin、加仓①=breadth_collapse反向、加仓②的120pips=macro已有cny_cnh_basis；减仓②"3882"写死常量。**硬性要求：阈值(3882/120pips/5%)必须引用项目实时信号源，不写死**。0.4%单笔风险与已实现的risk_multiplier吻合（印证不冲突）。
- **Q4 教练评审（需修改）**：架构通过；但①"事后否决"应改"事前无权"（LLM只渲染规则结果、数字由代码注入，冲突从根上不可能）②30s轮询读腾讯内存缓存不逐股拉新浪（否则放大egress/WAF）。情绪熔断、放弃填理由、"不动是默认输出"=通过（命中方法论第6条）。审计指标需先定义"执行一致性"。
- **Q5 集成点（需修改）**：教练是盘中实时、日批是两套时钟。挂点：盘中轮询在scheduler保留组(同paper_track_loop，不被READ_ONLY关)；预承诺计划在fill_pending_positions成交时生成；硬警报实时、独立于15:35矛盾扫描；日批只挂"收盘回写coach_plans+周报复盘"两处。
- **Q6 优先级（需修改）**：§4五漏洞现状=漏洞3已完成(#6)、漏洞2=P0#8、漏洞1=P1、漏洞5=P1#6、漏洞4=P2。**核心裁决：教练不得插队到数据面修复之前**（教练规则引擎引用regime/flow5/margin，信号有bug会"忠实执行错误规则"）。4周：W1数据面修复+教练纯规则骨架、W2滚动窗口+教练规则引擎/audit、W3-4模拟盘跑4周、真实持仓W5+。
- 简报文件位置：`外部研判与功能需求简报_20260913.md`（根目录，用户新增，非计划类）。
- **用户核实的权威口径（已回写简报 §1.1/§1.4）**：①联邦基金利率 3.50–3.75%=2025/12/10 设定、9-3 票型，2026 年五次 FOMC 均 hold，EFFR 3.63% ②6 月点阵图=18 点中 8 人预期年内≥1 次加息、两派均分(8 vs 10)、年末中值 3.8% ③美债 10Y=9/11 收报 4.9708%（盘中 4.98%）④6 月 FOMC 声明官方采用"供给冲击"措辞→供给型通胀从推断升格为官方定性，作 §1.4"滞胀对股市最差"的论据。
- **评审裁决已沉淀进 PLAN**：§2.4 新增"交易员教练（Coach）"条目（需求+4 处评审修改+红线），§6 P2 新增教练排期（**不插队到数据面修复之前**，W1 纯规则骨架→W2 规则引擎/audit→W3-4 模拟盘→W5+ 真实持仓）。教练功能的 4 处评审修改与"阈值引用现有信号源不写死"红线已固化为 PLAN 约束。

## ✅ §3b#8 flow5 曲线矛盾回测背书并上线（2026-09-13，**未提交**）
- 新建 `scripts/flow5_curve_backtest.py`，复用 `mainforce_factor_backtest.build_samples` 生成 5993 样本/9 截面（2026-06-29~08-24）。
- 对比三方案：**现行单调曲线**、**倒U曲线**、**原始曲线+高位极端流入×0.85 overlay**。
- 结论：**倒U完胜**。
  - 去超额 IC（5日/10日）：倒U +0.062/+0.039 vs 现行 -0.126/-0.106 vs overlay -0.096/-0.068。
  - 五分位 Q5 收益：倒U 1.26%/2.04% vs 现行 0.20%/0.94%。
  - 高位极端流入子集（price_pos>0.75 & flow5>+5）5日去超额：倒U -0.87% vs 现行 -3.99%。
- **已上线**：后端 `app/scoring/engine.py:_score_flow5`、前端 `frontend/src/utils/scoringEngine.js:scoreFlow5` 同步改倒U锚点（-10→0 / -6→15 / -3→30 / 0→55 / +2→80 / +5→55 / +10→30 / +20→5）；`股票逻辑全景_20260912.md` §3.3 与 `外部研判与功能需求简报_20260913.md` §2 已更新。
- 报告：`backend/backtest_reports/flow5_curve_20260913_0220.md`。

## ✅ §3b#9 regime 口径三件套（2026-09-13，**未提交**）
- 选择 #9 而非 #8 原因：#8 flow5 回测需大量拉价格数据，本机 DNS 间歇故障（Python `getaddrinfo` 时好时坏）会反复卡；#9 是纯代码/本地表结构改动，零网络依赖。
- **nb 两日确认**：`market_regime._apply_bearish_refine` 重构为查 `market_regime_history` 昨日 `bearish_refine_raw`：进入 nb = 昨日/今日 raw 均满足；退出 nb = 连续 2 日不满足（中间日保持 nb，避免单日反弹噪音）。`_nb_condition_raw()` 抽取 raw 条件逻辑（宽度恶化/外围恐慌/沪深300 近2日累计下跌）。
- **regime_score 死字段修复**：进入 nb 时 `regime_score=-20.0`，不再套在 raw neutral 的 0 上，形成 offensive(50+adx)/neutral(0)/nb(-20)/defensive(-50-adx) 连续轴。
- **defensive 权重矛盾澄清**：`get_regime_weights` 注释写清——450 快照否决的是 **neutral→nb 的防御化权重调整**（故 nb 权重=neutral），与 **defensive 档独立权重**不矛盾；defensive 触发=高波动+趋势向下 或 强趋势向下，nb 触发=低波阴跌+重心下移。
- **表结构**：`market_regime_history` 新增 `bearish_refine_raw BOOLEAN` 列，`_ensure_history_table` 幂等 `ALTER TABLE ADD COLUMN IF NOT EXISTS`。
- **验证**：本地 741 根 sh000300 跑 `detect_market_regime` + fake history 单元测试 → 进入/退出逻辑 OK、score=-20、近 30 天 switches=0（当前阴跌市 raw_nb 持续 True）；py_compile + lint 干净。
- 改动文件：`backend/app/backtest/market_regime.py`、`PLAN_2026-09-12.md`；测试脚本已删除。

## ✅ §3b#10 周报强制分 regime 切片（2026-09-13，**未提交**）
- `app/backtest/run.py:generate_summary()`（精简版周报/企微推送摘要）新增「战法 × 市场状态（强制切片）」小节：调用 `strategies.backtest_warfare_by_regime()`，输出进攻/震荡/防御三状态的样本数、胜率、平均单笔、盈亏比，并追加高波动 vs 正常波动汇总。
- 完整周报 `--strategy all` 本就包含 `regime_warfare` 与 `review`，现在精简版也强制展示 regime 切片，防止「全期超额被局部市况掩盖」。
- 本地验证：`generate_summary()` 输出正常；py_compile+lint 干净。

## ✅ P1-1 吸筹样本窗口专项（2026-09-13，**未提交**，负结论否决接入）
- 新建 `scripts/accum_pullback_backtest.py`：复用 strategy_mainforce_filter_test 数据路径（research_cache 本地缓存），对 544 条低吸信号（ma_pullback/single_yang_unbroken，462 覆盖 phase）撮合（T+1 开盘+涨停一字剔除+退出 v2）。
- **结论：phase=accumulation 不是低吸战法的有效准入过滤，否决接入**。数据：
  - 基线 A：379 条，胜率 41.7% / 均收益 -0.582% / PF 0.73
  - 吸筹过滤 B：53 条，胜率 43.4% / 均收益 -0.598% / PF 0.68（胜率 +1.7pp 但均收益/盈亏比双降）
  - 近窗口 D（≥09-01）：31 条，胜率 35.5% / 均收益 -1.169% / PF 0.51，显著恶化
- **根因判断**：phase=accumulation 规则（ret_60<12% + 均线粘合 |MA20/MA60-1|<3% + OBV20上行 + 缩量）在阴跌市把「下跌中继平台」误判为「吸筹」，正是 09-04→09-11 accum 样本 291→377 暴增的背景。
- **C_accum 组合验证（补做，同脚本扩展）**：`price_pos<0.35 & concentration<0.25 & flow5>0`（低位筹码密集 × 主力净流入）——**方向有效但样本仅 10 条**：均收益 +0.678% vs 基线 -0.582%（+1.26pt）、盈亏比 1.44 vs 0.73（翻倍）；近窗口 8 条均收益 +0.7%/PF1.38 同向改善；胜率 40% 略降（-1.7pp）但符合「低胜率+高盈亏比」型（判据沿用白名单方法论：看均收益/盈亏比，不看胜率）。分战法：ma_pullback×组合 2 条均收益 +3.393%/PF7.93（样本过少）；single_yang_unbroken×组合 8 条均收益 -0.001%/PF1.0（持平）。
- **最终结论**：形态标签（phase=accumulation）勿用；筹码+净流入组合方向有效但样本仅 10 条 → 标 P2 待样本积累复验，暂不接常设准入。
- 已回写 PLAN §2.2（买侧组合条目标否决+组合方向有效）+ §4b#10 完成；报告 `backend/backtest_reports/accum_pullback_20260913_0300.md`（终版）。

## ✅ P1-2 白名单重放滚动窗口（2026-09-13，**未提交**）
- `app/strategies/recommendation.py:_recompute_whitelist` 改为**双轨**：全期 + 近 `WHITELIST_ROLLING_DAYS`（默认 250）交易日；任一轨跌破判据即暂停推送；近轨样本 <30 视为「不可评估」→ 保守不通过。加**半衰期监控**：后半段胜率 < 前半段 ×`WHITELIST_HALF_LIFE_RATIO`（默认 0.5）时告警（不直接暂停）。开关 `WHITELIST_ROLLING=0` 回退单轨。
- 近轨日期集合 = trades 的 signal_date 去重升序末 N 个（**不依赖外部交易日历**）；交易日不足 N 时 `rolling_active=False` 降级为全期单轨（不会误拦）。
- 日志（`get_push_whitelist`）、复核脚本（`scripts/strategy_whitelist_review.py`）、日报（`daily_report._push_status_md`）均展示双轨状态 + 半衰期告警。
- **实测（09-13）**：白名单仍 `['dragon_turnaround']`（n136/48.5%/均+0.46%/PF1.39，全期✓近轨✓无告警，健康）；**半衰期告警成功捕捉衰减**——`ma_convergence_breakout` 后半段 17.0% vs 前半段 49.5%、`ma_pullback` 17.6% vs 50.0%，与 09-12 周报「后 30% 胜率 17.7%」吻合 ✓。其余：single_yang_unbroken n305/48.5%/均-0.17%/PF0.9（全期未过，比 09-12 的 +0.19% 进一步衰减）。
- ⚠️ 当前 `rolling_active=False`（strategy_results 去重交易日仅 ~16 < 250）→ 近轨降级全期；半衰期监控不依赖窗口、已独立生效，达成「提前 2-4 周报警」目标。
- **顺带修复**：`recommendation` 日志里的 `✓/✗` 与 `⚠️` 在 GBK 控制台抛 `UnicodeEncodeError`（本地复现，曾导致白名单误走静态兜底分支）→ 改中文/`[警告]`；复核脚本同步去掉 `✅/—/∞` 符号。
- 改动文件：`backend/app/strategies/recommendation.py`、`backend/app/daily_report.py`、`scripts/strategy_whitelist_review.py`、`PLAN_2026-09-12.md`。

## ✅ P1-3 主线拥挤度否决（2026-09-13，**未提交**）+ 顺带修复传导链两个既有致命 bug
- **离线验证** `scripts/mainline_crowding_backtest.py`（630 条主线候选，`--days 40`）：
  - 拥挤组（ret20>30% / ret60>50% / 距250日高点<5% 任一）：T+5 n46 胜率 **32.6%** / 均 **-1.411%**
  - 不拥挤组：T+5 n433 胜率 40.2% / 均 -0.292% → **差 -7.6pp / -1.12pt**；T+1 同向（-5.2pp/-0.40pt）；宽松口径亦同向 → 否决依据成立。
- **实现**：`industry_mainline` 加 `crowded`/`crowd_ratio` 列；`mainline._crowding_flags`（行业内拥挤候选股占比 ≥50% 判拥挤，个股判据无前视）；`compute_mainline` 落库；`get_mainline_summary`/推送展示「拥挤主线·只减不加」；`confluence.confluence_for_stock` 拥挤主线**不再 +1**（只减不加）。
- **★★ 顺带挖出并修复传导链两个既有致命 bug**（`app/mainforce/confluence.py`）：
  - **bug1**：模块顶部**从未 `import db`**，`_env_snapshot` 里 `db.fetch(...)` 每次抛 `NameError` 被 `except Exception: pass` 吞掉 → **板块资金流表 + 主线名单长期恒为空**（板块层从未生效：flow 永远"未知"、in_mainline 永远 False、传导链实际只有宏观+市场+个股三层）→ 新增 `db_fetch()` 延迟导入 helper 替换调用点。
  - **bug2**：`sector_daily.name` 是东财细分名（"通信线缆及配套/地面兵装Ⅲ"），`stock_industry.main_industry` 是新浪一级名（"电子信息/飞机制造"）→ key 永远对不上、资金层恒"未知" → 用 `_normalize_industry` 归一化并按一级行业**累加**净流入。
  - 修复后实测：`sector_flow` 34 个一级行业（原恒 0）；家电行业资金 -15.7 亿正确读出；拥挤主线 `sector=-1`（仅资金流出，主线 +1 被否决）✓。
  - ⚠️ **行为变更**：板块层从"长期 0"恢复为真实值，会改变推送环境链判级（属恢复设计意图）。
- 改动文件：`backend/app/mainline.py`、`backend/app/mainforce/confluence.py`、`scripts/mainline_crowding_backtest.py`（新增）、`PLAN_2026-09-12.md`。报告：`backend/backtest_reports/mainline_crowding_20260913_0322.md`。

## ✅ P1-4 换手率 nb 钳制（2026-09-13，**未提交**）+ 补 regime 传递缺口
- **实现**：`engine._score_capital` 加权求和后调 `_apply_nb_turnover_cap`——仅当 `engine.regime == neutral_bearish` 且换手 >8% 时**资金面封顶 40**（只封顶总分，**不改子指标锚点**；明细写入 `details["_nb换手钳制"]`）。开关 `NB_TURNOVER_CAP=0`；`NB_TURNOVER_CAP_PCT`/`NB_TURNOVER_CAP_SCORE` 可配。
- **★ 顺带补上 regime 传递缺口（否则钳制形同虚设）**：
  - `ScoreEngine.__init__` / `set_weights` 此前只把 `regime` 用于算权重，**未保存 `self.regime`** → 已补（`self.regime = regime`）。
  - `routers/scoring._sync_regime_weights` 缓存空时**先从历史表恢复**（日批 Actions 独立进程 `_REGIME_CACHE` 内存为空，不恢复则 `engine.regime` 恒 None）。
  - `live_ranking.compute_full_ranking` 补调 `_sync_regime_weights()`（日批全量精算榜此前不应用 regime）。
- **验证**：方法级 6 分支全过（nb+换手20%→40；nb+换手5%→不变；nb+资金面35→不变；neutral/defensive/None→不变）；端到端 `_sync_regime_weights` 从库恢复 `neutral_bearish`（09-10）并驱动钳制 ✓；`set_weights(regime=nb)` 路径生效 ✓。
- ⚠️ **当前市场正是 neutral_bearish（09-10）→ 下周一起钳制实际生效。**
- 改动文件：`backend/app/scoring/engine.py`、`backend/app/routers/scoring.py`、`backend/app/scoring/live_ranking.py`、`PLAN_2026-09-12.md`。
- **遗留**：前端本地评分链路 `frontend/src/utils/scoringEngine.js` **未同步**该钳制（可选模式，非默认；前端需拿到 regime state 才可加）。

## ✅ P1-5 午盘信号治理（2026-09-13，**未提交**）
- **背景**：LLM ETF 信号实测盘前 75%（8 笔）vs 午盘 **0%**（10 笔）——09-09 高开低走式轮动市里午盘追高即坟场。
- **实现**：`app/signals/tracker.py` 新增 `_midday_regime_gate`——午盘（`source=lunchbreak`）信号**仅 offensive 市放行**，其它市况拒绝；挂在 `add_signal_with_validation` 的**第一道门槛**（早于持仓数/相关性/总风险/技术分校验）。拒绝走既有 `rejectedSignals` 通道（保留 50 条，审计 `build_audit.by_phase` 可见），**不是丢弃**。
- 开关 `MIDDAY_REGIME_GATE=0` 回滚；**fail-open**（读不到 regime 缓存/状态时不拦截，绝不因规则引擎故障挡掉信号记录）。
- **验证** 6 分支全过：lunch+真实 regime(neutral_bearish)→拒；lunch+defensive→拒；lunch+offensive→放行；premarket/postmarket/无 source→放行；开关关→放行。
- ⚠️ 当前 nb 市 → **周一午盘 ETF 信号将不再进池**（只攒 rejected 样本）。
- 改动文件：`backend/app/signals/tracker.py`、`PLAN_2026-09-12.md`。

## ✅ P1-6 盘前简报加两融+情绪温度计（2026-09-13，**未提交**）—— P1 增补收官
- `app/trader_brief.py`：① `collect_brief_data` 采集 `margin_sentiment`（复用 `flash/margin_sentiment` 的 `margin_line`/`sentiment_line`）+ `margin_chg5`；② **净减 ≤ `MARGIN_DRAIN_ALERT`（默认 -200 亿，可配 `MARGIN_DRAIN_ALERT_YI`）时生成 `margin_alert`**（杠杆撤离→拉高出货）；③ 段体经 `_data_to_markdown` 喂 LLM（供"该防"段引用）；④ `_render_funding_md` **仅盘前**在简报正文固定输出「## 资金与情绪」。
- **实测（09-13）**：两融余额 26380 亿、**5 日净减 102 亿**（未达 -200 阈值，无 alert）、情绪温度计 67.3 分/平稳区（过热：巴菲特指标 92、日成交额 85；过冷：存款市值比 15、成交额M2比 15）。
- ⚠️ **重要观察**：PLAN 原假设"两融高位仍在净增"，**实测已转为 5 日净减 102 亿**——方向已变，是 `sentiment_vs_margin severe` 阈值的前兆，需密切跟踪（若继续扩大至 ≤-200 亿 → 拉高出货风险）。
- **P1 增补全部完成**：P1-1~P1-6 六项（含 3 个顺带修复的既有 bug：传导链 `import db` 缺失、sector 名口径不一致、regime 传递缺口）。

## 🔬 评分引擎体检报告 v0（2026-09-13，脚本 `scripts/engine_diagnosis_v0.py` 保留）
- **用户需求转折**：观察 top50 几周发现"评分前列 ≠ 对的股票"，要"用引擎选股买入→验证→完善"的雏形。先跑 `get_bucket_stats`（90 天 850 条）+ 新写诊断脚本（单因子 IC，固定 5 日收益，Spearman 秩相关）。
- **三大病灶（数据定位）**：
  1. **总分区分度崩塌**：total_score p10~p90 仅 62.8~70.1（std=2.91，max=77.1），87% 挤 60-70 桶，80+ 分恒为 0 → "前列"与"中游"无实质差别。根因＝五维加权平均天然压缩区分度。
  2. **主力标签覆盖率仅 3%**（584/600 为 none）。根因＝**双路径 bug**：scoring.py 09-11 修复传键名 `mainforce_signal` 但 `record_daily_ranking` 读 `mainforce` → 静默丢弃（修复从未生效）；scheduler 日批组装根本没带 mainforce 且 `replace_day=True` 覆盖掉带标签版本。09-04/07/09 三天有标签＝日批中断时前端旧路径写入的残留。**两处已修**（`mainforce`: r.get("mainforce")），lint 干净。
  3. **总分 IC≈0**：total_score IC=+0.016（无效）；总分分位组合"最低30%(+0.38%) > 最高30%(+0.22%) > 中间40%(-0.53%)"——排序完全失效，精确证实用户观察。
- **单因子 IC（550 条，持有 5 日）**：**基本面 +0.167（最强正因子）**、资金面 +0.075（正向）；**技术面 -0.069、成长 -0.196（反向——阴跌市杀成长/前期强势股补跌）**；质量 +0.017（无效，仅 240 样本）。⚠️ 全样本都在 neutral_bearish 市况，反向因子可能是状态依赖，牛市需重测。
- **★ 组合因子（v1 选股公式预验证）**：`rank(基本面)+rank(资金面)-rank(技术面)` **IC=+0.195**（总分 12 倍），高30% 胜率 48%/+0.81% vs 低30% 34%/-1.37%（分离 2.18pp/5日）。加成长反而变差（+0.082）→ 排除。
- **v1 雏形方向**：组合分选股器（基本面+资金面-技术面 Top 15/日）→ 回测 vs 总分对照 → 接模拟盘前瞻验证。注意事项：IC 样本 550 条/单一市况，只做方向性参考；维度 key 是中文名（"基本面"/"资金面"/"技术面"/"成长"/"质量"），成长/质量仅 09-09 后有值（490 条）。

## ✅ v1 落地：组合因子前瞻对照实验（2026-09-13，**未提交**）
- 用户确认"前瞻验证版"路线（不做买入清单版，防 in-sample 直接上线）。实现：
  - `ranking_history.py` 新增 `get_composite_stats(days=90, horizon=5)`：组合分 = 三因子（基本面/资金面/技术面）**跨样本秩归一化**平均（与诊断脚本同源）；复用 `get_verified_records(horizon_days=5)` 固定收益口径；**同样本 30/40/30 分位**组合分 vs 总分对照（避免"组合用秩、总分用绝对桶"的不公平比较）；`_composite_score_of()` 单条原始分（基本面+资金面-技术面）供当日 Top15 展示；结论自动生成（分离差 >0.5pp 显著优 → 继续积累；无正向分离 → 勿进选股链路）。
  - `routers/scoring.py` 新增 `GET /api/scoring/composite-stats`（照 `/bucket-stats` 的 600s 缓存模式）。
- **纯现算不落库**：组合分是 dimensions_json 的确定性函数，每天重算随快照自动扩样，零 schema 变更。
- 验证（550 样本）：组合分分离 **+2.24pp**（高30% 48.5%/+0.83% vs 低30% 33.3%/-1.41%）vs 总分 **-0.16pp**；latest_top 15 正常（09-11 快照，云铝股份总分 71.0 仅排组合分第 15——直观展示两套排序差异）。小 bug 修复：SQL 漏 SELECT rank_date 致 latest_date=None。
- 改动文件：`backend/app/scoring/ranking_history.py`、`backend/app/routers/scoring.py`、`PLAN_2026-09-12.md`。lint 干净。
- **判定规则（写入 PLAN）**：组合分前瞻持续优于总分（40+ 交易日）→ 才谈进选股链路/调总分权重；否则丢弃零沉没成本。
- 改动文件：`backend/app/trader_brief.py`、`PLAN_2026-09-12.md`。

## 🔍 P1-4/5/6 整体 diff 走查（2026-09-13）
- **范围确认**：P1-1~P1-3 已由用户提交（`42d491d` 主线拥挤度+传导链修复、`0be4ff9` 白名单双轨等）；未提交 = P1-4/5/6 + PLAN + memory（8 文件 / 191 insertions / 6 deletions），无未跟踪文件。
- **发现并修复 2 处**：
  1. **P1-4 details 契约破坏（真实前端 bug）**：`details["_nb换手钳制"]` 缺「分值」字段，而 `StockDetail.vue:141-146` 会 `v-for` 遍历 `dim.details` 渲染 `val.分值` → 展开子项时显示 "undefined"；`_extract_factors` 也按 `分值/100` 判加减分项。**已改为 `details["高换手钳制"] = {"分值": 40, "满分": 100, "换手率":…, "原资金面":…, "市场":…}`**（0.4 落中性区，不误入 up/down）。验证：前端显示 40、`factors_up/down` 均未被污染。
  2. **P1-6 无谓网络开销**：`collect_brief_data` 原在**所有 phase** 拉金十两融（mp-api timeout 20s），但盘中/盘后并不渲染该段。**已改为仅 `phase == "premarket"` 采集**。验证：intraday 无 `margin_sentiment`（4.7s）、premarket 有（chg5 -102.2）。
- **确认无问题**：`_sync_regime_weights` 缓存空→从库恢复逻辑正确；`set_weights` 全仓唯一调用点传了 `regime`（不会把 `self.regime` 清空）；scheduler `generate_trader_brief("premarket")` **显式传 phase**（9:30 后补跑也不会丢资金段）；P1-5 fail-open 设计正确；两个开关 `NB_TURNOVER_CAP`/`MIDDAY_REGIME_GATE` 齐全；`py_compile` + `import app.main` 通过。

## 🔍 P1-1~P1-3 整体 diff 走查（2026-09-13）—— 发现 3 处，全部在 P1-3 传导链修复里（已修）
- **范围**：`1d439c7`（P1-1 脚本）、`0be4ff9`（P1-2 recommendation/daily_report/review）、`42d491d`（P1-3 mainline/confluence）。
- **P1-1**（`accum_pullback_backtest.py`，纯研究脚本）：无功能缺陷。
- **P1-2**（`recommendation.py` 双轨 + `daily_report.py` 告警行 + review 脚本）：逻辑走查无缺陷——`_passes_criterion`/`_calc`/`_half_life_alert` 的除零与短路均安全（`mid<15` 保护、`recent_ok and ...` 短路）；`rolling_active=False` 时降级为全期单轨、结果与旧口径一致 ✓。**设计取舍记录（非缺陷）**：近轨样本 <30 视为"不可评估→不通过"会让"近期无信号"与"近期衰减"无法区分（符合 PLAN"宁可静默"意图，且无信号本来也没得推）。
- **P1-3（`42d491d`）发现 3 处缺陷，已修（形成新的未提交改动）**：
  1. **【严重】「其它行业」兜底桶污染资金层**：`sector_flow` 归一化后把 156 个未识别东财板块聚合进"其它行业"（实测净流入 **-250 亿**、占 |总额| **16%**），而 `stock_industry` 里 **687 只个股**归该桶 → 会被**系统性打 -1**（兜底副作用，非真实信号；修复前所有股票都是 0 中性，等于修复引入新偏差）。→ `_env_snapshot` 跳过 `其它行业` 不做聚合。
  2. **【严重】「其它行业」未从主线名单排除**：`snap["mainlines"]` 直接取表中所有行业，而 `industry_mainline` 里也有"其它行业"桶 → 这 687 只个股反而白拿 **+1**（实测 301035 `sector=+1`）。与 `get_mainline_summary` 的"其它行业不参与主线判定"惯例不一致。→ `mainlines`/`crowded_mainlines` 均排除 `其它行业`。
  3. **【中】`get_mainline_summary` 旧库缺列即整体报错**：SQLite 不支持 `ADD COLUMN IF NOT EXISTS`（`init_mainline_table` 的 ALTER 静默失败）→ 查询 `crowded/crowd_ratio` 会抛异常。→ 加 try/except 降级为不含这两列的基础查询（取不到时视为 False，与迁移前行为一致）。
- **复验**：`get_mainline_summary ok`（主线 10 条、`crowded/ratio` 正常）；`sector_flow` 33 个一级行业（排除"其它行业"）；家电行业 -15.7 亿正确；其它行业个股 301035 `sector=0`（资金未知，而非 ±1）✓；`py_compile` 通过。
- ⚠️ 这 3 处修复落在**已提交的** `42d491d` 之上 → 目前未提交改动新增 `backend/app/mainforce/confluence.py`、`backend/app/mainline.py`。

## 🔍 Supabase Egress 318M（09-12）诊断 + 生产泄漏修复（2026-09-13）
- **时间口径（关键）**：Supabase Egress 按 **UTC 日**统计 → 北京时间 09-13 凌晨 02:xx = **UTC 09-12 18:xx → 计入 12 号**。所以凌晨跑的研究脚本都算到 12 号。
- **开发产生的部分（有据可查）**：`backend/data/research-cache.db` mtime = **2026-09-13 02:19（北京）**，该次重建拉了 `backtest_prices` 全表 **503832 行**（表 61MB）+ `mainflow_history` 83956 行（13MB）≈ **50-60MB**；当天凌晨还跑了 `flow5_curve_backtest` / `accum_pullback_backtest` / `mainline_crowding_backtest` / `strategy_whitelist_review`（2-3 次，每次拉全量 K 线 20-40MB）/ `compute_mainline` 重算。**开发合计估计 100-180MB**。
- **★★ 顺带发现**并修复**生产级 egress 泄漏（本轮未提交）**：`recommendation._recompute_whitelist` 调 `_load_prices_map(codes)` **漏传 `start`** → 每次拉全部历史 K 线（~20-40MB）；该函数被 `get_push_whitelist`（日报/推送/前端都调）使用、6h 缓存 → **工作日每天最多 4 次 ≈ 80-160MB/天**。→ 改为 `start = min(信号日)`（撮合只需「信号日及之后」）。**验证结果完全不变**（白名单 / 各战法 n / 胜率 / 均收益 / PF / 半衰期告警全一致），流量降 ~95%。`_load_prices_map` 的注释早就写了「start 过滤…传输量降 95%+」，此处属历史遗漏。
- **⚠️ 同类修复 gate 被否决（重要教训，已回退只留注释）**：`mainforce/gate.py:gate_states_for_signals` 也调 `_load_prices_map(无 start)`，但**不能**用缩短历史来省——`chips.chip_series` 的价格网格 `p_min/p_max` 由**传入 bars 的 min/max** 决定（`chips.py` L84-89），缩短历史会改变网格 → `price_pos` 跨 0.75 阈值 → **闸门漂移**（实测 `dragon_turnaround` n **136→149**、`single_yang_unbroken` 305→320；白名单虽不变但统计变了）。**回退后 n 完全恢复**。要省这部分应走 `DATA_SOURCE=pack`（pack 命中读本地 sqlite，零 egress）。
- **还有同类点（本次未动）**：`routers/performance.py:186`（战法回放）、`backtest/regime_review.py:130`（周报复盘）也无 start；两者都只用「信号日之后」数据做撮合/收益，理论上可安全传 start，留待后续。
- **表规模备忘**：`backtest_prices` 61MB / 503832 行；`kline_cache` 16MB / 2220 行（每行大 JSON，全表拉取即 16MB）；`mainflow_history` 13MB；`indicator_cache` 7.5MB。
- **建议**：观察工作日 egress 是否下降；确认 Render 是否设了 `DATA_SOURCE=pack`。

## 🔥 Egress 596MB 根因确认 + 根治（2026-09-13）—— 本地 pack 陈旧导致静默回退 DB
- **用户提供 `logs.md`（Supabase PG 日志导出 6760 行）**，找到铁证：
  - `duration: 53257.513 ms / 20471.568 ms`，`Query Text: SELECT code,date,open,high,low,close,volume FROM backtest_prices WHERE code IN ('…约 400 个 code…')`，**无 `date >=` 条件**，plan 显示 `rows=294810`（29.5 万行）
  - 时间戳 **13 Sep 02:27:25 / 02:34:15** —— 与本地跑脚本的时间完全吻合（02:20 flow5 / 02:53 accum / 03:22 mainline）
- **根因（100% 确认，非猜测）**：本地 `.env` 是 `DATA_SOURCE=local`，但本地 pack **停滞在 `pack_date=20260910`**（mtime 09-11 01:31），而此刻"应可用"最新包日是 **2026-09-11** → `pack_source._is_stale()` 返回 **True** → `get_klines()` 直接 `return None` → `get_prices()` 返回 `[]` → `_load_prices_map` 认为"包里没这只"→ **全量回退查 Supabase**（拉每只全部历史）。本地每跑一个脚本就重来一遍（`_PRICES_CACHE` 是进程内的），叠加 `_recompute_whitelist`（我跑了 4-5 次）+ `research_cache` 首次重建（50-60MB），累计 ≈ 596MB。
- **治标（已执行）**：`python scripts/sync_local.py --force`（走 GitHub Pages，**零 Supabase 流量**）→ 新包 **183.5 MB**（原 126.8MB）/ `pack_date=20260911` → 复验 `is_stale()=False`、`600519/000001/002452 各 752 根 OK`、`_load_prices_map` 命中走 pack **零 DB 查询** ✓
- **防复发（代码，本轮未提交）**：`pack_source._is_stale()` 判定陈旧且 `DATA_SOURCE=local` 时 → `_warn_once` **显式警告**（"本地数据包陈旧 → K线/指标/回测价格读取将回退查 Supabase（egress 暴涨！）请运行 sync_local.py --force"）。此前是**静默回退**，用户毫无感知（这是 596MB 没被及早发现的直接原因）。pack 模式有 `_maybe_refresh` 自动重下，不会走该路径。
- **生产不受影响**：`DATA_SOURCE=pack` + `_maybe_refresh()`（30min 自检，`_pack_outdated()` 已按交易日历修）→ 会自动重下，不会静默回退。
- **教训 / 约定（重要）**：**本地开发前先跑 `python scripts/sync_local.py --force`**（尤其周一/长假后第一个交易日——那时"应可用包日"已前移，旧包必然判陈旧）。`sync_local.py` 的 docstring 写"包每天 16:00 由 GitHub Actions 生成"是**过时信息**，实际发布点在 **21:03**（`pack_source._PACK_READY_HHMM=(21,30)`）。
- 相关：上一轮的 `_recompute_whitelist` 传 `start` 修复（生产侧每次省 95%）仍是有效的第二道防线。

## 📌 数据包调度时间核实与文档修正（2026-09-13）
- **用户实际配置（cron-job.org，北京时间）**：**K 线包 18:00 触发**、**后端包 19:00 触发**。原因：**怕腾讯当日 K 线在 16:00 尚未更新完整**（历史上有"末根补齐重试"专门治这个）→ 原 16:00/17:00 会打出「缺末根」的当日数据。
- **全链路时间线（现行）**：18:00 K线包（约 37min）→ 19:00 后端包（实测约 1h43m）→ **~20:43 完成** → daily-batch 由 `workflow_run` 接棒（约 74min）→ **~21:57 结束**。比原 16:00/17:00 配置整体推后约 2 小时。
- **修正 4 处过时文档**（都还写着 16:00/17:00）：`.github/workflows/kline-data.yml`（`0 18 * * 1-5`）、`backend-pack.yml`（`0 19 * * 1-5`）、`daily-batch.yml`（19:00 接棒 → ~20:43）、`scripts/sync_local.py` docstring（"包每天 16:00"→18:00/19:00 并补陈旧自检提示）。
- **★ 顺带修正 `pack_source._PACK_READY_HHMM`：21:30 → 22:00**。该值决定 `_latest_available_pack_day()` 从几点起认为"今天该有新包"。后端包 20:43 完成，原 21:30 余量仅 47 分钟（Actions 排队易踩）；一旦误判陈旧 → **所有 K 线/指标读取全量回退查 Supabase**（正是今天本地 596MB 的机制）。22:00 余量 ~77 分钟；正常完成时不受影响（`pack_date` 更新即生效，20:43 时 `latest_available` 仍是前一天 → 不判陈旧 ✓）。验证：09-11 20:43/21:30 时 `latest_available=09-10`（安全），22:30 才转 09-11；09-14 19:30 → 09-11，22:30 → 09-14 ✓。
- **时序风险核实结论：无风险**。`SCORE_SNAPSHOT_WINDOW=(1080,1140)`（18:00-18:45）虽是评分快照窗口，但该 loop 在 **Render 只读模式（`RENDER_READ_ONLY=1`）× `_heavy()`** 与本机 **`ENABLE_HEAVY_JOBS=0`** 下均被关闭；实际写 `ranking_history` 的是 **Actions 日批**（后端包完成后的 `workflow_run`，20:43 起跑）→ 拿到的必然是当天新包 → **不会**出现"今日价+昨日技术特征"的旧 bug 复发。⚠️ 唯一前提：**若将来有非只读、开重任务的实例（如自建机 `ENABLE_HEAVY_JOBS=1`），18:00 的评分快照会拿到旧指标** —— 届时需把 `SCORE_SNAPSHOT_WINDOW` 挪到 21:00 后。

## 🔍 审查：Coach 前置两项（3f47064，2026-09-13）
- 审查对象：① `macro.py` US10Y 规则（v3）② `strategies/market_regime.py` 阴跌子档（`STRATEGY_GRIND_GATE`）+ 回测脚本 `strategy_lowvol_admission_test.py`。
- **结论：实现正确、口径自洽、无硬 bug**。逐行对比 `_recompute_whitelist`：G 闸门/退出 v2/T+1 撮合/start 限流全同源；`hold_days=5` == `WARFARE_HOLD_DAYS`；`562=305+223+33+1` 自洽；`_resolve_metric` 正确解析 derived(`us10y_bp_change`) 与嵌套(`us10y.price`)，price 字段语义=收益率（与 us30y 同约定）。阴跌闸门覆盖 neutral_bearish（nb 底层=neutral+ma_down，strategies 版拿到 raw neutral）✓，三处调用链（router/scheduler/paper_trading）均接入 ✓。
- 4 个要点（**已提交给用户，尚未改代码**）：
  1. **方法论矛盾（最重要）**：阴跌全禁是「扫描层」禁（`_do_scan` 直接 return []，不写 strategy_results），与 #7 白名单「照常扫描攒样本、只不推送」的推送层哲学矛盾 → 阴跌期间 strategy_results 零增长，未来无法复盘「阴跌段逆势战法」。非 bug，是 P3 既有架构选择，需用户明确是否改「扫描攒样本 + 推送全禁」。
  2. **归因瑕疵**：报告「高波白名单 morning_star/ma_pullback 阴跌段 33.3%/-1.465%」实际 morning_star 阴跌段 0 成交，33 笔全是 ma_pullback。
  3. **口径健壮性**：回测脚本无条件跑 G 闸门，生产有 `if exit_policy()=="v2"` 条件；当前默认 v2 一致，未来改 v1 会分叉。
  4. **回归保护缺失**：8 场景单元测试是临时的、未落地（全仓库 `*test*.py` 无任何引用 `GRIND_GATE`/`阴跌`/`us10y`），建议落成 `test_grind_gate.py`。

## ✅ 审查后落实：B 方案准入分档 + 脚本对齐 + 回归测试（2026-09-13，**未提交**）
- **要点 1 选 B（用户决策，已实现）**：阴跌全禁从「扫描层禁」改为「**入场层禁**」——
  - `strategies/market_regime.py`：`is_strategy_admitted(name, regime=None, volatility=None, **for_scan=False**)` 加 `for_scan` 分档；`for_scan=True` 时在 **base 类型判定后短路返回**「准入（扫描层·攒样本）」，阴跌闸门与高波白名单不再拦截。新增开关 **`STRATEGY_WIDE_SCAN`**（默认 on，设 off 回退为扫描层完整判定）。
  - 扫描链路改传 `for_scan=True`：`router.py`（scan 接口）、`router.py:_do_scan`、`flash/scheduler.py:strategy_scan_loop`。
  - **入场层保持完整判定**：`paper_trading.py`（模拟盘入池）不传 for_scan；**推送仍由 `get_push_whitelist` 独立把关**（框架本来就有这层，无需新增）。
  - 真实市况端到端（当前 neutral+down+normal）：扫描层 → `True（准入（扫描层·攒样本））`；入场层 → `False（阴跌市战法全禁）`；base 类型（趋势类在震荡市）扫描层仍禁 ✓
- **要点 3 已改**：`scripts/strategy_lowvol_admission_test.py` 补 `gate_on = exit_policy() == "v2"` 前置（非 v2 跳过 G 闸门，与生产一致）、`hold_days` 改用 `WARFARE_HOLD_DAYS` 常量（原硬编码 5，当前恰好相等但将来会静默分叉）。
- **要点 4 已改**：新建 `backend/test_grind_gate.py`（**13 场景全过**，独立可执行 + assert 风格，monkeypatch `detect_market_regime` → 零 DB/网络依赖）。
- **要点 2 已改**：PLAN ③ 归因修正（那 33 笔全部来自 `ma_pullback`，`morning_star` 阴跌段 0 成交）。
- **★ 又踩同一个编码坑**：测试脚本初版用圈号 `⑪`（U+246A）→ Windows GBK 控制台抛 `UnicodeEncodeError` **中断测试**（与今天 `recommendation.py` 的 `✓/✗`/`⚠️` 同源）→ 标签改 ASCII `[n]`，并在脚本开头加 `sys.stdout.reconfigure(encoding="utf-8", errors="replace")` 兜底。**约定：脚本输出的非 ASCII 标签一律避开 GBK 外字符（⑪+ 圈号、✓ ✗ ✅ ⚠️）**。
- 改动文件：`backend/app/strategies/market_regime.py`、`backend/app/strategies/router.py`、`backend/app/strategies/paper_trading.py`、`backend/app/flash/scheduler.py`、`backend/test_grind_gate.py`（新增）、`scripts/strategy_lowvol_admission_test.py`、`PLAN_2026-09-12.md`。

## ✅ Coach W1 纯规则骨架（2026-09-13，**未提交**）—— 上游前置三项齐后解锁
- **新模块 `backend/app/coach/`**（对齐简报 §3.4，约 600 行）：`rules.yaml`（阈值集中配置 + 第一周仅 2 条 push）· `rules.py`（求值器 + 数据源适配，纯代码可单测）· `audit.py`（coach_plans/coach_alerts 表 + 执行一致性度量 + T+5 结果回填）· `monitor.py`（coach_loop 轮询 + 硬警报 + 10:30/14:45 体检卡 + 预承诺剧本 write_plan）。
- **9 条规则**：A 持仓纪律（stop_loss_hit 止损触发、hold_3d_review 持有满3日、loss_over_7pct 浮亏超7%、crowded_trim 拥挤减仓、distribution_cut 高位出货砍）；B 市场闸门（gate_no_add 环境不支持加仓、gate_reduce 减仓、gate_add 加仓）；C emotion_fuse 情绪熔断。
- **评审 4 处全部落实**：① **事前无权**——数字由 `rules.py` 代码注入，LLM 只能渲染（W2）；② **30s 轮询零 egress**——只读 `routers/market._cache["stocks"]`（stock_cache_refresh_loop 每 120s 刷新），不逐股拉腾讯/新浪；重规则（ret20/price_pos）归 `tier="all"` 只跑体检卡/收盘回写；③ **挂点**——`scheduler.start()` 的 `asyncio.create_task(coach_loop())`（盘中保留，不被 READ_ONLY 关），日批只挂 `daily_close_job`（收盘回写 + 结果回填）；④ **执行一致性度量**——`audit.execution_consistency()` 第一版口径：执行率分母=已决策（executed∈{yes,no}），未响应单列不进分母。
- **红线落实**：阈值全在 rules.yaml（可调），判据输入取现有信号源（regime/macro/margin_sentiment/breadth/持仓），代码零硬编码点位。
- **集成点（关键发现）**：行情缓存=`routers.market._cache["stocks"]`（零网络）；两融=`flash.margin_sentiment.get_margin()/get_sentiment()`（24h 缓存）；出货判定=`mainforce.overlay.mainforce_overlay()`（signal=="distribution"）；预承诺剧本挂 `paper_trading.fill_pending_positions` 成交处（写 coach_plans）。
- **顺带修复**：`requirements.txt` 补 `PyYAML==6.0.2`（此前是传递依赖，CI python3.9 干净环境 `import yaml` 会挂，而 `app.coach.rules` 顶部 import yaml）。
- **验证**：22 场景单测全过（monkeypatch ctx，零 DB/网络）；`import app.main` OK（145 routes）；py_compile+lint 干净；真实数据端到端——当前 nb 市正确触发「环境不支持加仓」，两融净减 102 亿、10Y 4.968%、DXY 99.09 均正确读出。
- **W1 不含**（W2+）：LLM explainer、前端教练 Tab、模拟盘 4 周跑、执行率对照审计。
- 改动文件：`backend/app/coach/{__init__,rules,rules.yaml,audit,monitor}.py`（新增）、`backend/app/strategies/paper_trading.py`、`backend/app/flash/scheduler.py`、`backend/requirements.txt`、`backend/test_coach_rules.py`（新增）、`PLAN_2026-09-12.md`。
- **补：真实持仓数据源（用户诉求「教练要指导我的持仓」，2026-09-13）**：Coach 持仓上下文从「仅 paper_positions」扩展为「paper + user_portfolio 合并」。`user_portfolio` 的 `cost`=**成本价（每股）**、无止损字段、无买入日 → 默认止损=成本×0.92（`REAL_STOP_LOSS_PCT=-8%`，与前端 `usePortfolio.ALERT_CONFIG.stopLossPct` 一致，避免口径冲突）、持有日用 `created_at` 近似；`source: paper|real` 区分，警报/体检卡带「真实持仓」/`[真]` 前缀。
  - **发现数据问题**：user_portfolio 里 `000567 海德股份` 有 **2 条重复**（成本 6.82/7.087，分次买入未合并，后端 `db.upsert(conflict=["user_id","code"])` 未生效）→ Coach 侧按 code 去重（保留 created_at 最新）防御，但**数据层重复待用户清理**。
  - 验证：6 条真实持仓正确读出（京能电力/长江证券/中油资本/海德股份×2/九安医疗）→ 去重后 5 条；26 场景单测全过。

- **W1.5 仓位建议引擎（2026-09-13，**未提交**）**：用户诉求「客观仓位管理 + 项目数据联动」（现状：前端 `calcPositionSize`/`evaluatePositionAction`/`predictionEngine` 都是写死规则，未联动真实数据；趋势健康度盘中实时但靠逐只实时拉 K 线）。新增 `app/coach/position_sizing.py`：
  - **市场层（总仓位上限）** = `trade_gate.REGIME_POSITION[regime]` 基准（defensive 0/nb 20/neutral 50/offensive 80）× 降档因子（10Y>5% ×0.8 / 两融5日净减 ×0.85 / 情绪过热≥80 ×0.7 / 跌停≥100 ×0.7 / 涨跌比<0.3 ×0.7），`_snap` 档位化（0/5/10/20/30/40/50/60/80）。
  - **个股层** = 复用 `mainforce.trade_gate.evaluate(code)`（已含 regime+筹码拥挤+主力根据+当日矛盾，读落库缓存零网络）。
  - **组合层** = `suggested = _snap(min(个股档位, 总上限))`——**不按持仓数稀释**（初版归一化导致总上限 10% 摊 5 只每只 2% → snap 到 0 全归零，已修正）；总上限是「天花板」、个股档位是「意愿」，min 取两者交用户判断砍哪只。
  - 路由 `GET /api/user/position-sizing`（routers/user.py，JWT 隔离读 user_portfolio 去重 codes）；前端 `Portfolio.vue`「建议仓位」列改调该接口（`getUserPositionSizing`，替代 `calcPositionSize`，import 已清理）。
  - **实测**：当前 nb 市 → 总上限 10%（基准 20% × 两融净减 0.85）、5 只持仓每只建议 10%（轻仓试探，A△无主力根据）；16 场景单测全过、`import app.main` 146 routes、lint 干净。
  - 改动文件：`backend/app/coach/position_sizing.py`（新增）、`backend/app/routers/user.py`、`frontend/src/api/index.js`、`frontend/src/views/Portfolio.vue`、`backend/test_position_sizing.py`（新增）。

- **W1.5 续：趋势健康本地算 + 预测/智能建议数据联动（2026-09-13，**未提交**）**：
  - 用户诉求：①趋势健康度盘中本地计算（减少后端实时拉 K 线）②预测/智能建议从「写死规则」改「数据联动」。
  - ① **趋势健康本地算**：移植 `engine._calc_trend_health` → `frontend/src/utils/trendHealth.js`（5 维度：量能/支撑/深度/动量/均线，同口径）。Portfolio 复用 `getStockTechnical` 的 techData 本地算（与「今日预测」共用同一次 K 线请求），替代 `getStockScore` 的 trend_health（保留 fallback）。收益：趋势健康不再单独依赖 getStockScore 的实时拉取。
  - ② **智能建议数据联动**：`evaluatePositionAction` 加第 4 参 `sizing`（position_sizing 个股层）——主力出货/防御市（`position_pct===0`）→ 强制「清仓」，优先级高于技术面止损（简报 §2.1「price_pos>0.75+出货→直接砍」）。
  - ③ **预测数据联动**：`generatePrediction` 加 `env` 参数，在多空得分后、归一化前按 `position_pct` 调整概率（0→看跌+10 / ≤10→+3 / ≥50→看涨+4）。Portfolio `fetchPrediction` 传 sizing 个股层（异步竞态下 env 可能 null，退化为纯技术面——可接受，智能建议列由 computed 自动补上）。
  - 验证：前端 `pnpm build` 成功（10.79s，仅 chunk size 既有警告）、lint 干净。
  - 改动文件：`frontend/src/utils/trendHealth.js`（新增）、`frontend/src/utils/predictionEngine.js`、`frontend/src/views/Portfolio.vue`、`frontend/src/composables/usePortfolio.js`。

- **✅ W1 补漏（2026-09-13，已提交推送 `f714222`）**——审查发现两个功能闭环缺口，已修：
  1. **`daily_close_job` 无调用点**：定义了但既没挂 scheduler 也没挂日批 → heavy 规则（拥挤减仓/出货砍）永不落库、`outcome_pct` 永远空。**修**：挂 `coach_loop` 盘后分支（`940 <= t < 1440` + `schedule_done("coach_daily_close")` 幂等，与 `paper_track_loop` 盘后兜底同模式，评审③不被 READ_ONLY 关）。
  2. **`write_back_execution` 无入口**：企微警报文案「确认/放弃（放弃须填理由）」但用户无回写途径 → 教练 KPI 永远 0。**修**：新建 `app/routers/coach.py`（JWT）——`GET /api/coach/alerts`（历史）、`POST /api/coach/alerts/{id}/execute`（回写，**放弃必须填理由否则 400**、404 检查）、`GET /api/coach/consistency`（执行一致性）、`GET /api/coach/abandon-reasons`（周报复盘）；`main.py` 注册 `/api/coach`。
  - 修过程中踩了一个小坑：`from app.routers.coach import router as coach_router` 后 `include_router(coach_router.router)` 多取一层报 `AttributeError`（对比 `paper_router` 的注册模式：别名已是 router 对象，直接用）。
  - 验证：150 routes（+4 coach 路由）；冒烟全过——放弃不填理由拒 / 放弃+理由落库 / 改判执行理由清空 / `execution_consistency` 分母=已决策正确 / 测试行清理；py_compile+lint 干净。
  - 改动文件：`backend/app/coach/monitor.py`、`backend/app/routers/coach.py`（新增）、`backend/app/main.py`。
  - **遗留到 W2**：前端教练 Tab（消费这 4 个路由）；企微卡片按钮直连回写（需企微应用回调配置，先用前端 Tab 承接）。

- **✅ 教练买侧决策卡（2026-09-13，**未提交**）**：用户诉求「教练盯盘→发现好股票→决定买入+买入点位有依据」。探索发现**现有 `format_signal_message`（recommendation.py）已是决策卡雏形**——已含 trade_gate 三条件+建议仓位（`render_advice`）、环境传导链（`confluence_line`）、买入理由（`build_buy_reason`：形态逻辑+目标推导+止损依据+市场匹配）、开盘确认提示、v2 退出策略；**唯一缺评分 `buy_point`**（买入区间/支撑/时机）。
  - **实现**：`recommendation.py` 加 `_buy_point_md(code)` helper（复用 `routers.scoring._compute_top5_extras`，零网络读 indicator_cache，盘后=今日收盘）；`format_signal_message` 补「📍 买入点位」段（买入区间+参考价+支撑位+时机+距支撑偏离）。
  - **关键发现**：买入点位依据的组件早已齐备（`engine._calc_buy_point`：买入区间 `[下界,上界]`+参考价中轨+支撑位 MA20/MA60/布林/20日低点+时机「适合介入/等回调/追高风险」；`_target_derivation`：每战法目标量度；`build_open_confirmation`：开盘低吸规律），缺的只是**串进推送**，非新造计算。
  - 验证：冒烟三态通过（完整/空 buy_point/无支撑）；29 场景 coach 测试全过；lint+compile 干净。
  - **后续（未做，已规划）**：盘中候选池实时重算（路径 A——昨收指标+实时价筛候选池，只对几十只实时拉 K 线）；全市场 `incremental_update` 接线（路径 B，远期正解）。当前阴跌市 `is_strategy_admitted` 全禁 → 买侧卡上线后正确「闭嘴」，等 regime 修复才推。
