# 2026-09-13

## 🔍 排行榜 Edge 轨道只显示 9 个快照日（08-17~09-01）的根因（**数据覆盖缺口，待修**）
- 现象：`/api/system/performance` 排行榜 Top10（每日等权 T+5）窗口止于 09-01、仅 9 日。
- **ranking_history 本身完整**：08-17~09-11 共 **17 个快照日** ×50 行 = 850 行全齐（含价格），每日 Top10 价格全 >0。仅 08-18/19/21 三天快照缺失（快照任务早期不稳，非近期问题）。
- **真根因 = 个股 `backtest_prices` 回填覆盖不全**：回填任务只覆盖 ETF 池 + sh000300 + **战法涉及个股**，评分股不回填。实证：09-04 上榜的 10 只里 8 只日线停在 09-07~09-10（仅 000685/000933 两只到 09-11 完整）→ 09-02 起的快照日 T+5 凑不满 6 根 K 线（performance.py L99 `len(b)==fwd+1` 过滤）或每日凑不满 5 只 → 全部缺席。09-01 能显示纯粹因其上榜股恰好多只近期被战法回填过。
- **影响**：排行榜 edge 轨道把 09-02 起的样本全丢了——而这正是"近期衰减"最需要观察的窗口（09-12 周报"后 30% 胜率 17.7%"同期）。
- **修法（未实施）**：① 日批/回填任务把当日 ranking_history 上榜股（50 只）纳入 backtest_prices 回填（低成本保未来）② 一次性补历史：08-17 以来全部上榜 distinct code（约百余只）批量回填到最新 → 立即解锁 09-02~09-04 的 T+5。③ 顺带：performance.py `_ranking_track` 每股单独查库（10×N 天 = 上百次查询），可改批量 IN 查询。
- 网络注意：本机 DNS 间歇故障已解决——用户把网卡 DNS 手动改为 223.5.5.5/119.29.29.29（截图确认设置正确），flushdns 后解析 0.03s 正常。

## ✅ 排行榜 T+5 缺口修复（09-13，**未提交**；补历史后台运行中）
- **根因修正**：`_collect_strategy_codes` 其实已含近 30 天评分股（backfill_history.py L95-101）——不是"不在池里"，是**每晚 150 只配额排队 + 09-12 换库打断**：实测全池 643 只个股仅 144 只同步到 09-11，499 只滞后（141 只停 09-07）。数据源拉取完全正常（000567/000807 增量拉取返回全 5 根实证）。
- **①回填任务改造**（backfill_history.py）：新增 `_collect_rank_codes(days=7)`——近 7 天上榜股从 ranking_history 取 distinct code，在 pending 排序中**插队最优先**（`key=(上榜0/普通1, 滞后日期)`），保证排行榜 T+5 窗口内数据必然就绪；`backfill_daily(quota=DAILY_STOCK_QUOTA)` 参数化，`quota=None` 不限量；stats 新增 `rank_priority`。默认行为不变（scheduler/daily_batch 调用无参）。missing 检测段遍历全量 pending，不受影响。
- **②补历史**：`backfill_lagging()` + CLI `--lagging`（幂等可重跑），09-13 本地后台执行中（499 只 × 1s 限速 ≈ 10 分钟）。跑完后排行榜轨道应显示 08-17~09-04（17 个快照日 - 缺失的 08-18/19/21 = 14 天；09-02/03/04 的 T+5 = 09-09/10/11 均已满足）。
- ⚠️ 验证注意：`/api/system/performance` 有 **1h 进程缓存**（`_perf_cache`），补完后要等缓存过期或重启 Render 才能看到窗口变化。
- LIKE 查询注意：psycopg2 参数化 SQL 里 LIKE 的 `%` 要写 `%%`（调试时踩过）。

## ✅ 补历史完成 + 排行榜轨道闭环验证（09-13）
- 后台补历史结果：`stock_pending=480` 全部处理、`stock_rows=15466` 写入、**`stock_missing=[]`**（全部同步到基准 09-11）、`rank_priority=116`（近 7 天上榜股插队生效）。注：stats["rows"]=7 是历史设计——个股行数只记在 `stock_rows` 不计入 `rows`，勿误读为"只补了 7 行"。
- **排行榜轨道恢复**：9 天（08-17~09-01）→ **12 天（08-17~09-04）**，09-02/03/04 三个快照日解锁 ✓。cum_return **-1.14%** vs 沪深300 **-3.76%**（跑赢 +2.62pp）。
- **观察点**：近 5 个快照日（08-28~09-04）4 天 mean 为负（-1.63/-2.14/-1.50/-2.42），仅 09-02 +1.94——评分榜在普跌市也在亏（虽跑赢基准），与 09-12 周报"后 30% 胜率 17.7%/近期衰减"互为印证。
- 缺失的 08-18/19/21 三天是**快照本身缺失**（非本次修复范围）；09-07 起的 T+5 会随周一后的交易日推进自然解锁。

## 📋 评审《外部研判与功能需求简报_20260913.md》§5（6 题：2 通过 / 4 需修改 / 0 驳回）
- **Q1 数据核验（通过，个别需手动核实）**：联网复核——8月CPI同比3.4%✓、核心同比3.7%（简报漏掉的更关键边际）、非农+16.2万✓、Warsh 5/22上任✓、特朗普施压✓、布油破100✓、非农后加息概率60%/9-2 66%✓。**无法精确复核需用户手动核实**：CPI后85-88%FedWatch、Kalshi57%/Polymarket49%、PPI5.4%、10Y"一度4.91-5.0%"（搜到9/10是4.85%）、联邦基金3.50-3.75%、胡塞攻占穆哈港事件。三情景55/30/10/<5=主观先验不可证伪，"年内1次55%"偏高（非农强+核心CPI3.7%+PPI+油价→"2次+"可能被低估），建议改用点阵图年内次数的离散场景。
- **Q2 历史复核（需修改）**：方向对（首次加息≠牛市终结、市场跑在Fed前），但"回测12/24月收益分布"无量化、只定性；供给型样本(1994/2004/2021-22)缺位——而本次最像2022还是1994正是置信度分叉点；A股两段主因是自身因素（简报已自认），只可作"天花板"不可作"幅度预测"。
- **Q3 规则冲突（需修改，红线）**：§2.3 五触发器里 4 条与现有信号重复——加仓③=regime offensive判定、减仓③=margin_sentiment/sentiment_vs_margin、加仓①=breadth_collapse反向、加仓②的120pips=macro已有cny_cnh_basis；减仓②"3882"写死常量。**硬性要求：阈值(3882/120pips/5%)必须引用项目实时信号源，不写死**。0.4%单笔风险与已实现的risk_multiplier吻合（印证不冲突）。
- **Q4 教练评审（需修改）**：架构通过；但①"事后否决"应改"事前无权"（LLM只渲染规则结果、数字由代码注入，冲突从根上不可能）②30s轮询读腾讯内存缓存不逐股拉新浪（否则放大egress/WAF）。情绪熔断、放弃填理由、"不动是默认输出"=通过（命中方法论第6条）。审计指标需先定义"执行一致性"。
- **Q5 集成点（需修改）**：教练是盘中实时、日批是两套时钟。挂点：盘中轮询在scheduler保留组(同paper_track_loop，不被READ_ONLY关)；预承诺计划在fill_pending_positions成交时生成；硬警报实时、独立于15:35矛盾扫描；日批只挂"收盘回写coach_plans+周报复盘"两处。
- **Q6 优先级（需修改）**：§4五漏洞现状=漏洞3已完成(#6)、漏洞2=P0#8、漏洞1=P1、漏洞5=P1#6、漏洞4=P2。**核心裁决：教练不得插队到数据面修复之前**（教练规则引擎引用regime/flow5/margin，信号有bug会"忠实执行错误规则"）。4周：W1数据面修复+教练纯规则骨架、W2滚动窗口+教练规则引擎/audit、W3-4模拟盘跑4周、真实持仓W5+。
- 简报文件位置：`外部研判与功能需求简报_20260913.md`（根目录，用户新增，非计划类）。
- **用户核实的权威口径（已回写简报 §1.1/§1.4）**：①联邦基金利率 3.50–3.75%=2025/12/10 设定、9-3 票型，2026 年五次 FOMC 均 hold，EFFR 3.63% ②6 月点阵图=18 点中 8 人预期年内≥1 次加息、两派均分(8 vs 10)、年末中值 3.8% ③美债 10Y=9/11 收报 4.9708%（盘中 4.98%）④6 月 FOMC 声明官方采用"供给冲击"措辞→供给型通胀从推断升格为官方定性，作 §1.4"滞胀对股市最差"的论据。
- **评审裁决已沉淀进 PLAN**：§2.4 新增"交易员教练（Coach）"条目（需求+4 处评审修改+红线），§6 P2 新增教练排期（**不插队到数据面修复之前**，W1 纯规则骨架→W2 规则引擎/audit→W3-4 模拟盘→W5+ 真实持仓）。教练功能的 4 处评审修改与"阈值引用现有信号源不写死"红线已固化为 PLAN 约束。

## ✅ §3b#9 regime 口径三件套（2026-09-13，**未提交**）
- 选择 #9 而非 #8 原因：#8 flow5 回测需大量拉价格数据，本机 DNS 间歇故障（Python `getaddrinfo` 时好时坏）会反复卡；#9 是纯代码/本地表结构改动，零网络依赖。
- **nb 两日确认**：`market_regime._apply_bearish_refine` 重构为查 `market_regime_history` 昨日 `bearish_refine_raw`：进入 nb = 昨日/今日 raw 均满足；退出 nb = 连续 2 日不满足（中间日保持 nb，避免单日反弹噪音）。`_nb_condition_raw()` 抽取 raw 条件逻辑（宽度恶化/外围恐慌/沪深300 近2日累计下跌）。
- **regime_score 死字段修复**：进入 nb 时 `regime_score=-20.0`，不再套在 raw neutral 的 0 上，形成 offensive(50+adx)/neutral(0)/nb(-20)/defensive(-50-adx) 连续轴。
- **defensive 权重矛盾澄清**：`get_regime_weights` 注释写清——450 快照否决的是 **neutral→nb 的防御化权重调整**（故 nb 权重=neutral），与 **defensive 档独立权重**不矛盾；defensive 触发=高波动+趋势向下 或 强趋势向下，nb 触发=低波阴跌+重心下移。
- **表结构**：`market_regime_history` 新增 `bearish_refine_raw BOOLEAN` 列，`_ensure_history_table` 幂等 `ALTER TABLE ADD COLUMN IF NOT EXISTS`。
- **验证**：本地 741 根 sh000300 跑 `detect_market_regime` + fake history 单元测试 → 进入/退出逻辑 OK、score=-20、近 30 天 switches=0（当前阴跌市 raw_nb 持续 True）；py_compile + lint 干净。
- 改动文件：`backend/app/backtest/market_regime.py`、`PLAN_2026-09-12.md`；测试脚本已删除。
