# 2026-09-21

## ✅ subfactor_ic_backtest.py 默认 step 5→1（用户提交昨日纠偏批次后，要求执行此项）
- **改动**（compile OK / lint 0 / **未提交**）：
  - `run()` 默认参数 `step=5` → `step=1`；`--step` argparse default 同步；
  - docstring 写明依据（密度敏感性检验：同窗 step 5→1 技术面负 IC 砍半、KDJ 转正，
    见体检报告 §6.2）+ 两点提醒：与 flow5 回测（step=5）不再同源；
    **首轮 step=1 与旧基线（step=5）滚动复核会出现"变化偏大"标记，属预期，需人工确认一次**；
  - `daily_batch.py` 任务注释：耗时 3 分钟 → **10~15 分钟**（截面 10→~50 个，月度任务可接受）。
- 实测 `inspect.signature(run)` 默认 = 1 ✓；`task_subfactor_ic` 调 `run_ic()` 无参 ⇒ 自动跟随新默认。
- **效果**：10 月首个交易日的首次自动体检即用正确密度，回答"弱负（-0.05~-0.09）+
  尾部驱动是否在新样本复现"。

## 今日观察项（接 9-20 plan）
- dragon_turnaround 加码确认 / 白名单空决策处置（等新交易日数据）
- P1 前端渲染实测（任一"极端流入"股的 factors_down 是否显示新标签）
- 教练新规则 `external_lead_no_rebound` 首次落库记录（只攒样本不推送）

---

## ✅ zzshare token 复测完成（路线图 ①，用户领了 token 并配 Render + 本地 .env）
新增 `scripts/zzshare_probe.py`（可复跑探测：18 接口、参数对齐 pip 包 SHORTCUTS 源码）。
**15/18 可用，0 异常**。结果已写入盘点文档 §7。

**关键结论**：
- **T3 财务解锁** ⭐：`finance_pit(table,trade_date)` 单日全市场 **5521 行**、
  `finance_valuation(date_value)` **5221 行**（pe/pb/pcf/市值）⇒ **路线图 ⑤（基本面
  历史截面体检）可行性实锤**，绕过"攒 60 截面日等 3 个月"——10 月体检收口后的
  第一个大活就选它（此前 9-20 的建议不变）。
- **T1 涨停全通**：`uplimit_hot.ban_info` 直出连板分布（1板61/2板11/3板4/4板2），
  连板高度零成本；`review_uplimit_reason` 174 行题材归因。
- **T0 部分通** → **后补读 vendored README 后全通**：`sentiment_trend(model=1)` 
  **241 行/日**、`sentiment_trend_range` **2410 行/2 周**（分时情绪序列）⇒ 
  **② 情绪回测的回填障碍彻底消除**（model 是**整数**，"default" 会 0 行）；
  `uplimit_trend` 字段全 0 可疑仍挂。
- **★ 用户纠错的方法论教训**：首轮我读了 pip 包 client.py 源码核参，却**没读
  vendored 目录里的 README.md（27KB）与 FUNDAMENTALS.md（31KB 字段文档）**——
  三个"待解"（trend model 取值 / finance_pit 表名与 codes 参数 / lhb_detail 参数）
  README 里**全有现成答案**。**测新 SDK：先读它自带的 README + 字段文档，再读源码**；
  另 FUNDAMENTALS.md 是 ⑤ 开发时的现成字典书。
- **stock_moneyflow 确认死亡**：pip 包 client.py L248-252 **整段被注释** ⇒ 404 必然，
  盘点文档 §0.3 表述已修正，PLAN 遗留关闭（除非升级 zzshare）。
- **坑实锤**：仓库根 `zzshare/` vendored 副本遮蔽 pip 包（`python -c` 直接
  ModuleNotFoundError）—— §4.1 处理建议（改名 vendor_reference/）等用户确认。
- **方法论**：首轮 18 接口"0 行/异常"8 个的真正原因是**参数错位**（无参调用 ≠
  匿名问题）——对照 SDK 源码的 SHORTCUTS 表核参后全部恢复。测新数据源先读源码签名。

**未提交**：`scripts/zzshare_probe.py`（新）、`zzshare功能盘点_20260920.md`（+§7）、
`scripts/subfactor_ic_backtest.py`（step 5→1）、`scripts/daily_batch.py`（耗时注释）、
memory 2 份。

---

## ✅ zz_daily 每日数据积累任务落地（用户确认"把积累任务做出来"）
**定位**（"数据积累型 vs 分析开发型"拆分）：积累有**时钟属性**（晚一天少一天历史）+
本身就是 zzshare「30 天影子验证」载体 ⇒ 现在启动；分析开发（②回测/⑤体检）等 10 月窗口。

**改动（3 文件，compile OK / lint 0 / 未提交）**：
- **新增 `backend/app/zzshare_daily.py`**：
  - 表 `zz_daily_snapshots(date PK, payload TEXT, created_at)`（PG/SQLite 双分支，惯例 ensure_table）
  - `collect_daily(date)`：5 接口（sentiment_trend(model=1) / market_style / uplimit_hot /
    uplimit_stocks / review_uplimit_reason），**单接口 fail-open**（`_error` 记录不阻断）
  - **★ 体积纪律（实测教训）**：全存原始返回 **1541KB/日**——Supabase 免费额度撑不住
    （890KB 是 uplimit_hot 的 `plate_stocks/_bx/_zb` 三个 187 题材成分股 dict 冗余 +
    629KB 是 review_reason 全字段）⇒ `_slim_*` 裁剪：uplimit_hot 只留
    ban_info/max_count/plate/stocks/today（题材→个股映射 review_reason 已覆盖、
    个股详情 uplimit_stocks 已有）；review_reason stocks 裁成 code/name
    ⇒ **71KB/日（瘦身 95%），18MB/年** ✓
  - `run_daily(date)`：幂等（date 主键，已有跳过——补跑安全）；`load_range(from,to)`
    供未来回测消费
- **`daily_batch.py`**：新增 `task_zz_daily` + TASKS 注册 + `DEFAULT_ORDER`（"zz_finance" 后）。
  ⚠️ 不能挂在 task_zz_finance 里——它是**仅周一**任务。
- **`daily-batch.yml`**：env 加 `ZZSHARE_TOKEN: ${{ secrets.ZZSHARE_TOKEN }}`
  （⚠️ **用户待办：GitHub 仓库 Secrets 新增 ZZSHARE_TOKEN**——Render 已配但 Actions 没有，
  缺则日批匿名调用 0 行/慢）。

**验证（真跑）**：落库 5/5 接口 71KB ✓；二跑幂等跳过 ✓；明细体积核对 ✓。
表里已有一行真实数据（2026-09-18）。

**方法论**：payload 体积必须在**真实数据上实测**（预估 50KB vs 实际 1541KB，30 倍偏差
——嵌套明细的膨胀超出直觉）；写入前先算 `len(json.dumps)`。
