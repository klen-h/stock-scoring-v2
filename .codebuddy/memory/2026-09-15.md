# 2026-09-15（周二）

## 🔧 午间预警「下跌板块主力净流入」未列名单修复
- **用户反馈**：收到午间预警（`先知雷达·午间预警`），其中"10 个下跌板块出现主力净流入"只给了计数，**看不到具体是哪些板块**，要求全部列出。
- **三处根因（先截断 + 再根本不渲染）**：
  1. `contradictions/scanner.py:scan_sector_narrative_vs_flow` 的 `*_samples` 存样本时 **`[:5]` 截断**（统计到 10 个只存 5 个）。
  2. `contradictions/labels.py:fmt_metric_value` 渲染列表时 **`v[:6]` 截断**（通用保护，所有列表键都受影响）。
  3. **最根本**：`flash/scheduler.py:_run_midday_scan`（午间雷达）只推 `title + summary + signal`，**`evidence.metrics` 从未渲染** → 板块名单压根没进企微消息（盘后矛盾报告走 `report.py` 才有渲染，午间这条独立路径没有）。
- **修复**：
  1. `scanner.py`：`[:5]` → 全量（统计池本就是涨/跌幅前 20，最多 20 项，消息可控）。
  2. `labels.py`：`fmt_metric_value` 对 `*_samples` 结尾的键**全量渲染**（`_lim=len(v)`），非 samples 仍截 6 项；`more` 提示同步改用 `_lim`。
  3. `scheduler.py:_run_midday_scan`：遍历 `evidence.metrics`，把 `*_samples` 键用 `metric_cn + fmt_metric_value` 追加到预警正文（`  - 下跌但净流入板块：板块A -1.5%（净额 +1.2亿）、…`）。
- **验证**：合成 10 项样本——samples 键列出全部 10 个（无「等N项」）、非 samples 键仍截 6 + 「等10项」✓；三文件 lint 干净。
- 改动文件：`backend/app/contradictions/scanner.py`、`backend/app/contradictions/labels.py`、`backend/app/flash/scheduler.py`。**未提交**。
- 备注：`report.py`（盘后矛盾报告）此前已有 metrics 渲染，本次仅 `labels.py` 的截断放开让它也受益；午间雷达是本次新接的渲染。

## 🔧 教练体检卡企微美化（Markdown 表格 → 列表式，2026-09-15）
- **用户反馈**：交易教练发的持仓体检卡是 Markdown 表格，**企微不支持表格渲染** → 竖线/表头与内容错位，要求美化。
- **根因**：`coach/monitor.py:health_card()` 用 `| 标的 | 成本 | ... |` Markdown 表格（企微自定义机器人 markdown **不支持表格**）。
- **修复**：改为「**每票一行**」列表式——字段用**全角空格**分隔（不依赖列对齐，规避比例字体问题）；盈亏与警报用 **`<font color>`** 着色（企微支持 `info`=绿 / `warning`=橙 / `comment`=灰）；持仓标题加 📋；`[真]/[模]` 前缀保留；环境闸门段保留。
- 预览（合成数据）：
  ```
  **📋 持仓 3 只 ｜ regime=neutral_bearish**

  **1. [真] 平安银行** 000001　10.00→10.50　<font color="info">+5.0%</font>　距止损 -11.4%　持有 3d　<font color="warning">⚠️ 止损触发</font>
  **2. [模] 万科A** 000002　8.00→7.60　<font color="warning">-5.0%</font>　距止损 +2.6%　持有 1d
  ```
- **验证**：lint 干净；health_card 合成数据渲染正常（本地无持仓故实跑返空，用 monkeypatch 合成）。`_push` 走 `notify("coach", ..., force=True)` → markdown，颜色生效。
- 改动文件：`backend/app/coach/monitor.py`。**未提交**。
- **★ 同类待办（已发现，未改，等用户确认）**：backend 里还有多处 Markdown 表格会推企微/前端——`daily_report.py`（ETF/评分榜 Top10/**你的持仓表**）、`backtest/run.py`（周报多表）、`backtest/regime_review.py`、`contradictions/validator.py`（验证准确率表）、`data_files.py`（运行时文件表）。若要统一美化，建议抽一个「企微友好列表渲染」公共函数复用。

## 🔧 企微表格统一美化 + regime 中文化（2026-09-15，用户选「全部修」）
- **★ 关键甄别（避免误改前端）**：逐一核实"哪些表格真的推企微"——
  - **推企微**（需改）：`coach/monitor.health_card` ✓、`backtest/run.py:generate_summary`（周报摘要，`scheduler.py:1190` 推）、`data_files._notify`（运行时数据告警，`data_files.py:262` force 推）。
  - **不推企微（只落库/前端）→ 不改**：`daily_report.run_daily_report(push=False)`（docstring 明说"日报已有前端页，每日推送会刷屏"）、`contradictions/report.run_report`（只 `save_report` 落库）、`regime_review`（只进 `generate_report` 前端报告）。**前端浏览器能正常渲染表格，改成列表反而降级**。
- **新建 `backend/app/wechat_fmt.py`**：`table_to_lines(headers, rows, *, numbered, kv, bold_first, sep)`——表格转「每行一条」，字段用**全角空格**分隔（企微比例字体不做列对齐），首列加粗作锚点，`kv=True` 时其余列渲染「列名 值」。仅用于推企微的内容。
- **改动**：
  - `backtest/run.py`：`_metrics_table(m, wechat=False)` 加参数（默认 False 保前端表格不变）；`generate_summary` 的 3 处表（LLM 信号追踪 / 战法选股指标 / 战法×市场状态 / 宏观指标）改走 `wechat=True` 或 `table_to_lines`。
  - `data_files.py:_notify`：表格 → `table_to_lines`。
  - `coach/monitor.health_card`：表格 → 列表（前一轮已做）；**regime 值中文化**——复用 `strategies.market_regime.REGIME_LABELS`（`neutral_bearish → 震荡偏空（阴跌）`），此前直接显示英文。
- **验证**：`table_to_lines(kv=True)` 输出 `**进攻**　样本 20　胜率% 55.0%　…`；`_metrics_table(wechat=True)` 列表 / `wechat=False` 表格各归其位；教练卡渲染 `**📋 持仓 1 只 ｜ 震荡偏空（阴跌）**`（中文 ✓）；四文件 lint 干净。
- 改动文件：`backend/app/wechat_fmt.py`（新增）、`backend/app/backtest/run.py`、`backend/app/data_files.py`、`backend/app/coach/monitor.py`。**未提交**。
- **★ 追加：`daily_report` 开推送时自动转列表**（用户确认）——新增 `wechat_fmt.markdown_tables_to_lists(md)`：扫描 markdown 里所有 `|` 表格块整体转列表，非表格内容原样保留、**幂等**（无表格时不变）。`daily_report.run_daily_report` 推送分支改推 `markdown_tables_to_lists(md)`——**落库/前端仍存表格原文**，只有企微那份转列表。
- **加粗锚点启发式**：`table_to_lines` 新增 `bold_idx` 参数 + `_label_col(rows)`（猜「标签列」= 第一个非纯数字列）——避免首列是排名/代码时 `**1**` 无信息量。实测：持仓 `代码 000001　**平安银行**　股数 1000　…`、评分榜 `排名 1　代码 000807　**云铝股份**　…`；默认 `bold_idx=0` 路径无回归（`**进攻**　样本 20　…`）；转换幂等 ✓；lint 干净。
- 追加改动文件：`backend/app/daily_report.py`。**未提交**。
- **★ 全局兜底（用户确认，最终方案）**：在 `flash/wechat.py` 的 **`notify` + `push_markdown_batched` 入口**各加一次 `markdown_tables_to_lists(content)`（幂等）——**任何**推企微的内容（含 LLM 生成的盘前简报/复盘/午间预警等）都自动去表格化，调用方无需各自处理。至此 `coach/daily_report/backtest` 三处的单独改造都可视为冗余但无害（幂等）。验证：monkeypatch `_send_batched` 抓取，两条路径（无 category / notify）均把表格转成列表、正文标题原样保留 ✓；lint 干净。
- 改动文件：`backend/app/flash/wechat.py`。**未提交**。

## 📌 周一（09-14）验收对账（2026-09-15）
- **egress（本周 P1 头号项）**：用户报 **周一柱子 ≈150MB**。目标 `<167MB/天` → **达标 ✓**（治理前 09-10 为 458MB、09-12 318MB；本地 596MB 事故已根治）。判据原文还要求「**周一柱子不再高于周二~周五**」（历史上周一凸起= `kline_cache` 单只回退指纹，现包覆盖 2059 只应已消除）——**需对比其他交易日的柱子才能确认凸起是否消失**（用户暂只给了周一）。
- ⚠️ 150MB 仍**高于投影 70~95MB**：残留可能是 ① 周一是 zz_finance 全池 5400+ 只写入日 ② 主池扩到 2059 只（vs 原 1173）③ ranking_live payload 仍未列裁剪（P0-3 只做了 mainforce_state 缓存 + live_ranking 快路径续用）。后续可继续按 §2.1 压。
- **其他验收项核对（依据 09-14 日批日志）**：日批 **19/19 成功**（rank_live 假成功已修+重跑 300 行）；包覆盖 **mainforce_state 2059 只**零回源 ✓（egress 降下来即证明）；精算榜池 1542/指标命中 1518 ✓；**B 方案分档生效**（趋势类"未准入跳过扫描"、震荡类照常 74 信号）✓；**半衰期告警机制工作** ✓；白名单**双轨机制工作但结果为空**（`判据=win_rate；双轨=关(交易日<250)`）——判据 Actions env 已于 09-14 修（`03aa9e3`），**待下次日批验证 expectancy 生效**。
- **待验证**：决策简报已生成（09-14 len 291/4115）但**未推送**（`WECHAT_BUSINESS_ALERTS=0`）→ 要评估"首周推送质量"需先开该开关（Actions workflow + Render）。

## 📋 周二晚（09-15 日批）验证清单
1. **env_check 自检**：日志开头应 `[env_check] 全部一致 ✓`（9 项全 OK，含 `WHITELIST_CRITERION=expectancy`）——验证 Actions env 修复生效。
2. **白名单判据**：`[recommendation] 动态白名单: ...（判据=**expectancy**；...）`——不再是 win_rate。⚠️ 结果可能仍为空（双轨关：交易日<250 + 样本<30），那是样本问题非配置。
3. **rank_live 真落库**：应 `全量精算榜: 300 行落库`；若 `0 行` → 会 ❌ + 企微告警（新加的校验）。
4. **★ 主力标签覆盖率**（本次重点，修了双路径 bug）：日批后查 `ranking_history.mainforce_signal` 不再 97% NULL——应有 accum/distribution 标签。
5. **regime 幂等**：15:40 与日批两条日志，一条「市场状态 …」重算、另一条「已有判定 → 幂等跳过重算」。
6. **模拟盘**：不再出现 `timeout_no_trigger` 平仓。
7. **egress**：周二柱子 vs 周一（判「周一是否仍凸起」）。
8. **包陈旧告警**：若出现 → 说明回退 DB（须关注）。

## 🎯 下一阶段开发建议（2026-09-15）
- **主推：Coach W2**（PLAN §6 排期，W1 已上线）：① **LLM 翻译层** `explainer.py`（只渲染规则结果、数字由代码注入，评审①"事前无权"）② **前端教练 Tab**（含 P1-17「放弃填理由」回写 UI → 执行一致性 KPI 才有数据，W1 闭环的最后一米）③ **模拟盘完整接入**。
- **并行止血（低风险高价值）**：① `zz_finance` 连续失败熔断（周一实测 41min，无熔断时 zzshare 全挂可拖 1-2h）② 排查同类「算完写库不校验结果」的假成功（`rank_live` 是典型，`compute_and_store` 已修）③ egress 视周二柱子决定是否继续压（150→投影 70-95）。
- **等数据（不宜现在开发）**：组合因子 v1 前瞻（需 40 交易日，`/api/scoring/composite-stats` 每天自动积累）→ 通过后才谈改总分权重；消息分 IC / 矛盾兑现率等 P2 数据积累项。
- **待拍板**：P1-11（防御市扫描层是否放行攒样本）、P2-①（us10y 双阈值统一，需回测校准）。

## ✅ Coach W2 第一块：前端教练 Tab + 执行回写 UI（2026-09-15，未提交）
- **背景**：W1 的 `audit.write_back_execution` 是**死代码**、执行一致性 KPI（教练核心度量）恒为空——后端 API 齐全（`/api/coach/alerts|consistency|abandon-reasons|alerts/{id}/execute`）但**前端零入口**（P1-17）。
- **实现**：
  - `frontend/src/views/CoachView.vue`（新建）：① **执行一致性 KPI 卡片**（执行率/已决策/未响应/放弃率/已推送 + **UI 明示"分母=已决策、未响应单列"口径**，对齐评审④"先定标尺"）② **建议历史列表**（severity 三档 `alert/warn/info` 配色、规则原文、T+5 结果回填、标的下钻）③ **执行回写**（✓已执行 / ✗放弃**必须填理由**，前端禁用+后端 400 双校验，理由输入内联展开）④ **放弃理由清单**（高频=最易失守纪律点）。顶部标注「**规则引擎生成，LLM 不参与决策**」（评审①红线）。
  - `api/index.js`：`getCoachAlerts/executeCoachAlert/getCoachConsistency/getCoachAbandonReasons`。
  - `router/index.js`：`/coach` → CoachView。`App.vue`：**交易**组加「交易教练」入口。
- **验证**：`npm run build` ✓（built in 9.92s，CoachView 9.91 kB 已打包）；lint 干净。注意：构建命令用 **`npm run build`**（`npx vite build` 会被本工具误判为 watch 命令吞掉输出）。
- **W2 剩余**：LLM 翻译层 `explainer.py`（只渲染规则结果、数字代码注入）、模拟盘完整接入。

## ✅ Coach W2 第二块：LLM 翻译层 `explainer.py`（2026-09-15，未提交）
- **分工**：`rules.py` = 判定 + 数字（事实）；`explainer.py` = 只负责**措辞**。
- **★ 结构性保证（评审①「事前无权」的真正落地）**：LLM 输出**禁止出现任何数字**（连复述规则数字都禁）——输出含数字（正则 `\d+(?:\.\d+)?`）即**丢弃回退规则原文**。数字只能由代码注入 → "LLM 篡改数字/与规则冲突"结构上不可能（而非事后检测丢弃）。
- **挂点**：`monitor.coach_tick` 推送前，对每条新警报补一行解说（`explain_batch`，≤3 条/次防刷屏）→ 推送 = 规则原文（含数字） + 解说（无数字）。
- **fail-open 降级（5 条路径）**：开关 `COACH_EXPLAINER=off`（默认 on）/ **情绪熔断闭嘴**（当日 `emotion_fuse` 已落库 → 不解释，防亏损时被安慰话术劝住止损）/ LLM 不可用（`llm_blocked_reason`）/ 输出含数字 / 任何异常 → 一律回退规则原文，绝不挡住纪律提醒。
- **Prompt 硬约束**：不许数字、不许买卖建议/预测、≤50 字一句话、只解释"为什么该照做"（不做安慰式解读）。
- **验证** 6 分支全过：关开关→''、干净输出→采用、**含数字→丢弃**、LLM 不可用→''、熔断→闭嘴、batch 正常；`py_compile` + coach 单测 29/29 + `import app.main` OK；lint 干净。
- 改动文件：`backend/app/coach/explainer.py`（新增）、`backend/app/coach/monitor.py`。**未提交**。**W2 仅剩**：模拟盘完整接入。

## 🚀 改造：`sync_local.py` 下载慢 → 并发分片 + 断点续传（2026-09-15）
- **用户报**：`python scripts/sync_local.py --force` 下载 `backend-pack.db.gz` 很慢。
- **实测真相（不是慢，是卡死）**：旧进程 PID 6412 跑了 **8 分 22 秒、CPU 仅 0.55s、`backend-pack.db.gz.tmp` = 0 字节**（12 分钟没收到任何数据，`requests.get` 卡在等响应）；**同时用户浏览器里还有一个 `.crdownload` 在下同一个包**（两个下载抢带宽）。本地 `backend-pack.db` 是 09-13 旧包。
- **★ 实测数据驱动（关键）**：GitHub Pages（Fastly）对**单连接**是「首段突发 + 随后强限速」——6s 窗口单线程 1.19 MB/s（突发段），**15s 窗口掉到 0.10 MB/s**（60s 才 1.5MB）；而 **4 并发分片 15s 达 0.82 MB/s（8.2x）**，因为新连接各拿一份突发带宽。
- **排除的"更快源"**：**jsDelivr 对 44.9MB 文件直接 403**（单文件限 20MB）；**cdn.statically.io 连接超时**不可达 → GitHub Pages 仍是唯一可用源。（顺带：今天包 44.9MB，不是本地旧 gz 的 31.7MB。）
- **改动 `scripts/sync_local.py`**：改为**并发分片下载**（默认 6 线程，`--threads` 可调 / 环境变量 `PACK_DOWNLOAD_THREADS`）——每片独立 HTTP Range（gh-pages `Accept-Ranges: bytes` 已验证）**天然支持断点续传**（分片落盘 `*.part{i}`，中断/Ctrl-C 后重跑从断点续，不再从头下）；每片 5 次退避重试；**进度条**（每 5s 打印 MB/速率/剩余分钟）；完成后按序拼接 + 校验总长；**解压改流式**（`shutil.copyfileobj`，与 `pack_source` 口径一致，原 `f_in.read()` 会把 180MB 一次性读进内存）。
- **实测效果**：44.9MB / **85.9s → 0.52 MB/s 成功**（对比旧脚本卡死 12 分钟 0 字节）；新包已就位 `backend/data/pack/backend-pack.db` 181.6MB，**数据日期 2026-09-14**（152 万行 K 线）✅。
- **未动 `pack_source._download_and_unpack`**（Render/Actions 生产路径，其网络快、改动风险不值当）。
- 改动文件：`scripts/sync_local.py`（重写）。**未提交**。

## 🔧 诊断：`/api/coach/alerts?limit=50` 本地超时（2026-09-15）—— 实为**整个后端卡死**
- **用户报**：coach 建议列表接口本地请求超时。**实测复核**：`/api/health`（纯静态）**也超时** → **不是 coach 接口的问题**。
- **证据链**：
  - `Get-NetTCPConnection`：8000 有服务（PID 1276 `python run.py`）
  - 进程树：1276(reloader, 31MB, CPU 0) + 10304(app 子进程, 109MB, **8 分钟仅耗 0.98s CPU**) + 6412(`sync_local.py --force`, **比 app 晚 8 分钟启动，非诱因**)
  - **决定性**：app 进程有一条 `Established 52.77.146.31:6543`（**Supabase Supavisor 连接池端口**）的连接，**挂了 5.5 分钟无响应** → lifespan 永不完成 → 所有请求排队超时。
- **根因**：`database.py:_get_pg_conn` 只有 `connect_timeout=10`（建连超时），**无 statement_timeout / lock_timeout** → 连上后 SQL 一旦在服务端等锁就**永久 hang**。为什么本地会等锁：**本地开发后端与 Render 生产共用同一个 Supabase**，启动期 `db.init_tables()` + 各模块 `ensure_table` 连发大量 DDL（取表级锁），与生产写入争锁。
- **修复（两处，均已验证）**：
  1. **`database.py:execute`** DDL 分支加 `SET LOCAL lock_timeout = '15s'`（**事务内**——实测 Supabase Supavisor 事务池**忽略连接级 `options`**：传 `options='-c lock_timeout=20000'` 后 `SHOW lock_timeout` 仍是 0；而事务内 `SET LOCAL` 生效返回 15s）。等锁超 15s 即报错 → `execute` 自带重试一次兜底。
  2. **`coach/audit.ensure_tables`** 先探 `to_regclass('coach_alerts'/'coach_plans'/'ux_coach_alerts_dedupe')`（廉价 catalog 查询、**零 DDL**），全存在即返回 → 冷启 **3.5s → 0.64s**（`CREATE TABLE` 即使表已存在也会触发 PostgREST schema cache 全量重载，见 `database.py:137` 注释）。
- **⚠️ 我的误判与修正（教训）**：中途曾推断"`uvicorn --reload` + `sync_local` 写 183MB 触发 reload 风暴"，并改了 `run.py`。**读 uvicorn 0.30 源码后推翻**：`FileFilter.default_includes = ["*.py"]` —— **只监视 `.py`**，`.db/.gz` 写入根本不触发 reload。**已撤销 `run.py` 改动**。（教训：断言前先读实现，别凭直觉下结论。）
- **⏳ 待用户操作**：杀掉卡住的进程（PID 1276/10304）并重启 `python run.py`（新代码才生效）。
- 改动文件：`backend/app/database.py`、`backend/app/coach/audit.py`。**未提交**。

## 📌 概念澄清：A 股配色语义（2026-09-15，用户问「净值回撤 1.19% 绿色=亏损吗」）
- 项目配色（`frontend/tailwind.config.js`）：**`rise: '#ef4444'`（红=涨）｜ `fall: '#22c55e'`（绿=跌）**——A 股习惯，**红涨绿跌**（与美股相反）。
- `PaperTrading.vue:41` 净值回撤：`drawdown > 0 ? 'text-fall' : 'text-muted'` → **回撤为正（有回撤）显示绿色 = 对**，因为回撤是负面指标、"跌"语义，绿色正确，**没弄反**。
- 注：同页 `🔒 回撤熔断中` 用 `text-rise`（红）——是「红色=警示/危险」的通用 UI 语义（与涨跌色无关），两者并不矛盾。

## ✅ Coach W2 第三块：模拟盘完整接入 · B 后端闭环（2026-09-15，未提交）
- **范围确认**：用户选择 **A+B+C 全做**，优先级 **B（后端闭环，度量才有意义）**。
- **实现**：
  1. **`backend/app/coach/audit.py`**：
     - `coach_plans` 表新增 `status DEFAULT 'open'` / `actual_exit_date` / `actual_exit_reason` / `followed` / `closed_at`
     - `_ensure_columns()` 幂等列迁移（探测 `information_schema.columns`，缺则 `ALTER TABLE ADD COLUMN`）——兼容老库
     - `close_plan(position_id, code, fill_date, actual_exit_date, actual_exit_reason)`：平仓时关闭对应剧本；`followed=1` 当且仅当 `exit_reason in ('stop_loss', 'expire')`（与 `write_plan` 的 exit_conditions 对齐）
     - `plan_execution_rate(days)`：预承诺执行率 = 按剧本离场的平仓数 / 已平仓剧本数
  2. **`backend/app/strategies/paper_trading.py`**：`_close_position` 两处平仓分支（正常结算 + fill/exit_price 异常）均调用 `close_plan`，失败不影响平仓。
  3. **`backend/app/routers/coach.py`**：新增 `GET /api/coach/plans/execution-rate`。
  4. **前端 `CoachView.vue`**：新增"预承诺执行率" KPI 卡片行（执行率 / 已平仓 / 持仓中 / 按剧本离场）+ 口径说明；`api/index.js` 加 `getCoachPlanExecutionRate`。
- **模型选择（用户附图）**：CodeBuddy IDE 内置模型列表——推荐 **Kimi-K2.7-Code（0.57x）** 或 **Deepseek-V4-Pro（0.51x）** 用于继续开发；两者都是代码/复杂任务档。Flash 档（0.03x/0.06x）能力不足，Kimi-K3 1.62x 偏贵。
- **验证**：
  - `py_compile` 三个后端文件 + `import app.main` OK（154 routes）
  - 路由注册确认：`/api/coach/plans/execution-rate` ✅
  - 前端 `npm run build` ✅（built in 10.37s，CoachView 11.97 kB）
  - 真实 DB 冒烟：`ensure_tables()` 自动补 `status` 列；`close_plan` 测试命中 → `status='closed', followed=1, follow_rate_pct=100.0`；数据已还原
- **W2 模拟盘完整接入剩余**：A（PaperTrading.vue 持仓行展示教练剧本/建议 + 可回写执行）+ C（模拟盘绩效归因：按战法/入场理由分组胜率）。
- 改动文件：`backend/app/coach/audit.py`、`backend/app/strategies/paper_trading.py`、`backend/app/routers/coach.py`、`frontend/src/views/CoachView.vue`、`frontend/src/api/index.js`。**未提交**。

## ✅ Coach W2 第四块：模拟盘完整接入 · A 前端持仓展示 + 放弃回写（2026-09-15，未提交）
- **实现**：
  1. **`backend/app/coach/audit.py`**：
     - `coach_plans` 再加 `abandon_reason TEXT`
     - `get_plans(position_ids)`：按 position_id 列表批量查询剧本
     - `abandon_plan(plan_id, reason)`：open 剧本可主动放弃（status='abandoned' / actual_exit_reason='abandon' / followed=0）
     - `plan_execution_rate` 口径更新：分母从"已平仓"改为"已结算"（closed + abandoned），并返回 `plans_abandoned` / `plans_settled`
  2. **`backend/app/routers/coach.py`**：新增 `GET /api/coach/plans?position_ids=` 与 `POST /api/coach/plans/{id}/abandon`。
  3. **前端 `frontend/src/api/index.js`**：加 `getCoachPlans` / `abandonCoachPlan`。
  4. **新建 `frontend/src/components/CoachPlanDetail.vue`**：展示剧本状态、止损/复核日/移动止盈触发/开仓日、退出条件；open 状态提供"放弃剧本"（必填理由）；closed/abandoned 展示结果。
  5. **`frontend/src/views/PaperTrading.vue`**：待确认 / 持仓中 / 已平仓三表均加"教练"列，点击"剧本"展开 `CoachPlanDetail`；加载仓位后批量拉取对应剧本。
  6. **`frontend/src/views/CoachView.vue`**："已平仓剧本"改为"已结算剧本"，子标签显示已平仓 / 放弃数量；口径说明加入"主动放弃"为未严格按剧本。
- **验证**：
  - `py_compile` 后端 + `import app.main` OK（157 routes）
  - 路由注册确认：`/api/coach/plans`、`/api/coach/plans/{plan_id}/abandon` ✅
  - 前端 `npm run build` ✅（built in 11.06s，PaperTrading 18.23 kB）
  - 真实 DB 冒烟：`abandon_plan` 测试命中 → `status='abandoned', followed=0, abandon_reason=...`；数据已还原
- **W2 模拟盘完整接入剩余**：C（模拟盘绩效归因：按战法/入场理由分组胜率）。
- 改动文件：`backend/app/coach/audit.py`、`backend/app/routers/coach.py`、`frontend/src/api/index.js`、`frontend/src/components/CoachPlanDetail.vue`、`frontend/src/views/PaperTrading.vue`、`frontend/src/views/CoachView.vue`。**未提交**。

## ✅ Coach W2 第五块（收尾）：模拟盘完整接入 · C 绩效归因（2026-09-15，未提交）
- **实现**：
  1. **`backend/app/strategies/paper_trading.py`**：新增 `paper_attribution()` —— 已平仓多维切片（复用 `_strategy_stat` 口径），5 个维度：`by_strategy`（战法，转中文）/ `by_exit_reason`（退出原因，`_EXIT_REASON_ZH` 转中文）/ `by_hold_bucket`（持有天数分桶 ≤2/3-5/6-10/11-20/>20）/ `by_confidence`（从 `confirmation_json.confidence_level` 提取，高/中/低）/ `by_plan_followed`（join `coach_plans.followed` → 按剧本/未按剧本/无剧本）。每组按成交数降序。
  2. **`backend/app/routers/paper.py`**：新增 `GET /api/paper/attribution`。
  3. **前端 `frontend/src/api/index.js`**：加 `getPaperAttribution`。
  4. **`frontend/src/views/PaperTrading.vue`**：新增「归因」tab（tab 计数显示 `closed_total`）；5 张维度表（分组/成交/胜率/均盈亏/盈亏比/累计/均持仓），`by_plan_followed` 用 accent 高亮框标注为"教练闭环核心"。
- **真实数据冒烟**（33 笔已平仓）：按战法 单阳不破 18/均线粘合突破 13/均线回踩 2；按退出原因 仅"超时未触发 17 + 超期 16"（退出 v2 下无止损/止盈触发）；按置信度 全"中置信"；按剧本执行 全"无剧本"（这 33 笔均 09-13 教练剧本上线前平仓，属正常历史数据）。关联链路 `write_plan(p["id"])` 与 `coach_plans.position_id` 已确认对齐。
- **验证**：`py_compile` 后端 + `import app.main` OK；前端 `npm run build` ✅（PaperTrading 20.69 kB）；lint 0 错误。
- **W2 模拟盘完整接入已全部完成（A+B+C）**：A 前端持仓展示+放弃回写 / B 后端平仓闭环+执行率 / C 绩效归因。
- 改动文件：`backend/app/strategies/paper_trading.py`、`backend/app/routers/paper.py`、`frontend/src/api/index.js`、`frontend/src/views/PaperTrading.vue`。**未提交**。

## ✅ 完整校验（2026-09-15）
- `py_compile` 6 个后端文件（audit/rules/monitor/explainer/routers.coach/routers.paper/paper_trading）全过。
- `import app.main` OK，**157 routes**（本次新增 `/coach/plans`、`/coach/plans/{id}/abandon`、`/paper/attribution` 均注册）。
- 单测：`test_coach_rules.py` **29/29 通过**（注意：本机无 pytest，测试是 `python <file>.py` 直跑 + `ASSERTION PASSED` 自检风格）；`test_position_sizing.py` **16/16 通过**。
- 前端 `npm run build` ✅（built in ~10.9s；chunk>500kB 仅为既有体积警告）。
- 真实 DB 冒烟（前两轮已做）：`abandon_plan` → `status='abandoned', followed=0`；`paper_attribution` → 33 笔 5 维切片正确。

## ✅ 提交与文档（2026-09-15）
- **已 push 到 origin/main**（`1013be4..59ea6ec`），3 个 commit：
  - `e4ffc1e feat(coach): 模拟盘剧本生命周期闭环与绩效归因`（后端 4 文件）
  - `d6ef520 feat(coach): 模拟盘持仓剧本展示 + 放弃回写 + 归因 Tab`（前端 4 文件）
  - `59ea6ec chore: 更新工作记忆（Coach W2 模拟盘接入完成 + MEMORY 精简）`
- **新增功能文档** `交易员教练_模拟盘接入说明_20260915.md`（根目录，13 节）：定位/四条红线/模块结构/数据模型/运行流程/9 条规则表/两个 KPI 口径/绩效归因 5 维/API 清单/前端入口/配置开关/已知限制/校验基线。
- 提交风格遵循仓库既有 `type(scope): 中文描述`（body 用多个 `-m` 分段）。

## ✅ 教练规则健康度周报脚本（2026-09-15）
- **决策**：用户问「要不要细化教练规则」，结论是**不要**——09-13 才上线，9 条规则 7 条仍只落库不推送，处于零样本期，此时调阈值=拍脑袋（同类教训：白名单用胜率≥55% 算出空集）。真正该做的是**先攒样本、盯证据**。
- **产出** `scripts/coach_rule_health.py`（原为空占位文件，已填充）：只读 `coach_alerts`/`coach_plans`/`paper_positions` 三张小表，零行情回源；`--days N`（默认 30）/`--json`。
- **盯三个信号**（用户认可）：① 规则触发率异常（零触发=阈值太松/上游数据问题；天天触发=阈值太紧→警报疲劳）② 放弃理由高频项 ③ 守纪律 vs 不守纪律（`by_plan_followed` 差值 >0 → 止损位/到期天数可能定错）；另附执行一致性 + 预承诺执行率两个 KPI。
- **样本不足保护**：`MIN_ACTIVE_DAYS=5`，低于则零触发降级为「未判定」（修了一处误报）、结论明示「勿据此调参」。
- **首跑实测（09-15，仅 09-14 一个交易日有记录 → 样本不足）**：触发 stop_loss_hit 3 / hold_3d_review 7 / loss_over_7pct 5 / gate_no_add 1；**执行一致性 已决策 10 / 执行率 0% / 放弃率 100%**，放弃理由集中「模拟盘自动任务，暂不操作」（9 次）+「仓位较轻，暂不操作」（6 次）。已确认 `executed` 唯一写入点是 `write_back_execution`（仅 `POST /coach/alerts/{id}/execute` 手动触发，无自动化）→ 属**真实用户行为信号**：用户把教练警报误当「模拟盘自动任务」全部放弃，W3 复盘需重点看。
