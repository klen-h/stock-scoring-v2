# 2026-09-20（周日）

## ✅ 已办（#1）：主力闸门批量还原加读库重试（`app/mainforce/gate.py`）
- **动机**：`strategy_whitelist_review.py` 两次复现
  `[recommendation] 门控状态读取失败（降级放行）: SSL connection has been closed unexpectedly`
  ⇒ 主力闸门在回放里失效 ⇒ **整轮白名单重算在"无主力闸门"口径下完成**（数字偏悲观）。
- **改动**（lint 0，**未提交**）：新增 `_retry_read(fn, what, tries=4)`（重试 +
  `db._reset_pg_conn()` + 1.5s 递增退避，与 `scripts/*.py` 的 `q()` 同款），
  包裹 `gate_states_for_signals` 的三处取数：`load_flow_map` / `_float_map` /
  `_load_prices_map`。
- **前置确认（避免修错）**：`flow.load_flow_map` 失败**不缓存空值**（缓存只在自己成功之后写）
  ⇒ 调用方重试**有效** ✓
- **验证**：重跑回放 ⇒ **未再出现 SSL 失败**，且**数字口径整体切换**（见下表）⇒ 闸门真正生效。
  ⚠️ 诚实说明：本次 `mainflow_history` 命中**本机磁盘缓存**（`[mainflow] 整表读命中本机缓存（749 只）`）
  ⇒ 没走到会断连的那条路径，所以**重试代码本身未被触发**；它的价值在"下次真断连时兜住"。

## ⚠️⚠️ 顺带发现（比 #1 更重要）：**白名单为空 ⇒ 战法推送静默**
```
当前判据 = expectancy（均收益 >0.3% 且 盈亏比 ≥1.2，样本 ≥30）→ 白名单 []
期望判据（同上）→ 白名单 []
[警告] 当前白名单为空 → 战法信号不会推送企微（功能静默）
+ [recommendation] 动态白名单（落库缓存）: []      （日批日志里也能看到）
```
- **★ 关键区分：这不是配置问题，是"真的没有达标战法"** ——
  记忆里旧结论是「win_rate 判据 ⇒ 天花板 50% < 55% ⇒ 恒空 ⇒ 切 expectancy 可恢复」，
  **现在已切 expectancy 且仍然空** ⇒ **不要再靠调判据来"恢复推送"**（那等于把不合格信号推出去）。
- **闸门生效前后的口径对比（同一份 66 天样本）**：
  | 战法 | 无闸门（降级放行） | **有闸门（本次）** |
  |---|---|---|
  | dragon_turnaround | n=175 46.3% **+0.38%** PF1.28 | n=157 45.2% **+0.10%** PF1.07 |
  | single_yang_unbroken | n=510 41.2% -0.81% PF0.63 | n=391 **45.0%** **-0.43%** PF0.78 |
  | ma_pullback | n=75 29.3% -1.41% | n=45 28.9% -1.22% |
  | ma_convergence_breakout | n=406 32.0% -1.21% | n=324 29.9% -1.24% |
  ⇒ **闸门对 `single_yang` 提纯明显**（胜率 +3.8pt、亏损减半）✓
  ⇒ **对 `dragon_turnaround` 反而轻微负作用**（+0.38%→+0.10%）⚠️
  ⇒ **闸门不该一刀切**，值得按战法评估（新待办）。
- **半衰期告警（衰退结论稳固）**：`ma_pullback` 后半 13.0% vs 前半 45.5%；
  `ma_convergence_breakout` 后半 14.2% vs 前半 45.7%
  （另有 `single_yang` 在有闸门口径下改善到 45.0%，衰退程度需重估）。
- **脚本小 bug（待修）**：`strategy_whitelist_review.py` 的提示文案「要恢复推送可设
  WHITELIST_CRITERION=expectancy」**已过时** —— 现在判据就是 expectancy，仍为空。

## ✅ 已办（#2）：战法衰退拐点定位 —— **拐点 = 2026-09-07/08，两战法独立一致**
- **新增 `scripts/strategy_decay_diagnosis.py`**（未提交）：与 `recommendation._recompute_whitelist`
  **同管线**（读 strategy_results → 主力闸门 → 退出 v2 → `engine.match_signals` 撮合），
  但保留**逐笔明细**（生产函数只返回汇总）⇒ 可按扫描日聚合 + 叠加 `market_regime_history`。
  零 egress（`DATA_SOURCE=local`）✓ 支持 `--strategy/--window/--min-n`。
- **滚动窗口（5 个扫描日累计）胜率 —— 两个战法同一天崩**：
  | 窗口止于 | ma_convergence_breakout | ma_pullback |
  |---|---|---|
  | 09-02 | 53.2%（+0.44%） | 40.0%（-0.28%） |
  | 09-03 | **50.0%**（+0.47%）← 最后一次 ≥50% | 43.8% |
  | 09-04 | — | **57.1%**（+1.48%）← 最后一次 ≥50% |
  | 09-06 | 42.2% | 47.1% |
  | **09-07** | **33.3%**（-0.49%） | **41.2%**（-0.33%） |
  | **09-08** | **27.0%**（-1.13%） | **23.5%**（-2.53%） |
  | 09-10 | 19.4% | 5.6% |
  | 09-11 | 16.0% | 0.0% |
  | 09-14 | **14.6%**（-2.61%） | **0.0%**（-4.24%） |
  ⇒ **拐点 = 09-07/08，之后一路向下、从未恢复** ⇒ 两个独立战法、独立信号在同一时点崩
  ⇒ **是市场环境变了，不是战法本身退化** ✓
- **★ 但市况表解释不了全部恶化（关键发现）**：
  · 拐点前市况 = `neutral` 为主（7~8 天）；拐点后 = `neutral_bearish` 4 + `neutral` 2
    ⇒ 方向吻合"震荡→弱震荡" ✓
  · **但 09-08 那天 regime 标注是 `neutral`（不是 bearish），却恰恰是最差的一天之一**
    ⇒ **指数 regime 没捕捉到真实的个股普跌/宽度崩塌** ⚠️⚠️
- **★★ 由此得出的最高杠杆建议（待决策）**：
  **「宽度崩塌钩子」的保护范围要从"趋势类"扩展到震荡类（或全部战法）** ——
  理由：崩坏的正是**震荡类**（ma_convergence / ma_pullback），而
  `contradictions/risk_hook.py` 的 `TREND_STRATEGIES` 只保护趋势类 ⇒
  **震荡类在普跌市里完全裸奔**；而钩子本就是为"普跌日（宽度崩塌）"设计的，
  比 `market_regime`（基于指数 MA/ADX，滞后且看不见个股普跌）**更对症**。
  开关已在（Render 已配 `CONTRADICTION_RISK_HOOK=1`）⇒ 属**改范围、不改机制**，风险低。
- **其他观察**：
  · **9-14 仍在恶化区**（14.6% / 0%）⇒ **不要等 regime 转 `defensive` 才停**（那太晚）。
  · 扫描日里出现 **09-06（周日）** ⇒ 是今天修的 `_scan_day` 之前的旧口径产物（自然日）✓ 非新问题。
  · 诊断脚本 `_recompute_whitelist` 的管线是**复刻**的（为取逐笔明细）⇒ 生产管线若改动需同步。

## 待办更新（战法优化队列）
1. ~~修 `gate_states_for_signals` 的 SSL 重试~~ ✅ 本次完成（待真断连时验证）
2. **定位 3 个衰退战法的拐点**（分前后半段 + 叠加 `market_regime_history`）← 下一步
3. **决策：白名单空要不要处理**（调门槛 / 接受不推 / 靠 2+4 改战法）
4. **加码 `dragon_turnaround`**（唯一接近达标的）：确认周一恢复后在 offensive 下能放行
5. **闸门按战法评估**（对 dragon 负作用 ⇒ 是否豁免某些战法）
6. defensive 的 quality 0.35 权重重定（9-17 结论）
7. 6 个僵尸战法定性（形态太严 vs 准入挡死）
8. 修 `strategy_whitelist_review.py` 的过时提示文案

## 未提交文件（跨 9-19 / 9-20）
`scripts/daily_batch.py`、`backend/app/flash/scheduler.py`、`backend/app/mainforce/gate.py` + memory

---

## 📊 评审：`feat/flow5-extreme-inflow` 分支（2026-09-20，用户要求审查）
- **定位**：`ssv-flow5` 是 `stock-scoring-v2` 的 **worktree**；分支基于 main 加 3 提交：
  `82cdc48`（001368 个案 docs）→ `c92d60c`（P1 标签 + P2 位置维度裁决）→ `1157026`（子指标因子体检）。
  改 8 文件：`engine.py`+9、`scoringEngine.js`+8（P1 标签，前后端阈值 **`>5` 一致**）、
  `flow5_pos_backtest.py`+184、`subfactor_ic_backtest.py`+250（新脚本）、报告 ×3、个案 doc。
- **P2 裁决（不支持）**：price_pos 覆盖率 **100%**（7481，**推翻文件1"覆盖率低"的担忧**，
  001368 的 None 是个例）；主判定 flow5>+10&pp<0.5 去超额 **-0.301%**（标准 >+0.5%）⇒ **维持倒U** ✓
  纪律正确（判定标准预先定死、红线"先回测再改曲线"守住）。
- **★ 最强发现：技术面 8/8 负 IC**（5日去超额）：RSI **-0.218**、MA **-0.192**、涨跌动量 **-0.186**、
  布林 **-0.179**、MACD **-0.140**、成交额 -0.103、量价 -0.083、KDJ -0.056；5 个 |IC|>0.13、
  **强市截面不衰减** ⇒ **A 股 5-10 日窗的截面内反转效应**（非市况错配）；与"周报买入信号 5 日
  胜率 41.5%"的长期困惑**闭环**。倒U后主力净流入 IC +0.004（中性），"极端流入降分"子样本 IC
  **+0.093**（正，佐证砍得对）。换手率 5 日 +0.012≈噪声、10 日 +0.075（弱正）。
- **报告建议清单**：① 技术面降权/重定位（震荡市 27%→20% 或 技术分只做过滤）；② 反转子项模拟盘 A/B；
  ③ 换手率重校准；④ flow5 倒U维持（已做）；⑤ 因子体检周期化（每月例行）。
- **我的评审结论**：**高质量、可合并**。但 3 点：
  1. ★ 综合分两大块（技术面**反向** + 资金面**中性**）在 5-10 日窗都弱 ⇒ **排序逻辑短期预测力存疑**；
     报告克制地落到"技术面降权"，但张力本身该被点名。
  2. **窗口矛盾**：warfare h3 说"超额集中前 3 日"（短窗动量有效）vs IC 说 5-10 日反转
     ⇒ 是"冲高 3 日见顶回落"，不是简单反转 ⇒ 建议 2 别误砍 3 日动量。
  3. **样本期单一（6-8 月震荡回落市，反转被放大）** ⇒ 建议 1（改上线权重）**必须等 9-10 月样本复核**，
     先落地建议 5（周期化）。
- **与 main 侧关联**：战法拐点（9-07/08 崩）+ "宽度崩塌钩子只保护趋势类" + 这里"技术面短窗反转"
  **可能同源**（都是"短窗追高→中窗回落"）⇒ 下次合并一起看。
- **合并前待验证**：P1 前端 `factors_down` 是否真渲染新文案（diff 注释称"未知 key 回退为 key 名"，
  未实测前端渲染）。

---

## ✅ 已办：落地「因子体检周期化」（体检报告 §建议5，2026-09-20）
> 改在 **`ssv-flow5` worktree（`feat/flow5-extreme-inflow` 分支）**，2 个文件，lint 0 / py_compile OK / **未提交**。

**① `scripts/subfactor_ic_backtest.py`**：`main()` → **`run(holds, step, refresh, quiet, out_dir)`**
（`main()` 变薄 CLI 包装，新增 `--quiet`）：
- **落库**（`report_store.save_report`）：md 全文（tag=`subfactor_ic`）+ **机器可读基线**
  `subfactor_ic_latest.json`（tag=`subfactor_ic_meta`，覆盖式）。
- **滚动复核** `_load_prev()` / `_review()`：每轮与上轮对比 → md 加「滚动复核」表
  （各子项 IC 变化 + 复现/符号翻转判定）+ **「负 IC 复现度 N→M」**汇总行。
- **告警判据写死在 `_review()`**（防事后挑格子）：① 上轮负 IC 本轮转正 → 复现度下降；
  ② 上轮 |IC|≥0.04 符号翻转；③ **倒U两个子项 IC 转负 → 「倒U基础动摇」**。
- **数据不足守卫**：日线或资金流 <100 只 / 截面 <3 个 ⇒ 返回 `{skipped, reason}` 且**不写库**
  （避免污染「本月已体检」判据 —— 残缺报告比没有报告更糟）。

**② `scripts/daily_batch.py`**：新增 **`_monthly_due(tag, batch_day)`** + **`task_subfactor_ic()`**
（注册 TASKS + `DEFAULT_ORDER[18]`，排在 `weekly_report` 后、`daily_report` 前）。
- 触发：本轮交易日**所属月份**无体检报告（每月首个交易日例行）／`--tasks subfactor_ic` 点名强制
  ⇒ 与 `task_weekly_report` 的「常规窗口 + 漏跑自愈 + 点名」三判据完全同构。
- **★ 决定性设计**：幂等判据**必须读库**（`report_store.list_reports`）——
  日批跑在 **Actions，工作区每次全新（checkout）** ⇒ 文件级判据恒为"没有报告" ⇒ **会天天跑**。
  这一点 `_weekly_due()` 早就是读库的（`report_store.list_reports`），照抄即可。

**验证（全部真跑）**：
| 项 | 结果 |
|---|---|
| 编译 / lint | `compile OK`、lint **0** |
| 第 1 轮（无基线） | `[db] 落库 OK`、IC 与体检报告**逐项一致**（RSI -0.218…）、**无告警** ✓ |
| 第 2 轮（有基线） | md 出现「滚动复核」表：11 项全 `复现`、**「负 IC 复现度：上轮 8 项 → 本轮 8 项 —— 全部复现」** ✓ |
| 告警三判据（注入假基线单测） | 三条全部触发 ✓（复现度下降 / 符号翻转 / 倒U基础动摇）；空基线 ⇒ 无告警 ✓ |
| 月度幂等 | `2026-09-20/09-25 → False`（本月已体检，跳过）、`2026-10-01 → True`（新月份要跑）、
  `weekly → False`（按 tag 独立判定） ✓ |
- **踩坑（Py3.11）**：`print(f"...按"需要跑"处理...")` —— **f-string 内嵌同类双引号 = 语法错误**
  （PEP 701 仅 3.12+ 支持，Actions 是 3.11）⇒ 改用 `『』`。**写含中文引号的 f-string 要特别当心。**
- **测试产物**：本地跑两轮生成 `backend/backtest_reports/subfactor_ic_20260920_0148.md` /
  `_0149.md`（并已落库）⇒ **9 月的「已体检」标记已因此建立**（9 月剩余交易日会正常跳过，符合预期）。

### 追加：挂企微推送（用户要求）
- **`subfactor_ic_backtest.py` 新增 `summary_markdown(res)`**：推送摘要（告警置顶 →
  负 IC 复现度 → IC 明细 → 报告名）。**故意不用 markdown 表格** —— 企微不支持表格，
  `push_markdown_batched` 会经 `wechat_fmt.markdown_tables_to_lists` 转成列表，不如直接行内拼接。
- **`daily_batch.py` 新增 `_push_ic_summary(res) -> str`**，**分级推送**：
  · **有告警 ⇒ `force=True`**（倒U背书失效/因子转向属**关键通知**，`force` 语义 = 不受业务开关限制）；
  · **无告警 ⇒ 普通推送**（尊重 `WECHAT_BUSINESS_ALERTS`）。
- **★ 关键发现**：`wechat.BUSINESS_ALERTS_ENABLED` = `env.get('WECHAT_BUSINESS_ALERTS','0')=='1'`
  ⇒ **默认关闭** ⇒ 普通推送会被**静默拦掉**（`push_markdown_batched` 返回 None，无法判断是否真发）。
  ⇒ 所以**返回值明确写出状态**（"未推送（业务推送开关已关…）"/"已推送企微（force：关键告警）"），
  并在任务汇总里带出 —— **杜绝"以为挂了推送、其实静默没发"**（同 env_check 的教训）。
- **验证（monkeypatch 不打真接口，四分支）**：无 webhook ⇒ `未推送（未配置…）`；
  开关关 ⇒ `未推送（业务推送开关已关…）`；开关开 ⇒ `已推送企微`（force=False）；
  **有告警 + 开关关 ⇒ `已推送企微（force：关键告警）`（force=True 确实穿透）** ✓
  摘要格式核对无误（无表格）✓ compile OK / lint 0。

### ✅ 收尾：提交 + 合并 + 推送（用户授权「不加 WECHAT_BUSINESS_ALERTS，提交合并到 main」）
- **`f21fd36`** feat(体检)：因子体检周期化（5 files / 653+）—— 提交在 `feat/flow5-extreme-inflow`；
- **`2f293d3`** docs(memory)：本次评审与周期化的记忆归档（提交在 main）；
- **`4ea36fc`** `Merge branch 'feat/flow5-extreme-inflow'`（`ort` 策略，**无冲突**；12 files / 1434+）；
- **已 push**：`8ef37bc..4ea36fc main -> main` ⇒ **Render 自动部署** ✓
- **合并内容**：P1 标签（`engine.py`+`scoringEngine.js`，阈值均 `>5`）、倒U位置维度裁决报告、
  `flow5_pos_backtest.py`、`subfactor_ic_backtest.py`（周期化版）、`daily_batch.py`（月度任务+推送）、
  5 份回测报告、2 份分析文档（001368 个案改 + 评分系统体检 20260920）。
- **上线影响**：001368 类个股的 `factors_down` 由「主力净流入」变为「主力极端流入(散户陷阱降分)」；
  因子体检任务进入日批 `DEFAULT_ORDER[18]`，**10 月首个交易日首次自动跑**（届时回答
  「技术面负 IC 在新样本上是否复现」）。
- **遗留**：`feat/flow5-extreme-inflow` 的**远程分支落后一个提交**（缺 `f21fd36`，因只推了 main）
  ⇒ 分支若还要用需补 push，否则可删（内容已全部在 main）。

---

## ⚠️ 已办：宽度崩塌钩子「范围扩展」评估 → **结论：不可评估（钩子从未生效）**
> 用户「按你意思来」，我推荐 ★★（钩子范围扩到震荡类的回测对照）+ 两处 1 分钟清理。

**新脚本 `scripts/breadth_hook_review.py`**（正式产物，未提交）：
- 复用 `strategy_decay_diagnosis.load_trades()`（**同生产逐笔管线**，不另起一套），只做分组；
- 重建钩子历史：扫描日 d 生效 ⟺ `contradictions` 在 **[d-7, d)** 有 severe 宽度类矛盾
  （与 `risk_hook.breadth_collapse_active` **同判据、同 `_MAX_AGE_DAYS=7`、同 「s < d」**）；
- 四格分组（趋势/震荡 × 钩子生不生效）+ **判定标准预先写死**（差 ≥1.0pt 且 A 组 n≥30
  ⇒ 有价值；<1.0pt ⇒ 证据不足；A 优于 B ⇒ 有害）——防事后挑格子。

**★★ 实测结论（重要）**：
| 项 | 值 |
|---|---|
| 逐笔 / 扫描日 | 918 笔 / **21 个（2026-08-20 ~ 2026-09-14）** |
| severe 宽度崩塌日 | **仅 1 天：2026-09-15** |
| **钩子生效的扫描日** | **0 天**（A 组为空 ⇒ 评估不可行） |
- **★ 根因 = 时间错配**：钩子只在 severe 日**之后**的扫描日生效，而唯一的 severe 日（9-15）
  正是 `strategy_results` **断档的第一天**（9-15~9-18 缺，即 `9a0809f` 修的那个断档）
  ⇒ **钩子自 9-13 上线以来从未真正触发过**（≠ 配置错误，判据本身正常）。
- 对照数据（仅作参考，因 A 组空无法判定）：震荡类 B 组 n=760 胜率 37.6% 均 **-0.82%** PF0.59；
  趋势类 B 组 n=158 45.6% **+0.11%** PF1.08 ⇒ 震荡类整体确实差（但**未**证明是宽度崩塌日造成的）。
- **处置：不改生产**（无证据不改 = 纪律）。脚本已内置"生效日为 0"的显著诊断块（含扫描日范围对比），
  可**周期化重跑**：断档已修 ⇒ 下次再出 severe 就能积累 A 组样本。

**顺手清理（两件）**：
- ✅ 修 `strategy_whitelist_review.py` 的**过时提示**：原文案「要恢复推送可设
  WHITELIST_CRITERION=expectancy」**已过时且方向误导**（当前判据**就是** expectancy 仍为空）
  ⇒ 改为「调判据不能恢复推送」+ 指向 `strategy_decay_diagnosis.py` 的真因 + 三个处置选项。
- ⚠️ `backend-pack.yml` 的三个业务开关（`WHITELIST_CRITERION` / `CONTRADICTION_RISK_HOOK` /
  `MAINFORCE_MODE`）**查证后发现早已补齐**（L90-95，非密钥写死）⇒ **又一条过时待办**。

**★ 队列校准（两条过时待办被剔除，避免下次再推荐）**：
1. ~~`signal_persistence.py` 跨日口径根治~~ ⇒ **9-19 已修完**（4 处全用 `rules.latest_completed_trading_day()`，
   连`_calc_consecutive_days` 的自然日断链 bug 也改成按交易日历回溯）；表数据实证：**9-07 起无周末脏行**
   （残留只到 9-06，回溯窗 30 交易日 ⇒ 约 10 月中自然失效，无需清理）。
2. ~~`backend-pack.yml` 补三个开关~~ ⇒ 见上，早已有。
⇒ **教训：给出"下一步"前必须先读代码/查库核验，不能凭记忆的待办清单**
（今天两次都因此推荐了已完成的事）。

---

## 🔍 已完成：队列整体核实（数据 + 代码，2026-09-20）
> 用户「做核实」。逐条查库/读码，**判决如下**（这是目前的权威队列状态）。

| 队列项 | 判决 | 依据 |
|---|---|---|
| ① 白名单为空 | ✅ **真待办（决策项）** | 判据=expectancy 仍空；非配置问题（文案已修） |
| ② 6 僵尸战法定性 | ✅ **真待办，且已定性** | 见下「趋势类准入挡死」+ 形态太严 |
| ③ defensive 权重重定 | ❌ **不可做** | `market_regime_history`：defensive **仅 4 天**（9-15~9-18，**正是扫描断档期**）⇒ 有效样本≈0，等积累 |
| ④ dragon_turnaround 加码 | ⏸ **等周一** | 扫描日止于 **9-14**（9-15~9-18 断档），无新数据 |
| ⑤ render.yaml checklist | ✅ **真待办（清单已产出，见下）** | 代码读 **78** 个 env 键，render.yaml 仅声明 **11** 个 |
| ⑥ 9-18 验证清单 | ✅ **已核** | `ranking_history` 9-18 正常(50)；`backtest_prices` 749 只/54.7 万行/到 **9-18** ✓；**`shadow_rank_daily` 缺 9-18** ⚠️ 见新发现1 |

### ★ 新发现 1（真 bug 候选）：`shadow_rank_daily` 日期口径错位
- 数据：`shadow_rank_daily` 有 **9-16 / 9-17 / 9-19**（各 3 variant × 50），**独缺 9-18**；
  而 **9-19 是周六（非交易日）** ⚠️。
- 对照：`ranking_history` 的 rank_date = **9-14/15/16/17/18 全是交易日**、**没有 9-19**
  ⇒ 它的 `_is_trading_date()` 守卫**生效且正确**（docstring 明确"混入周末会把连续上榜天数虚增"）。
- ⇒ **判定：`task_shadow_rank` 写 `rank_date` 用了自然日**（凌晨/跨午夜跑 ⇒ 落成周六），
  与 `ranking_history`（交易日口径）**不一致** ⇒ 两周后的 `compare_shadow_rank.py` 会**对不齐交易日**。
- ⇒ **修法同 `signal_persistence`**：改用 `rules.latest_completed_trading_day()`。
  （**待确认**：需先读 `task_shadow_rank` / `live_ranking` 的 rank_date 来源再动手。）

### ★ 新发现 2：趋势类战法自 8-28 起被**准入完全挡死**（不是形态太严）
- 数据（近 40 天信号留存）：
  | 战法 | 信号 | 最后有信号 |
  |---|---|---|
  | single_yang_unbroken / ma_convergence_breakout / ma_pullback | 511 / 407 / 76 | 9-14 |
  | morning_star | **0**（18 日） | 9-14 |
  | old_duck_head | 3 | 9-14 |
  | dragon_turnaround / advance2retreat1 / limit_up_boomerang / wizard_pointer / double_cannon | 175 / **0** / 4 / **0** / **0** | **全停在 8-28** |
- 根因（`strategies/market_regime.py` 的 `ADMISSION_MATRIX` + `_get_recommended_strategies`）：
  **base 类型准入 = 进攻市→趋势类、震荡市→震荡类、防御市→全禁**；而
  `market_regime_history` 自 8-20 起 **offensive = 0 天**（neutral 13 / neutral_bearish 5 / defensive 4）
  ⇒ **趋势类在非进攻市连扫描都被挡**（`STRATEGY_WIDE_SCAN=on` 只放宽了"阴跌闸门/高波白名单"，
  **base 类型仍完整判定**）⇒ 5 个趋势类战法集体 0 信号。
- **★ 连带结论**：`risk_hook.TREND_STRATEGIES`（钩子保护范围）保护的正是这 5 个
  ⇒ **钩子保护的是一个"已不产信号"的集合**；而真正在产的震荡类**不在保护内**
  ⇒ 强化了「扩展钩子范围」的动机（但仍**无样本可验证**，见前一条）。

### ⑤ render.yaml 核查结论（清单）
- **代码读取 78 个 env 键，render.yaml 只声明 11 个**（TZ / LLM_* ×6 / FLASH_COOKIE /
  WECHAT_WEBHOOK / JWT_SECRET）⇒ 缺口 67，但**多数有默认值**（安全）。
- **无默认值 ⇒ 必须显式配**（高风险，含 Render 与 Actions）：
  `DATABASE_URL`、`WHITELIST_CRITERION`、`MAINFORCE_MODE`、`STRATEGY_MAINFORCE_GATE`、
  `TRADE_GATE`、`WARFARE_EXIT_POLICY`、`LLM_FREE_API_KEY`/`_BASE_URL`/`_MODEL`、
  `ZZSHARE_TOKEN`、`GITHUB_TOKEN`、`DAILY_BATCH_WORKFLOW`、`DAILY_REPORT_PUSH`、`PRIMARY_USER_ID`
- **默认值 = 关闭/空 ⇒ 不配就静默失效**：`CONTRADICTION_RISK_HOOK="0"`、
  `WECHAT_BUSINESS_ALERTS="0"`、`RENDER_READ_ONLY="0"`、`LLM_FREE_SHADOW=""`、
  `DATA_SOURCE="db"`（Render 未配 ⇒ 回源 Supabase ⇒ egress）
- ⇒ 印证：**render.yaml 只是"不完整的清单文档"，不是事实来源**；控制台实际值仍须人工核对。

---

## ✅ 已办：修复 `shadow_rank_daily` 日期口径（真 bug，用户指派）
**★ 根因不在 shadow 脚本，在 `live_ranking`**（差点改错文件）：
```
backend/app/scoring/live_ranking.py
    def _today_bj() -> str: return datetime.now(_BJ)...   ← 北京自然日
    def compute_full_ranking(...): return _today_bj(), ...  ← ranking_live.rank_date 就是它
      ↓
scripts/shadow_decay_ranking.py: data, rank_date = load_live()
        （rank_date ← ranking_live 的 MAX(rank_date)，**照搬**，不是自己算的）
      ↓
    shadow_rank_daily.rank_date = 同一个错日期
```
⇒ 9-18 深夜启动的日批跑到 `rank_live` 时已跨到 **9-19（周六）** ⇒ 两表都写成周六、
**9-18 整日缺失**。（`_today()` 只用于算公告龄 `age`，自然日**是对的**，不要动。）

**改动（`live_ranking.py`，2 处，lint 0 / compile OK）**：
- **新增 `_rank_day()`** = `rules.latest_completed_trading_day()`（15:00 分界 + 跳周末/节假日），
  与 `signal_persistence._scan_day()` / `base.save_scan_result` / `paper_trading` 同源；
  docstring 里**显式记录语义**：盘后=当天、跨午夜/周末=上一交易日、**盘中=上一交易日**
  （本任务读 pack 指标 + 收盘行情，盘中本就无"今日已完成"数据）⇒ 将来要做"盘中实时榜并标当日"
  应加开关，**不要**改回自然日。
- `compute_full_ranking` 的返回值 `_today_bj()` → **`_rank_day()`**；`_today_bj()` **保留**（他处仍用）。

**★ 关键判断：`routers/scoring.py:1218` 的 `_today_bj()` 不能改**
  —— 那里是「库里的榜是否**今天**的（新鲜）」判据，**盘中必须不匹配**才会走在线两阶段用实时行情；
  若改成 `_rank_day()`，盘中（15:00 前）会与库里的上一交易日榜**匹配** ⇒ **误用昨日静态榜** ⚠️。
  （复核过：改后"盘后当天查询命中/盘中不命中"两个关键场景行为不变。）

**数据修复（3 条 DML，均经用户批准）**：
1. **铁证**：`shadow_rank_daily` 9-19 那批 `snapshot_price` **0/150 有价**（因为
   `backtest_prices` 9-19 = **0 行**，周六无行情）⇒ 日期错位确凿。
2. `UPDATE ranking_live SET rank_date='2026-09-18' WHERE rank_date='2026-09-19'` → **1000 行**；
3. 同上修 `shadow_rank_daily` → **150 行**（两表 9-18 原本为空 ⇒ 无 UNIQUE 冲突，改标即补齐）；
4. **补 `snapshot_price`**（272 行）：`compare_shadow_rank.py` 的 `fwd_return` 里
   `if not p1 or not snapshot_price: return None` ⇒ **光改日期不够，没买入价照样被跳过** ⇒
   按 `(code, rank_date)` 从 `backtest_prices` 回填（**与脚本自身取价同源**）。
5. 修复后：**9-18 由 0 → 132/150 有价**（顺带把 9-17 从 51→133、9-16 从 127→140 也补上）；
   两表**非交易日残留 = 0** ✓
- ⚠️ 仍未满 150 的原因：部分股票不在 `backtest_prices`（回填覆盖不足），与本次无关。

---

## 🌙 今日收尾（2026-09-20 深夜，用户确认「没有必须开发的」）
- **全部已提交并推送**：`ab72673`（榜单日期修复）在远端，`main...origin/main` 无 ahead，
  两个 worktree 工作区均干净 ✓
- **今日完成**（按时间）：
  1. **A2**：两个回测脚本接 `data.anomaly_index()`/`window_has_anomaly()`（实测暴露≈0，既有结论不受污染）
  2. **`env_check` LLM provider 链自检**（启动第一屏打印链 + 归因告警；`ENV_CHECK_SKIP_LLM` 抑噪）
  3. **评审 `feat/flow5-extreme-inflow`** 并**合并上线**：P1 标签（前后端 `>5`）、倒U位置维度裁决、
     因子体检（技术面 8/8 负 IC）
  4. **因子体检周期化**（`run()` + 落库 + 滚动复核 + 三告警判据 + 月度日批任务 + 企微分级推送）
  5. **钩子范围扩展评估** ⇒ 结论**不可评估**（钩子自上线从未触发：severe 日 9-15 撞上扫描断档）
     新增 `scripts/breadth_hook_review.py`（判定标准预先写死，可周期化重跑）
  6. **队列整体核实** ⇒ 剔除 3 条过时待办，查出 2 个新问题
  7. **修 `shadow_rank_daily` 日期口径**（根因在 `live_ranking._today_bj`）+ 数据修复（1000+150 改标、272 补价）
- **下轮开工入口（都不是"必须"，按条件触发）**：
  | 入口 | 触发条件 |
  |---|---|
  | 白名单空 / 趋势类准入挡死 | **等用户决策**（两者同源：offensive=0） |
  | `dragon_turnaround` 加码确认 | 周一有新交易日数据后 |
  | defensive 权重重定 | `market_regime_history` 攒够 defensive **有效**样本（现仅 4 天且在断档期） |
  | 钩子范围扩展裁决 | 下次 severe 宽度崩塌 + 扫描正常（有 A 组样本） |
  | 因子体检首次自动跑 | **10 月首个交易日**（届时看「技术面负 IC 是否复现」+ 有告警会自动推企微） |
  | `shadow_rank_daily` 灰度对比 | 攒够 ~10 个快照日（现 9-16/17/18 三天可用） |
  | `mainforce_factor_backtest.py` 完整跑 | 有空（>10 分钟，接线已验、静态可证 0 命中） |
  | render.yaml 控制台核对 / egress 曲线 | **只有用户能做** |
- **⚠️ 观察项（明天可看）**：日批耗时（新增任务是否压到 180min 上限内）、
  `rank_live`/`shadow_rank` 是否写出**周一**（验证本次修复在真实跨午夜场景下生效）。

---

## ✅ 已办：外部冲击「能否前瞻」——从提问到**有数据支撑的结论**（2026-09-20 深夜）
> 起因：用户问"美联储加息前后美股/A股走势 + 系统能否察觉 + 历史转折点共性"。

### ① 事实（2026-09-16 美联储**三年来首次加息** 25bp，3.75%→4.00%，通胀反弹 CPI 3.4%）
- A 股 9 月「先扬后抑 + 缩量 + 切防御」；系统 `market_regime` 9-15~9-18 判 `defensive` ✓
- **美债 10Y 已到 ~5.0%、30Y ~5.32%、美元指数 ~100.2**（加息后）

### ② 系统现状核实（**推翻我此前两次判断**）
- 系统**早就有**外部传感器：`app/macro.py` 面板含 美债 2Y/10Y/30Y、**美元指数 DXY**、纳指、日经、
  恒科、布伦特/金/铜、A50、汇率（**新浪** `hq.sinajs.cn`，规则引擎 v3 + 分组权重）
- 已接入 **4 处**：`regime._external_panic()`（三选二：日经<-2% / 油胀金跌 / **美债10Y单日>+5bp**）、
  `coach`（10Y>5% 且 dxy>100 ⇒ 减仓；10Y>5% ⇒ 仓位×0.8）、日报§1.1 外围段、
  `mainforce/confluence` 环境传导链（宏观方向分 ≥+30 ⇒ +2）
- **有意不进个股评分**（`macro.py:20`"避免顺周期、保留可解释性"）
- `macro_daily` 落库**已在跑**（2026-09-19 设为**只读模式唯一例外**，Render 每交易日 08:55-13:00 锁一份；
  **明确不塞进日批**——日批 19:00 是"盘后锁定"，时点语义会变）⇒ 库中仅 11 行是**刚开始积累**，非断档。

### ③ 真缺口 = **历史序列**（这才是 Phase 1 该做的）
- 数据源实测（**重要**）：
  | 源 | 结果 |
  |---|---|
  | **东财 push2his** | ✅ 可用：`171.US10Y`/`171.US30Y`/`100.UDI` 均有日线；⚠️ **对高频请求会 `RemoteDisconnected`**（实测连续请求被断，`lmt=100000` 亦被断）⇒ **必须限速+重试** |
  | 新浪 | ❌ 历史接口**已废**（`NewForexService.getDayKLine` Fatal error / `Service not found`）；实时通 |
  | FRED | ❌ 网络不可达（Read timeout） |
- **新增 `scripts/macro_series_backfill.py`**：拉东财日线 → 落 `backend/data/macro_series.json`
  （本地缓存，零 Supabase，与 research_cache 同模式）。限速 1.5s + 4 次递增退避重试。
  实测：`171.US30Y` **867 根**（2023-05~2026-09-18）、`100.UDI` **1000 根**（2022-11~2026-09-18）✓；
  `171.US10Y` 因限流失败（**待补**，重跑 `--secids 171.US10Y` 即可）。

### ④ ★★★ 核心结论（`scripts/macro_lead_backtest.py`，判定标准预先写死 |IC|≥0.10 且 n≥100）
**领先性 IC = corr(宏观变量当日变化, A股未来 N 日收益)**（负值 = 该变量上行预示 A 股下跌）：

| 变量 | 1日 | 3日 | 5日 | 10日 | 判定 |
|---|---|---|---|---|---|
| **美元指数 UDI** | -0.097 | -0.099 | **-0.1005** | -0.081 | **有效领先（5日）** ✓ |
| 美债 30Y | +0.004 | -0.066 | -0.045 | -0.040 | 弱领先（3日） |

**事件窗口（2026-09-16 加息前后）**：
```
日期        沪深300   涨跌%    US30Y   UDI
09-09      4572.6     -       5.29   98.79
09-10      4548.4   -0.53     5.38   99.09
09-11      4510.2   -0.84     5.36   99.09
09-14      4480.1   -0.67     5.35   99.47
09-15      4450.0   -0.67     5.37   99.63
09-16      4480.3   +0.68     5.37  100.33 ★加息日
09-17      4460.2   -0.45     5.29  100.23
09-18      4507.4   +1.06     5.32  100.22
```
⇒ **A 股在加息落地前就跌完了（9-09~9-15 累计 -2.7%）**，同期**美元 98.79→99.63、加息当日破 100**
  ⇒ **教科书的"美元先动、A 股后跌"**；加息落地日反而 **+0.68%**（利空出尽）。
⇒ **系统这次是"看到 A 股在跌"才判 defensive（滞后），而不是"看到美元在涨"提前预警** ——
  **它有前瞻的原料（macro 面板含 DXY），但没有前瞻的用法**（只做当日 `_external_panic` 脉冲判定）。
⇒ 而 `_external_panic` 本次**没触发**：美债 30Y 加息前后累计仅 ~8bp、单日未过 +5bp 门槛。

**下一步（Phase 2/3）**：把**美元指数**从"当日脉冲"升级为"领先因子/周期级"（5 日窗 IC -0.10 是背书），
带开关、先只进 regime 判定；US10Y 序列补齐后复核。**未提交**：
`scripts/macro_series_backfill.py`、`scripts/macro_lead_backtest.py`、`backend/data/macro_series.json`。

### ⑤ ★★ 修正 + 补全：新浪历史接口**没废**（用户提示后重试，收获极大）
> 我上一条写"新浪历史接口已废"——**错了**。错在**试错了 service 与符号**：
>   `NewForexService.getDayKLine`（美元指数）确实废，但
>   **`GlobalFuturesService.getGlobalFuturesDailyKLine`（外盘期货族）完全可用** ✓✓
>   返回 GBK jsonp `var _d=([{date,open,high,low,close,volume},...])`（**升序**），历史很长：
>   `CL`(原油)1996 起、`CHA50CFD`(A50)2016 起、`NQ/NK/VX` 2018 起。
>   ⚠️ **但美债不在期货族**（`symbol=globalbd_us10yt` → `null`）⇒ 美债只能走东财。
>   ⚠️ `DX`（美元指数期货）**数据是坏的**（仅 13 根、卡在 2019-04/05）⇒ 美元指数用东财 `100.UDI`。
>   ⇒ `macro_series_backfill.py` 已改**双源**（新浪期货族 + 东财债券）。

**可用序列 6 个**（`backend/data/macro_series.json`）：
`100.UDI`(美元指数 1000根/2022-11~)、`171.US30Y`(美债30Y 867根/2023-05~)、
`sina:VX`(VIX 1200根)、`sina:NQ`(纳指 1200根)、`sina:NK`(日经 1200根)、`sina:CHA50CFD`(A50 1200根)
（东财 `171.US10Y` 仍待补——IP 限流未恢复，旧数据保留）

### ⑥ ★★★ 六个外部变量的领先性 IC（判定标准预先写死）
| 变量 | 最优窗口 | \|IC\| | 符号 | 判定 |
|---|---|---|---|---|
| **纳指期货 NQ** | **1日** | **0.1609** | **正（同向）** | **可前瞻** ✓✓ 最强 |
| **VIX** | 1日 | 0.1124 | **正** ⚠️ | 数值达标，但**符号与直觉相反**（见下） |
| **美元指数 UDI** | 5日 | 0.1005 | **负** | **可前瞻** ✓ 符合直觉 |
| 美债 30Y | 3日 | 0.0661 | 负 | 弱参考 |
| A50 | 1日 | 0.0389 | 负 | 无 |
| 日经 NK | 5日 | 0.0338 | 正 | 无 |

- **纳指 1 日 IC=+0.16（正向）** ⇒ **"隔夜美股→A股"风险偏好传导的量化证据**（美股涨 → A 股次日涨）✓✓
- **美元指数 5 日 IC=-0.10（负向）** ⇒ 美元走强 → A 股未来 5 日承压 ✓ 与"美联储加息→美元强→新兴市场受压"吻合
- ⚠️ **VIX 正号存疑**：IC=+0.11 但符号与"恐慌→利空"直觉相反
  （可能是"急跌后反弹"的短期均值回归，或美盘/A股时序对齐问题）⇒ **需复核**，暂不可直接采用
- ⇒ **结论：系统"有前瞻的原料（纳指/美元指数都在 macro 面板里），但没有前瞻的用法"**
  （现在只做当日 `_external_panic` 脉冲判定，未做领先因子）。

**下一步（Phase 2/3）**：优先 **纳指（1日）+ 美元指数（5日）** 做成领先因子/周期级判定，
带开关、先只进 regime；VIX 需先解释正号；US10Y 补齐后复核。

### ⑦ ★★ 东财失败的**真根因是协议，不是限流** + US10Y 补齐
- 我此前判断"东财 IP 限流" ⇒ **错**。实测对照（同参数/同头/同 secid）：
  ```
  https://push2his.eastmoney.com/...  → ConnectionError（RemoteDisconnected）
  http://push2his.eastmoney.com/...   → ✓ 正常返回（美国10年期国债收益率 800 条）
  ```
  ⇒ **本机走 HTTPS 到 push2his 会被断连**；换 `http://` 立刻通。
  **教训：https 失败不要直接判定为"被限流/封 IP"——先试协议与域名。**
  （`macro_series_backfill.py` 已改用 http 并注释根因。）
- 补齐后：`171.US10Y` **871 根**（2023-05-08~2026-09-18，末值 **4.994**）、
  `100.UDI` 扩到 **1200 根**（2022-02 起）、`171.US30Y` 867 根 ⇒ 宏观序列共 **7 个可用**
  （`sina:DX` 仍是坏的 13 根，弃用）。

### ⑧ ★★ 七变量完整领先性（含美债 10Y）——**一个反直觉结论**
| 变量 | 最优窗口 | \|IC\| | 判定 |
|---|---|---|---|
| **纳指 NQ** | 1日 | **0.1609** | 可前瞻 ✓✓ |
| VIX | 1日 | 0.1124 | 可前瞻（**符号存疑**） |
| **美元指数 UDI** | 5日 | **0.1005** | 可前瞻 ✓ |
| 美债 30Y | 3日 | 0.0661 | 弱领先 |
| **美债 10Y** | 3日 | **0.0656** | 弱领先 |
| A50 | 1日 | 0.0389 | 无 |
| 日经 NK | 5日 | 0.0338 | 无 |

- **★ 反直觉**：**本次加息的"直接指标"美债反而是最弱的**（10Y/30Y 都只有 \|IC\|≈0.066，未过 0.10 门槛）
  ⇒ 美债对 A 股更像**同步/滞后**变量，不是领先变量。
- **10Y 与 30Y 的 IC 几乎一致（0.0656 vs 0.0661、同为 3 日窗）** ⇒ 两者互为替代，
  这也解释了"此前缺 US10Y 不影响结论"。
- 事件窗口（含 10Y）：**美债 10Y 从 4.85（9-09）升到 5.02（9-16 加息日）**、突破 5% 心理关口；
  同期 A 股已跌 -2.7%、美元 98.79→100.33 ⇒ 仍是"**美元/美债先动、A 股后跌、落地反弹**"的剧本。
- ⇒ **若要做前瞻，应选「纳指（隔夜）+ 美元指数」而不是美债** —— 这是本次验证最有价值的取舍结论。
