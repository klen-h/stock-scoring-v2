# 2026-09-18

## 🔍 Supabase 流量逐项对账：日志里哪些会耗流量（实测）
- 用户贴出本地 `run.py` 日志，问「这些会耗流量吗？」。用 `pg_stat_statements` 对比昨晚快照作答。

### ✅ 昨晚的修复实测生效
- `mainflow_history` 整表读：calls **70 → 70**、rows **6,173,221 → 6,173,221**（**完全没变**）。
- 这段日志跑了 749 只 `mainflow` + 2045 只 `mainforce_state`，**一次都没回源**；日志也打印了
  `[mainflow] 整表读命中本机缓存（746 只）→ 零 Supabase 流量`。

### 逐项判定
**不耗（外部源 / 本地包 / 写入）：**
- `[backtest] 东财连续失败 → 全部走腾讯源`（外部 HTTP）
- `[llm] 429 限流 → 退避` / `[llm] 所有 provider 均失败`（外部 API）
- `[llm] 用量历史已从文件导入 DB`（本地文件 → 写库，写入不产生 egress）
- `[mainforce_state] 数据源=pack: 2045 只`（K 线读本地数据包）
- `增量 xxx（已有数据至 ...）` + `[OK] xxx: N 条` —— "已有数据至"查单只 `LIMIT` 小查询
  （实测 `SELECT date, close ... WHERE code=$1 AND date>=$2 ... LIMIT` 平均 5 行/次）；数据来自腾讯/新浪；写入不耗 egress
- `[scheduler] 矛盾扫描/报告`（聚合小查询，mainflow 只取近 6 行）
- `[main] 系统绩效预热完成`（1h 缓存）
- `SELECT * FROM pgbouncer.get_auth($1)` 29807 次 —— pgbouncer 认证，返回 1 行 1 列，**不是数据集**，可忽略

**⚠️ 确定耗 —— 新发现（未修，待确认）：**
- **`[float_shares] market_snapshot ... → 3227 只`**：`get_float_shares_from_snapshot()`
  （`app/mainforce/flow.py:291`）读**整行 `stocks_json`（1.1MB 文本 / ~336KB 过网）**。
- 实测：调一次 → calls **+1**；同进程再调 → **+0**（30min **进程内** TTL）⇒ **又是「每进程一次」**，
  与 `load_flow_map` **完全同构的病**。
- `pg_stat_statements`：`SELECT stocks_json, saved_at FROM market_snapshot ORDER BY saved_at DESC LIMIT 1`
  = **70 次**（≈ 同一批 70 个进程）→ **~23.5MB / 6 天 ≈ 4MB/天**。
- 讽刺点：`market_snapshot` 每天只 15:10 更新一次，30min TTL 纯属浪费。
- **修法同构**：`saved_at` 天然就是版本号，可用与 flow-map 同一套跨进程缓存覆盖。**未做**。

**⚠️ 小反常：** `SELECT name FROM pg_timezone_names` 701 次（6 天）—— 量小但可疑（疑似每次建连触发）。

### ★ 但那段日志之外的大盘没变
- `backtest_prices` 批量 IN 读（calls 12/4/11/25/14/64/3）**与昨晚完全相同** ⇒ **42MB/天 是研究脚本的
  历史积累，不是这次日志产生的**。它仍是**最大常态项**（常态基线合计 ≈140MB/天，贴 167MB/天 天花板）。

### ★ 修正自己引入的 bug（`backend/app/mainforce/flow.py`）
- 初版 `_FLOW_DISK_PATH` 只上溯 **2 层** → 落到 `backend/app/data/flow-map.db`（**不在约定数据目录**）。
- **路径约定（见 `research_cache.py:32-34`）**：`_APP_DIR = dirname(__file__)`、
  `_BACKEND_DIR = dirname(_APP_DIR)`、数据落 `backend/data/`。
  `flow.py` 在 `app/mainforce/` 下 ⇒ **必须上溯 3 层**（mainforce → app → backend）。
- 已修；放错的 `backend/app/data/flow-map.db` 与空目录已删。

### 验证（零 egress 往返测试）
- 路径 = `backend\data\flow-map.db` ✓；父目录存在 ✓
- `sync_meta` 版本 = `2026-09-17T14:13:32.129413` ✓
- **同版本读回「往返一致 = True」** ✓；**版本不符 → None（回源重拉）** ✓
- `backend/.gitignore:5: data/` 已覆盖该文件（**不会误提交**）✓
- lint 0；临时脚本 `_tmp_egress_check.py` 已删

### 顺带信号
- `[backtest] 东财连续失败 → 全部走腾讯源` ⇒ **东财又封了本机 IP**，再次印证
  「东财封禁是天级、只能当兜底源」的判断（见 2026-09-17 笔记）。

**改动文件**：`backend/app/mainforce/flow.py`。**未提交**。

---

## 💾 market_snapshot 跨进程缓存（2026-09-18，已修，实测已验证）
- **用户要求**：把 `market_snapshot` 也做成跨进程缓存。
- **★ 病根：两个读取点读同一行、各有一套进程内缓存**
  | 读取点 | SQL | 次数(6天) | 缓存 |
  |---|---|---|---|
  | `flow.get_float_shares_from_snapshot()` | `ORDER BY saved_at DESC LIMIT 1` | 70 | 30min 进程内 |
  | `store.load_market_snapshot()` | `WHERE key='latest'` | 85 | **5min** 进程内 |
  - 都读**整行**（≈1.1MB 文本 / **约 336KB 过网**）⇒ ① **每重启一个新进程必整份读一次**；
    ② **常驻进程里 300s TTL 反复重读**（最坏 ≈288 次/天 ≈ **97MB/天**）。
  - 而快照**每天只在 15:10 落库一次** ⇒ 短 TTL 纯属浪费。
- **★ 修法（`app/flash/store.py` + `app/mainforce/flow.py`）**：统一到 `store.load_market_snapshot()`，
  三层 —— 进程内存（300s）→ **本机 SQLite**（`backend/data/market-snapshot-cache.db`，
  **key = `saved_at`**，12h 兜底 TTL）→ 才回源。
  - **版本探测只取 `saved_at` 一列**（单行几十字节），不做整行读 ⇒ 版本没变就用本机，**零大流量**。
  - **为什么用 `saved_at` 而不是 `sync_meta` 版本号**：写入方可能在**别的机器**（Actions/自建服务器），
    靠数据自身的 `saved_at` 对比**无需写入方配合**即可跨进程失效。（`flow` 那个读取点原来还多走了一次
    `ORDER BY saved_at DESC LIMIT 1`，现在直接 `from app.flash import store` 复用，去掉了重复缓存。）
  - `flow.get_float_shares_from_snapshot()` 保留自己的 `_FS_CACHE` 作最外层内存缓存（行为不变），
    只把"回源那一跳"换成走 store。
- **验证（决定性，两个新进程）**：
  ```
  第一次: 整行读 85→86（回源并落盘 1.15MB） | 版本探测 0→1
  第二次: 整行读 86→86 【+0】              | 版本探测 1→2（几十字节）
  ```
  ⇒ **新进程整行读完全止住** ✓。lint 0。

## ❓ 「凌晨 00:03 会不会触发走线上流量」——逐窗口核对（结论：不会）
- **核对全部 loop 窗口（北京时间分钟）**：K线缓存 `h>=15 and m>=30`；指标缓存 `h>=16`；
  宏观快照/复盘/盘前简报/开盘确认/午间雷达 均 09:10–13:00 段；矛盾扫描/战法扫描 15:35+；
  回测回填 16:10+；资金流 17:00+；主力状态 17:30+；龙虎榜 17:45+；日报 19:30+；
  **最早的 `ZZ_FINANCE_SYNC_WINDOW=(270,1440)` = 04:30-23:59** ⇒ **00:03 不在任何窗口内** ✓
- **唯一常驻**：健康探针（每 300s）—— 只调 `get_macro_panel` / `get_sector_flow` /
  `tracker.get_etf_quotes`，**全是外部源轻量请求，不读大表** ✓
- **0 点跨日的影响**：`schedule_state.done_date` 用**北京日期**（`store._bj_date()`）⇒ 0 点一过所有
  "每日一次"标记变"今天未完成"，**但要等窗口开（最早 04:30）才会执行** ⇒ 0 点瞬间无任务被触发。
  这是设计意图（新的一天该重跑）。
- **★ 关键概念纠正**：**Supabase egress 是账号级的，本地与线上共用同一额度** —— 本地读一次
  就记在同一个账上（这才是"本地能刷爆额度"的原因），**不是"唤醒线上"**。
  本地与线上是**独立进程**，各跑各的 loop；`schedule_state` 是**互斥**机制（谁先跑谁标记，
  另一个跳过）⇒ 本地跑了某些任务，**线上反而会省**。
- **⚠️ 唯一真实路径**：本地回填 `mainflow_history` → `sync_meta.touch("mainflow")` 改版本号
  → **线上 `load_flow_map` 缓存失效 → 线上重拉一次 8.7MB**。这是设计意图（数据变了该重拉）且每天
  1~2 次；但**若本地反复重启反复回填，版本号反复变 → 线上反复重拉**。现在本地已不会反复整表拉，
  此路径已缓和。
- **给用户的建议**：不想让本地进程在 16:10 之后跑重活，**0 点前停掉即可**；或用
  `DEPLOY_READ_ONLY=1`（只读模式，只保留 API/快讯/持仓/模拟盘循环）。

**本轮改动文件**：`backend/app/flash/store.py`、`backend/app/mainforce/flow.py`。**未提交**。

---

## 📉 backtest_prices 批量读（42MB/天）→ 0：三个研究脚本接 research_cache（2026-09-18）
- **用户拍板**："现在开"（= 审查报告 `全项目审查_逻辑冲突与优化建议_20260913.md` **第 24 条 / P2-㉔**）。
- **★ 定位（取出完整 SQL，不靠截断猜）**：`pg_stat_statements` 里 `backtest_prices` 大查询分两类 ——
  - `WHERE code = ANY(ARRAY[...])` → **87,029 行/次 = 120 只 × 725 根** ⇒ 正是 `research_cache` 的
    `CH=120`（它的"库补齐"分支，已受控）。
  - `WHERE code IN ($1..$N)`（N 随信号数变化：43/48/51/61/108…）→ **227,408 行/次 ≈ 300 只 × 733 根**
    ⇒ **无 `date` 过滤 = 读全历史**。
  - 佐证：**带** `start` 的那条（`AND date >= $101`，7,339 行/次）说明带 start 的调用便宜 **30 倍**。
- **★ 源头**：`app/backtest/strategies.py::_load_prices_map(codes, start=None)` 三级 ——
  进程缓存 6h → pack 逐只 → **剩下的 `miss` 用一条不分块的 `code IN (...)` 回源**。
  三个脚本调用时**未传 `start`** ⇒ 回源读全历史：
  `strategy_exit_sweep.py:133`、`accum_pullback_backtest.py:109`、`limitup_filter_compare.py:57`。
  - **注意**：`_run_warfare` 是**传了** `start=min(s["date"])` 的（审查 P1-19 修过）⇒ **生产路径没事，
    问题只在研究脚本**。
  - `pack_source.enabled()` = `DATA_SOURCE in ("pack","local")`（`app/pack_source.py:53`）。
- **★ 修法**：给 `app/research_cache.py` 加 **`ohlc_for(codes, max_age_h)`** —— 与 `ohlc_all()` 语义等价
  但**按需读**（不进全量 154 万行内存）：
  ① 本机 `ohlc` 表按 code 读（**零 egress**）→ ② 缺的走 `_pack_bars`（数据包，零 egress）
  → ③ 仍缺才回源（分块 `code = ANY`）→ **回填本机**（下次即命中）。
  刻意**不改 `ohlc_updated_at`**：回填只是补几行，不该让 `ohlc_all()` 的全量缓存被误判为"刚更新"而延后重建。
- **三个脚本改动**：加 `from app import research_cache` + 调用点换成 `research_cache.ohlc_for(...)`
  （`accum_pullback_backtest` 顺带去掉未再使用的 `_load_prices_map` 导入）。
- **★ 验证（决定性：探针测 PG 表级读取增量）**：
  ```
  第一次: 本机缺 206/300 只 → 数据包优先 → 覆盖 300 只 / 219,890 根
          PG idx_tup_fetch / seq_tup_read 增量 = 0    ← 全部由数据包补齐，零回源
  第二次: 命中本机缓存 300 只（零 egress），增量 = 0
  ```
  ⇒ 修前一次 ≈ 7~8MB（219,890 行，与统计里 227,408 行/次同量级），修后 **0** ✓。lint 0。
- **改动文件**：`backend/app/research_cache.py`（新增 `ohlc_for`）、`scripts/strategy_exit_sweep.py`、
  `scripts/accum_pullback_backtest.py`、`scripts/limitup_filter_compare.py`。**未提交**。
- **同类待办（审查 P2-㉒㉓，本轮未做）**：`flash/store.py` events 整取 300 条含正文（210KB/次，
  需前端联动）；`indicator_cache.py` 逐股回退未全量替换为批量版。

## 🧰 本轮沉淀的方法论（可复用）
- **egress 排障标准动作**：`pg_stat_statements`（按 `rows` 排序）+ **必查 `pg_stat_statements_info.stats_reset`**
  否则会把 N 天累计当成一天；SQL 里 `LIKE '%...%'` 会被 psycopg2 当占位符（`IndexError`）→ 必须走参数传。
- **要看完整 SQL 就用 `SELECT query`，不要用 `LEFT(query, N)`** —— 截断会误判批大小（本次差点把 `$56`
  当成 56 只的批，实际是几百只）。
- **表级 `seq_tup_read` 高 ≠ egress 高**（服务器内部扫的行不产生出站流量）；**egress 只看返回行数 `rows`**。
- **本地/线上共用同一 Supabase 额度**（账号级），"本地跑"不是免费；但本地进程**不会唤醒线上进程**。

---

## 🏭 `_industry_dist()` 跨进程缓存（2026-09-18，已修，实测验证）
- **用户拍板**："按你建议来"（= 做审查报告 P2-㉒㉓ 那两条）。
- **★ 但先用数据推翻了报告那两条**（`pg_stat_statements` 实测 6 天）：
  | 报告条目 | 实测 | 判断 |
  |---|---|---|
  | P2-㉓ `indicator_cache` 逐股回退 | `WHERE code=$1` **101 次 / 27 行** | **可忽略**，不做 |
  | P2-㉒ `/flash/events` 含正文 | `flash_reviews` 7617 次但**只 13 行**（且 `flash_events` 表不存在） | **可忽略**，不做 |
  → 另：`get_cached_technical_batch`（逐股版）**已无任何调用方**（死代码，非 egress 问题）。
- **★ 同一次统计里找到更大的真目标**：
  | 查询 | calls/rows(6天) | 判断 |
  |---|---|---|
  | `SELECT name FROM pg_timezone_names` | 701 / 838,396 | **非本项目** —— `scripts/egress_probe.py:50` 的 `_NOISE` 已列明是 Supabase 平台 exporter 语句 → **跳过**（差点白改） |
  | `stock_industry si JOIN stock_finance sf` 全量 | **49 / 823,690** | **真目标** —— 49 次 ≈ 49 个进程 ⇒ 又是"每进程一次" |
- **★ 源头**：`app/scoring/engine.py::_industry_dist()`（行业内分位数打分的数据源）—— `_IND_DIST` 是**进程内** 24h 缓存 ⇒ 每重启一个新进程就重读 1.68 万行。
- **★ 关键正确性约束（差点做错）**：`dist` 里的 **`pe`/`pb` 两个分布来自「腾讯实时行情缓存」（每天变）**，
  只有 `debt_ratio`/`gross_margin`/行业映射来自低频 DB 数据 ⇒ **不能整体长缓存**。
  做法：**只把 DB 源行落本机**，重建时从本机读 + **照旧叠加实时 PE/PB** ⇒ 省 egress 但**口径完全不变**
  （每个新进程仍拿最新 PE/PB）。
- **★ 实现**：新增 `_ind_dist_fingerprint()`（`pg_stat_user_tables` 两张表的写入计数，系统视图零成本）、
  `_ind_dist_src_load/save()`（本机 `backend/data/industry_dist_src.json`，原子替换，7 天兜底 TTL）。
  `_industry_dist()` 的 DB 那步改为「指纹 → 本机 → 才回源并回填」。
- **★ 验证（决定性，两个新进程）**：
  ```
  第一次: calls 49→50（回源 16,810 行）+ 落本机 1.03MB
  第二次: 【calls 50→50，+0】打印"行业指标源命中本机缓存（16810 行）→ 零 Supabase 流量"
          结果完全一致：33,251 条 / 5,720 只 / debt_ratio 行业桶 52
  ```
  （`pe` 桶为 0 是**原有行为** —— 腾讯实时缓存未预热，非本次改动引入。）lint 0。
- **改动文件**：`backend/app/scoring/engine.py`。**未提交**。

## 🚦 「买入条件就绪 X/3」本地计算缺失（2026-09-18，已查清，待用户拍板）
- **用户提问**：前端本地计算模式下没有 `item.gate`（`ScoreRank.vue:363` 的 `v-if="item.gate"`，本地走 `v-else` 显示 `-`）。
- **事实**：**确实没有**，且**不是漏做**，是后端明确写了禁止 ——
  `backend/app/mainforce/trade_gate.py:195`：
  > ★ 口径唯一事实源：就绪度只由本函数产出，**前端不得自行推导**（项目已有"前后端两套评分实现
  > 导致口径漂移"的前车之鉴，本地 68.8 / 后端 72.6）。
- **★ 但本地数据其实全具备**（三条件逐项核对）：
  | 条件 | 后端判据（`trade_gate.evaluate`） | 本地数据 |
  |---|---|---|
  | A 主力根据 | `mf.signal == "accum"`（`cond_a = accum`） | ✅ `item.mainforce.signal` |
  | B 不追高 | `chip.price_pos > HIGH_POS_THRESHOLD(0.75)` 或 `chip.winner_ratio > WINNER_RATIO_CROWDED(0.7)` | ✅ `item.mainforce.chip` |
  | C 状态允许 | `regime in (offensive/neutral/neutral_bearish)` | ✅ **`useFrontendScoring.js:354` 已 `setRegimeState(wdata.regime_state)`**（`scoringEngine.js:99` 的 `_regimeState`） |
  - 后端 `_today_contradictions()` **只用于 reasons 文案**，不影响 `cond_a` ⇒ 本地不需要它。
  - `summarize()` 产出 `{ready, total, items:[{key,ok,label}], label, hint, position_pct, position_label, regime, active}`。
- **★ 若要补齐，消除漂移的干净做法**：把 2 个常量由**后端下发**（`/api/score/weights` 响应里加
  `gate_thresholds`）—— 该接口前端本地模式**已经在调**（拿 weights + `regime_state`），
  注释里正是"前端写死静态值会导致前后端分数漂移"的既有模式 ⇒ **完全同构**。
  判据只剩 3 个布尔 + 文案，漂移面极小。
- **权衡**：本地模式的定位本就是"前端独立算"（`scoreStock` 整个评分都在前端算，比 gate 复杂得多），
  所以"禁止推导 gate"在本地模式下与"本地≈线上"的目标矛盾；但该约束是**项目明确的设计决定**，
  改动需用户拍板。**未改动任何代码。**
- **顺带**：本地模式下这一列显示 `-`（`v-else`）容易被误解成"数据缺失"而非"本地模式不支持"，
  可改成带 tooltip 的说明文案。

### ✅ 已落地（A 方案，用户拍板，2026-09-18，25 组对拍全一致）
- **改动**：
  - `backend/app/mainforce/trade_gate.py`：① `cond_c` 里硬编码的状态元组提成常量 **`REGIME_ALLOWED`**；
    ② 新增 **`rules()`** 下发判据参数（`enabled / high_pos_threshold / winner_ratio_crowded /
    regime_allowed / regime_position / position_default / position_steps / ready_items`）。
  - `backend/app/routers/scoring.py`：`GET /api/score/weights` 增加 **`gate`** 字段 = `rules()`
    （取不到给 `None` ⇒ 前端自动隐藏该列，fail-safe）。
  - `frontend/src/utils/scoringEngine.js`：新增 `setGateRules()` + **`buildGate(result)`**
    （镜像 `summarize` + `evaluate` 的仓位档位规则）。
  - `frontend/src/composables/useFrontendScoring.js`：`setGateRules(wdata.gate)` 与 `setRegimeState` 并排；
    `attachMainforce()` 里给**每个** result 挂 `r.gate`（**与主力标签无关** —— 后端 `summarize` 在
    `mf={}` 时同样产出，所以不能因 `mfMap` 为空就跳过）。
- **★ 不漂移的机制**：**阈值 / 允许状态 / 仓位档位 / 条目名全部由后端下发**（走 `/api/score/weights`，
  与 `weights` / `regime_state` **同一套同步机制** —— 该接口存在的理由正是"前端写死静态值导致分数漂移"）。
  前端只做「3 个布尔 + 文案」组合，**无自由裁量**。
- **★ 决定性验证：前后端 25 组逐字段对拍全部一致**：
  - 用例 = 5 种 regime（offensive / neutral / neutral_bearish / defensive / 空）× 5 种 mainforce 组合
    （accum×正常 / accum×高位 / accum×高获利盘 / distribution / 无 mainforce）⇒ 覆盖全部分支。
  - 对比字段：`ready / total / label / hint / position_pct / position_label / regime / items` ⇒ **全部一致** ✓
  - **对拍手法（可复用）**：`frontend/package.json` 有 `"type": "module"` 且 `scoringEngine.js` **零 import**
    ⇒ 可直接用 `node` 以 ESM import 前端模块做端到端对拍（**前端纯逻辑函数都能这么验**）。
  - 后端侧隔离：monkeypatch `tg._regime`（喂用例值）与 `tg._today_contradictions`（→0；它只进文案）。
- **效果**：本地计算模式下该列不再恒为 `-`；`-` 现在只在"接口未下发/无数据"时出现，UI 歧义顺带消解。
- **lint 0**。**改动文件**：`backend/app/mainforce/trade_gate.py`、`backend/app/routers/scoring.py`、
  `frontend/src/utils/scoringEngine.js`、`frontend/src/composables/useFrontendScoring.js`。**未提交**。

## 📌 提交记录与本轮仓库卫生（2026-09-18）
- **egress 治理那批不是我提交的** —— 用户已提交为
  `bd03f36 perf: 研究脚本与行业分布改走本机缓存降低 Supabase 出站流量`
  （含 `research_cache.py` / `engine.py` / 三个研究脚本）。已确认 `flow.py` 与 `store.py` 的
  mainflow / market_snapshot 跨进程缓存**都在库**（`git grep _FLOW_DISK_PATH` / `_SNAP_DISK_PATH`
  HEAD 各 6 处）。
- **用户要求「分开提交」**，本轮落地两个提交（**均未推送**，`main` 领先 `origin/main` 2 个）：
  | commit | 内容 |
  |---|---|
  | `d9968b4` | `feat(score): 买入条件就绪（gate）支持前端本地计算`（4 文件 + memory，+188/−4） |
  | `d5bd3c2` | `chore: 移除误提交的临时探针并加 gitignore 规则`（−38 行） |
- **★ 发现并修掉的仓库卫生问题**：`bd03f36` 里混进了两个临时探针
  （`scripts/_tmp_sql_probe.py`、`scripts/_tmp_two_items.py`）→ `git rm` 删除，并给根 `.gitignore`
  增加 `scripts/_tmp_*` 规则防复发。
- **★ 我的一次失误（已修）**：`git --no-pager log ... 2>$null` 这条命令里，PowerShell 把 `2>$null`
  当成了**文件名** → 在仓库根创建了一个名为 `$null` 的未跟踪文件 ✗。已用
  `Remove-Item -LiteralPath '$null'` 删除。
  **教训：本项目 shell 是 PowerShell，`2>$null` 不安全（会被当路径）；要丢弃输出请用 `| Out-Null`。**
- **★ 中文 commit message 的稳妥做法**：把 message 写进 **UTF-8 文件**（放已 gitignore 的
  `backend/data/` 下）+ `git commit -F <file>`，**避开 PowerShell 传参编码风险**（本项目控制台
  中文常显示乱码，直接 `-m '...'` 有把 message 写坏的风险）。提交后删除该文件。

## 🚀 推送完成（2026-09-18）
- `git push origin main` → `bd03f36..d5bd3c2`，`main` 与 `origin/main` 已同步 ✓
- **★ 部署拓扑：`push → main` 只会触发两个 workflow**
  | workflow | 触发 | 作用 |
  |---|---|---|
  | `ci.yml` | `push:[main]` + PR | 后端编译检查 + 前端构建（挡语法/导入/构建错误） |
  | `deploy-preview.yml` | `push:[main]` | 前端部署到 **GitHub Pages** |
  - 其余**都不**由 push 触发：`backend-pack.yml` / `kline-data.yml`（cron-job.org → `workflow_dispatch`）、
    `daily-batch.yml`（`workflow_run`：等后端包完成）、`indicator-refresh.yml`（仅手动）、
    `keep-alive.yml`（`schedule */10`，防 Render 休眠）。
- **★ 部署时序与降级（重要，设计时已考虑）**：前端 Pages 几分钟上线，后端 Render 需另行部署 ⇒
  `/api/score/weights` 的 `gate` 字段**可能落后于前端**。此时前端 `setGateRules(null)` 会**优雅降级**
  （隐藏该列），**不会显示错值** —— 这正是 `d9968b4` 里加的 fail-safe ✓ 后端部署后自动出现。
- **本机查 CI 的手段受限**：`gh` CLI **未安装**，且 `backend/.env` **没有 `GITHUB_TOKEN`**
  （token 只配在 Render / Actions 侧）⇒ **本机无法用 API 查 Actions 运行状态**，只能看网页。

## 📄 产出：`EGRESS.md`（根目录，2026-09-18）
- 用户同意把这两天散在日记里的 egress 结论整理成**正式参考手册**。落在根目录 **`EGRESS.md`** ——
  与已有的 `DEPLOY.md` / `CONTEXT.md` / `logs.md` **同族**（无日期的专题手册，不是 `PLAN_*`）。
- 八节：① 怎么量（排障标准动作 + 三个实测踩过的坑）② 病根模式（进程内缓存 × 低频数据，
  四个实例表）③ **跨进程版本门控四手段**（`sync_meta` / `saved_at` / 写入计数指纹 / 兜底 TTL，
  含"探测只取版本列"与"路径要对齐 `backend/data/`"两个实现要点）④ 短 TTL 陷阱
  ⑤ 三个容易误判的对象（`pg_timezone_names` 平台噪声 / `pgbouncer.get_auth` / `seq_tup_read`）
  ⑥ 已治理清单（4 项，含验证方式与第 4 项的**正确性约束**）⑦ 待办/观察项 ⑧ 方法论。
- `MEMORY.md` 同步新增「Supabase egress 治理」一节（长期事实 + 指向 `EGRESS.md`）。
- **未提交**（按项目约定「未明确要求不自动 git commit」）。

## 🚨 重大发现：`backtest_prices` 前复权序列在「增量回填接缝」处不一致（2026-09-18，已定性，未修）
- **起因**：用户说"感觉回测数据可能有错或缺的"，要求排查。产出 `scripts/audit_backtest_data.py`。
- **★ 体检结果**：
  - 总量 **545,599 行 / 749 只 / 2023-08-23 ~ 2026-09-17**；基准 sh000300 = 745 交易日。
  - **数据质量全 0 异常** [OK]（NULL/非正价/high<low/close 越界/volume=0/重复 (code,date) 均为 0）。
  - 覆盖：不足 250 根 7 只、<500 根 11 只、<700 根 17 只；**末日落后基准 360 只** ——
    其中 `2026-09-16` 282 只 = 当天回填未跑完（正常），但 **`2026-08-21` 14 只 / `2026-08-28` 8 只 /
    `2026-09-11` 25 只 = 停更 ≥1 周**，需查（停牌 or 源失败）。
  - 连续性：抽样 150 只（≥500 根）**16 只有交易日缺口**（4~10 天），例 000612 缺 2025-03-03~05（疑似停牌）。
  - **★ 复权异常：|单日涨跌| > 11% 的「纯跳变」（前一根 bar 紧邻、无停牌）共 64+ 处** ⚠️
- **★ 机制定位（决定性）**：`app/backtest/data.py::fetch_history(code, start=...)` ——
  东财 `beg=start`、腾讯 `param="...,{beg},{end},800,qfq"` ⇒ **增量只取 start 之后**；
  而**两源都是「以取数时刻最新价为基准」的前复权** ⇒ **旧数据（旧基准）+ 新数据（新基准）
  在接缝（= start 那天）处价格跳变** ⚠️⚠️
  - **判据自洽**：跳变点**前一根 bar 紧邻** ⇒ 排除"复牌首日无涨跌幅限制"；
    `603508` 一只 **14 处**跳变 —— 3 年最多除权 6 次，**14 处 ≈ 增量回填次数** ⇒ 每次回填一个接缝。
  - 主板（±10%）出现 **+16.0% / −12.2%**（`603508` / `601138` / `605117` 均沪主板）⇒ 不可能是真实行情。
  - 影响面：只在**接缝之间发生过分红/送股**的股票（无分红则 qfq == raw，无跳变）；
    A股多数股票每年分红 ⇒ **影响面大**。**任何跨越接缝的收益率计算都会失真。**
- **修法选项（待用户拍板）**：
  - **A. 全量重拉覆盖**：请求数不变（每只 1 个），写入量变大（750 行 vs 几行）——**写不耗 egress**，
    成本可接受，但每日耗时增加。
  - **B. 存不复权价 + 复权因子**：最正确，但需改所有读侧。
  - **★ C（推荐）重叠日比对触发重拉**：增量本来从 `beg=start` 取，**start 那天与库里重叠** ⇒
    比对重叠日价格，不一致（= 基准变了）才触发**全量重拉**。平时零额外成本，分红季才重拉。
- **⚠️ 修机制之外还需「一次性重建历史」**（现有数据已被污染）。
- **产出**：`scripts/audit_backtest_data.py`（正式体检工具；遵守 egress 纪律 —— 聚合用 SQL、
  序列走 `research_cache.ohlc_for` 本机）。临时验证脚本已删。

## 🌡️ 市场状态（regime）判定与延时（2026-09-18 查明，回答用户提问）
- **判定**（`app/backtest/market_regime.py`，输入沪深300日线）——三层过滤：
  ① MA 趋势方向（`MA20 > MA60×1.005` = up / `< ×0.995` = down / 否则 flat）
  ② ADX(14) 强度（<20 weak / ≥20 moderate / ≥35 strong）
  ③ ATR(14) 分位（≥80% = high）。**`HYSTERESIS_DAYS = 3`**：状态切换最少持续 3 天。
- **defensive 的触发**：`高波动 + MA down`（恐慌）**或** `MA down + ADX≥20`（趋势向下）。
- **退出 defensive ⇒ 允许买入**（`trade_gate.REGIME_ALLOWED`）：
  | 到哪档 | 条件 | 建议仓位 |
  |---|---|---|
  | offensive | MA up + ADX≥20 + 波动非 high | 80% |
  | **neutral（最快解禁）** | **ADX < 20（趋势消失）**；或 MA up 但价格跌破 MA20 超 2%；或 MA down 但价格站上 MA20 超 2% | **50%** |
  | neutral_bearish | neutral + MA down +（宽度恶化 / 恐慌 / 连跌 3 日），进出加 2 日确认 | 20% |
  - 另有闸门：筹码拥挤 ×0.5 / 无主力根据 ×0.5 / 吸筹+不拥挤+允许 → 至少 50%。
- **★ 延时性（用户问）：会有，三重叠加** ——
  ① **指标本身滞后**（MA60 最滞后、ADX(14)、ATR 分位）；
  ② `HYSTERESIS_DAYS = 3` 防抖（**切出也要确认**）；
  ③ nb 进出需 2 日确认；④ regime 用**日线收盘**算 + `_REGIME_CACHE` 当日缓存 ⇒ 盘中不变。
  ⇒ **典型延时 2~5 个交易日**（设计意图，代价是"反弹初期拿不到仓位"）。

## 🔧 backfill 修复 + ⚠️ 一个被自己否证的归因（2026-09-18）
- **用户拍板**：方案 C（重叠日比对触发重拉）+ 一次性重建 + 查停更股票。
- **改动（`backend/backfill_history.py`，4 处）**：
  1. 新增 `_basis_changed(code, start, rows)` —— 比对增量与库中**重叠日**收盘价；
  2. `backfill()` 检出不一致 → **全量重拉覆盖**（`save_prices` 是 ON CONFLICT 幂等写，天然覆盖）；
  3. `backfill_daily()` **并入「已入库的全部代码」**（641 → 749，**新增 108 只**）；
  4. 新增 `rebuild_all_full()` + CLI `--rebuild`（749 只 × 1s ≈ 12 分钟，写 56 万行，**写不耗 egress**）。
- **★ 停更股票的真实根因（第二个独立缺口）**：73 只漏回填中 **70 只不在回填池里** ——
  `_collect_strategy_codes()` 只取「战法信号 + 近 30 天上榜股」，**股票掉出榜单即永久停更**
  （`601012` 停 08-21、`000027` 停 09-11）。**不是停牌**：源上明明有 19 个交易日新数据，
  且增量取数本身正常（`fetch_history(start='2026-08-21')` → 20 条 ✓）。
- **★★ 我的归因被自己否证（必须记住）**：
  - 我曾判定「|单日涨跌| > 11% 的纯跳变（`603508` 单只 **14 处**、主板 **+16%/−12.2%**）
    = 增量拼接的复权接缝」→ **错** ✗
  - **否证证据**：① 对 10 只全量重拉（单请求、单一基准）后跳变数 **48 → 48 完全未变**；
    ② 直接抓腾讯原始 `qfq` 序列（**不经本项目任何代码**）—— `603508` 那几天本身就是
    **−12.18% / +16.00%**，而**同期 `000062` 完全正常（−7.44%）**。
  - **⇒ 那些跳变在源里就存在，是单只、局部的源数据问题**，与拼接/解析/复权计算无关。
  - 已**改写 `_basis_changed` 的 docstring** 记录此更正（不留错误知识）。
- **`_basis_changed` 仍修的是另一件真事**：库里旧数据与当前源对不上 —— 实测 `603036`
  在 2026-08-21 **库 15.79 vs 源 15.71（−0.51%）**；因果是 `qfq` 以"取数时刻最新价"为基准，
  旧入库数据可能用旧基准复权。
- **验证（已做）**：`_basis_changed` 三用例全对（`603036`→True；`601012`/`002452`→False，
  正确 —— `601012` 的 08-21 与源一致，它的问题是"没轮到"，由池并入修）；池并入 641→749 ✓；lint 0。
- **未做**：① `--rebuild` 全量还没跑（12 分钟长任务，待择时）；② **源自身跳变**（外部源问题，
  需换源或加离群过滤，非本轮范围）。
- **产出**：`scripts/audit_backtest_data.py`（正式体检工具）。`EGRESS.md` 已由用户提交为 `2ad97be`。
- **⚠️ 归因教训**：**"数字自洽" ≠ "因果成立"** —— `603508` 的 14 处跳变恰好 ≈ 回填次数，
  这个吻合让我确信了因果；但**一个反证实验（全量重拉）就推翻了它**。
  **下结论前必须设计"能推翻它的实验"，而不是去找支持它的证据。**

## 📊 「源自身跳变」影响面统计（2026-09-18，选项 c 已跑）
- 把检测升级进 `scripts/audit_backtest_data.py` **第 ⑤ 项**（按品种分档 + 排除复牌/次新）：
  - `limit_pct(code)`：主板 ±10% / 创业板·科创板·ETF ±20% / 北交所 ±30%（ETF 一律按 20%，只少报不误报）。
  - 排除三类**合法**无限制情形：① 复牌首日（前一根 bar 不紧邻，中间有停牌期）
    ② 序列**前 5 根**（注册制新股上市初期无涨跌幅限制）③ 按品种放宽上限。
- **★ 全池结果（738 只 / 537,947 根 bar，本机零 egress）**：
  | 指标 | 值 | 判断 |
  |---|---|---|
  | 受影响股票 | **120 只（16.3%）** | 面不小 |
  | 跳变处数 | **334 处** | 绝对数不大 |
  | 占 bar 比例 | **0.0621%** | 极低 |
  | 年份分布 | **2024:250 / 2023:45 / 2025:39**（2024 占 **75%**） | 高度集中 |
  - 最严重：`603508` 14 处（+17.13% / +16.00% / −14.19% …）、`601138`/`603726`/`605117` 各 10 处。
  - 2024 年集中 ⇒ 与"2024 年初小微盘流动性危机"时段吻合，但**主板 ±10% 是硬制度**，
    +17% 不可能 ⇒ 源那段的复权因子不可信（**具体成因未定论**，不影响"不可信"这个结论）。
- **影响面估计**：每只平均 0.45 处；若按"跳变前后各 10 天持仓窗口"粗估，
  受影响 bar ≈ 1.2% ⇒ **对整体统计结论影响小，但会显著扭曲个别个股的收益数字**。
- **建议（待用户拍板）**：
  - **① 标记而非改数据**：把 334 处落成黑名单（本机/落库），回测读侧**排除跨越这些日期的样本**
    或至少**告警**。★ 最实用、最保守。
  - ② 换源交叉校验（东财 `fqt=1` vs 腾讯 `qfq`）—— 成本高（东财已封本机 IP）。
  - ③ 直接改数据 —— **不做**（篡改事实，且成因未定论）。
- 顺带修：⑥ 里 `strategy_results` 无 `code` 列导致查询失败（已按"信号在 results_json"改为不取 codes）。

## 🏷️ 源异常日：标记机制落地（2026-09-18，方案 ① 完成）
- **落库**：新增表 **`price_anomalies(code, date, chg, limit_pct, detected_at)`**（`UNIQUE(code,date)`）；
  由 `python scripts/audit_backtest_data.py --sample 1000 --write-anomalies` 生成 —— **334/334 条已落库**。
  - **为什么落库而不是本机文件**：回测跑在 **GitHub Actions（另一台机器）**，本机文件它读不到。
- **读侧接口**（`app/backtest/data.py`）：`anomalies_in(code, start, end)` / `has_anomaly(code, start, end)`，
  **fail-open**（表不存在 / 查询异常一律返回 `[]`/`False`，绝不中断回测）。
  **不调用则行为与从前完全一致** —— 不擅自改变既有回测结论的口径。
- **★ 交叉统计（⑧ 节，粗粒度）—— 结果出乎意料地好**：
  ```
  异常清单 334 条 / 涉及 142 个交易日（2023-09-11 ~ 2025-10-29）
  战法扫描日 22 个（2026-08-20 ~ 2026-09-14）
  ★ 信号买入窗口内含异常日的扫描日：0/22，涉及信号 0 条
  ```
  ⇒ **异常日全部在 2025-10 之前，而战法信号数据只有 2026-08 之后 ⇒ 零重叠**
  ⇒ **对当前「战法撮合」回测结论无影响** ✓
  ⇒ **但**：`quality_defense_backtest.py` / `mainforce_factor_backtest.py` 用**3 年数据**做因子/长期回测
    ⇒ **那些会命中异常日**，需在那边用 `has_anomaly()` 逐样本判定（建议下次动它们时一并处理）。
- **顺带观察（非本轮议题但值得记）**：`strategy_results` 只有 **128 行 / 22 个扫描日
  （2026-08-20 ~ 09-14）** ⇒ 战法回测的样本期**仅约 1 个月**。
- **本轮最终未做**：`backfill_history.py --rebuild` 全量重建（12 分钟长任务）仍未跑 ——
  它修的是"库≠源"（实测 `603036` 差 0.51%），**与异常日无关**，可随时跑（幂等）。

## ⚠️ 方法论：报告会过期，先量再改
`全项目审查_逻辑冲突与优化建议_20260913.md` 的 P2-㉒㉓ 是按 **9-13 的代码**写的；9-18 实测两者
都已被此前的治理顺手解决（或本来就极小）。**不要拿报告当结论** —— 先用 `pg_stat_statements`
量，再决定动不动手。本次就是"先量 → 推翻两条 → 找到更大的真目标"。
