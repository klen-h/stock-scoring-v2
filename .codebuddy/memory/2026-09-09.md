# 2026-09-09

## 🚀 腾讯云轻量服务器迁移准备（2b22899 已推送）
- 用户已购腾讯云轻量应用服务器（Docker CE 镜像）。**架构天然适合迁**：前端在 GitHub Pages（域名不变）、数据在 Supabase、日批在 GitHub Actions → 服务器只跑 API。
- **已交付文件**：`backend/Dockerfile`（Python 3.9-slim-buster → **3.11-slim**，对齐 Actions）、`backend/.dockerignore`（拦截 .env/data 进镜像）、`deploy/docker-compose.prod.yml`（只起 backend，绑 127.0.0.1:8000，挂 ../backend 持久化 pack，预设 DEPLOY_READ_ONLY=1/ENABLE_HEAVY_JOBS=0/DATA_SOURCE=pack/STOCK_CACHE_INTERVAL=300）、`deploy/nginx.conf`（443 反代 + 两种证书方案 + ACME 挑战）、`backend/.env.example`（完整变量清单）；`RENDER_READ_ONLY` 加通用别名 `DEPLOY_READ_ONLY`（scheduler.py 一处）；补 `.gitignore` 的 `.env` 规则（此前无显式规则，仅靠"从没 add 过"兜底，backend/.env 确认未被跟踪 ✓）。
- **🔴 硬前提：API 必须 HTTPS**。前端 https://klen-h.github.io 调 http://IP:8000 = mixed content 直接拦截（排行榜转圈）。需「域名 + 证书」；国内地域需 ICP 备案才能开 80/443，无备案只能选境外地域（香港/新加坡）。
- **迁移步骤**：① 服务器 clone 仓库 → ② cp backend/.env.example backend/.env 填凭证（从 Render Dashboard 抄）→ ③ docker compose -f deploy/docker-compose.prod.yml up -d --build → ④ nginx 反代 + certbot（防火墙只放行 80/443，不放 8000）→ ⑤ **改 frontend/.env.production 的 VITE_API_BASE_URL 为新域名/api** → ⑥ pnpm build → ⑦ python scripts/publish_packs_local.py --dir data/kline（orphan 发布会把 dist 一起带上）→ ⑧ 验证 /api/health、登录、排行榜 → ⑨ Render 保留观察 1-2 天再停。
- **CORS 无需改**：main.py 白名单已有 klen-h.github.io + `https://[\w-]+\.github\.io` 正则 ✓。
- 更新代码＝`git pull && docker compose restart`（requirements 变了才 --build）。

## ✅ 收尾批次：透支惩罚 + 小样本灰显 + GBK 修复（build ✅，待 push）
- **透支惩罚（前端 applyOverextensionGate）**：price_pos>0.9（贴 60 日区顶）的买入降一档（强烈买入→买入→观望），只降 signal 不动 total_score（分数=强度，信号=可操作性），factors_down 加「高位透支」标签。接入点：attachMainforce（排行榜双路径）+ computeLocalScore（详情页）。**后端排行故意不同步**（raw total 口径 + mult 闸门哲学不同），两边统计互不污染可对照降档效果。
- **BucketStats 小样本灰显**：n<20 的格子改「n=X · 样本不足」灰字（防 <60 桶 n=6 显示 100% 的噪声误导），说明区加规则。
- **publish 脚本 GBK 修复**：stdout/stderr reconfigure(errors="replace")——Windows 控制台打不出 ✅/⚠️ 不再崩（上次 push 完成却死在成功提示上）。
- 依据与决策详见下方胜率分析条目。

## 📊 "观望胜率反超买入"分析：短期反转 + 窗口径径问题，评分体系有效（已修面板口径）
- 用户发现排行榜"观望胜率 > 买入"。BucketStats 分桶数据实锤：**70-80 桶（买入）1日 39.7% → 5日 53.6%（均+1.09%，全表唯一显著正收益）→ 10日 56.5%；60-70 桶（观望）三窗口均收益全负**。
- **结论**：① 评分单调性成立（分越高 5/10 日越好），体系有效；② 倒挂只在 1 日窗口 = 快照时点技术形态最强=已涨过一段，次日反转（70-80 的 1 日均+0.02/中-0.25 右偏分布实锤）；③ 排行榜「各信号等级胜率」面板用混合窗口（快照价→现价，快照越老窗口越长）→ 被短期反转主导，系统性低估买入信号；④ 观望桶=不够格买入的，当前市况下拿了就亏；⑤ <60 桶 n=6 纯噪声。
- **修复**（已做，build ✅）：`get_verified_records(horizon_days=N)` 新增固定 T+N 交易日窗口（交易日历用 ranking_history 去重日期 _trading_days(120)，T+N 收盘批量查 backtest_prices 每目标日 1 查询；太新/停牌自然跳过）；`weight_advice` 自动模式改 horizon_days=5，返回加 `horizon_days` 字段；ScoreRank 面板标题动态标注口径+反转效应说明文案。
- **衍生纪律**：买入信号至少持有 5 日（1 日接力买在反转点）；60-70 观望桶按回避处理；透支惩罚（price_pos>0.9 降档）待做可改善 1 日体感；regime gate 不急（单调性成立说明评分没坏，是市况差——全样本 1 日 42.4%）。

## ✅ 事故全部恢复 + 修复全量入库（00:04-00:10）
- 重跑 publish（agent 环境直接跑成功，未卡 SSH）：`[站点] frontend/dist 并入 38 个文件` + 4 包护栏全过 → **页面 200 / kline-pack 200 2742KB 双恢复** ✅。
- 遇到并顺手修掉：脚本最后 `print("✅...")` 在 GBK 控制台崩 `UnicodeEncodeError`（push 已完成、异常仅在打印行）——之后给脚本加 stdout reconfigure 或弃用 emoji。
- commit 全链理清：786d92b（用户提交，message 叫 docs(memory) 但**内容正确**=deploy-preview dist/data 清理 + 小包删除）+ f3bc467（publish 脚本 dist 并入 + GBK 修复）。工作区干净，origin/main 同步。
- 提醒用户：786d92b 的 message 名不副实，不影响任何功能，不值得 amend 重写远端。
- 本地 publish 的 ⚠️ 细节：agent/GBK 环境跑脚本时 ✅/⚠️ emoji 会崩打印（不影响实际发布）；用户 PowerShell 若报同样错，加 `$env:PYTHONIOENCODING='utf-8'` 即可。
- 🟡 观察：今天 16:00 cron-job.org → kline-data；17:00 → backend-pack；daily-batch 接棒；模拟盘入池（时区修复后 stats 不再全 0）；Supabase 流量（DDL 去重生效）
- 🟡 昨晚 23:31 push 的三个 commit（c4a3b86/c6ef517/8cc8f03）需确认 Render 自动部署成功 + Supabase 不再刷 CREATE TABLE
- 🟢 pack 池扩容（1427→2063 战法池缺口）；增量包改造；Render 内存观察；阿里云 2C2G 决策

## ✅ 两个"落后 2 天"数据源修复（e848d08 已推送）
- **主力行为状态 09-04：真停摆，永不自愈** —— `mainforce_state_refresh_loop` 在 scheduler 的 `_heavy()` 列表里，**RENDER_READ_ONLY=1 时根本不启动**（09-04 后周末+Render 崩溃/只读切换，循环死了）。消费方却都活着：后端排行榜主力标注（scoring.py:1038）、trade_gate、confluence 吸筹+2出货-2、daily_report 交叉扫描。
- **财报扩展 09-06：同样是 Render 循环被关**（zz_finance_sync_loop 周一 04:30，也在 _heavy）+ 状态页按日频判定对周任务误报 stale。
- **修复**：① 两任务迁入 daily_batch.py——`mainforce_state`（refresh_all(None, regime)，regime 缺省 None 与 scheduler 一致；排在 mainflow+market_snapshot 之后：依赖资金流与流通股本快照）+ `zz_finance`（任务内部判 weekday==0 才跑，其余秒过）；DEFAULT_ORDER 现在 12 任务。② system.py `_SOURCES` 加第 4 元素容忍滞后交易日数（默认 2，财报 8），判定逻辑对 max_lag>2 的任务在窗口内判 ok——周任务不再常年黄。
- daily_report 依赖 mainforce_state → 排序保证在其前 ✓；daily-batch.yml 无需改（--tasks all 走 DEFAULT_ORDER）。
- 提醒：mainforce_state 在日批跑（19:00 后），比原 17:30 晚——后端排行榜 19:00 前读到的是昨日数据，可接受（前端本地模式读 pack 的 ind.mainforce，是打包时算的当日口径，不受影响）。

## 🔴 Supabase Egress 超额 402 倒计时 09-11（紧急）+ 腾讯云部署完成
- **用户贴的 Supabase 警告：egress 超额，宽限期至 09-11，之后全部请求 402 → 全系统瘫痪**。必须两天内把流量打下来。
- 今晚日志 21:42-21:58 的 DDL 洪水 = **腾讯云容器反复重启**（每重启 _DDL_DONE 清零→全套 ensure_table 重发），不是修复失效。容器稳定后自停。
- **前端已切腾讯云（试用）**：.env.production → trycloudflare 域名（camel-contributions-anyone-ancient）→ build → orphan 发布 ✓ git 通道验证新 JS 已含新域名、无 onrender。Pages CDN 缓存几分钟过期，用户需 Ctrl+F5。Render 保留兜底 1-2 天。
- **pack 池扩容（egress 止血主招，7ae7119 已推送）**：pack 1442 vs 战法池 2081 的 640 缺口根因 = _pack_quality 多剔了创业板(300/301/302)和亏损股(PE<=0)，而战法池 filter_stock_pool 保留它们 → 战法扫描每天回源 640 只撞 WAF + Supabase 大流量。已对齐口径（只剔 ST/科创板688/<50亿市值）。明天 17:00 backend-pack 打出 ~2000 只新包 → 战法扫描零回源。包体 31→~43MB（可接受）。
- **腾讯云部署完成明细**：SSH 密钥 deploy.pem（已加 .gitignore 防泄露——用户把私钥放在项目根目录，一次 git add -A 就会泄到 GitHub）；Ubuntu 24.04 / 1.7G 内存 + 1.9G swap / 33G 盘；docker 27.5.1 + compose v2.32.4；容器 stock-backend 跑通（只读模式、pack 127MB 本地上传——服务器直连 github.io 仅 20kB/s、ghfast.top 镜像绕过 GitHub 封锁下载 cloudflared、清华 pip 源 45MB/s）；cloudflared 隧道 systemd 化（deploy/cloudflared.service）；**API: https://camel-contributions-anyone-ancient.trycloudflare.com**（快速隧道，cloudflared 重启域名会变！固定域名需用户买域名+接入 CF 后配 named tunnel）。
- **本机 C 盘爆满事故**：publish 的 tempfile 在 C 盘 → No space left on device。C 盘 54.8G 用满（0 free）→ 清理后 0.85G。已用 D:\tmp 作 TEMP 绕过；ghpages 残留是空的不是元凶，C 盘大头待查。
- ⚠️ 引号地狱教训：PowerShell→ssh→bash 三层引号，heredoc/JSON/awk 全部会坏。远端配置文件一律本地生成+scp 上传；pkill -f 的模式会匹配自身命令行（用 [x] 正则 trick 规避）。
- 待办：① 明天 17:00 backend-pack 出 ~2000 只新包 ② 日批战法扫描零回源验证 ③ Supabase 流量观察 ④ 用户买域名后配 named tunnel 固定域名 ⑤ Render 观察 1-2 天后停用 ⑥ 本机 C 盘清理。

## ✅ '企微推送了但模拟盘待确认没有'排查（351ccce 已推送）
- **根因实锤**（容器内查库）：single_yang_unbroken 的 holding=8 正好顶满 MAX_PER_STRATEGY_POSITIONS=8 → auto_ingest_signals 的静默 continue（不记 stats）→ 推送的 8 条全部跳过，stats 全 0 无从排查。待确认里的 ma_convergence(5)/ma_pullback(1) 是其它战法攒样本入池的（入池设计=所有准入战法攒样本，推送=仅白名单——两者本来就不同步，by design）。
- 时区修复其实已生效（今日 strategy_results scan_date=2026-09-09 北京日 ✓）；昨晚 ingested=0 的主因大概率也是持仓满（而非时区——22:51 运行时 UTC/北京同日，bug 未触发）。
- **修复**：静默分支加 skipped_at_limit 计数 + 明确日志（'已达上限 N 笔，今日新信号全部跳过——需等止损/止盈退出释放额度或手动平仓'）。
- **用户侧解法**：模拟盘页手动平仓/取消部分单阳不破持仓释放额度，新信号明天即可入池；或等 holding 自然止损退出。
- 数据快照：single_yang cancelled=7/closed=10/holding=8（09-07、09-08 各 4 笔）；ma_convergence pending=5 holding=5（未满，正常入池）。

## ✅ 'AI 解读未生成（未配置 LLM_API_KEY）'排查（dba1ad1 已推送）
- 用户说 Actions secrets 已配好 LLM_API_KEY/LLM_MODEL，但日报仍显示未配置。**根因：daily-batch.yml 的 Run daily batch 步骤 env 只传了 DATABASE_URL/WECHAT_WEBHOOK，从未引用 LLM secrets** → secrets 配了但进程拿不到。已补 LLM_API_KEY/LLM_MODEL/LLM_BASE_URL 三行。
- 验证方式：Actions → daily-batch → dispatch tasks=daily_report,contradiction_report force=true → 生成含 AI 解读的日报；明天正式日批自动生效。
- 教训：GitHub secrets 配了不等于生效——**workflow 步骤的 env 必须显式引用**。检查清单里要加"新 secret 后核对所有 workflow 引用"。

## ✅ 吸筹/出货胜率验证功能（1be90f0 已推送，数据 09-09 起积累）
- 用户问'验证吸筹/出货胜率值得加吗'→ 值得（mainforce 是唯一未被数据验证的维度；支撑冲突badge/透支降档决策）。已实现轻量版：
- ① ranking_history 加列 mainforce_signal（ALTER IF NOT EXISTS，走 DDL 去重）② record_daily_ranking 写入（兼容 dict/扁平）③ task_score_snapshot 构建带 mainforce_signal ④ get_bucket_stats 加 mf_buckets 分桶（distribution/accum/none × 1/5/10日，_stats 复用）⑤ BucketStats.vue 加'主力行为标签胜率'区块（n<20 灰显同款）。
- **数据积累时间线**：09-09 晚日批首次写入标签 → 2-4 周后 n>20 有统计意义（出货~29/天 吸筹~72/天）。当天看仍是'样本不足'。
- 判读要点：若 distribution 5日胜率显著 < accum 且均收益差为负 → mainforce 标签有效（保留冲突badge+透支降档）；若无差异 → 标签降级为装饰。
