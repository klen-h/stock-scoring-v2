# 2026-09-09

## 🔴 orphan 重建抹掉前端页面（第三事故，已修复待用户重发）
- 用户重发数据包后发现 gh-pages 只剩 data/ + .nojekyll——**orphan 重建只放数据包，把 deploy-preview 发布的前端站点（index.html/assets 根目录）抹掉** → 网页 404。三写者模型中 keep_files 能防"同名覆盖"，防不了 orphan 全量重置。
- **修复**：publish_packs_local.py orphan 重建时并入 frontend/dist 全部内容（**排除 dist/data 子目录**——那是 Vite 从 public/data 拷出的旧小样本残留位置）；dist 不存在时提示可先 pnpm build。这样单次发布=页面+数据完整树。
- 用户操作：直接重跑 publish（本地 dist 是 09-08 晚最新 build）→ 页面+数据一次恢复；再 push 脚本修复。
- 结构定论：gh-pages 树 = dist 根内容（页面）+ data/（数据包）+ .nojekyll；三写者（deploy-preview 根目录 / kline-data+backend-pack data/ / 本地 publish 全量）靠串行锁互斥。

## 待办
- 🔴 用户重跑 publish 恢复页面+数据 → 验证 https://klen-h.github.io/stock-scoring-v2/ 可打开、kline-pack 2742KB
- 🔴 push publish 脚本修复（dist 并入）
- 🟡 观察：今天 16:00 cron-job.org → kline-data；17:00 → backend-pack；daily-batch 接棒；模拟盘入池（时区修复后 stats 不再全 0）；Supabase 流量（DDL 去重生效）
- 🟡 昨晚 23:31 push 的三个 commit（c4a3b86/c6ef517/8cc8f03）需确认 Render 自动部署成功 + Supabase 不再刷 CREATE TABLE
- 🟢 pack 池扩容（1427→2063 战法池缺口）；增量包改造；Render 内存观察；阿里云 2C2G 决策
