# 2026-09-10

## 🔍 456MB 流量 / "k线没更新" 定位（1ddfacf 已推送，四处修复）
- **不是库爆**：`pg_database_size`=169MB；456MB 是 **egress（流量）**。
- **流量元凶**（`extensions.pg_stat_statements`，统计自 08-20 共 21 天）：
  ① `SELECT * FROM backtest_prices WHERE code=$1 ORDER BY date ASC` 18,037 次 / **1112 万行**（≈90MB/天）；
  ② 同表 IN 批量变体合计 ≈8M 行（≈65MB/天）；③ `mainflow_history` 全表读 91 次×7.2 万行（≈52MB/天）；
  ④ `kline_cache` 单只查 303,385 次（≈78MB/天）；⑤ flash_news/pg_timezone_names 若干。合计 ≈450MB/天 ✓ 与 456MB 吻合。
- **根因 = pack 覆盖缺口 659 只**：包只覆盖 1431 只，战法池（market_snapshot 3235 → 总市值≥50亿+成交额≥1000万+非ST）是 **2052 只**。
  包池口径错在：拿「前端行情文件 realtime-quotes.json」（口径=流通市值≥50亿+非ST+非亏损+股价≥3元，只剩 1530 只）∩ kline_cache 当池子 → 上限被锁死。
  缺口 650 只每天在日批/前端回源：kline_cache（停在几天前）→ 判过期 → 实时拉腾讯 → **[WAF] K线请求被拦截** + Supabase 流量。缺口市值分布：50-100亿 419 / 100-300亿 203 / >300亿 37。
- **"k线没更新"两条真相**：① 日批 20:34 首下拿到 **CDN 旧包 09-09**（Pages 部署有 1~2min 空窗 + Fastly max-age=600），而重试循环只置 `_ready_checked`，`_db_fresh()` 看 mtime 判"刚下过=新鲜"→ 30 分钟重试 **一次都没重下** → 昨晚日批全程跑 09-09 旧数据；② **Render 长驻进程挂着 09-09 的包**（`/api/score/kline-cache/status` newest_update=20260909，线上已 09-10）——`_ready_checked` 短路后 `_db_fresh()` 再也不评估 → 不重启就永不复查。
- **修复（1ddfacf）**：pack_source ① `_download_and_unpack(bust_cache=True)` 默认带唯一查询串击穿 CDN；② 新增 `redownload()`，daily_batch 重试改为 **sleep 后强制重下**；③ 新增 `_maybe_refresh()`：就绪快路径上 30 分钟一次轻量自检（看 mtime + meta），过期即重下；④ `_parse_pack_date` 兼容 YYYYMMDD（此前只认 %Y-%m-%d → 陈旧判定**形同虚设**，Render 挂旧包也不会回退）；⑤ `generate_backend_pack.py` 新增 `snapshot_market_pool()`：池子改从 Supabase `market_snapshot` 按战法同口径构建（2052 只），quotes 失败时退化旧口径；backend-pack.yml 超时 150→180 分钟。
- **明天验收**：backend-pack 日志应打印「战法池口径覆盖: 2052/2052（缺口 0）」；日批不再刷「判过期走实时拉取」/WAF；Supabase 流量应显著下降。Render 因 autoDeploy 会自动加载新代码并重下包。

## 🔴 Supabase 402 倒计时明日截止（09-11）——Render 已挂起（09-10 用户操作）
- 腾讯云接住全部：前端已指向 trycloudflare 隧道、API 全绿、Render 挂起省流量。
- 明晚核对 Supabase 流量面板：验证 pack 扩容零回源 + DDL 去重效果。若 402 生效：数据库读写全挂，需临时升 Supabase 计划或紧急降流量（届时再议）。
- 服务器代码同步方式（后端改动后）：ssh git fetch --depth 1 + reset --hard + docker compose restart（GitHub 大传输会断，浅拉取绕过）。

## ✅ Phase 1 决策简报首个成果（b91dcbe 已推送，实测生成成功）
- **trader_brief.py**：collect_brief_data（聚合 regime/矛盾/主线/候选Top10/模拟盘持仓/战法计数，全读已有表）→ _data_to_markdown → call_llm（_SYSTEM_PROMPT 资深交易员人格：三段式「该关注/该做/该防」+ 强制引用数据出处 + 不喊单）→ 落表 trader_briefs(date,phase) 幂等。
- API：GET /api/system/trader-brief（phase 自动按时间选 + refresh 强制重生成 + JWT 认证；system.py 已补 import Query）。
- **实测首份简报质量高**：L3 财报断层 severe 置顶、L2 板块资金背离（引用行业数据）、风格切换（电子信息 3.4→8.0）、长高电气 72.7 分候选；「该做」诚实写观望（neutral+未解决矛盾）；「该防」点名持仓股城投控股。LLM=DeepSeek-R1。
- 前端简报区块（DailyReport 顶部）明天接。调度（盘前 9:10/盘后 19:35 企微推送）Phase 1 收尾。

## 评分调整（d358af4 已推送，已随 6fd07b1/e95c4d2 同步服务器）
- 资金面第 5 因子主力5日净流入：scoreFlow5 曲线（≥+5 满分/-8.9→4.1/≤-10 零分），前后端同曲线；flow5_amt 缺失自动退回 4 因子（权重归一）。
- 消息面强负面（news_score≤-2）买入信号降档；三条路径注入 flow5_amt；后端 score_top 预载 mainforce_state.flow5_amt。
- 000567 实测：资金面 46.8→39.6（主力因子 4.1），total 约 -1.9 分 + 消息降档。
- 待观察：000567 后续走势验证惩罚力度是否合适。

## 服务器运维要点（腾讯云 129.204.194.66，用户 ubuntu，密钥 deploy.pem 已 gitignore）
- 引号地狱规避：PowerShell→ssh→bash 内联 python/JSON/awk 必坏，**一律本地写文件 scp 上传 docker exec**；pkill -f 模式匹配自身（用 [x] trick）。
- 服务器直连 github.io 仅 20kB/s、GitHub releases 本地/服务器均被 reset → 大文件走 ghfast.top 镜像或本地 scp 上传。
- pack 数据流：本地上传 backend-pack.db.gz 到 data/pack/ 解压 → _db_fresh 跳过下载。

## ✅ 决策简报前端上线（git 通道验证：DailyReport-C9WIsK4N.js 含简报区块 ✓）
- DailyReport.vue 顶部接入简报区块：自动加载（onMounted loadBrief）+ phase 中文徽章（盘前/盘中/盘后）+ 重新生成按钮（refresh=true，消耗 1 次 LLM）。API getTraderBrief 在 index chunk（trader-brief 字符串在共享 chunk 属正常）。
- 插入方式：PowerShell 内联替换失败 → 本地写 _insert_brief.py 执行（assert 锚点防错）→ 删脚本。build ✅。
- 注意：orphan 发布与 deploy-preview 并发曾撞 'cannot lock ref'（GitHub 端 ref 锁竞争），sleep 15 重试即成功。
