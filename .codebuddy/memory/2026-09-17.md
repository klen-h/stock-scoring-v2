# 2026-09-17（周四，FOMC 决议公布日）

## 🔬 评分底层审查：000567 海德股份「跌 16.8% 总分稳居榜首」（用户质疑质量/成长有效性）
- **用户观察**：000567 稳居榜首但持续下跌；质疑 ① 质量/成长维度真的有用吗 ② 高质押风险 ③ "数据好看但没转为现金"（利润含金量）。
- **★ 我第三次犯同一查询错误（必须根除）**：`fetch_one("SELECT * FROM 表 WHERE code=...")` **不带 ORDER BY** → 先后在 `mainforce_state`、`stock_finance` 上拿到任意旧行、得出错误结论。**这两张表都是"一股多行"（多日期/多报告期）**。教训固化：**凡"取最新"必须 `ORDER BY <日期> DESC LIMIT 1` 或 `DISTINCT ON`**；`get_finance` 本身的实现是对的。
- **数据真相**：000567 引擎用的是 2026 中报（8/28 公告）：营收 **+452.3%**、净利 **+815.8%**、ROE 21.45、负债率 32.9、毛利率 90.2 → 成长 92 / 质量 91 **有真实财报支撑**（我最初拿 2025 中报 -43%/-61% 说事是查错了行）。
- **但剪刀差触目惊心（09-01→09-16）**：价格 7.09→5.90 = **-16.8%**；总分 76.6→74.6 = **-2 分**。期间技术面 67.0→40.8、资金面 71.5→45.4（两个日频维度崩掉），成长 92.0 / 质量 91.3 **十二天纹丝不动**（季频数据）。
- **加权解剖（09-17，defensive 权重）**：质量 91.3×0.35=31.96 分（43%）｜基本面 75.4×0.25=18.85（25%）｜成长 92.0×0.13=11.96（16%）｜资金面 45.4×0.15=6.81（9%）｜技术面 40.8×0.12=4.90（7%）。→ **质量+成长（季频）= 43.9 分占 59%；正在恶化的技术+资金（日频）只占 16%** → 总分对"正在发生的恶化"结构性钝感。
- **三个质疑逐一裁定（全部成立或部分成立）**：
  1. **质量/成长有效性**：近期横截面 IC（08-31~09-09，369 样本）**成长 -0.170、质量 -0.079（双负）**，而 defensive 权重给两者合计 48% → **权重方向与横截面证据相反**。注意：defensive 权重的设计逻辑是"抗跌"而非"预测收益"，但"质量高=抗跌"**没有分层回测证据**，且 000567 本身就是反例（质量 91 却在防御市 5 天跌 10.6%）。
  2. **高质押**：评分体系**零质押因子**（"质押"仅作为 news_sentiment 的负面新闻词，而消息分不进综合分）→ 完全盲区。
  3. **利润含金量**：`stock_finance` 无现金流列、评分无现金流因子 → 无法验证"利润是否转为现金"。AMC 类公司（项目制收入、利润大量来自资产处置/公允价值变动）恰是最需要现金流验证的类型。
- **成长 92 分的成色问题**：+452%/+816% 是**低基数暴增**（2025 同期 -43%/-61%）；海德股份为 AMC 公司，三年增速轨迹 -43% → -47% → +452%，波动极端。`_score_growth` 只看单期 yoy，**不识别基数效应、不验证连续性**。
- **改进建议（分优先级）**：
  - A（低成本立即可做）：① 成长维度加基数效应衰减（|yoy|>200% 且上期为负 → 打折）② 前端加"评分-价格剪刀差"警示（N 日价格跌幅大而评分几乎不动 → 灰字提示）
  - B（需新数据源）：③ 质押比例（东财 F10）→ 高质押减分/标签 ④ 经营现金流 → "净利现金含量"因子
  - C（需回测背书，改权重的前提）：⑤ defensive 市况下质量五分位分层收益（验证"质量抗跌"是否成立）⑥ growth IC 最差（-0.17）可考虑下调、差额给基本面（IC +0.17）
- **关键证据查询**：`ranking_history`（000567 十二天五维）、`backtest_prices`（14 日 -7.38%/5 日 -10.61%）、`stock_finance`（三报告期）、`engine._score_growth/_score_quality`（1010/1067 行）、defensive 权重（`get_regime_weights`）。

## 🧪 实验（建议 5）：验证「防御市里质量高=抗跌」→ 未获支持、方向相反（2026-09-17）
- **产出 `scripts/quality_defense_backtest.py`**（~230 行）：用 sh000300 三年数据重算市况（`detect_market_regime` 纯价格、无前视）绕过 `market_regime_history` 仅 20 行的限制；质量分复用 `ScoreEngine._score_quality`（与生产同源）；财报按 **notice_date ≤ 截面日** 过滤（防未来函数）；组内质量五分位 → 未来 5/10 日收益 + Welch t；含前后半段分段稳健性。**预注册判据**写死在 docstring（需 Q5−Q1 显著为正才成立）。
- **样本**：685 判定日（neutral 291 / **defensive 188** / offensive 206）→ 133 截面日（2023-12-07 ~ 2026-08-28）→ **35185 条**。
- **★ 结果（defensive，6 个截面日 / 4285 条）**：
  | 持有 | Q1(低质量) | Q5(高质量) | **Q5−Q1** | t |
  |---|---|---|---|---|
  | 5 日 | +1.43% | +0.97% | **-0.46pt** | -1.47 |
  | 10 日 | +3.41% | +2.17% | **-1.24pt** | **-2.81** |
  分段：前半 -0.20pt（t=-0.42）/ 后半 -0.71pt（t=-1.72）→ **两段同向为负**。
  对照：**neutral +0.09pt（t=+0.58）/ offensive -0.13pt（t=-0.63）→ 全市场都为零**。
- **判读**：按预注册判据（需显著为正）→ **不成立**；10 日甚至**显著为负**。→ **「防御市质量抗跌」未获支持、方向相反；defensive 的 quality 0.35 属信仰、无依据**；且质量因子在三种市况下**均无效**。
- **⚠️ 局限（必须带上）**：defensive 仅 **6 个截面日**（样本集中在少数时段）→ 有效独立样本≈6、日块相关会**高估** t 值。结论宜表述为「拿不到支持、方向相反」，**不宜反向下注**。
- **连带发现（独立线索）**：运行打印 `[engine] 行业指标分布已加载: 33251 条 / 5720 只 / **0 行业**` → **行业分位实际未生效**（质量分的负债率/毛利率走的是降级绝对曲线），与 engine.py 注释「stock_industry 映射已 100% 覆盖」矛盾，**待单独排查**（本实验因此反而避开了行业分位的前视风险）。
- **下一步**：defensive 权重应重定（IC 证据：基本面 +0.170 最强、成长 -0.170 最差）→ 用本脚本框架做「权重候选 × defensive 市况」对比后再上线（守「未验证不进决策链」）。
- **改动文件**：`scripts/quality_defense_backtest.py`（新增）。**未提交**。

## 🔁 实验扩展（质量 + 成长 双维度）→ 结论反转，且我上一条的推断错了（2026-09-17）
- **★ 先纠正我自己的两个错误**：
  1. **"行业分位失效"是误报**：`[engine] ... / 0 行业` 里的 0 是 `len(dist['pe'])`（PE/PB 分布来自**腾讯行情内存缓存**，离线脚本里为空）；而质量分用的 `debt_ratio`/`gross_margin` 分布**正常加载**（33251 条 / 5720 只 / 49 行业）。质量分的行业分位**是生效的**。建议改进该日志（分 metric 打印，避免误读）。
  2. **我用全样本 IC 推断"成长无效"是错的**（又一次 Simpson 混杂）：`rate_hike_weight_backtest` 的成长 IC -0.170 是**混合市况**的横截面值，**按市况分层后方向反转**。我明知这个陷阱却仍犯了 —— 教训：**任何"某因子在市况 X 下有效吗"的问题，必须分层回答，不能拿全样本 IC 代言**。
- **★ 完整 3×2 矩阵（Q5−Q1，5日 / 10日，正=高分位更好）**：
  | 市况 | 质量 | 成长 |
  |---|---|---|
  | **defensive**（6 截面日） | **-0.46**(t=-1.47) / **-1.24**(t=-2.81) | **+1.12**(t=**+3.29**) / **+1.53**(t=**+3.25**) |
  | **neutral**（25 截面日） | +0.09(t=+0.58) / +0.08(t=+0.34) | -0.27(t=-1.71) / -0.21(t=-0.91) |
  | **offensive**（23 截面日） | -0.13(t=-0.63) / -0.23(t=-0.80) | **+0.40**(t=+1.99) / **+0.66**(t=+2.20) |
  defensive 分段（成长）：前半 **+1.63**(t=+3.31) / 后半 **+0.66**(t=+1.44) → **两段同向为正**。
- **结论（对用户三问的最终回答）**：
  1. **「质量有用吗」→ 无用**：三种市况全无效，defensive 下 10 日**显著为负**（-1.24, t=-2.81）→ **quality 0.35 无依据** ✓ 用户直觉正确。
  2. **「成长有用吗」→ 在趋势市显著有效**：defensive **+1.12/+1.53（t>3）**、offensive +0.40/+0.66（t≈2）；仅 neutral 无效（-0.27 不显著）。→ 成长是**趋势市因子**（涨跌皆有效，符合"趋势市业绩驱动定价"）。**用户的直觉在这一点上不成立**。
  3. **★ 最锋利的结论：defensive 权重配置正好反了** —— 把**最有效**的 growth 压到 0.13、把**无效**的 quality 抬到 0.35。正确方向应 **↑growth、↓quality**。
- **后续（未做）**：可用本脚本框架做「growth/quality 组合权重 × defensive 市况」的分离度寻优（只动这两维，另三维无数据不动），得出候选权重后再上线（守「未验证不进决策链」）。
- **改动文件**：`scripts/quality_defense_backtest.py`（扩展为双维度）。**未提交**。

## 🎯 寻优：defensive 档 growth/quality 配比（2026-09-17）
- **产出**：`scripts/quality_defense_backtest.py` 加 `--optimize`——网格 w∈[0,1]，组合分 = `w·rank_norm(成长) + (1−w)·rank_norm(质量)`；主口径 = **截面内 rank 归一化 + 分日 IC/分离取均值**（不混入日间水平差异），混合口径作对照；附分段稳健性。
- **★ defensive 曲线（5 日 / 10 日 同形、单调上升）**：
  | w(成长) | 分日IC(5d/10d) | 分日分离pt(5d/10d) |
  |---|---|---|
  | 0.00（纯质量） | -0.034 / -0.054 | -0.48 / -1.09 |
  | **0.271（现役折算）** | **-0.014 / -0.033** | **-0.26 / -0.82** |
  | 0.50 | +0.019 / +0.005 | +0.29 / +0.21 |
  | 0.70 | +0.049 / +0.041 | +0.82 / +0.93 |
  | 0.90 | +0.064 / +0.059 | +1.13 / +1.54 |
  | 1.00 | +0.068 / +0.065 | +1.11 / +1.55 |
  → **单调**、两条持有期同形 → **成长权重越高越优，现役 0.271 明显偏低**。
- **⚠️ 削弱激进改动的三条限定**：① 分段 IC 显示 **defensive 后半段（最近 3 个截面日）所有 w 都为负**（w=1.0：前半 +0.145 / 后半 -0.015）→ 单调性**主要由前半段驱动，存在时段依赖**；② 仅 **6 个截面日**（日块效应）；③ **仅 2 维子空间**（另三维无历史数据、固定不动；实际总分里这两维合计 48%）。
- **对照（不可用）**：neutral 曲线随 w 微降（IC ≤0.016）且分段**完全反向**；offensive 有峰值（w≈0.5~0.85）但分段反向 → **这两个市况下两维配比无稳定信号，不动**。
- **候选权重（保持两维合计 0.48、另三维不变）**：现役 growth 0.13 / quality 0.35（w=0.271）→ 建议 **growth 0.30 / quality 0.18（w≈0.63）**（5 日 IC +0.037 / 分离 +0.68；10 日 +0.026 / +0.71）。**不取端点 w=1.0**（虽最优，但属时段依赖的端点最优，且会让"质量维度"整体归零）。
- **上线建议**：按「未验证不进决策链」——本轮是**单一时段 + 2 维子空间**的验证，建议**先记录候选、暂不改生产权重**；若要动，先灰度观察或等 regime/样本积累后重验。
- **改动文件**：`scripts/quality_defense_backtest.py`（+`--optimize`，约 140 行）。**未提交**。

## 💬 探讨：财报「拉升出货」下，质量/成长常驻评分可行吗（2026-09-17，未改代码）
- **用户质疑**：公司出财报常有「拉升出货」（利好兑现后主力出货），质量/成长这两个财报因子一直放在评分里是否可行。
- **代码事实（已查证）**：① 质量=ROE+负债率+毛利率、成长=营收/净利同比，均来自 `stock_finance`（东财 F10，**季度更新**=慢变量）；② `notice_date`（公告日）**已存表、回测 `get_finance_asof` 也用它防未来函数**；③ 但**在线评分 `get_finance_batch` 取"最新一期财报"、`notice_date` 流不到评分环节**（SELECT 了却没用）→ **无"公告后多久"的时滞/衰减处理**，财报一公告分数跳变后锁死数月。
- **核心判断**：「该不该放」（因子长期有没有 alpha）与「怎么放」（频率/新鲜度）是两回事；用户戳中的是后者——**季频慢变量被当成日频快因子、固定权重无衰减**，在"公告→拉升→兑现→出货"窗口内评分是瞎的。
- **与已有实证自洽**：质量在 defensive 下 Q5−Q1 = -1.24（t=-2.81）反向，恰可用"利好兑现出货 + 白马拥挤交易"解释；`crowding.py` 注释本就写着"高质量/高成长股跌更惨——机构重仓白马是拥挤交易"（但该认知只做了拥挤度风险调节，未反哺质量维度本身）。
- **★ 待办方向（事件研究，未做）**：用 `stock_finance.notice_date` 把样本按「距公告日天数」分桶（0-5/5-20/20-60/60+ 天），每桶算成长/质量因子 IC——若"短窗口 IC 负、长窗口转正"即坐实拉升出货，据此加「公告后 N 天降权/暂退」；若短窗口反而正（PEAD 漂移）则维持现状。可复用 `quality_defense_backtest.py` 框架。

## 📊 财报公告事件研究：坐实「拉升出货」（2026-09-17，已跑）
- **产出**：`quality_defense_backtest.py` 加 `--event`——样本记录财报公告后年龄（age = 截面日 − notice_date；notice_date 取自 stock_finance，`finance_asof` 本就按 notice_date ≤ 截面日防未来函数），按年龄分桶（0-5/6-20/21-60/61-120/120+ 天）→ 桶内截面 IC → 跨日平均（同 `_optimize` 口径）。
- **★ 结果（全样本，5日/10日 同向、10日更强）**：
  | 年龄桶 | 质量IC(5d/10d) | 成长IC(5d/10d) | 质量分离pt(5d/10d) |
  |---|---|---|---|
  | **0-5天（刚公告）** | **-0.076 / -0.138** | **-0.076 / -0.115** | **-1.89 / -3.42** |
  | 6-20天 | -0.059 / -0.025 | -0.050 / -0.037 | -0.82 / -1.16 |
  | 21-60天 | **+0.086 / +0.049** | **+0.045 / +0.035** | +0.27 / -0.07 |
  | 61-120天 | -0.027 / -0.057 | -0.023 / -0.039 | -0.48 / -1.33 |
  | 120天+ | +0.001 / +0.016 | -0.014 / -0.004 | -0.02 / +0.21 |
- **★ 最强信号（neutral 市况 · 成长维度）**：0-5天 IC **-0.200 / -0.209**（两条持有期稳定 -0.20）vs 21-60天 +0.088/+0.080 → 震荡市里"刚公告的高成长股"未来收益系统性反向，正是"利好兑现、无趋势承接 → 出货"的最典型环境。
- **判读**：**用户「拉升出货」直觉获数据支持**——财报公告后 0-20 天（尤其 0-5 天）质量/成长因子方向反转（利好兑现、正要出货），21-60 天转正（信息沉淀）。→ 现评分「财报一公告立即切最新、之后锁死数月」恰好把出货窗口当成高分，**踩在出货点上**。
- **⚠️ 局限（判读须带）**：① IC 绝对值弱到中等（质量 0-5天 -0.138、neutral 成长 -0.20）；② "0-5天"桶仅 16 个截面日（财报季频披露，"刚公告"窗口天然窄）；③ 61-120天 又回落为负 → 非干净单调，是"短窗口负→中期转正→再回落"；④ defensive 市况样本不足（0-5天仅 17 条/2 截面日）无法单独验证。
- **落地方向（未做）**：给成长/质量加「公告后新鲜度衰减」——公告后 ~20 天内降权/暂退；但先回测验证"加衰减 vs 不加"的 IC/收益差 + 扫描最优窗口/力度，守「未验证不进决策链」。
- **改动文件**：`scripts/quality_defense_backtest.py`（+`--event`，约 55 行）。**未提交**。

## 🧮 衰减寻优：公告后新鲜度衰减的量化（2026-09-17）
- **产出**：`quality_defense_backtest.py` 加 `--decay`——扫描（公告后阈值 T∈{5,10,20,30} 天，衰减系数 α∈{0,0.3,0.5,0.8}）网格，α=1 为基线（不衰减）；age≤T 样本因子 ×α、age>T ×1.0，分日截面 IC 取均值（同 `_optimize` 口径）。
- **★ 结论（5日/10日 同向，最稳健）**：衰减**只在 neutral 市况有效**，且 **α=0（彻底剔除）明显优于部分衰减**——说明刚公告的成长信号在震荡市是**反向**的，任何保留都混入噪声：
  | 维度·市况 | 基线IC(5d/10d) | 最优(α=0) | Δ |
  |---|---|---|---|
  | **成长·neutral** | **-0.013 / -0.009** | **+0.021(T=30) / +0.023(T=20)** | **+0.034 / +0.032** |
  | 质量·neutral | +0.014 / +0.016 | +0.030 / +0.032 | +0.016 / +0.016 |
  | 成长·全样本 | +0.002 / +0.003 | +0.017 / +0.015 | +0.015 / +0.012 |
  | 质量·全样本 | -0.004 / +0.001 | +0.012 / +0.013 | +0.016 / +0.012 |
- **无信息/不一致**：**defensive 衰减几乎无效果**（刚公告样本仅 17 条/2 截面日，α 各档几乎相同）——而 defensive 恰是成长/质量权重最高（48%）的市况 → **最大缺口**；offensive 无一致结论（成长在 α=0 时反而更差，10日 T=20/α=0 掉到 -0.010）。
- **判读**：① neutral 下成长因子基线为负，公告后 20-30 天内**彻底剔除（α=0）**能把 IC 从负转正，两条持有期完全一致；② 但 Δ IC 绝对幅度温和（~0.03），且是**单因子口径**（成长仅占权重 13-18%，落到总分的改善更小）；③ 属「边际改善」非「质变」。
- **落地方向（未做）**：neutral 市况下、财报公告后 ~20-30 天内，成长维度暂退（α→0）、质量降权；但需先确认生产口径（总分而非单因子）下的改善，且 defensive 缺口未补。
- **改动文件**：`scripts/quality_defense_backtest.py`（+`--decay`，约 65 行）。**未提交**。

## 🛠 旁路衰减灰度功能落地（2026-09-17）
- **用户需求**：把"衰减后的成长分"旁路输出，对比两周两套排名的 top30 收益（灰度验证衰减是否有效，不直接改生产）。
- **实现（零生产回归，不碰 score_top/engine/live_ranking）**：
  1. `scripts/shadow_decay_ranking.py`：读 `ranking_live`（日批全量精算五维分）→ 按 `stock_finance.notice_date` 算公告后年龄 → 公告后 ≤25 天把成长/质量分 ×0（α=0，对应衰减寻优最优）→ 复刻 `engine._combine` 重加权排序 → base/decay 两套 top30 落库新表 `shadow_rank_daily`（rank_date/variant/code/rank_pos/total_score/decay_age/snapshot_price/dimensions_json）。
  2. `scripts/compare_shadow_rank.py`：读 shadow_rank_daily，用 backtest_prices 算快照日 → T+5/T+10 交易日固定窗口收益（与 ranking_history 口径同源），对比 base vs decay 平均收益/胜率。
  3. 接入日批：`daily_batch.py` 加 `task_shadow_rank`（`from shadow_decay_ranking import run`），排在 rank_live 之后（读其精算五维分）。
- **★ 关键口径修正（dry-run 暴露）**：本地 regime 缓存为空 → 权重 fallback 默认权重，与 ranking_live 的 defensive 权重不一致 → base/decay 各一套权重、对比失真。修法：① 权重获取回退 `market_regime_history` 最新行；② 加「不衰减重算 vs ranking_live 总分」校验（实测平均差 0.05 分 OK）。
- **验证**：dry-run 市况=defensive、权重正确、校验 OK；生产 top30 vs 衰减 top30 **重叠 12/30**（符合预期——9 月中旬正是中报公告后 0-25 天窗口，生产 top 里大量"刚公告高成长股"被衰减掉榜，这正是灰度要验证的现象）。lint 0；`daily_batch --list` 已含 shadow_rank。
- **用法**：日批自动每天落库；两周后 `python scripts/compare_shadow_rank.py` 看两套 top30 收益对比（需 ≥10 个快照日才可信）。
- **⚠️ 待观察/风险**：α=0 的「×0」语义会让刚公告高成长股总分暴跌（growth+quality 权重 48% 贡献归零），重叠仅 12/30 偏激进；若两周后 decay 未优于 base，可调 α 或改「剔除（权重分摊给其他维度）」语义再试。另：shadow 榜依赖 ranking_live 当日落库（9/16 快照），新快照从日批接入后开始积累。
- **改动文件**：`scripts/shadow_decay_ranking.py`（新增）、`scripts/compare_shadow_rank.py`（新增）、`scripts/daily_batch.py`（+task_shadow_rank）。**未提交**。

## 🔧 修 bug：指标包漏打包 flow5_amt_yuan → 前端本地详情页「5日主力净流入」恒显示空（2026-09-17）
- **用户现象**：本地点 002452 详情页，「主力行为」区块「5日主力净流入」显示 '-'，但「占额累计 +23.91」有值；且获利盘 82%/筹码位置 85%/集中度 25%。
- **根因一（bug，已修）**：`mainforce_overlay` 已算 `flow5_amt_yuan`（近5日主力净流入金额元，2026-09-17 新增），但 `generate_backend_pack.py:compute_mainforce_batch` 打包指标包时**漏了这个字段** → 前端本地详情页读 `mainforce.flow5_amt_yuan` 恒 null → 显示 '-'。数据库 `mainforce_state` 里其实有值（002452 = 3268 万）。**修法**：打包输出 dict 补 `"flow5_amt_yuan": ov.get("flow5_amt_yuan")`。下次 backend-pack 重打包后前端即有值。
- **根因二（时序，非 bug）**：用户看到的 chip 82%/85%/25% 是「换手率单位 bug 修复前」的旧值；数据库 mainforce_state 9/16 已是修复后的 65.3%/55.5%/10.4%（`chips.py` 注释明确写了 002452 案例：82.0/25.4/84.8 → 65.3/10.4/55.5）。原因：前端本地读的是浏览器 IndexedDB 缓存的**旧指标包**（backend-pack 打包时 chips.py 还是修复前版本），等 backend-pack 重打包 + 前端刷新即更新。
- **字段口径备忘**：`flow5_amt` = 5日主力净流入占成交额%累加（评分因子，倒U锚点输入，改动需重标定）；`flow5_amt_yuan` = 5日主力净流入金额元（纯展示，可与行情软件对照）。两者口径不同、不可互换。
- **改动文件**：`scripts/generate_backend_pack.py`（+flow5_amt_yuan 打包）。**未提交**。

## 🖥 页面直接看两套排名（衰减对比 tab）（2026-09-17）
- **用户需求**：在排行榜页面直接看「生产 top30 vs 衰减 top30」两套排名。
- **实现**：① 后端 `routers/scoring.py` 加 `GET /score/batch/shadow-rank`（读 shadow_rank_daily 最新快照的 base/decay 两套，返回 date/base/decay/overlap）；② 前端 `api/index.js` 加 `getShadowRank`；③ `ScoreRank.vue` 加「衰减对比」tab（tabs 数组 + loadShadowRank + switchTab 分支 + 模板并列两表，绿色高亮衰减后新进榜，标注快照日期）。
- **★ 踩坑（动态路由拦截）**：接口最初写 `/score/shadow-rank`（单段），被 `@router.get("/{symbol}")`（907 行，个股详情）**抢先匹配** → 返回 `{"error":"未找到股票 shadow-rank"}`。FastAPI 按定义顺序匹配，单段 `/shadow-rank` 与 `/{symbol}` 冲突。修法：改两段 `/score/batch/shadow-rank`（与 batch/top 同段数，不冲突）。
- **★ 口径修正（快照价）**：初版 `get_prices` 用「今天」查 backtest_prices、且优先行情缓存，而 shadow 榜是 9/16 快照 → snapshot_price 全 None。修法：`get_prices(codes, rank_date)` 读快照日 backtest_prices 收盘价，与 compare 收益口径同源（买入价=快照日收盘、卖出=未来 T+N 收盘）。
- **验证**：接口 200，date=2026-09-16，base 30/decay 30/overlap 12；base[0]=000567(74.6, 快照价5.9, age20)，decay[0]=603986(72.9, 快照价373.14, age29)。lint 0。
- **数据说明**：shadow 榜为日批盘后快照（非实时），前端已标注快照日期；当前仅 9/16 一天数据，需日批 task_shadow_rank 持续落库后才有对比价值。
- **改动文件**：`backend/app/routers/scoring.py`（+shadow-rank 接口）、`frontend/src/api/index.js`、`frontend/src/views/ScoreRank.vue`、`scripts/shadow_decay_ranking.py`（快照价口径）。**未提交**。

## ⚡ 盘中两份 top50 实时榜（2026-09-17）
- **用户需求**：盘中也要两份 top50（生产 + 衰减）实时排行榜。
- **实现（零额外精算成本）**：把 `/score/batch/shadow-rank` 从「读盘后快照 shadow_rank_daily」改为**实时计算**——读 `ranking_live` 最新精算结果（全市场 ~1000 行五维分）+ `get_finance_batch`（notice_date 算公告龄）+ `get_regime_weights`（当前权重），复刻 `_combine` 做衰减重加权（公告后 ≤25 天成长/质量 ×0），返回 base/decay 两套 top N。前端默认拉 50。
- **★ 为什么「准实时」足够**：衰减对象（成长/质量）是季频慢变量、公告龄按自然日算，盘中不随时间变；用 ranking_live 最新（可能昨日盘后）算衰减，衰减本身不过时，与技术/资金/基本面（两套榜共通、日间波动）无关 → 无需盘中重跑全量精算。
- **验证**：接口 200，base 50/decay 50/overlap 8；base[0]=000567(74.6, age20)，decay[0]=002925(69.0, age26)。lint 0。
- **保留 shadow_rank_daily 落库**：盘后快照 + `compare_shadow_rank.py` 的两周收益对比仍走日批 task_shadow_rank（离线灰度验证衰减是否有效）；盘中实时榜只用于观察、不替代收益验证。
- **改动文件**：`backend/app/routers/scoring.py`（shadow-rank 改实时 + `_decay_total` helper）、`frontend/src/api/index.js`、`frontend/src/views/ScoreRank.vue`。**未提交**。

## 🔎 主力行为标签机制澄清（2026-09-17，回答用户"本地只有一个是正常的吗"）
- **★ 覆盖率天然很低（正常）**：`mainforce_state`（9/16）全市场 2028 只 → **None 1898 / accum 107 / distribution 23**，有标签仅 **130 只（6.4%）**；榜单 top50 里 8 只（16%）。原因：标签是**条件触发式**——吸筹区=低位筹码密集+主力净流入；出货嫌疑=高位高获利+主力流出；绝大多数股票处中间态 → 无标签。所以"榜单里只看到一两个"是正常现象，不是 bug。
- **前端两榜跳转行为**：主表（top tab）代码列是指向雪球的 `<a @click.stop>`（点代码→雪球，点行→详情页 goDetail）；衰减对比 tab 原为纯文本（点哪都跳详情），**已统一**为代码列雪球链接，两榜一致。
- **衰减对比 tab 已纳入盘中 60s 自动刷新**（startAutoRefresh 里 activeTab==='shadow' 走 loadShadowRank）。
- **对比榜加主力列**：后端 shadow-rank 返回每行 `mainforce_signal/mainforce_phase`（读 mainforce_state，与主榜同源）；前端两表加"主力"列（吸筹=绿/出货=红，无标签显示 '-'）。
- **改动文件**：`backend/app/routers/scoring.py`、`frontend/src/views/ScoreRank.vue`。**未提交**。
