# 2026-09-17（周四，FOMC 决议公布日）

## 🔬 评分底层审查：000567 海德股份「跌 16.8% 总分稳居榜首」（用户质疑质量/成长有效性）
- **用户观察**：000567 稳居榜首但持续下跌；质疑 ① 质量/成长维度真的有用吗 ② 高质押风险 ③ "数据好看但没转为现金"（利润含金量）。
- **★ 我第三次犯同一查询错误（必须根除）**：`fetch_one("SELECT * FROM 表 WHERE code=...")` **不带 ORDER BY** → 先后在 `mainforce_state`、`stock_finance` 上拿到任意旧行、得出错误结论。**这两张表都是"一股多行"（多日期/多报告期）**。教训固化：**凡"取最新"必须 `ORDER BY <日期> DESC LIMIT 1` 或 `DISTINCT ON`**；`get_finance` 本身的实现是对的。
- **数据真相**：000567 引擎用的是 2026 中报（8/28 公告）：营收 **+452.3%**、净利 **+815.8%**、ROE 21.45、负债率 32.9、毛利率 90.2 → 成长 92 / 质量 91 **有真实财报支撑**（我最初拿 2025 中报 -43%/-61% 说事是查错了行）。
- **但剪刀差触目惊心（09-01→09-16）**：价格 7.09→5.90 = **-16.8%**；总分 76.6→74.6 = **-2 分**。期间技术面 67.0→40.8、资金面 71.5→45.4（两个日频维度崩掉），成长 92.0 / 质量 91.3 **十二天纹丝不动**（季频数据）。
- **加权解剖（09-17，defensive 权重）**：质量 91.3×0.35=31.96 分（43%）｜基本面 75.4×0.25=18.85（25%）｜成长 92.0×0.13=11.96（16%）｜资金面 45.4×0.15=6.81（9%）｜技术面 40.8×0.12=4.90（7%）。→ **质量+成长（季频）= 43.9 分占 59%；正在恶化的技术+资金（日频）只占 16%** → 总分对"正在发生的恶化"结构性钝感。
- **三个质疑逐一裁定（全部成立或部分成立）**：
  1. **质量/成长有效性**：近期横截面 IC（08-31~09-09，369 样本）**成长 -0.170、质量 -0.079（双负）**，而 defensive 权重给两者合计 48% → **权重方向与横截面证据相反**。注意：defensive 权重的设计逻辑是"抗跌"而非"预测收益"，但"质量高=抗跌"**没有分层回测证据**，且 000567 本身就是反例（质量 91 却在防御市 5 天跌 10.6%）。
  2. **高质押**：评分体系**零质押因子**（"质押"仅作为 news_sentiment 的负面新闻词，而消息分不进综合分）→ 完全盲区。
  3. **利润含金量**：`stock_finance` 无现金流列、评分无现金流因子 → 无法验证"利润是否转为现金"。AMC 类公司（项目制收入、利润大量来自资产处置/公允价值变动）恰是最需要现金流验证的类型。
- **成长 92 分的成色问题**：+452%/+816% 是**低基数暴增**（2025 同期 -43%/-61%）；海德股份为 AMC 公司，三年增速轨迹 -43% → -47% → +452%，波动极端。`_score_growth` 只看单期 yoy，**不识别基数效应、不验证连续性**。
- **改进建议（分优先级）**：
  - A（低成本立即可做）：① 成长维度加基数效应衰减（|yoy|>200% 且上期为负 → 打折）② 前端加"评分-价格剪刀差"警示（N 日价格跌幅大而评分几乎不动 → 灰字提示）
  - B（需新数据源）：③ 质押比例（东财 F10）→ 高质押减分/标签 ④ 经营现金流 → "净利现金含量"因子
  - C（需回测背书，改权重的前提）：⑤ defensive 市况下质量五分位分层收益（验证"质量抗跌"是否成立）⑥ growth IC 最差（-0.17）可考虑下调、差额给基本面（IC +0.17）
- **关键证据查询**：`ranking_history`（000567 十二天五维）、`backtest_prices`（14 日 -7.38%/5 日 -10.61%）、`stock_finance`（三报告期）、`engine._score_growth/_score_quality`（1010/1067 行）、defensive 权重（`get_regime_weights`）。

## 🧪 实验（建议 5）：验证「防御市里质量高=抗跌」→ 未获支持、方向相反（2026-09-17）
- **产出 `scripts/quality_defense_backtest.py`**（~230 行）：用 sh000300 三年数据重算市况（`detect_market_regime` 纯价格、无前视）绕过 `market_regime_history` 仅 20 行的限制；质量分复用 `ScoreEngine._score_quality`（与生产同源）；财报按 **notice_date ≤ 截面日** 过滤（防未来函数）；组内质量五分位 → 未来 5/10 日收益 + Welch t；含前后半段分段稳健性。**预注册判据**写死在 docstring（需 Q5−Q1 显著为正才成立）。
- **样本**：685 判定日（neutral 291 / **defensive 188** / offensive 206）→ 133 截面日（2023-12-07 ~ 2026-08-28）→ **35185 条**。
- **★ 结果（defensive，6 个截面日 / 4285 条）**：
  | 持有 | Q1(低质量) | Q5(高质量) | **Q5−Q1** | t |
  |---|---|---|---|---|
  | 5 日 | +1.43% | +0.97% | **-0.46pt** | -1.47 |
  | 10 日 | +3.41% | +2.17% | **-1.24pt** | **-2.81** |
  分段：前半 -0.20pt（t=-0.42）/ 后半 -0.71pt（t=-1.72）→ **两段同向为负**。
  对照：**neutral +0.09pt（t=+0.58）/ offensive -0.13pt（t=-0.63）→ 全市场都为零**。
- **判读**：按预注册判据（需显著为正）→ **不成立**；10 日甚至**显著为负**。→ **「防御市质量抗跌」未获支持、方向相反；defensive 的 quality 0.35 属信仰、无依据**；且质量因子在三种市况下**均无效**。
- **⚠️ 局限（必须带上）**：defensive 仅 **6 个截面日**（样本集中在少数时段）→ 有效独立样本≈6、日块相关会**高估** t 值。结论宜表述为「拿不到支持、方向相反」，**不宜反向下注**。
- **连带发现（独立线索）**：运行打印 `[engine] 行业指标分布已加载: 33251 条 / 5720 只 / **0 行业**` → **行业分位实际未生效**（质量分的负债率/毛利率走的是降级绝对曲线），与 engine.py 注释「stock_industry 映射已 100% 覆盖」矛盾，**待单独排查**（本实验因此反而避开了行业分位的前视风险）。
- **下一步**：defensive 权重应重定（IC 证据：基本面 +0.170 最强、成长 -0.170 最差）→ 用本脚本框架做「权重候选 × defensive 市况」对比后再上线（守「未验证不进决策链」）。
- **改动文件**：`scripts/quality_defense_backtest.py`（新增）。**未提交**。

## 🔁 实验扩展（质量 + 成长 双维度）→ 结论反转，且我上一条的推断错了（2026-09-17）
- **★ 先纠正我自己的两个错误**：
  1. **"行业分位失效"是误报**：`[engine] ... / 0 行业` 里的 0 是 `len(dist['pe'])`（PE/PB 分布来自**腾讯行情内存缓存**，离线脚本里为空）；而质量分用的 `debt_ratio`/`gross_margin` 分布**正常加载**（33251 条 / 5720 只 / 49 行业）。质量分的行业分位**是生效的**。建议改进该日志（分 metric 打印，避免误读）。
  2. **我用全样本 IC 推断"成长无效"是错的**（又一次 Simpson 混杂）：`rate_hike_weight_backtest` 的成长 IC -0.170 是**混合市况**的横截面值，**按市况分层后方向反转**。我明知这个陷阱却仍犯了 —— 教训：**任何"某因子在市况 X 下有效吗"的问题，必须分层回答，不能拿全样本 IC 代言**。
- **★ 完整 3×2 矩阵（Q5−Q1，5日 / 10日，正=高分位更好）**：
  | 市况 | 质量 | 成长 |
  |---|---|---|
  | **defensive**（6 截面日） | **-0.46**(t=-1.47) / **-1.24**(t=-2.81) | **+1.12**(t=**+3.29**) / **+1.53**(t=**+3.25**) |
  | **neutral**（25 截面日） | +0.09(t=+0.58) / +0.08(t=+0.34) | -0.27(t=-1.71) / -0.21(t=-0.91) |
  | **offensive**（23 截面日） | -0.13(t=-0.63) / -0.23(t=-0.80) | **+0.40**(t=+1.99) / **+0.66**(t=+2.20) |
  defensive 分段（成长）：前半 **+1.63**(t=+3.31) / 后半 **+0.66**(t=+1.44) → **两段同向为正**。
- **结论（对用户三问的最终回答）**：
  1. **「质量有用吗」→ 无用**：三种市况全无效，defensive 下 10 日**显著为负**（-1.24, t=-2.81）→ **quality 0.35 无依据** ✓ 用户直觉正确。
  2. **「成长有用吗」→ 在趋势市显著有效**：defensive **+1.12/+1.53（t>3）**、offensive +0.40/+0.66（t≈2）；仅 neutral 无效（-0.27 不显著）。→ 成长是**趋势市因子**（涨跌皆有效，符合"趋势市业绩驱动定价"）。**用户的直觉在这一点上不成立**。
  3. **★ 最锋利的结论：defensive 权重配置正好反了** —— 把**最有效**的 growth 压到 0.13、把**无效**的 quality 抬到 0.35。正确方向应 **↑growth、↓quality**。
- **后续（未做）**：可用本脚本框架做「growth/quality 组合权重 × defensive 市况」的分离度寻优（只动这两维，另三维无数据不动），得出候选权重后再上线（守「未验证不进决策链」）。
- **改动文件**：`scripts/quality_defense_backtest.py`（扩展为双维度）。**未提交**。

## 🎯 寻优：defensive 档 growth/quality 配比（2026-09-17）
- **产出**：`scripts/quality_defense_backtest.py` 加 `--optimize`——网格 w∈[0,1]，组合分 = `w·rank_norm(成长) + (1−w)·rank_norm(质量)`；主口径 = **截面内 rank 归一化 + 分日 IC/分离取均值**（不混入日间水平差异），混合口径作对照；附分段稳健性。
- **★ defensive 曲线（5 日 / 10 日 同形、单调上升）**：
  | w(成长) | 分日IC(5d/10d) | 分日分离pt(5d/10d) |
  |---|---|---|
  | 0.00（纯质量） | -0.034 / -0.054 | -0.48 / -1.09 |
  | **0.271（现役折算）** | **-0.014 / -0.033** | **-0.26 / -0.82** |
  | 0.50 | +0.019 / +0.005 | +0.29 / +0.21 |
  | 0.70 | +0.049 / +0.041 | +0.82 / +0.93 |
  | 0.90 | +0.064 / +0.059 | +1.13 / +1.54 |
  | 1.00 | +0.068 / +0.065 | +1.11 / +1.55 |
  → **单调**、两条持有期同形 → **成长权重越高越优，现役 0.271 明显偏低**。
- **⚠️ 削弱激进改动的三条限定**：① 分段 IC 显示 **defensive 后半段（最近 3 个截面日）所有 w 都为负**（w=1.0：前半 +0.145 / 后半 -0.015）→ 单调性**主要由前半段驱动，存在时段依赖**；② 仅 **6 个截面日**（日块效应）；③ **仅 2 维子空间**（另三维无历史数据、固定不动；实际总分里这两维合计 48%）。
- **对照（不可用）**：neutral 曲线随 w 微降（IC ≤0.016）且分段**完全反向**；offensive 有峰值（w≈0.5~0.85）但分段反向 → **这两个市况下两维配比无稳定信号，不动**。
- **候选权重（保持两维合计 0.48、另三维不变）**：现役 growth 0.13 / quality 0.35（w=0.271）→ 建议 **growth 0.30 / quality 0.18（w≈0.63）**（5 日 IC +0.037 / 分离 +0.68；10 日 +0.026 / +0.71）。**不取端点 w=1.0**（虽最优，但属时段依赖的端点最优，且会让"质量维度"整体归零）。
- **上线建议**：按「未验证不进决策链」——本轮是**单一时段 + 2 维子空间**的验证，建议**先记录候选、暂不改生产权重**；若要动，先灰度观察或等 regime/样本积累后重验。
- **改动文件**：`scripts/quality_defense_backtest.py`（+`--optimize`，约 140 行）。**未提交**。

## 💬 探讨：财报「拉升出货」下，质量/成长常驻评分可行吗（2026-09-17，未改代码）
- **用户质疑**：公司出财报常有「拉升出货」（利好兑现后主力出货），质量/成长这两个财报因子一直放在评分里是否可行。
- **代码事实（已查证）**：① 质量=ROE+负债率+毛利率、成长=营收/净利同比，均来自 `stock_finance`（东财 F10，**季度更新**=慢变量）；② `notice_date`（公告日）**已存表、回测 `get_finance_asof` 也用它防未来函数**；③ 但**在线评分 `get_finance_batch` 取"最新一期财报"、`notice_date` 流不到评分环节**（SELECT 了却没用）→ **无"公告后多久"的时滞/衰减处理**，财报一公告分数跳变后锁死数月。
- **核心判断**：「该不该放」（因子长期有没有 alpha）与「怎么放」（频率/新鲜度）是两回事；用户戳中的是后者——**季频慢变量被当成日频快因子、固定权重无衰减**，在"公告→拉升→兑现→出货"窗口内评分是瞎的。
- **与已有实证自洽**：质量在 defensive 下 Q5−Q1 = -1.24（t=-2.81）反向，恰可用"利好兑现出货 + 白马拥挤交易"解释；`crowding.py` 注释本就写着"高质量/高成长股跌更惨——机构重仓白马是拥挤交易"（但该认知只做了拥挤度风险调节，未反哺质量维度本身）。
- **★ 待办方向（事件研究，未做）**：用 `stock_finance.notice_date` 把样本按「距公告日天数」分桶（0-5/5-20/20-60/60+ 天），每桶算成长/质量因子 IC——若"短窗口 IC 负、长窗口转正"即坐实拉升出货，据此加「公告后 N 天降权/暂退」；若短窗口反而正（PEAD 漂移）则维持现状。可复用 `quality_defense_backtest.py` 框架。

## 📊 财报公告事件研究：坐实「拉升出货」（2026-09-17，已跑）
- **产出**：`quality_defense_backtest.py` 加 `--event`——样本记录财报公告后年龄（age = 截面日 − notice_date；notice_date 取自 stock_finance，`finance_asof` 本就按 notice_date ≤ 截面日防未来函数），按年龄分桶（0-5/6-20/21-60/61-120/120+ 天）→ 桶内截面 IC → 跨日平均（同 `_optimize` 口径）。
- **★ 结果（全样本，5日/10日 同向、10日更强）**：
  | 年龄桶 | 质量IC(5d/10d) | 成长IC(5d/10d) | 质量分离pt(5d/10d) |
  |---|---|---|---|
  | **0-5天（刚公告）** | **-0.076 / -0.138** | **-0.076 / -0.115** | **-1.89 / -3.42** |
  | 6-20天 | -0.059 / -0.025 | -0.050 / -0.037 | -0.82 / -1.16 |
  | 21-60天 | **+0.086 / +0.049** | **+0.045 / +0.035** | +0.27 / -0.07 |
  | 61-120天 | -0.027 / -0.057 | -0.023 / -0.039 | -0.48 / -1.33 |
  | 120天+ | +0.001 / +0.016 | -0.014 / -0.004 | -0.02 / +0.21 |
- **★ 最强信号（neutral 市况 · 成长维度）**：0-5天 IC **-0.200 / -0.209**（两条持有期稳定 -0.20）vs 21-60天 +0.088/+0.080 → 震荡市里"刚公告的高成长股"未来收益系统性反向，正是"利好兑现、无趋势承接 → 出货"的最典型环境。
- **判读**：**用户「拉升出货」直觉获数据支持**——财报公告后 0-20 天（尤其 0-5 天）质量/成长因子方向反转（利好兑现、正要出货），21-60 天转正（信息沉淀）。→ 现评分「财报一公告立即切最新、之后锁死数月」恰好把出货窗口当成高分，**踩在出货点上**。
- **⚠️ 局限（判读须带）**：① IC 绝对值弱到中等（质量 0-5天 -0.138、neutral 成长 -0.20）；② "0-5天"桶仅 16 个截面日（财报季频披露，"刚公告"窗口天然窄）；③ 61-120天 又回落为负 → 非干净单调，是"短窗口负→中期转正→再回落"；④ defensive 市况样本不足（0-5天仅 17 条/2 截面日）无法单独验证。
- **落地方向（未做）**：给成长/质量加「公告后新鲜度衰减」——公告后 ~20 天内降权/暂退；但先回测验证"加衰减 vs 不加"的 IC/收益差 + 扫描最优窗口/力度，守「未验证不进决策链」。
- **改动文件**：`scripts/quality_defense_backtest.py`（+`--event`，约 55 行）。**未提交**。

## 🧮 衰减寻优：公告后新鲜度衰减的量化（2026-09-17）
- **产出**：`quality_defense_backtest.py` 加 `--decay`——扫描（公告后阈值 T∈{5,10,20,30} 天，衰减系数 α∈{0,0.3,0.5,0.8}）网格，α=1 为基线（不衰减）；age≤T 样本因子 ×α、age>T ×1.0，分日截面 IC 取均值（同 `_optimize` 口径）。
- **★ 结论（5日/10日 同向，最稳健）**：衰减**只在 neutral 市况有效**，且 **α=0（彻底剔除）明显优于部分衰减**——说明刚公告的成长信号在震荡市是**反向**的，任何保留都混入噪声：
  | 维度·市况 | 基线IC(5d/10d) | 最优(α=0) | Δ |
  |---|---|---|---|
  | **成长·neutral** | **-0.013 / -0.009** | **+0.021(T=30) / +0.023(T=20)** | **+0.034 / +0.032** |
  | 质量·neutral | +0.014 / +0.016 | +0.030 / +0.032 | +0.016 / +0.016 |
  | 成长·全样本 | +0.002 / +0.003 | +0.017 / +0.015 | +0.015 / +0.012 |
  | 质量·全样本 | -0.004 / +0.001 | +0.012 / +0.013 | +0.016 / +0.012 |
- **无信息/不一致**：**defensive 衰减几乎无效果**（刚公告样本仅 17 条/2 截面日，α 各档几乎相同）——而 defensive 恰是成长/质量权重最高（48%）的市况 → **最大缺口**；offensive 无一致结论（成长在 α=0 时反而更差，10日 T=20/α=0 掉到 -0.010）。
- **判读**：① neutral 下成长因子基线为负，公告后 20-30 天内**彻底剔除（α=0）**能把 IC 从负转正，两条持有期完全一致；② 但 Δ IC 绝对幅度温和（~0.03），且是**单因子口径**（成长仅占权重 13-18%，落到总分的改善更小）；③ 属「边际改善」非「质变」。
- **落地方向（未做）**：neutral 市况下、财报公告后 ~20-30 天内，成长维度暂退（α→0）、质量降权；但需先确认生产口径（总分而非单因子）下的改善，且 defensive 缺口未补。
- **改动文件**：`scripts/quality_defense_backtest.py`（+`--decay`，约 65 行）。**未提交**。

## 🛠 旁路衰减灰度功能落地（2026-09-17）
- **用户需求**：把"衰减后的成长分"旁路输出，对比两周两套排名的 top30 收益（灰度验证衰减是否有效，不直接改生产）。
- **实现（零生产回归，不碰 score_top/engine/live_ranking）**：
  1. `scripts/shadow_decay_ranking.py`：读 `ranking_live`（日批全量精算五维分）→ 按 `stock_finance.notice_date` 算公告后年龄 → 公告后 ≤25 天把成长/质量分 ×0（α=0，对应衰减寻优最优）→ 复刻 `engine._combine` 重加权排序 → base/decay 两套 top30 落库新表 `shadow_rank_daily`（rank_date/variant/code/rank_pos/total_score/decay_age/snapshot_price/dimensions_json）。
  2. `scripts/compare_shadow_rank.py`：读 shadow_rank_daily，用 backtest_prices 算快照日 → T+5/T+10 交易日固定窗口收益（与 ranking_history 口径同源），对比 base vs decay 平均收益/胜率。
  3. 接入日批：`daily_batch.py` 加 `task_shadow_rank`（`from shadow_decay_ranking import run`），排在 rank_live 之后（读其精算五维分）。
- **★ 关键口径修正（dry-run 暴露）**：本地 regime 缓存为空 → 权重 fallback 默认权重，与 ranking_live 的 defensive 权重不一致 → base/decay 各一套权重、对比失真。修法：① 权重获取回退 `market_regime_history` 最新行；② 加「不衰减重算 vs ranking_live 总分」校验（实测平均差 0.05 分 OK）。
- **验证**：dry-run 市况=defensive、权重正确、校验 OK；生产 top30 vs 衰减 top30 **重叠 12/30**（符合预期——9 月中旬正是中报公告后 0-25 天窗口，生产 top 里大量"刚公告高成长股"被衰减掉榜，这正是灰度要验证的现象）。lint 0；`daily_batch --list` 已含 shadow_rank。
- **用法**：日批自动每天落库；两周后 `python scripts/compare_shadow_rank.py` 看两套 top30 收益对比（需 ≥10 个快照日才可信）。
- **⚠️ 待观察/风险**：α=0 的「×0」语义会让刚公告高成长股总分暴跌（growth+quality 权重 48% 贡献归零），重叠仅 12/30 偏激进；若两周后 decay 未优于 base，可调 α 或改「剔除（权重分摊给其他维度）」语义再试。另：shadow 榜依赖 ranking_live 当日落库（9/16 快照），新快照从日批接入后开始积累。
- **改动文件**：`scripts/shadow_decay_ranking.py`（新增）、`scripts/compare_shadow_rank.py`（新增）、`scripts/daily_batch.py`（+task_shadow_rank）。**未提交**。

## 🔧 修 bug：指标包漏打包 flow5_amt_yuan → 前端本地详情页「5日主力净流入」恒显示空（2026-09-17）
- **用户现象**：本地点 002452 详情页，「主力行为」区块「5日主力净流入」显示 '-'，但「占额累计 +23.91」有值；且获利盘 82%/筹码位置 85%/集中度 25%。
- **根因一（bug，已修）**：`mainforce_overlay` 已算 `flow5_amt_yuan`（近5日主力净流入金额元，2026-09-17 新增），但 `generate_backend_pack.py:compute_mainforce_batch` 打包指标包时**漏了这个字段** → 前端本地详情页读 `mainforce.flow5_amt_yuan` 恒 null → 显示 '-'。数据库 `mainforce_state` 里其实有值（002452 = 3268 万）。**修法**：打包输出 dict 补 `"flow5_amt_yuan": ov.get("flow5_amt_yuan")`。下次 backend-pack 重打包后前端即有值。
- **根因二（时序，非 bug）**：用户看到的 chip 82%/85%/25% 是「换手率单位 bug 修复前」的旧值；数据库 mainforce_state 9/16 已是修复后的 65.3%/55.5%/10.4%（`chips.py` 注释明确写了 002452 案例：82.0/25.4/84.8 → 65.3/10.4/55.5）。原因：前端本地读的是浏览器 IndexedDB 缓存的**旧指标包**（backend-pack 打包时 chips.py 还是修复前版本），等 backend-pack 重打包 + 前端刷新即更新。
- **字段口径备忘**：`flow5_amt` = 5日主力净流入占成交额%累加（评分因子，倒U锚点输入，改动需重标定）；`flow5_amt_yuan` = 5日主力净流入金额元（纯展示，可与行情软件对照）。两者口径不同、不可互换。
- **改动文件**：`scripts/generate_backend_pack.py`（+flow5_amt_yuan 打包）。**未提交**。

## 🖥 页面直接看两套排名（衰减对比 tab）（2026-09-17）
- **用户需求**：在排行榜页面直接看「生产 top30 vs 衰减 top30」两套排名。
- **实现**：① 后端 `routers/scoring.py` 加 `GET /score/batch/shadow-rank`（读 shadow_rank_daily 最新快照的 base/decay 两套，返回 date/base/decay/overlap）；② 前端 `api/index.js` 加 `getShadowRank`；③ `ScoreRank.vue` 加「衰减对比」tab（tabs 数组 + loadShadowRank + switchTab 分支 + 模板并列两表，绿色高亮衰减后新进榜，标注快照日期）。
- **★ 踩坑（动态路由拦截）**：接口最初写 `/score/shadow-rank`（单段），被 `@router.get("/{symbol}")`（907 行，个股详情）**抢先匹配** → 返回 `{"error":"未找到股票 shadow-rank"}`。FastAPI 按定义顺序匹配，单段 `/shadow-rank` 与 `/{symbol}` 冲突。修法：改两段 `/score/batch/shadow-rank`（与 batch/top 同段数，不冲突）。
- **★ 口径修正（快照价）**：初版 `get_prices` 用「今天」查 backtest_prices、且优先行情缓存，而 shadow 榜是 9/16 快照 → snapshot_price 全 None。修法：`get_prices(codes, rank_date)` 读快照日 backtest_prices 收盘价，与 compare 收益口径同源（买入价=快照日收盘、卖出=未来 T+N 收盘）。
- **验证**：接口 200，date=2026-09-16，base 30/decay 30/overlap 12；base[0]=000567(74.6, 快照价5.9, age20)，decay[0]=603986(72.9, 快照价373.14, age29)。lint 0。
- **数据说明**：shadow 榜为日批盘后快照（非实时），前端已标注快照日期；当前仅 9/16 一天数据，需日批 task_shadow_rank 持续落库后才有对比价值。
- **改动文件**：`backend/app/routers/scoring.py`（+shadow-rank 接口）、`frontend/src/api/index.js`、`frontend/src/views/ScoreRank.vue`、`scripts/shadow_decay_ranking.py`（快照价口径）。**未提交**。

## ⚡ 盘中两份 top50 实时榜（2026-09-17）
- **用户需求**：盘中也要两份 top50（生产 + 衰减）实时排行榜。
- **实现（零额外精算成本）**：把 `/score/batch/shadow-rank` 从「读盘后快照 shadow_rank_daily」改为**实时计算**——读 `ranking_live` 最新精算结果（全市场 ~1000 行五维分）+ `get_finance_batch`（notice_date 算公告龄）+ `get_regime_weights`（当前权重），复刻 `_combine` 做衰减重加权（公告后 ≤25 天成长/质量 ×0），返回 base/decay 两套 top N。前端默认拉 50。
- **★ 为什么「准实时」足够**：衰减对象（成长/质量）是季频慢变量、公告龄按自然日算，盘中不随时间变；用 ranking_live 最新（可能昨日盘后）算衰减，衰减本身不过时，与技术/资金/基本面（两套榜共通、日间波动）无关 → 无需盘中重跑全量精算。
- **验证**：接口 200，base 50/decay 50/overlap 8；base[0]=000567(74.6, age20)，decay[0]=002925(69.0, age26)。lint 0。
- **保留 shadow_rank_daily 落库**：盘后快照 + `compare_shadow_rank.py` 的两周收益对比仍走日批 task_shadow_rank（离线灰度验证衰减是否有效）；盘中实时榜只用于观察、不替代收益验证。
- **改动文件**：`backend/app/routers/scoring.py`（shadow-rank 改实时 + `_decay_total` helper）、`frontend/src/api/index.js`、`frontend/src/views/ScoreRank.vue`。**未提交**。

## 🔎 主力行为标签机制澄清（2026-09-17，回答用户"本地只有一个是正常的吗"）
- **★ 覆盖率天然很低（正常）**：`mainforce_state`（9/16）全市场 2028 只 → **None 1898 / accum 107 / distribution 23**，有标签仅 **130 只（6.4%）**；榜单 top50 里 8 只（16%）。原因：标签是**条件触发式**——吸筹区=低位筹码密集+主力净流入；出货嫌疑=高位高获利+主力流出；绝大多数股票处中间态 → 无标签。所以"榜单里只看到一两个"是正常现象，不是 bug。
- **前端两榜跳转行为**：主表（top tab）代码列是指向雪球的 `<a @click.stop>`（点代码→雪球，点行→详情页 goDetail）；衰减对比 tab 原为纯文本（点哪都跳详情），**已统一**为代码列雪球链接，两榜一致。
- **衰减对比 tab 已纳入盘中 60s 自动刷新**（startAutoRefresh 里 activeTab==='shadow' 走 loadShadowRank）。
- **对比榜加主力列**：后端 shadow-rank 返回每行 `mainforce_signal/mainforce_phase`（读 mainforce_state，与主榜同源）；前端两表加"主力"列（吸筹=绿/出货=红，无标签显示 '-'）。
- **改动文件**：`backend/app/routers/scoring.py`、`frontend/src/views/ScoreRank.vue`。**未提交**。

## 🔧 衰减口径二版：×0 归零 → 梯度剔除（用户质疑 000612 掉榜后）（2026-09-17）
- **用户质疑**：000612 公告后才 19 天，成长/质量 ×0 就直接掉榜了？
- **实测四种口径**（000612：技术 53.2/资金 53.8/基本面 75.8/成长 76.7/质量 86.3，defensive 权重，age 19）：
  | 口径 | 总分 | 结果 |
  |---|---|---|
  | ① 不衰减 | 73.6 | 生产第 2 |
  | ② **×0（初版）** | **33.4** | 暴跌 40 分、掉榜 |
  | ③ 剔除（权重分摊） | 64.2 | 贴近门槛也掉出 |
  | ④ 半衰 ×0.5 | 53.5 | 掉榜 |
  → **"掉榜"是衰减方向的必然**（该股高分全靠成长/质量撑，动这两维就下去）；但 **×0 的"判 0 分"语义过激**（把"信号不可信"判成"基本面极差"），且把所有刚公告股压到 30-40 分档、**分数尺度割裂**。
- **改动**：新增 `backend/app/scoring/decay.py`（衰减**单一事实源**）。二版口径 **grad = 梯度降权**：按公告龄调整成长/质量的**权重乘数**（0-5 天 → 0 剔除、6-20 天 → 0.5、>20 天 → 1.0）——语义中性（"不看"非"判 0 分"）、尺度连续；**保留 zero（×0）作对照 variant**。四处同步：接口 `/score/batch/shadow-rank`、`shadow_decay_ranking.py`（落 base/zero/grad 三套）、`compare_shadow_rank.py`（多 variant 对比）、`daily_batch.py`。
- **★ 两个 bug（dry-run/实测抓到，都已修）**：① `decay_total` 曾判 `mode=="gradient"` 而实传 `VARIANT_GRAD="grad"` → 字符串不匹配、**静默不衰减**（grad 重叠 15/15 暴露）；② 接口权重兜底只到默认权重、脚本兜底到历史表 → **两套权重算出不同榜单**（接口 overlap 13 vs 脚本 42）。修法：mode 取值与 variant 名统一 + 新增 `resolve_weights()`（regime 缓存→历史表→默认）两边共用。
- **效果**（000567 age20：base 74.6 / zero 33.4 掉榜 / grad 69.1 第 9）；top15 重叠 zero 4/15、grad 11/15；接口与脚本一致（overlap 42/50）。
- **改动文件**：`backend/app/scoring/decay.py`（新增）、`backend/app/routers/scoring.py`、`scripts/shadow_decay_ranking.py`、`scripts/compare_shadow_rank.py`、`scripts/daily_batch.py`。**未提交**。
- **页面三列（2026-09-17 续）**：接口 `/batch/shadow-rank` 改为返回 `{date, base, zero, grad, overlap{zero,grad}}` 三套；前端「衰减对比」tab 改为 `grid-cols-3` + `v-for="col in shadowCols"`（收敛了原来两份重复表），列序 = 生产 / 梯度(主) / ×0(对照)，非生产榜的"新进榜"行绿色高亮；列 `# / 代码 / 名称 / 分 / 主力 / 龄`。实测接口三榜各 50 行、overlap {zero:20, grad:42}。lint 0。**改动文件追加**：`frontend/src/views/ScoreRank.vue`。
- **★ 影子榜新鲜度（用户问"盘中实时吗"，重要）**：前端 60s 自动刷新 + 接口每次重算 ✓，**但数据基础 `ranking_live` 是盘后日批落库的**——盘中读到的是**昨日**快照。对比主榜：盘中主榜走 `_batch_with_precise_top` **实时精算**（今日行情），盘后主榜也直接读 ranking_live。→ **盘中两榜数据源不同**（差异仅技术/资金/基本面日内波动；成长/质量两维季频一致，三列内部对比公平）；**盘后两榜同源完全对齐**。若要盘中严格对齐需让影子榜复用主榜实时精算池（代价：盘中多跑 ~1000 只全池精算），暂未做。

## ⚡ 影子榜改「前端本地算」（采纳用户意见，2026-09-17）
- **用户质疑（正确）**：影子榜本就是前端本地评分模式，本地算不消耗后端性能与 Supabase 流量，我先前给的"多跑一次全池精算"是错算了后端路径。
- **核实**：前端 `computeRanking` 已经（1）`getFinanceBatch(全池)` 拉财报含 notice_date、（2）roughScore 全池取 **top150 候选**、（3）`preciseScoreBatch` 算五维分——**影子榜要的全都现成**，只是最后 `slice(0, limit)` 丢了中间结果。
- **改动（本地方案，零额外成本）**：① `utils/scoringEngine.js` 加 `decayTotal`/`decayWeightMult`（与后端 `decay.py` 同口径）；② `composables/useFrontendScoring.js` 加 `shadowBoards` ref + `buildShadowBoards()`，在 computeRanking 里用**同一次精算结果**顺带产出三套榜并导出；③ `ScoreRank.vue` 加 `shadowLocal()` 判定——本地模式走 `loadData()`（同源产出），否则走后端接口；`loadFrontendRanking` 的 mode 把 `shadow` 归为 `top` 候选池。
- **★ 前后端 parity 实测（node 直调 JS 模块，6/6 全对）**：000612（技术53.2/资金53.8/基本面75.8/成长76.7/质量86.3，defensive 权重）→ base 73.6、zero 33.4、grad(age19) 70.4、grad(age3) 64.2、grad(age25) 73.6、grad(null) 73.6，与 Python `decay.py` 完全一致。
- **覆盖度取舍**：本地精算池 = 150（后端 ranking_live ~1000）。**池子是主榜/影子榜共用的一次精算**，调大会同时拖慢主榜（300≈2×、1000≈6-7×）；且衰减让"财报强"的票**掉下去**，进榜的"低财报+高动量"票本就在 roughScore 前 150 → **暂不动池子**，真出现覆盖不足再给影子榜开独立大池（只在 shadow tab 打开时算）。
- **收益**：影子榜 = 盘中实时 + 与主榜**完全同源** + 零后端/Supabase 成本（原后端方案是盘中昨日快照 + 多跑一次精算）。
- **改动文件**：`frontend/src/utils/scoringEngine.js`、`frontend/src/composables/useFrontendScoring.js`、`frontend/src/views/ScoreRank.vue`。**未提交**。
- **影子榜加「涨跌」列（2026-09-17 续）**：三套榜每行展示涨跌幅（红涨绿跌，同主表）。字段来源：本地 `scoreStock` 返回本就含 `change_pct`（来自 `fetchRealtimeQuotes` 实时行情 → **盘中实时**），`buildShadowBoards` 透传；后端接口从 `ranking_live` payload 的 `change_pct` 取（盘后口径）。列序改为 `# / 代码 / 名称 / 涨跌 / 分 / 主力 / 龄`。实测接口返回正常（000612 +5.42%、002558 -0.84%），lint 0。

## 🐌 WAF 冷却复盘：45s 反而更慢，且是"方向错"（2026-09-17）
- **用户报告**：9-15 把 `PACK_WAF_COOLDOWN` 120→45s 后，今天后端包更慢，日志满屏 `[WAF]`、进度整段停滞。
- **★ 硬证据（统计用户日志）**：当天 `[WAF]` 拦截 **~117 次**（改前 workflow 注释记载「40+ 次」）；进度多次整段零成功 —— `506/2057` 连续 3 个 50 只窗口 0 成功、`967/2057` 连续 150 只仅 +6、第 2 轮 `1757` 连续 150 只 0 成功。→ **45s 没减少拦截，反而把封禁拉成连续惩罚**。
- **机制（代码级定位，`generate-kline-pack.py`）**：
  1. `_waf_blocked_until = time.time() + WAF_COOLDOWN` **无条件续期** —— 5 个并发 501 各推一次窗口，实际冷却 = 最后一条 501 + 45s（日志里 8~12 条连发即此）。
  2. 冷却中「睡一轮（≤冷却+5s + rand×25）→ 若窗口被续期则 `return None`」→ **每股白睡一整个冷却周期且被误记为失败**；这些「假失败」进重试轮再打一遍请求（第 1 轮 378 / 第 2 轮 193），把请求打到正烫的 IP 上 → 正反馈放大封禁。
  3. 抖动固定 `rand×25`，不随冷却缩放 → 5 线程被拦时刻差 <1s，错峰不足。
  4. 腾讯按**滑动窗口速率**判定：45s 静默排不空窗口 → 冷却结束立刻又被拦 → 连续惩罚；120s 静默够 → 能退出惩罚区（拦截 40+ vs 117 的差异即此）。
- **★ 已改（`scripts/generate-kline-pack.py`，5 处）**：① 新增 `WAF_MAX_COOLDOWN`（默认 300）+ **自适应退避**：非冷却期 501 才开新窗，**冷却期内再 501 就 ×2 加码**（45→90→180→300），任一成功回落基准；② `_waf_wait_until_clear()` 循环睡到真解除（替代「睡一轮就放弃 + `return None`」），期间不发请求=让窗口排空；③ 抖动随冷却缩放 `max(8, min(30, cur×0.4))`；④ 重试前停顿由固定 15s 改为「撞过 WAF 按退避倍数给足（≤180s）」；⑤ 跑完打印 `_waf_report()`（拦截次数 / 累计等待 / 末次退避）。
- **验证**：lint 0；模拟测试通过（45 基准 → 90 → 180 自动加码；`_waf_wait_until_clear` 睡到解除后正确返回）。
- **`backend-pack.yml`**：`PACK_WAF_COOLDOWN` 保持 45 作为**基准**（自适应会自行上调），注释已改写记录本次否证；要立刻回到已验证的 120 可 dispatch 填 120。
- **⚠️ 混淆因素**：池子 9-10 由 1431 扩到 2080（请求量 +45%），若「40+/103min」基线是扩池前测的，则「更慢」有一部分来自池子变大；但**拦截 117 vs 40+ 与池子无关**。下跑看 `_waf_report()` 数字即可定论。
- **通用教训**：「冷却时长」不是可以靠直觉砍的调参项 —— 对滑动窗口型限流，缩短静默**会让封禁变连续**。应交给自适应退避，并用硬指标（拦截次数/累计等待）而非体感做 A/B。

## 🤖 DeepSeek-R1「LLM 一直失败」根因：fast 档超时 600→90（2026-09-17，已修）
- **用户质疑**：「llm 的 deepseek r1 一直失败，你是不是把响应时间改小了？」→ **属实**。
- **改动定位**：9-14 `6cc0e02`（feat(llm): Provider 链）把原先**硬编码的 `_session.post(..., timeout=600)`** 拆成两档 —— `LLM_TIMEOUT_FAST=90` / `LLM_TIMEOUT_SLOW=600`。全项目**只有一处**用 fast 档：`trader_brief.py:374` 的**盘前简报**（`phase=="premarket"`）；盘后日报/周报/诊断仍 600s，未动。
- **真实生效配置（实测打印）**：`LLM_MODEL=deepseek-ai/DeepSeek-R1` @ `api.siliconflow.cn/v1`；免费站已配（`deepseek-v4-flash`）但 **`LLM_FREE_SHADOW=1` → 免费站不进正式链 ⇒ provider chain = `['main:DeepSeek-R1']` 单站**。
- **★ 失败链**：fast 档的 90s 是**为免费站（可关思考的快模型）设计的**，前提是"主力站只兜底"。而链首恰好就是 R1 → 盘前简报拿 90s 超时 → R1 思考分钟级 → `ReadTimeout` → 站内 3 次重试全超时 → `_provider_mark(main, False)` → 再来一次即 **fails=2 → 熔断 30min** → 熔断期内**连 600s 的盘后日报/周报都被"跳过"** → 这就是"一直失败"（不只是慢，是全链路瘫）。
- **★ 修复（`backend/app/flash/llm.py`，2 处）**：① `call_llm` 加**链首判定** —— fast 短超时仅在「链首是免费站」或「显式配置了 `LLM_TIMEOUT_FAST`」时生效，否则退回 slow（等价恢复 9-14 前的 600s），并打印降级原因；② `LLM_TIMEOUT_FAST` 默认 90 → **300** 兜底。
- **验证**：lint 0；实测三场景 —— `chain=main + fast → 600` ✓ / `slow → 600` ✓ / `显式 LLM_TIMEOUT_FAST=90 → 90` ✓（显式配置仍尊重）。
- **语义陷阱（记入 MEMORY.md）**：`_providers()` 里 main 的 `"reasoning": False` 语义是「不注入 reasoning_effort」，**不是**「非推理模型」—— R1 恰恰是推理模型，这个标记被当成模型类型用才会踩坑。
- **备选（未做，视需要）**：给 R1 配 `LLM_MODEL_FALLBACK`（非推理模型）作第 2 次重试兜底；或让**超时不触发 30min 熔断**（超时=慢≠坏，熔断会连累 slow 档）—— 需权衡"挂死 provider 不再熔断"的风险，故先不动。
- **改动文件**：`backend/app/flash/llm.py`。**未提交**。

## 🔄 数据源评估：东财 K 线替代腾讯（2026-09-17，已实测，方案否证）
- **用户提议**：腾讯 K 线不支持批量 + WAF 冷却把后端包拖到 2h → 改东方财富 `push2his.eastmoney.com/api/qt/stock/kline/get`（"一次请求拿整段历史、对量宽容"）。
- **★ 实测四条**：
  1. ✅ **"一次拿全"成立**：**`lmt` 参数被忽略**，永远返回全历史（600519 = 6007 根，自 2001-08-27）。字段顺序与腾讯兼容：`日期,开,收,高,低,量,额,振幅,涨跌幅,涨跌额,换手率`（前 6 项可直接对齐现有解析）。沪/深/指数/ETF 均可用（secid：`1.`=沪、`0.`=深）。
  2. ❌ **"对量宽容"不成立**：约 10~24 个无节流请求后 `push2his` 直接 `RemoteDisconnected`；换子域（`1.`/`82.push2his`）、加 `ut` token、`cb`/`_`、换 http **全部无效**；**等 150s 未恢复**。而 `push2.eastmoney.com/api/qt/clist/get` 与 `quote.eastmoney.com` 静态页同时 200 → 是 push2his 对**本 IP 的针对性封禁**。
  3. ⚠️ **项目自己的经验（`backend/app/eastmoney.py:126`）**：「主站封 IP（**高频触发后封 24~48h**）时切 delay 端点」→ 东财封禁是**天级**，一次误触发 = **两天没有 K 线**；远劣于腾讯 501 的分钟级软冷却。
  4. ⚠️ **★ 换源并未解决请求数问题（关键洞察）**：实测腾讯 K 线**也不支持批量**（分号拼接/逗号拼接/重复 param 全部返回空或只取最后一个）→ **两个源都是"一只股票一个请求"**，2057 只 = 2057 个请求。东财只是"**同样多的请求 + 响应大 8 倍**（6007 根 vs 腾讯 752 根/55.6KB）+ **封禁更狠**"。→ 该方案对根因（请求数 × 速率阈值）**无帮助**。
- **★ 换源的真正前置门槛 = 前复权口径**：前端包（`kline-pack`）走腾讯 qfq，若只把后端包换成东财 fqt=1 → **两套价格序列** → 本地评分与后端**全序列**对不上（项目已有"末根不一致 → 本地 68.8 / 后端 72.6"前例，换源会把这个从末根放大到全序列）。必须逐日比对后才谈换源。
- **产出**：`scripts/verify_kline_source.py`（正式验证工具；mode = compare/beg/size/stress，默认 1s 节流，检测到 RemoteDisconnected 立即停止并提示封禁 24-48h）。**因 IP 被封，`compare`（复权口径）未能完成 → 该结论待补**。临时探针 `_tmp_em_probe.py` 已删。
- **正确用法（若将来要用东财）**：**兜底源而非主源** —— 腾讯撞 WAF 冷却时只对那批失败股票走东财（量小 + 1s 节流 + `push2delay` 兜底），补完即退。
- **改动文件**：`scripts/verify_kline_source.py`（新增）。**未提交**。

## 📦 数据源评估：baostock（2026-09-17，实测，不适用）
- **提议背景**：文档站 `baostock.com` 是 JS 动态渲染，`web_fetch` 只拿到 "Loading..."（`home.md`/`pythonAPI.md` 均如此）→ 改为**直接实测**（本机已 `pip install baostock==0.9.3`）。
- **实测**：① 匿名 `bs.login()` 可用、无 key；② 数据好：600519 回溯到 **2006-01-04（5032 根）**、**末根=当日 2026-09-17**、字段丰富（`preclose/turn/tradestatus/pctChg/isST/peTTM/pbMRQ`）；③ ❌ **速度无优势**：**4.41s/只**（730 根）→ **2057 只 ≈ 151 分钟（2.5h）**，与现状（2h）同量级；瓶颈是行读取 ~2.8ms/行；④ ❌ **前复权口径与腾讯不一致**（600519 max 0.983%/mean 0.343%、000651 max 2.156%、000567 近 400 日为 0）；规律：末几根完全一致、越往前分歧越大。
- **结论**：**不能用** —— ① 不能混用（只换一端 → 两套序列 → 本地与后端**全序列**对不上）；② 不能当兜底补拉（同一只股今天腾讯补、明天它补 → 前复权基准变 → **假跳空**，比缺数据更糟）；③ 要换只能全量换 + 重建 `kline_cache`/`backtest_prices`，而速度又没改善 → 不划算。
- **真正价值**：**极好的离线校验源**（数据新/覆盖深/免费/无 WAF），可抽查腾讯有无漏根、复权异常；**不进主链路**。
- **未竟**：多进程并发吞吐测试（用户选后台跑、未取结果）；**不影响结论**（并发只解决"值不值得换"，口径才是"能不能换"）。

## 🔎 数据源评估：AKShare（2026-09-17，实测）
- **★ 核心纠正：AKShare 不是数据源，是"数据源的封装库"（聚合器）**：
  1. **读源码**（GitHub `akshare/stock_feature/stock_hist_em.py`）：最常用的 `stock_zh_a_hist` 底层 URL = **`https://push2his.eastmoney.com/api/qt/stock/kline/get`** —— **正是今天下午封我 IP 24-48h 的那个域名**，参数几乎一致（`secid/klt/fqt/beg/end/ut`）。
  2. **实测**（本机 akshare 1.18.78）：`ak.stock_zh_a_hist(...)` 报**同一个错** `RemoteDisconnected`；而 `ak.stock_zh_a_daily(symbol='sh600519', adjust='qfq')`（**新浪**路径）**成功**。→ **换 AKShare 不换源**，只多一层依赖。
  3. 源码里 `stock_zh_a_hist` **无 sleep / 无重试 / 无节流 / 无 UA 伪装** → 在 2000 只规模上直接跑，**比自建更容易打死 IP**。
- **但它多包了一条新浪路（有价值的副产品）**：`stock_zh_a_daily` 实测 **0.66s/只**（900 根/3.5 年）→ **2057 只 ≈ 22.7 分钟**，比腾讯 2h **快约 5 倍**；末根到当日；字段 `date,open,high,low,close,volume,amount,outstanding_share,turnover`。
- **★ 口径差异 + 一个更本质的发现**：新浪/baostock 各自与腾讯的差异**几乎逐位相同**（600519：0.982% vs 0.983%；000651：2.167% vs 2.156%；000567 近 400 日均为 0）→ 不是它们各有偏差，而是**腾讯的前复权口径偏离标准**（新浪/baostock 走标准除权处理）。**项目现行 K 线口径是"少数派"。**
- **★ 日收益率差异（决定性，评分/回测用的就是收益率）**：新浪 vs 腾讯 751 日 —— 600519 max 0.787%/mean 0.053%（110/751 日 >0.1%）；000001 max 2.470%（242/751）；**000651 max 2.383%/mean 0.160%（330/751 = 44%）**；601988 max 0.885%；600276 max 0.143%（仅 1 天）；002452 max 0.533%；000567 max 0.472%（价格近 400 日一致、更早 351 日有差）；600036 max 1.209%。
  → **差异量级与项目要测的 alpha 同阶（IC 0.02~0.2）→ 不能静默换源**（会污染指标与回测）。
- **结论**：**AKShare 不是"换数据源"的方案**（换不了源）；**但是"新数据源"的省力入口** —— 封装了本项目没有的另类数据（股东户数/龙虎榜明细/财务指标/板块/宏观），正好可补前面记的 B 类因子（质押比例、经营现金流）。**用它必须自己加节流**。
- **若认真考虑去腾讯化**：现象指向"腾讯偏离标准口径"，换源可能是**纠正**而非将就；但需单独论证 + 三处同源全量换（前端包/后端包/`backtest_prices`）+ 重建历史 → 大工程，**不进本轮**。

## 📉 WAF 外部方案评估 + 主动限速落地（2026-09-17）
- **背景**：用户贴来一份外部方案（称"日志看总耗时超 8 小时、WAF 冷却累计 29572s、占 97%"）要求评估。
- **★ 先纠正其核心论据（也是我的指标缺陷）**：`_waf_wait_total` 是**跨线程累加（线程·秒）**，**不是墙钟** —— 5 线程并行冷却时约等于墙钟的 5 倍。29572 线程·秒 ≈ 墙钟 1.6 小时；若总耗时真是 8h，冷却墙钟占比约 20%，**不是 97%**。→ **已修 `_waf_report()`**：同时输出墙钟与"平均并行等待线程数（占并发 %）"，杜绝再被误读。
- **方案事实核查（与实测/项目现状矛盾处）**：
  | 方案主张 | 核查 |
  |---|---|
  | 东财阈值 ">5 req/s、并发≥10、1 分钟≥200 次" | ❌ 与实测严重不符：**约 10~24 个无节流请求即被封**；且本轮讨论的是腾讯不是东财 |
  | "腾讯 5000+ 次后返回空数据、是限流非封 IP" | ❌ 与项目日志矛盾：实际是 **HTTP 501**（WAF 拦截），非空数据 |
  | "日志中东财被封时首只 10.9s 完成检测+降级、之后每只 0.4s 走腾讯" | ❌ **编造**：项目无多源降级逻辑；且 0.4s/只会与"2 小时"现状自相矛盾 |
  | "新浪 getKLineData 不复权 ⇒ 新浪无前复权" | ❌ 不准确：AKShare `stock_zh_a_daily` 走新浪且**提供前复权**（已实测） |
  | "K 线历史不变 ⇒ 只拉增量" | ❌ **前复权序列的历史每天都在变**（除权后整条重算）→ 该条对前复权不成立，照做会错 |
  | Tushare "免费可稳定取日 K" 与 "捐助 200 元/年才有 daily" | ⚠️ 自相矛盾 |
  | baostock "稳定性较好" | ⚠️ 未提口径不一致 + 实测 4.41s/只（**更慢**） |
  | "本地缓存 + 增量拉取" | ❌ 项目**早已做**（pack 复用 / `kline_cache` / 日批"输入复用"原则） |
  | `curl_cffi` TLS/JA3 指纹 | ❓ **不能证伪、值得试**但理由不足：日志中的拒绝是 **HTTP 501（应用层响应）**而非 TCP 断开/403 → 请求已过 TLS 指纹层；优先级低 |
  | 代理池（P3） | ⚠️ GitHub Actions 出口 IP 每次随机，IP 封禁本就不跨 job 累积 |
  - **✅ 方向正确的部分（已采纳）**：降低请求速率、避免"触发→冷却→再触发"自锁循环 —— 这是方案里唯一真正对症的。
- **★ 我上轮修复引入的退化（必须认）**：把"睡一轮就放弃"改成"循环睡到解除"后，若窗口被反复续期，线程会被一支股票**无限占住**（退避升到 300s 时吞吐掉到 5 只/300s ≈ 1 只/分钟）。
- **★ 本轮改动（`scripts/generate-kline-pack.py`，5 处）**：
  1. **主动限速 = 全局令牌桶**：`PACK_QPS`（默认 2，无突发匀速），`_rate_limit()` 每次请求前调用 —— **与并发数解耦**，才是打不破阈值的关键。实测 5 线程 × 4 次 = 20 请求 @2QPS = **9.50s**（并发没能把速率推上去）✓
  2. `_throttle_concurrent` 取消每线程 sleep（避免与令牌桶叠加成双重节流、让 QPS 旋钮失效），仅留 0~0.05s 抖动
  3. `_waf_wait_until_clear()` 加**累计上限** `WAF_WAIT_MAX`（默认 900s），超限返回 False → 该股记失败交下一轮，不再无限占线程
  4. 抖动由最多 30s 收紧到 1~5s（错峰职责已交给令牌桶；实测原 3s 阻塞被抖动拖到 19.5s），并**夹在预算内**（原先抖动会冲掉上限：cap=2s 实测等 8.5s，已修 → 精确 2.0s）
  5. `_waf_report()` 口径修正（见上）
- **workflow**：新增 `qps` dispatch 输入 + `PACK_QPS` env（注释写明它是主旋钮、2 QPS = 120 请求/分钟、2057 只理论下限 ~17 分钟）。
- **验证**：lint 0 / py_compile OK；令牌桶速率精确（7 次 @2QPS = 3.00s）；5 线程 20 次 = 9.50s；等待上限 2.0s 精确触发；正常路径抖动仍生效。
- **预期与判据**：QPS=2 若不再触发拦截 → 2057 只约 17 分钟（+重试）≈ 20-25 分钟；若仍有拦截 → 降到 1；若全程零拦截且嫌慢 → 试 3~4。**判据看日志 `WAF 汇总:` 一行**（拦截次数 / 平均并行等待线程数 / 限速值）。
- **改动文件**：`scripts/generate-kline-pack.py`、`.github/workflows/backend-pack.yml`。**未提交**。

## 🔖 查证：603489「主力吸筹」标签 + 当前权重占比（2026-09-17，只读）
- **当前市况 = `defensive`**（`market_regime_history` 9/17 `regime_score=-74.56`；9/15 起进入，9/11~9/14 为 `neutral_bearish`）。`get_regime_weights` 四档（`market_regime.py:523-535`）：
  | 维度 | **defensive（现役）** | neutral / neutral_bearish | offensive | 引擎默认（regime 缺失兜底） |
  |---|---|---|---|---|
  | 质量 quality | **0.35** | 0.11 | 0.05 | 0.12 |
  | 基本面 fundamental | **0.25** | 0.23 | 0.08 | 0.18 |
  | 资金面 capital | **0.15** | 0.23 | 0.25 | 0.20 |
  | 成长 growth | **0.13** | 0.17 | 0.12 | 0.18 |
  | 技术面 technical | **0.12** | 0.27 | 0.50 | 0.32 |
  → 现役 defensive：**季频财报因子（质量+成长）= 48%**、日频（技术+资金）= 27%、基本面 25%。验证：603489 五维 64.7/57.7/66.2/80.0/67.7 按此加权 = **67.06**，与榜单 67.2 吻合 ✓（口径确认无误）。
- **603489 = 八方股份。其 `accum` 标签在规则上是「对的」**：判定式（`mainforce/overlay.py:110-131`）`accumulation = (price_pos<0.35 and concentration<0.25) and flow5_amt>0`；9/17 实测 `price_pos=0.3075 / concentration=0.2202 / flow5_amt=+22.53(2282万)` → 三条全满足，且近 5 日一致（price_pos 0.12~0.31、concentration 0.20~0.22、flow 全正）。注意 `phase` 9/15 起已由 accumulation→sideways 而 signal 不变 —— 因 accum **不看 phase**，只看 chip+flow（设计如此）。
- **★ 但该规则的可靠性，项目自己已打折（内部矛盾）**：`overlay.py` 头部自述结论 —— `concentration` 修复后 **IC 反号(+0.04)「弱因子，不宜作为阈值条件」**，可它**恰恰是 accum 的必要条件之一**；且明写「**买侧组合仅输出标签（accumulate/watch），不参与排序**」、`MAINFORCE_MODE` 默认 `off`；不对称现实：**识别出货灵（去超额 5日 -10.25 / 10日 -15.23，n=32），识别吸筹不灵（仅 +1.1pt）**。
- **★★ 顺带发现的真实缺口：`ranking_history.mainforce_signal` 近 8 个日期 7 天全空**（9/08~9/17 每日 50 行）：9/09 有 10 行，其余 **tagged=0**。而 `mainforce_state` 表标签齐全 → **排行榜页面的"主力"是实时算的（`ranking_live` 路径），但 `BucketStats`（吸筹/出货分桶胜率验证）读的正是这一列**（`ranking_history.py:586-591`、`626-627`）→ **该验证实质处于「无样本」状态**。与代码注释记载吻合：9/11 修过一次、9/13 体检发现"修复未生效"、注释称"09-04/07/09 有标签是旧路径残留"—— 本次实测证实**仍未生效**（9/14 起全空）。→ 待排查（写入路径 `scoring.py:737` 传 `mainforce` dict vs `ranking_history.py:141` 读 `mainforce`，但日批 `replace_day` 覆盖后依然为空，说明**日批那条路径也没带上**）。

## 🚫 评估用户提议「吸筹埋伏」功能（2026-09-17，只读，不改代码）
- **用户提议**：加一个"吸筹埋伏"功能（在吸筹阶段提前建仓等拉升）。**我的结论：不建议按此形态做**，理由四条：
  1. **功能已存在** —— `mainforce/trade_gate.py` 就是它：「条件A 有根据=主力吸筹区 / 条件B 不追高 / 条件C 状态允许」，输出档位化建议仓位（defensive 0% / nb 20% / neutral 50% / offensive 80%；吸筹+不拥挤+非防御 → 至少半仓 80% 封顶）。**且当前市况=defensive → `REGIME_POSITION["defensive"]=0` → 这个功能现在就会直接输出「禁止」**。用户要的可能不是"新增"，而是"改用它/把它可视化"。
  2. **"埋伏"踩项目自己的红线** —— 北极星写死：「跟对主力节奏，但永远慢一步——识别状态、顺应方向、**不猜时点**」；「③时点…样本少→仅形态参考，**永不进决策链**」；「**不抢跑未发生的节奏**」。"埋伏"= 抢跑未发生的节奏。**"吸筹状态"本身属①（有背书可决策），但"埋伏"这个用法是③。**
  3. **吸筹侧实证极弱 + 存在已知设计缺陷**：`overlay.py` 记录去超额 **5 日 +0.24pt / 10 日 +0.69pt（n=1748）**（trade_gate 注释里的 "+1.1pt" 是修复前旧值）→ 项目自己判「**买侧仅输出标签、不参与排序**」、`MAINFORCE_MODE` 默认 off。**更关键**：项目已实测「主力资金流与未来收益呈**倒 U**：温和净流入最优、**极端流入最差**（单看大单流入去超额 IC -0.10~-0.13）」—— 而 `accum` 判定只是 `flow5_amt > 0`，**不区分温和/极端** → 埋伏会系统性命中已知最差的那一类。→ **低成本改进**：把 flow 条件改成"温和区间"（0 < flow5 < 阈值），可能救回吸筹侧。
  4. **验证数据是空的** —— 上一节查实 `ranking_history.mainforce_signal` 近 8 天 7 天 tagged=0 → 该信号**从未被真正统计过**；在没测过的信号上加功能 = 加杠杆。
- **建设性替代（按优先级）**：
  - **A（推荐，零成本）**：不叫"埋伏"，做「**条件就绪清单**」—— 复用 `trade_gate.evaluate()` 的 A/B/C 三条件可视化（如 A✓B✓C✗ → "等状态"），**不产生买入信号**。语义 = "准备好了但还没到出手时候"，与"慢一步"一致。
  - **B**：先修 `ranking_history.mainforce_signal` 落库 + 跑「吸筹标签 → 未来 5/10 日收益」分层统计（本体/去超额/控制 regime）→ 显著再谈。
  - **C**：**只做出货侧**（项目已验证有效的那半：去超额 5日 -10.25 / 10日 -15.23pt）——"回避"比"埋伏"证据强得多，且符合「防守优先」座基。
- **附带说明**：观察池容器**早已存在**（`user_watchlist` 表、`/api/strategies/{name}/watch`、`strategies/base.save_watch_pool`），不需要新建；排行榜前端已有"主力"列，所以"看到吸筹就关注"这个动作**现在就能做**，无需开发。

## ✅ 落地：买入条件就绪（原「吸筹埋伏」的合规形态）（2026-09-17，用户已同意）
- **定位声明（写进代码注释）**：**状态展示，非买入信号** —— 不参与排序、不改总分、不新增信号、不新增网络请求。语义 = "准备好了吗 / 还没到出手时候"，与北极星「识别状态、不猜时点、不抢跑未发生的节奏」一致。
- **后端 3 处**：
  1. `mainforce/trade_gate.py` 新增 `summarize(result)` → `{ready(0~3), total, items[{key,ok,label}], label, hint, position_pct, position_label, regime, active}`；label ∈ 三条件就绪 / 等状态 / 缺主力根据 / 追高 / 不满足。
  2. 同文件 `_today_contradictions()` 加 **30s 进程缓存**（原每次 evaluate 读 L1+L2 两遍 → 榜单 50 只 = 100 次 DB 查询）。
  3. `routers/scoring.py` 两处挂载：`/api/score/batch/top`（复用已批量加载的 `_mf` → 零额外 DB）+ `/api/score/{symbol}`（新增 `gate` 字段）。
- **前端 2 处**：`StockDetail.vue` 在「主力行为」区块内加「买入条件就绪」块（3 个 ✓/✗ 徽标 + hint + 建议仓位口径）；`ScoreRank.vue` 在「主力」列加 `N/3` 徽标 + tooltip（**不加新列**，避免表格过挤）。
- **★ 验证（全通过）**：lint 0（4 文件）；`/api/score/603489` → `{ready:2, label:'等状态', items:A✓B✓C✗, position_pct:0, position_label:'禁止', regime:'defensive'}`；`/batch/top?limit=30` → **30/30 全部带 gate**（002452 `---` 因获利盘 82% 追高、603986/002466 `++-` 等状态）；`npx vite build` → **✓ built in 13.34s（699 模块）**。
- **603489 是完美范例**：A✓B✓C✗ =「主力有根据、不追高，但市况 defensive 不允许」→ **"等状态" + 建议仓位 0%（禁止）**。这正是想表达的"准备好了但还不能出手"。
- **改动文件**：`backend/app/mainforce/trade_gate.py`、`backend/app/routers/scoring.py`、`frontend/src/views/StockDetail.vue`、`frontend/src/views/ScoreRank.vue`。临时脚本已删（本地 vite build 重写了 `frontend/dist`，该目录应为 gitignore）。**未提交**。
- **未做但值得做（两项，留待用户决定）**：① 修 `ranking_history.mainforce_signal` 落库缺口（吸筹/出货分桶验证现在分母为 0）；② 按项目已有的"主力资金流倒 U（温和流入最优、极端最差）"结论，把 `overlay.accum` 的 `flow5_amt > 0` 改成**温和区间**判定 —— 这是让吸筹侧从 +0.24pt 变好的最低成本改动。

## 🚨 本地开发烧 600MB Supabase egress：根因与启动器（2026-09-17）
- **用户报告**：昨天 Egress 600MB，怀疑是本地开发导致，不敢再本地开发。
- **★ 根因（当场复现）**：`backend/.env` 是 **`DATA_SOURCE=local`**，而本地包 `pack_date=20260916 < 应可用 2026-09-17` → `pack_source._is_stale()`=True → `get_klines()` 返回 None（"宁缺毋旧"）→ **各调用方静默回退查 Supabase**（K线/指标/backtest_prices 全是大表）。**`local` 模式没有任何自动重下** → 会一直回退到手动同步为止。
- **★ 本次新定位的口径矛盾（复发根因）**：两个新鲜度判据不一致 ——
  · `_is_stale()` 用**交易日历**（`_PACK_READY_HHMM=(22,0)`：交易日 22:00 后要求"当天"包）
  · 重下用 `_db_fresh()` 判 **mtime<30h**
  → mtime 还"新"时**不会重下**，但日历已判陈旧 → 静默回退。**每天 22:00 后到文件变旧之前是必然的泄漏窗口**。`pack_source.py:363-371` 已记录 9/12 的 596MB 同型事故（这是第 3 次）。
- **★ 产出 `scripts/dev_local.py`**（严格按顺序，**顺序不可颠倒**）：
  ① **最先**设 `DATA_SOURCE=local`（在 import `app.*` 之前，只影响本进程与子进程，不改 .env）；
  ② 预检 `pack_date >= _latest_available_pack_day()` —— **判据直接 import 复用 pack_source**（刻意不自己写近似公式，避免"自检通过、运行时仍回退"的假安全）；
  ③ 不满足 → 从 **GitHub Pages** 重下（零 Supabase），**且必须 `force=True`**（sync_local 默认「mtime<20h 跳过下载」与日历判据不一致，沿用会白跑）；
  ④ 复检仍不满足 → **硬失败退出（拒绝启动）**，绝不带病启动。
  用法：`python scripts/dev_local.py`（默认启动）/ `--check`（只报告，**exit 2 = 陈旧**）/ `--no-start` / `--readonly`（加 `RENDER_READ_ONLY=1`）/ `--force-sync` / `--port`。
- **验证**：`--check` 当场复现陈旧（exit 2）→ `--no-start` 全流程走通：下载 44.7MB（GitHub Pages，实测仅 0.09MB/s、约 8.5min，比注释里的 0.82MB/s 慢）→ 解压 180.7MB → 复检 `pack_date=20260917 >= 应可用 2026-09-17` **通过** ✓。**用户环境已修复（当前不再回退）**。
- **环境事实**：`backend/.env`：`DATA_SOURCE=local` + `DATABASE_URL` 指向生产 postgres（业务小表走 Supabase 量小；**但大表回退时全打这里**）。
- **建议后续（未做，防第 4 次复发）**：让 `pack_source` 的新鲜度只留**一个**判据（统一 `_db_fresh()` 的 mtime 阈值与 `_pack_outdated()`），或让 `local` 模式也自动重下。
- **改动文件**：`scripts/dev_local.py`（新增）。**未提交**。
- **★ 追加：把关也做进了 `backend/run.py`（用户问"直接跑 run.py 可以吗"）**。`run.py` 原只有一行 `uvicorn.run(..., reload=True)` —— 读 `.env` 所以模式对，但**不做包新鲜度把关**。新增 `_preflight()`（**fail-closed**）：先 `load_dotenv`（必须早于 `import app.*`）→ 读 `DATA_SOURCE`；非 `local/pack` 直接放行；否则**直接调用 `pack_source._is_stale()`（同一条判据，刻意不写近似公式）**，陈旧则打印醒目告警 + `sys.exit(1)` **拒绝启动**。**安全窗口说明**：同步后安全到「下一个交易日 22:00」—— 例如 9/17 23:15 同步了 9/17 包 → 安全到 **9/18 22:00**；之后本地包判陈旧（而那时新包已发布，同步即可解，不会死锁）。`reload=True` 会放大泄漏（每次改代码重启都重新触发一轮读取），所以要挡。
  - 验证：lint 0；正常路径 `[preflight] 数据包 OK：pack_date=20260917 (DATA_SOURCE=local)` ✓；模拟陈旧 → 打印告警并 `exit code = 1` ✓。
  - **改动文件追加**：`backend/run.py`。
