- 北向资金接入日报（commit `2b70c1f`）：新增 `_northbound()` 函数读 `eastmoney.get_northbound()`，在日报 1.3 节显示当日净流入（沪/深拆分），LLM prompt 加约束要求点评北向动向。休市时显示 +0.0 亿属正常。
- 模拟盘 timeout_no_trigger 分析结论：5 笔全因"止损止盈均未触发，盘后兜底离场"，是震荡偏空环境下战法信号质量差的预期表现，非 bug。MAX_HOLD_DAYS=20 天未走出方向即强制离场。
- **ETF 字段 bug 修复**（commit `a4689cd`）：日报 `_etf_pulse()` 读 `h.get("change_pct")` 但 `get_etf_quotes()` 实际返回字段是 `change`，导致 ETF 区块恒空。修复后读 `change` 并兼容 `change_pct`，同时盘后实时源空时回退 `store.load_etf_close()` 收盘快照。修复后"指数红/个股绿"结构失真判断首次触发（沪深300 +0.02% vs 个股平均 -0.29%）。
- **日报 LLM 结构化输出 + 前端阅读页**（commit `76d5f3d`）：
  - LLM 解读改为 JSON 模式（`llm._call_json`），代码层 `_render_llm_json` 渲染成 markdown，防幻觉校验：持仓 code 必须在真实持仓集合、note 含买卖指令词则剔除、key_level 必须为正数。JSON 失败回退纯 markdown `_llm_interpret_markdown`。
  - 后端新增 `routers/daily_report.py`（`/api/report/list` + `/api/report/daily`），main.py 注册 prefix `/api/report`。
  - 前端新增 `views/DailyReport.vue`（左日期列表 + 右 markdown 正文，markdown-it 渲染含表格样式），路由 `/report`，导航"每日日报"。
- **日报关闭企微推送**（commit `c48755a`）：`run_daily_report(push=False)` 默认不推（scheduler 无参调用即不推），需要时 `push=True` 或 `DAILY_REPORT_PUSH=1` 显式开启；脚本 `--no-push` 改为 `--push`。日报改由前端 `/report` 页面阅读。
- **用户偏好**：日报不需要推送到企微，前端页面查看即可（避免每日刷屏）。
