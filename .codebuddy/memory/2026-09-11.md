# 2026-09-11

## 📋 下一阶段计划已重写（PLAN_NEXT_PHASE.md，d840702）
- 结构：**背景（09-10/09-11 暴露的 8 个数据面问题 + 对应 commit）→ P0（今天~明早：Supabase 配额决策 + 今晚 5 个验收数字 + 前端验证 + 两项配置补齐）→ P1 周末（域名+命名隧道 → DB 切换 → 外部写入方 DB 访问三方案 α/β/γ → 前端切换 → Render 下线 → 备份外迁）→ P1 本周（简报自动化 / 榜单单一事实源 / backtest_prices 读放大 / 回填配额）→ P2 本月（承接 09-05 版未完成项）→ 观察清单 → 风险回滚表**。
- 关键待办（用户侧）：① GitHub secrets 补 `LLM_BASE_URL`/`LLM_MODEL` ② Render 确认部署到最新 commit ③ Supabase 流量面板判读（决定 Pro / 迁库 / 降级）④ 域名（决定能否进 P1 周末闭环）。
- 今晚验收 5 个数字：backend-pack「战法池口径覆盖 2052/2052」· kline-data「末根补齐」· daily-batch「[2/15] 市场状态权重」+「[12/15] 全量精算榜 300 行落库」· 无 `[WAF]`/`判过期走实时拉取`/`数据包日期 != 今天`。

## ✅ \"本地 68.8 vs 后端 72.6\" + \"K 线只到 9 日\" 定位与修复（041d552 已推送）
- **实锤**：000567 海德股份 前端包 kline-pack-latest（09-10 18:00 生成，123 根）末根 **09-09**；后端包 backend-pack.db（09-10 20:30 生成，752 根）末根 **09-10**。→ 本地图少一根、本地评分用 09-09 口径，后端用 09-10 → 差 3.8 分。**不是前后端评分引擎口径分歧**。
- **根因**：前端包 739/1534 只（48%）末根停在上一交易日，791 只正常；后端包 1425/1431 正常。同一接口（腾讯 fqkline qfqday）、同一交易日，唯一差异是**拉取时刻**——腾讯复权序列收盘后是**逐只**补齐的，18:00 那一轮近半数还没补完，20:30 那轮基本齐全。（另有 5 只 extremes 是停牌股：如 605577 末根 09-08，属正常。）
- **修复两处**：① `generate-kline-pack.py` 新增 `refetch_stale_lastbar()`：对末根 < 池内最新交易日的股票补拉（最多 2 轮，复用 WAF 冷却/退避；>90% 都落后则判定"当日数据未发布"跳过），kline-data.yml 前端 job 超时 60→90 分钟；② 前端 `loadLocalKline` 加**包新鲜度门**：本地包日期 < `expectedPackDate()`（工作日 15:00 前算上一工作日）→ 返回 null 回退后端接口（否则"旧本地压过新后端"）。
- 备注：前端包日期=**生成日**（非末根日期），所以停牌股不会误触发新鲜度门；节假日 18:00 也会生成当日包（包日期=节日当天）→ 门不误判。
- **002452 本地榜上有、后端榜没有 —— 是「两阶段候选池」截断（非数据问题）**：002452 在两包里都有（末根都到 09-10、流通市值 62.04 亿过 50 亿门槛）；后端单只精算 **69.8 分**（技术 64.4/资金 58.5/基本面 76.7/成长 72.2/质量 88.0），但 `/batch/top?limit=200`（池 1561）里没有它。本地复算简化分：**第 352/1561 名（61.6 分）** → `_batch_with_precise_top` 的候选池 = 简化分前 `limit+margin`（limit=50 → 前 100）→ **它从未被精算过**，所以永不入榜；前端本地是"包内全量精算后排序"，所以它上榜。简化分只看 动量+换手+PE（当日 -3.07% → 动能弱），而它的高分来自基本面/质量（PE、ROE、毛利率），简化分不参与 → 出现"精算 69.8 / 简化 61.6"的错位。
- **修法（待定）**：A 放大候选池（margin 50→~400，覆盖简化分前 ~450）——改动最小，代价是每次精算数 100→450（结果有 3 分钟缓存）；B（推荐）日批在 Actions 全量精算榜单落库/pack，后端直接读，与前端本地口径完全一致；C 前端以本地全量精算为准。
- **顺带发现（待核实）**：`/api/score/{code}`（详情）返回的资金面只有 4 个因子（量价配合/涨跌动能/换手率/成交额），**没有主力5日净流入**；而榜单路径（score_top）显式注入 `flow5_amt` → 详情分与榜单分本身也可能不一致。

## ✅ 阶段 1 完成：数据已迁到腾讯云本机 Postgres（冷备库，190c988）
- 用户选择：**暂不买域名** → 只做阶段 1（迁数据当冷备/备用库），前端继续吃 Supabase，接受 402 一来切本地模式。
- **服务器现状**：`stock-postgres`（**postgres:17-alpine**，healthy，`expose` 内网 only，未映射宿主端口）+ `stock-backend`（仍连 Supabase，未切）；数据卷 `deploy_pgdata`；库大小 **138MB**。
- **迁移结果（源 Supabase vs 目标，逐项一致）**：backtest_prices 478,795 / mainflow_history 81,735 / mainforce_state 4,459 / kline_cache 2,220 / indicator_cache 1,144 / ranking_history 850 / flash_news 300 / paper_positions 67 / user_watchlist 5 / user_portfolio 6。
- **踩坑三连（都已修进脚本 migrate_supabase.sh）**：① Supabase 是 **PG 17.6**，pg_dump 16 直接 "server version mismatch" 中止 → 客户端与目标库统一 **17**（16 数据卷不能给 17 用，需删卷重建）；② dump 里有 `CREATE EXTENSION ... WITH SCHEMA extensions` → 必须先 `CREATE SCHEMA IF NOT EXISTS extensions`；③ **`--exit-on-error` 会让 Supabase 平台对象（pgrst_ddl_watch / grant_pg_net_access / pg_stat_statements）直接中断整个恢复** → 去掉，以行数比对为准（实测 ignored errors: 9，数据全对）。
- **备份**：cron `0 3 * * * bash /opt/stock-scoring-v2/deploy/backup_db.sh` 已配（保留 14 份），并已立刻验证一份 `deploy/backups/stockapp_20260911_0141.dump (31M)`。敏感临时文件（`/tmp/supabase_url.txt` 含密码、dump）已清理。
- **应急（402 到达时）**：① 排行榜页开「本地计算」手动开关（本地包在 Pages，零 API，模式会持久化）② 要真正启用服务器库：重跑迁移同步 → `backend/.env` 的 `DATABASE_URL=postgresql://stockapp:<POSTGRES_PASSWORD>@postgres:5432/stockapp` → `docker compose restart backend`（注意：这只让服务器自己能读；Render/Actions 仍连 Supabase，需域名 + 隧道才能一起迁）。
- **域名到位后的完整顺序**：重跑 migrate（冷备库会落后）→ 切 DATABASE_URL → 命名隧道（API + 可选 db 通道）→ 前端 `VITE_API_BASE_URL` 指过去 → Render 下线观察。
- **引号地狱教训（再次验证）**：PowerShell→ssh→bash 内联带引号/括号的命令必坏（`$( )`、`|`、`( )` 都会被本地或远端吃掉一层）——**一律本地写 .sh（`-join "`n"` + `-NoNewline` 保证 LF）→ scp → 简单命令执行**，这次绕了三轮才对。

## 🅱️ B 方案（数据库迁腾讯云）材料就绪（0e84f91 已推送）+ 关键卡点
- 用户决定 **B（Supabase → 自建 Postgres）**，09-11 是 402 宽限期最后一天。
- **已交付**：`deploy/docker-compose.prod.yml` 新增 `postgres:16-alpine`（数据卷 pgdata、healthcheck、2C2G 内存参数、`expose` 不映射端口 → 只有 backend 内网可达，5432 永不公网）+ backend `depends_on: service_healthy`；`deploy/migrate_supabase.sh` 重写（**免装 postgresql-client**：pg_dump/pg_restore 走一次性容器；自动探测容器；源/目标**行数比对**）；`deploy/backup_db.sh` 容器名自动探测（原写死 `deploy-postgres-1` → 备份一直失败）；`deploy/env.template` 补 `POSTGRES_DB/USER/PASSWORD`。
- **🔴 关键卡点（决定顺序）**：库搬到服务器后，**三个外部写入方**都要访问它——① GitHub Actions 日批（写快照/资金流/龙虎榜/模拟盘）② backend-pack 包生成（读 kline_cache/market_snapshot、写 mainflow）③ Render（前端 API）。Postgres 不能开公网，所以只有两条路，**都需要一个域名（¥30-60/年，接入 Cloudflare）**：
  - 路线 1（彻底）：API + 日批都搬到腾讯云（服务器自己写自己的库），Actions 只留不碰库的 kline-data；backend-pack 需改造或搬走。
  - 路线 2（快）：Cloudflare **命名隧道** 暴露 `db.<域名>:5432`（TLS + 服务令牌，客户端 `cloudflared access tcp`）给 Actions/Render 用。
  - 国内地域 80/443 需 ICP 备案 → 隧道（出站）是不备案的唯一出路；快速隧道随机域名不能当生产。
- **应急**：若 402 先到、域名未就绪 → 前端切回**本地模式**（K线/指标包在 Pages，不走 API），核心评分可用，仅缺服务端写入/日报/模拟盘。
- 备注：包生成读的 `supabase_kline_codes()`/`market_snapshot` 是迁库后需要一并改地址的点（同 DB 连接串，无需改代码）。

## ✅ 日批全量日志复盘：13/13 成功，暴露 3 个新问题（6fd1d7b 已推送其一）
- 用户贴的是 **09-10 晚那批**（包 09-09 陈旧）的完整日志，**13 个任务全成功**（此前我推测"strategy_scan 中断"是错的——它没中断，只是拿了旧包）。任务表当时还没有 `rank_live`/`market_regime`。
- **① 市场状态判定停在 2026-09-08（已修 6fd1d7b）**：`regime_cache_loop`（每日 15:40）在 Render `_heavy()` 里、只读模式被关，而**日批任务清单漏了它** → 日志 `应用市场状态权重 2026-09-08 neutral`，评分权重/战法准入/trade_gate/主力乘数全用 3 天前的 regime。已新增 `task_market_regime`（`refresh_regime_cache()`，排在 backfill 之后、strategy_scan 之前）。任务总数 13 → **15**（+market_regime +rank_live）。
- **② LLM 全失败（需用户操作）**：`Invalid URL '/chat/completions': No scheme supplied` → GitHub secrets 里 **`LLM_BASE_URL` 为空**（workflow 已引用）→ 矛盾报告/日报都没有 AI 解读（报告本身仍生成，len 1658/3173）。需用户补 secret：`LLM_BASE_URL=https://api.siliconflow.cn/v1`、`LLM_MODEL=deepseek-ai/DeepSeek-R1`（值同 backend/.env）。
- **③ 包陈旧的连锁反应（今天已修）**：包 09-09 时，扫描侧按"今天"判过期 → **每一只都走实时拉取** → 日志满屏 `[WAF] K线请求被拦截` + `判过期走实时拉取` → 战法扫描耗时 57 分钟。修掉包陈旧 = 同时修掉 WAF 风暴。
- 其他正向信号：**模拟盘首次正常入池 `ingested: 10`**（时区修复见效，且主力过滤拦截可见：603217 高位 0.91 / 600150 高位+拉升段）；**板块快照恢复 996 条**（东财 delay 端点兜住，此前 written=0）；龙虎榜 547 行、矛盾 4 条、日报 3173 字、评分快照 50 条。
- 注意：`mainforce_state date=2026-09-09`（因包陈旧）→ 今晚才会追到 09-11；回测回填的巨型 `stock_missing` 列表是每日 150 只配额所致（属正常）。

## ✅ 主力分桶恒为空的两个原因（b1b5a1a 已推送 + 回填 36 行）
- **① 代码 bug**：`get_bucket_stats` 里 `if mf_key != "none": mf_buckets[mf_key][h].append(ret)` —— mf_key 默认就是 "none"，**"none" 桶永远装不进数据**。改为三桶无条件累加。
- **② 数据缺失**：`ranking_history` 850 行 `mainforce_signal` **全 NULL**。写入有两条路径：前端「保存快照」(`/snapshots/capture`，每天 15:10 自动保存) **漏写该字段**；日批 `task_score_snapshot` 有写，但 09-09/09-10 日批因 pack 陈旧（strategy_scan 抛"K线未就绪"）中断，没跑到 score_snapshot → 全是无标签版本。已给 capture 路径补 `mainforce_signal`。
- **一次性回填**：按 code+date 精确匹配 `mainforce_state`（只有 09-04/09-07/09-09 三天有数据）→ 补进 **36 行**（accum 26 / distribution 10）。之后 `mainforce_state` 里约 8% 的股票有标签 → 日批每天给 Top50 写约 4 行带标签 → distribution/accum 桶要 2-4 周才有统计意义（n≥20）。
- 验证口径：push 后 Render 自动部署 + `bucket_stats` 有 10 分钟缓存 → 稍等再看 `mainforce.none` 应有样本（814 行基数），accum/distribution 为小样本。

## ✅ B 方案落地：全量精算榜单（9baeef3 已推送）
- **新模块** `backend/app/scoring/live_ranking.py`：`_build_pool()`（复刻 score_top 的池子：PE>0 + 流通市值≥50亿 + 价≥3元 + 注入 flow5）→ `compute_full_ranking(limit=300, pool_cap=None)`（pack 预计算指标批量预载 + 财务批量 + 池内**全量**精算，无指标的直接跳过、绝不实时拉腾讯）→ `save()`（`ranking_live` 表，当日先删后 `db.upsert_many` 批量插）→ `load()`（5 分钟内存缓存）/`load_codes()`。
- **日批任务** `rank_live`（DEFAULT_ORDER 排在 mainline 之后、lhb 之前；依赖 mainforce_state 的 flow5 + 全市场行情缓存）；`daily-batch.yml` 走 `--tasks all` 无需改。
- **后端 `/batch/top`**：① 当日（北京时间）榜单已在库 → **直接服务**（零计算、`total` 取池子大小）② 否则（盘中/次日早盘）→ 走原两阶段，但把库内代码并入候选池（`_batch_with_precise_top(..., extra_codes=...)`）→ **简化分盲区消失**（002452 那种"精算高/简化低"的股票盘中也会被重算）。
- **顺带修**：`score_single`（详情接口）补 flow5 注入 —— 此前只有榜单路径注入，导致同一只股票的详情分与榜单分资金面因子不同（4 vs 5 因子）。
- **已验证**：建表/批量写/读回/清理实测通过（写入 2 行 → 读回字段完整 → 清理）；三个文件 py_compile ✅。**待 Actions 验证**：精算步骤（今晚日批日志 `[live_ranking] 精算池: … / 指标命中 … / 精算完成 …s` 与 `rank_live` 任务摘要）。

## 排程建议
- **排程建议（用户问"要不要延后 kline-data"）**：现状 cron = kline-data **18:00 北京**（UTC 10:00）、backend-pack **19:00**（UTC 11:00）。18:00 那轮腾讯复权数据对近半股票还没补齐 → 每天必然触发补拉（约 20 分钟 + 一轮额外请求，WAF 风险）。建议延后到 **19:30**（UTC 11:30），首轮即齐全、补拉基本不触发；唯一代价 = backend-pack 19:00 从 Pages 取到的 `realtime-quotes.json` 变成前一天（只影响包内名称/兜底市值，股票池已走 Supabase market_snapshot，影响极小）。**不要动 backend-pack 的时间**——它直接决定 daily-batch 的启动（2h15m），延后会推到 23:00 后才结束。也可先按现状跑一天，看补拉残留与耗时再定。


## ❓ "10 号早上的旧 k 线包是不是腾讯云干的"（结论：不是） + 腾讯云去留决策
- **三条独立成因**（别混为一谈）：
  1. **直接肇事者 = 本机（Windows）凌晨 00:04-00:10 的 orphan 重建发布**：重跑 `publish_packs_local.py` 恢复页面时，本地 `data/kline` 只剩 09-08 的包（`kline-pack-20260908.json.gz`），force push 把 gh-pages 上刚出炉的 09-09 新包**整批换成 09-08 旧包**。7a33cff 的提交说明写的就是这件事，10:24 加的第三道新鲜度护栏就是防它。
  2. **腾讯云 = 被动持有旧包，不是"操作"**：服务器上的 pack 是 09-09 晚手工 scp 上传的（服务器直连 github.io 仅 20kB/s），长驻进程旧代码 `_ready_checked` 置位后永不复查 → 一直供旧包；09-09 晚 21:42-21:58 容器还反复重启（DDL 洪水），每次装载的仍是同一旧包。
  3. **日批（09-10 20:34）**：从 Pages 首下拿到 CDN 缓存的 09-09 包 + 重试 bug 不重下（1ddfacf 已修）。
- **现场证据（09-11 实测）**：Render `/api/score/kline-cache/status` → `newest_update=20260909 / source=pack`；Pages 上是 `Thu, 10 Sep 2026 12:33:52 GMT` 的包（31.75MB，pack_date=20260910）→ **Render 未部署 1ddfacf**（进程早于 09-10 20:33 那次发布就起来了）。**行动：Render → Manual Deploy → Deploy latest commit**（既上昨日修复，又刷新包 → 前端/API 才不再吃 09-09 旧数据）。
- **腾讯云现状**：快速隧道 `camel-contributions-anyone-ancient.trycloudflare.com` **DNS 已失效**（不可达）→ 当前无任何外部入口 = 白付钱；前端已在 Render（`VITE_API_BASE_URL=https://stock-scoring-v2.onrender.com/api`）。
- **扶正腾讯云的两个前置**：① 固定 HTTPS 地址——国内地域（ap-guangzhou）80/443 需 ICP 备案，正解是「自有域名接 Cloudflare + named tunnel」（出站，不需备案；快速隧道重启即换域名，绝不能当生产）；② pack 通道——服务器直连 Pages 仅 20kB/s，池扩容后包 ~43MB，30 分钟自检会变成永远在下载 → 需镜像（ghfast.top 代理 raw.githubusercontent 的 gh-pages 文件）或 Supabase Storage 分发。
- **建议**：短期留 Render 跑（前端已指它、已验证）；腾讯云**降级为备用**，不要维持"半可用"状态（隧道换域名 → 前端连不上 → 用户被迫切回 Render，就是 09-10 的剧本）。真要扶正再一次性解决上面两个前置。
- 待验证（今天 18:00/19:00 Actions）：backend-pack 日志应打「战法池口径覆盖: 2052/2052（缺口 0）」→ 次日日批不再出现「判过期走实时拉取」/`[WAF]`，Supabase 流量应显著下降。
