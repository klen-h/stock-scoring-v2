# 2026-09-24

## 🧭 决策：缺口②（跨 tab 深链 / 选中态）**不做完整版**，只做「零成本通道路」

**用户问**：缺口②成本中等，建议做吗？

**核实后纠正了我自己上一轮的估计（上轮说「成本中等」= 低估）**：
1. `ScoreRank.vue:1152` `activeTab = ref('top')` = **纯进程内 ref、零 URL 联动** ⇒ 加深链要
   **新引入 `useRoute` + 双向同步**（现在只 import 了 `useRouter`，仅用于 push）；还会引入
   **新行为**（刷新后停留在某 tab），可能反而干扰。
2. **`switchTab` 带异步副作用链**：`switchTab('watch')` → `loadGateWatch().then(loadWatchPrices)`
   ⇒ 「定位到某行 + `scrollIntoView`」必须**等数据到位**才能做 ⇒ ★ **真正的复杂度在异步时序，
   不在 DOM**（加 `:id`/`:ref` + 高亮 class 本身只要几行）。
3. ★ **关键：「差什么」悬停就能看到** —— top 的 `3/3` 标签 `title` 里**已经**有 3 条判据的
   ✓/✗ + `hint` + 建议仓位 ⇒ 「切过去定位那只票」的边际收益只剩**省一次视觉搜索** ⚠️
4. **需求原文辨析**：缺口② 的定义是「缺 `?code=` 深链 + 选中高亮（**「在持仓/观察池中查看」
   小按钮**）」⇒ 括号里那个**小按钮才是真需求**；`?code=` 深链是我当初顺手写的**技术方案**，
   不是需求本身。
5. **URL 深链对单用户系统价值 ≈ 0**（不会把 `?tab=watch&code=002709` 发给别人，也不会存书签）。

**⇒ 结论：值「通道路」，不值「深链机制」。**

**已做（2 处，各 1 行，`06fd1e0` 之后）**：top 的「池」标签 + positions 的「池」标签 ⇒ 改成
`<a @click.stop="switchTab('watch')">`（**`.stop` 防冒泡到行的 `goDetail`**）+ `cursor-pointer`
+ hover 反馈。构建 ✓ 11.21s、lint 0。

**再往上做（切 tab + 定位 + 高亮，约 25 行）的触发条件**（现在都不成立 ⇒ 不做）：
① 观察池长到需要滚动查找（现在几十只、一屏内）；② 「差什么」变成必须跳过去才看得全。
**若将来要「从 Dashboard / 详情页深链回榜单」** ⇒ 那是**跨页导航**需求（另一个问题），
那时 URL 深链才有必要。

---

## 🔥 线上内存实测 ⇒ 找到真根因并修复（用户 2026-09-24 提供首次线上读数）

**用户贴的读数**（诊断端点 + `memory_watch` 趋势首次真实数据）：
```
RSS 366.7MB / 500MB（73.3%）｜峰值 365.8MB｜线程 10
已知缓存 359.88MB｜**未归类仅 6.8MB**
占用最多：strategies._PRICES_CACHE 234.09MB（808条）｜flow._FLOW_MAP_CACHE 107.55MB（1条）
         store._SNAP_CACHE 7.58MB（2条）｜tencent._cache 6.52MB（3条）
趋势（24 次，跨重启）：78.4 → 79.2 → 79.2 → 81.9 → 82 → 83.8 → 83.9 → 83.9%
```

**★★ 三个关键结论**：
1. **不是泄漏，是「两个大缓存的设计上限太高」** —— **未归类仅 6.8MB** ⇒ 几乎全部内存都在
   已知模块级缓存里 ⇒ **没有框架层泄漏、不是 RSS 高水位** ⇒ **修缓存 ＝ 修问题**。
2. **我上轮的判断错了**：我曾怀疑 `tencent.KLINE_CACHE`（本地 209 条/13MB）是头号 ——
   线上它**根本没进榜**（`tencent._cache` 仅 6.52MB）⚠️ **本地量级不能外推线上**。
3. **起点基线就 78%**（重启即高）⇒ 缓爬 +5.5pt 到 84%+ 再 OOM ⇒ 与监控图锯齿吻合。

**★ 根因：同类缓存「写了两遍、只修一遍」**
| | `backtest/data.py`（**正确样板**）| `backtest/strategies.py`（**线上炸的**）|
|---|---|---|
| 条数上限 | 200 ✓ | **无** |
| 取用时清过期 | 过期即 pop ✓ | **只当 miss，条目留着占内存** |
| 单条上限 | >900 根不缓存 ✓ | **无** |
| 淘汰 | 满 200 丢最旧 1/4 ✓ | **无** |

⇒ `strategies.py` 是 `data.py` 的**残缺版**。key 是 `(code, start)` ⇒ **同一只股票的不同信号日
各自缓存一整段 bars**（数据高度重叠）⇒ 只增不减；且 `_PRICES_TTL` 只是**逻辑过期**（命中判断
`now - ts <= TTL`），过期仍占内存 ⇒ 唯一释放时机 = 回填后的 `invalidate`（**每天一次**）
⇒ 形态就是「**每天从 0 涨到 ~808 条 = 234MB，回填时清空**」= 监控图的锯齿 ✓

**★ 修法（`backend/app/backtest/strategies.py`，对齐 data.py 成熟做法）**：
新增 `_PRICES_CACHE_MAX = 200` / `_PRICES_BAR_MAX = 900` + `_prices_cache_get()`（**过期即 pop**）
+ `_prices_cache_put()`（单条 >900 不缓存 + 满 200 丢最旧 1/4）；两处读、两处写全部改走新入口。
**语义零变化**（缓存只加速，miss 即回源 pack/DB）⇒ 最坏只是回测多花点时间（低频手动操作）。

**验证 10/10**（打桩，未触网）：条数上限 len=200｜单条 >900 不缓存、=900 仍缓存（边界）｜
过期取用返回 None **且条目被物理删除**｜新鲜命中返回**同一对象**｜淘汰丢最旧｜invalidate 清空。
**外推：234MB → ≤56MB（省 ~178MB）⇒ 366MB → 约 190MB（38%）** ✓

**⚠️ 剩余（107MB，本轮未动）**：`mainforce/flow._FLOW_MAP_CACHE` = **整表资金流的 Python 表示**
（8.4 万行 × 9 字段，单条 107MB）。服务路径**真的需要**（`mainforce/state.py:197` 全池主力状态、
`gate.py:152` 批量闸门）⇒ **不能简单删**；要优化得改数据结构（dict 列表 → 紧凑结构）或分层的
「近 N 天」小 map，属**中等风险改动**，留待评估（评分冻结期不轻动）。

**⚠️ 我上一条回复的错误**：「采样趋势/企微告警不是我写的」—— **错了**，`app/memory_watch.py`
（采样落库 `memory_probe` 表 + 30 分钟定时 + 80% 阈值企微告警每日一次 + 14 天保留 + 5 分钟去抖）
就是我在此前（summary 被省略的）轮次里做的。**报状态前先读代码，不凭 summary 印象。**

---

## 💾 Supabase 数据库体积：231.13MB / 500MB（46%）—— 能撑多久？

**用户贴的读数**：Supabase Free Plan `0.5 GB per project`，当前 **231.13 MB**。
**⇒ 剩余 268.87MB。估算可撑 4~7 个月（中位 ≈6 个月，到 2027 年 3~4 月）。**

### 估算依据（用代码注释里的**实测硬数字**校准，非凭空）
| 表 | 实测线索（代码注释）| 日增 |
|---|---|---|
| `backtest_prices` | **48.9 万行 / 82MB**（176 B/行）| ~544 行/日 ≈ 0.1MB |
| `mainflow_history` | **8.4 万行 / 21MB**（262 B/行）| ~544 行/日 ≈ 0.14MB |
| `mainforce_state` | chip_json ≈ **300B/行**（0.6MB / 2000 行）| **~2098 行/日 ≈ 0.6MB** ← 最大增长源 |
| `ranking_live` | payload 明细 | **~1000 行/日 ≈ 0.5MB** ← 次大 |
| `lhb_history` / `sector_daily` / `zz_daily_snapshots` / 报表类 / `strategy_results` | — | 合计 ~0.5MB |
| **合计** | | **≈ 2.0~2.5 MB/交易日 ⇒ 45~52 MB/月**（21 交易日）|

⇒ 268.87 ÷ 50 ≈ **5.4 个月**；若日增变 4MB（回填扩到全市场 / ranking_live 上调）⇒ **约 3 个月**。
⇒ **给区间 4~7 个月**更诚实（含加速风险）。

### ★★ 最重要的一点：这个故障比 Render OOM **更致命**
Supabase 免费档超 500MB ⇒ **项目变只读**（写入全部失败）⇒ **日批全挂**，
而且**不会自动恢复**（不像 Render OOM 会重启）⇒ **必须提前预警，不能靠感觉**。

### 无「保留期/清理」的大表（只增不减，按增长驱动排序）
`mainforce_state`（2098行/日）> `ranking_live`（1000行/日）> `backtest_prices` > `mainflow_history`
= `zz_daily_snapshots` = `lhb_history` = `sector_daily`，以及 `news_history`、`ranking_history`、
`macro_daily`、`market_regime_history`、`contradictions`、`contradiction_reports`、
`daily_reports`、`trader_briefs`、`industry_mainline`、`gate_snapshot_history`、`signal_history`、
`strategy_results` —— **全部无裁剪、无归档、无分区、无 VACUUM**。
已有清理的（**不受威胁**）：`flash_news`(300条)、`flash_analyses`(50)、`flash_reviews`(20/phase)、
`macro_history`(150)、`etf_close`(30天)、`push_log`(30天)、`llm_usage_*`(30天)、`memory_probe`(14天)、
`kline_cache`/`indicator_cache`/`market_snapshot`（主键覆盖式，总量固定）。

### ★ 231MB 里可能有相当部分是「膨胀」而非有效数据（可零风险回收）
`kline_cache` / `indicator_cache` / `market_snapshot` 都是**每天全量覆盖**（`ON CONFLICT DO UPDATE`）
⇒ PostgreSQL 每次 UPDATE 都产生 **dead tuple**，autovacuum 回收前仍占空间。
⚠️ 注意：**普通 `VACUUM` 不缩小文件**（只标记可复用）⇒ dashboard 的 231MB **只有 `VACUUM FULL`
（有表锁 + 需临时空间）才会降**。

### 诊断 SQL（给用户在 Supabase SQL Editor 跑，10 秒看到真相）
```sql
SELECT relname AS 表, pg_size_pretty(pg_total_relation_size(relid)) AS 总大小,
       pg_total_relation_size(relid) AS 字节, n_live_tup AS 活行, n_dead_tup AS 死行
FROM pg_stat_user_tables ORDER BY pg_total_relation_size(relid) DESC LIMIT 25;

SELECT relname AS 表, n_live_tup AS 活, n_dead_tup AS 死,
       round(n_dead_tup::numeric / GREATEST(n_live_tup,1), 2) AS 死行比
FROM pg_stat_user_tables WHERE n_dead_tup > 500 ORDER BY n_dead_tup DESC LIMIT 15;

SELECT pg_size_pretty(pg_database_size(current_database())) AS 全库;
```

### 处置优先级（先量后修）
1. 跑上面的 SQL ⇒ 拿到**真实分表体积**（我们的估算是推算，精度有限）
2. 若死行比高 ⇒ `VACUUM FULL` 回收膨胀（**不删任何数据**，零风险，但会短暂锁表）
3. 给**无争议表**加保留期（`ranking_live` 90 天、`sector_daily`/`news_history`/`lhb_history` 1~2 年、
   报表类按月保留）—— ⚠️ `mainforce_state`/`backtest_prices`/`mainflow_history` **动之前要评估**
   （回测/信号跟踪依赖长历史）
4. **结构性解法**：`backtest_prices`（最大单表 82MB）**外迁到 gh-pages 数据包**
   （`backend-pack.db` 的基础设施已存在，正是同一类思路）
5. 加「DB 用量」监控（像 `memory_watch` 那样落库 + 80% 告警）⇒ 避免无预警撞线

### ✅ 已实现：`GET /api/system/db-usage` + 首页常显（用户问「去哪里跑 SQL」后落地）
**动机**：用户问「去哪里跑 SQL」⇒ 说明"要他去外部面板找"本身是摩擦 ⇒ 直接做进首页，
以后不用跑 SQL。（Supabase 的路径：Dashboard → 左侧 **SQL Editor** → New query → Run。）
**实现**（`backend/app/routers/system.py` 新增，紧邻 `/memory`）：
- `_db_usage_impl()`：PG 走 `pg_database_size(current_database())`（总量）+ `pg_stat_user_tables`
  （**分表体积** `pg_total_relation_size` / `n_live_tup` / `n_dead_tup`），**按体积降序**；
  `<1KB` 的空表不入榜；`dead_ratio = dead/live`
- `_db_num()`：PG 的 bigint/numeric → float（前端与算术都要数字）
- **SQLite 降级**：无 `pg_stat_*` ⇒ 只报库文件大小（本地开发可判读）
- **60s 进程内缓存**（首页会频繁刷新，库体积以"天"为尺度 ⇒ 缓存无副作用）
- **fail-open**：权限不足/异常 ⇒ `available=false` + `error`，**不抛错**（看护类接口不反噬主流程）
- `summary` 人类可读 markdown；**≥100% ⇒ 🔴「只读、写入会失败、不会自动恢复」**；
  ≥80% ⇒ ⚠️ 清理建议
- `dead_note`：只统计**入榜的 top N** 并在文案里写明（不让人误以为是全库）
**前端**：`api.getDbUsage` + `Dashboard.vue` 在「进程内存」下方加「数据库」块
（用量/剩余/占用最多 Top4/死行提示，配色复用 `memColor`）⇒ **两个运维出口并列常显**。
**验证**：后端编译 ✓；**测试 22 项全过**（SQLite 降级不报错｜总量 231MB→46.2%→剩余 269MB｜
按体积降序｜<1KB 过滤｜dead_ratio 0.61｜60s 内命中缓存且**未再查库**｜104% 给只读+不会自愈｜
84% 给清理建议｜异常 fail-open）；前端 `vite build` ✓ 12.19s。
⚠️ 测试脚本自己踩了**项目已记录的坑**：GBK 控制台 `print` 含 `⇒` 抛 `UnicodeEncodeError`
（`memory_watch.py` 注释里警告过）⇒ 补测时 `$env:PYTHONIOENCODING="utf-8"` 解决。
**部署后**：首页能直接看到分表体积 ⇒ 才知道"该砍谁"（**先量后修**）。

### 🔬 真实分表数据到手（用户 2026-09-24 跑 SQL）—— 结论大改

| 表 | 实测 | 行数 | B/行 | 日增（估）| 性质 |
|---|---|---|---|---|---|
| **backtest_prices** | **100 MB（46%）** | 608,526 | 172 | 0.09MB | **存量最大、增速极低** |
| mainflow_history | 22 MB | 108,000 | 214 | 0.11MB | 同上 |
| **kline_cache** | 18 MB | 2,257 | 8.4KB | 覆盖式 | **全量重写 ⇒ dead tuple 源** |
| **stock_finance_zz** | 15 MB | 5,386 | **2.9KB** | 季度 | 上轮**完全漏了** |
| indicator_cache | 7.7 MB | 1,206 | 6.7KB | 覆盖式 | 同 kline_cache |
| mainforce_state | 7.5 MB | 23,039 | 333 | **0.67MB** ⚠️ | **仅 11 天 ⇒ 疑似新功能、无清理** |
| ranking_live | 6.9 MB | 7,600 | 932 | **0.89MB** ⚠️ | 仅 7.6 天；**有同日删（`DELETE ... WHERE rank_date`）但无跨日清理** |
| stock_finance | 5.2 MB | 18,663 | 290 | 季度 | |
| lhb_history | 4.5 MB | 7,941 | 589 | 0.06MB | ~79 天 |
| stock_finance_ocf_hist | 3.9 MB | 20,790 | 197 | 季度 | |
| sector_daily | 3.6 MB | 15,545 | 241 | 0.09MB | ~39 天 |
| 其余（stock_industry/signal_history/strategy_results/zz_daily_snapshots）| 各 0.6~1.4MB | | | | |

Top15 ≈ **200MB**（占全库 218MB 的 92%）。

**★★ 关键结构洞察：「大的不涨、涨的不大」**
- **存量层**（占 75%）：`backtest_prices` 100 + `mainflow_history` 22 + `kline_cache` 18 +
  `stock_finance_zz` 15 + `indicator_cache` 7.7 ≈ **163MB**，但**合计日增仅 ~0.5MB**
  ⇒ 它们**不威胁撞线，但占着空间** ⇒ 迁/裁可**一次性换 1~2 年**。
- **增量层**：`ranking_live` 0.89 + `mainforce_state` 0.67 + `zz_daily_snapshots` 0.15 +
  报表类 0.2 ≈ **~2.3MB/交易日（45~65MB/月）** ⇒ 269MB 剩余 ⇒ **4~6 个月**（比上轮 4~7 收紧）。

**⚠️ 上轮估算的三处修正**：
1. `backtest_prices` 我估 **82MB**（旧注释 48.9 万行）⇒ 实际 **100MB / 60.8 万行**（**仍在追加**）。
2. 我说 `mainforce_state`/`ranking_live` "无任何清理" ⇒ `ranking_live` **有**（`live_ranking.py:236`
   同日先删后插），但**只管当日、不管跨日** ⇒ 逐日在长。
3. **漏了 `stock_finance_zz`（15MB）**（zzshare 四表 JSON）与 `stock_finance`/`ocf_hist`（季度增长）。

**★★★ 反直觉陷阱（做保留期前必须知道）**：保留期**不是"立刻变小"，而是"先涨到上限再平"**！
- `mainforce_state` 现在 **11 天 = 7.5MB** ⇒ 若设 **90 天** ⇒ 会**先涨到 61MB**（净增 54MB）⚠️
- `ranking_live` 现在 **7.6 天 = 6.9MB** ⇒ 设 90 天 ⇒ 先涨到 **80MB** ⚠️
⇒ **保留期必须设"短"（14~30 天）**，否则设保留期的动作本身就是"涨 10 倍"。**先算上限，再设天数。**

**★ 已核实的两条关键事实**：
1. `backtest_prices` 的读取点**几乎全在 `scripts/`**（回测/研究脚本，如 `audit_backtest_data.py`、
   `regime_external_lead_test.py` 等，多为 `WHERE code=%s ORDER BY date ASC` **全历史**）；
   服务侧 `backtest/data.py::load_prices` 是「**优先 pack、未命中才回退 DB**」⇒ pack 是主、DB 兜底。
2. `generate_backend_pack.py` 的 `klines` 表**是自己拉腾讯 + 写 SQLite**（`BACKEND_KLINE_CAL_DAYS=1100`
   ⇒ ~750 根），注释写"与 DB backtest_prices 的深度对齐" ⇒ **两者是并行维护的两份**，但 DB
   **更深**（608,526 ÷ ~544 只 ≈ **1,119 根/只**）⇒ **pack 目前不能完全替代它** ⚠️。

**⇒ 处置方向（两种解法的组合）**：
- **止血（低风险）**：给增长快的表设**短**保留期（14~30 天）⇒ 斜线变平。
- **抬起点（收益大）**：`backtest_prices`（100MB，占 46%）裁到近 N 年 或 加深 pack 后迁走
  ⇒ 一次性换 1~2 年 —— ⚠️ **但会改变回测样本长度，必须先确认"回测要多久历史"**（用户决策）。

### ✅ 死行数据（用户 SQL）⇒ **排除"膨胀"这条路**
用户跑死行诊断，**只有两张表 >1000**：
- `backtest_prices`：活 608,568 / **死 70,769**（比 **0.116**）
- `mainflow_history`：活 108,000 / **死 7,018**（比 **0.065**）
⇒ ⇒ **`kline_cache`(18MB) / `indicator_cache`(7.7MB) / `market_snapshot` 都没进榜**
（死行 ≤1000）⇒ **我上轮"覆盖式写入会膨胀"的担忧不成立**，**autovacuum 工作正常** ✓
⇒ **`VACUUM FULL` 只值 ~13MB（占 218MB 的 6%）** ⇒ **不值得折腾**，方向排除。

### ✅ 已实现：`app/db_retention.py` + 日批 `retention` 任务（用户"继续"后落地）
**先核实（决定安全性）—— 两张表服务侧读取**全部只读「最新」**：
- `mainforce_state` 6 处：`state.py:287`（`DISTINCT ON (code) ... ORDER BY date DESC` 每只取最新）、
  `state.py:208`（当天断点续传）、`system.py:69`（只看 `MAX(date)`）、`scoring.py:1996`（只取 code/name）、
  `daily_report.py:425/787`（`WHERE date = MAX(date)`）。唯一读历史 = 体检脚本 `rhythm_profile.py`
  （统计 `COUNT(DISTINCT date)`，天数变小不影响判定）。
- `ranking_live` 2 处：`live_ranking.py:287,300`、`portfolio_radar.py:172,177` —— **都是先
  `MAX(rank_date)` 再按该日查** ⇒ 完全不读历史日期。
**实现**：默认 `RETENTION_DAYS=14`（**当前数据仅 7~11 天 ⇒ 首轮不删任何东西**，零风险，
可用环境变量覆盖）；★ **安全阀 `KEEP_MIN_DAYS=3`**（无论保留期怎么设，**至少留最近 3 个不同日期**
—— 防日批连挂时 `date < cutoff` 把表删空）；DELETE 双条件 `col < cutoff AND col < keep_from`；
失败逐表静默（不反噬日批）；排在**所有写入任务之后**（`DEFAULT_ORDER` 末尾）。
**预期效果**：`mainforce_state` 7.5MB→~9.5MB、`ranking_live` 6.9MB→~13MB ⇒ **净增 ~8MB 后不再增长**
（不处理的话一年后会到 ~250MB）。
**验证 15/15**（本地 SQLite 真实执行 SQL）：首轮零删除｜正常滚动删 32 留 8｜★**安全阀：5 个日期
全超期只删 2 个最旧日期、保住最近 3 个**｜仅 3 个日期时全保留｜空表跳过｜受管清单仅两张｜
默认值/安全阀常量正确。

**⚠️⚠️ 一个必须记住的事故与教训**：上一轮测试**误连了线上 PostgreSQL**（`.env` 里的
`DATABASE_URL` 生效）⇒ 我在**真实 Supabase 上建了 `_t_ret` 测试表并删了数据**（仅测试表，
未碰业务表；脚本末尾 `DROP TABLE` 成功，**已复查确认线上无残留** ✓）。意外收获：**PG 语法被
真实环境验证**（12/13 过，唯一 FAIL 是我期望值算错 —— 安全阀语义是"至少留 3 个日期"，
所以只有 3 个日期时应当全保留）。⇒ **以后测试脚本必须在 `import app.database` 之前**
设 `os.environ["DATABASE_URL"] = "sqlite:///backend/data/app.db"` 并**断言 `db._use_postgres is False`**。
（另：本地 Windows GBK 控制台 `print` 含 `⇒` 会抛 `UnicodeEncodeError` ⇒ 日志用 ASCII，
或临时 `$env:PYTHONIOENCODING="utf-8"`。）
