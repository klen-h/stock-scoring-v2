# 2026-09-16（周三，FOMC 决议前最后一个交易日）

## 🧭 主力节奏跟随框架 · 顶层设计定稿（项目北极星）
- **背景**：用户从「事件应对剧本」延伸，提出把「跟对主力节奏」作为整个系统的**底层座基**——跟对主力节奏事半功倍，以此统一所有模块。
- **产出 `主力节奏跟随框架_20260916.md`**（根目录，9 节）：一句话定位 / 为什么是这个座基 / 三层节奏模型 / 慢一步红线 / 数据底座诚实分层 / 决策流 / 现有模块归位 / 落地路径（增量不重构）/ 风险边界。
- **核心（三层节奏模型，可靠度递减）**：
  - ①**状态**（吸筹/拉升/出货/洗盘，`mainforce_state`，有回测背书 -7.5pt/+1.1pt）→ 可决策
  - ②**方向**（flow5 倒U/北向/两融持续趋势，IC 正但单日噪声大）→ 看连续趋势，作辅助
  - ③**时点**（哪天进撤，样本仅半年 4 次 FOMC）→ 仅形态参考，**永不进决策链**
- **红线（区分跟节奏 vs 追涨杀跌）**：跟「已确认的节奏」（慢一步），不抢跑「未发生的节奏」；盘中观察→盘后确认→次日执行。
- **关键认知**："主力"= 可观测代理指标，非机构意图（大单轧差口径、解释非事实、东财/新浪两源不可互校验）——评审1 第六节点破，已制度化。
- **现有模块归位（不重构）**：评分资金面=方向、主力标签=状态、白名单闸门=状态闸门、教练=纪律、风控=逆势止损、组合分=错杀识别、事件应对=切换点不慌乱。结论：状态/方向两层已覆盖，时点层**刻意空缺是正确的**。
- **落地路径**：立原则（新功能须回答"跟哪层节奏"）+ 回填标注 + 守住时点空缺（"盘中跟单/精准抄底"类默认拒绝，除非 ≥30 次回测背书）。
- **已入 MEMORY.md 长期记忆**（"主力节奏跟随框架"节，替代原"事件应对方法论"节）。
- **一句话原则（挂进 MEMORY）**：跟对主力节奏，但永远慢一步——识别状态、顺应方向、不猜时点。

## 承接昨日（09-15）的待办
- 事件应对剧本：Phase 1 复盘脚本已完成并提交（`event_playbook_backtest.py`，24 次 FOMC）；评审意见已消化（触发器 0 次触发、无对照组、n=6 过度解读、阈值 4bp 拍脑袋等），Phase 2 备案脚本**待按修订做**（触发器改用"指数企稳"、资金流降级形态参考、补对照组）。
- 评分 nb 市组合分排序：已上线+前端标注完成；ranking_live 存满 1000 行需今晚日批自动生效。
- backend-pack WAF 冷却：`PACK_WAF_COOLDOWN=45`（+ workflow_dispatch 输入）已配，今晚 19:00 触发看效果。

## 📐 主力节奏框架 · 开发计划 v1.0（融合两份文档，能直接开工）（2026-09-16）
- **产出 `主力节奏框架_开发计划_v1.0.md`**（根目录，10 节 + 2 附录）：融合「北极星（认知论）」+ 用户 v0.9（机制/验证设计），修掉五缺口。
- **三文档分工**：北极星=信什么（可靠度分层）、开发计划=怎么验证所信（机制+Gate）、事件剧本=框架实例。**接口契约：三层信息模型 + Gate 判定结果 + 参数表（校准来源非空）**。
- **★ 五个缺口（用户评审点破，全部修入）**：
  1. **证据类型错位（最重要）**：flow5 IC / distribution-7.5pt / accum+1.1pt 全是**横截面证据**（选股），而"跟节奏"座基是**时间序列命题**（市场级流入期该重仓）。显式拆成命题A（横截面，归选股）+命题B（时间序列，座基待验证核心，当前=4次FOMC+半年=约零证据）。Phase 0 只检验命题 B。
  2. **不对称现实·防守优先**：识别出货灵(-7.5pt)、识别吸筹不灵(+1.1pt)；流入是脉冲、流出是趋势 → 座基第一形态大概率是**防守型**（回避流出），进攻侧跟随挂 Gate B 通过后。
  3. **中间档补齐**：S0-S4 相位状态机 = 「状态×方向」组合编码器（只用慢变量，不违反时点死刑）。
  4. **Phase 0 补齐**：4.A episode 切分 / 4.B 内生性(B0白噪声/B2核心 R_t~E_{t-1}) / 4.C 滞后成本+对照组 / 4.D **宽度序列**（accum 占比+flow5 市场分位→市场级序列，样本半年→三年；第一步先确认 mainforce_state 能否用 backtest_prices+pack 重算三年）；「拉升/洗盘」列为叙事词汇不实现；「≥30次」改为 Phase 0 校准。
  5. **散点**：regime 归位（裁决顺序 教练熔断>regime天花板>事件加严>相位姿态>内容）；选股归 L4 内容维（错杀是交叉非归属）；grep 验收改依赖闭包静态检查；教练标「元层」。
- **本周动作**：①北极星文本修订(半天) ②09-17 附B手动规则照旧执行(成为第5样本) ③第一课=P0.D 数据可得性。
- **下一步开发 = 写 `scripts/rhythm_profile.py`，从 P0.D（mainforce_state 标签能否重算三年）+ P0.A（episode 切分）+ P0.B（B0 白噪声检验）起步**。

## 📊 Phase 0 首跑：rhythm_profile.py 落地与画像结果（2026-09-16，未提交）
- **产出 `scripts/rhythm_profile.py`**（~600 行，纯只读 + numpy 手写统计）：
  - 模块 `data-check`/`episode`/`causality`/`lag`，`--theta-scale`/`--json` 可选
  - 手写 OLS+Newey-West HAC / winsorize / ACF+Ljung-Box（无 scipy/statsmodels 依赖）
  - 口径：F_t=全池 SUM(main_net)（**库内单位=元，加载即 /1e8 转亿**——曾因元/小数 12 个数量级尺度差把 X'X 弄病态、t 值算崩，亿口径根治）；E_t 剥离 β 用 winsorize 回归、E 用原始值；C_t 燃烧 30 日；zigzag θ=θ_scale×σ(E)；d1=起点极值确认日（前向无前视）
- **P0.D 数据可得性答案**：mainflow_history **127 交易日**（03-16~09-15，715 只）→ A/B/C 立即可跑但 n 小；mainforce_state 标签**仅 6 天**（09-04~09-15）→ 宽度序列现有历史不可用，需重算器用 backtest_prices（3 年、714 只个股）回算筹码标签扩样。
- **★ Phase 0 画像核心发现（127 日，初判非正式 Gate）**：
  1. **B0：E 接近白噪声**（ACF1=-0.009，Q=5.4<11.07）→ episode 疑似随机切割，Gate A HOLD
  2. **B1：内生性极强**（指数+1%→当日流+245 亿，t=17.96，R²=0.784）
  3. **B2：E 对次日零信息**（每+100亿→次日 2.09bp，t=0.286；分组差 0.123%，t=0.53）→ **进攻侧跟随无证据**
  4. **B3：负影子**（指数+1%→次日 E -27.3 亿，t=-2.25，涨后兑现流出而非追涨）
  5. **对照组（最锋利）**：流入确认日 d1 后 5/10/20 日 **-0.8%/-1.5%/-2.2%**（比无条件差 -73/-151/-236bp）→ **确认流入再跟=系统性买在短期顶部**；流出确认日后 -0.25%/-1.28%/-1.94% → 回避有价值但与流入侧同为负，疑似共同源于**短期反转效应**（R_{t-1} 系数 -0.169，t=-2.33），防守特异性待宽度序列区分
  6. Gate 形式判：A HOLD（n=10 流入 episode、中位 4 日但白噪声）/ B 无信号 / C 形式 PASS（滞后占比 33%）但与对照组矛盾——**以对照组为准，进攻侧应 DOWNGRADE**
- **结论**：与"不对称现实·防守优先"预测一致——进攻侧零证据，防守侧初步证据但需特异性检验。**下一步 = P0.D 宽度序列重算器**（backtest_prices 三年重算 accum/distribution 标签→市场级宽度序列→样本 127 日→~730 日，唯一能把 n=10 变 n≈50+ 的路径）。

## ⚖️ 评审修订 + Gate 判定书 + 重算器预注册（2026-09-16，未提交）
- **用户评审要点（全部接受并落地）**：①「救样本」→「**换数据源重问同一个问题**」（防 favorable 设计偏倚，写进预注册）；②Gate A 的 HOLD 给多了——白噪声下"形式达标"是 null 的预言（随机游走 zigzag 天然产生那种统计）→ 补 bootstrap 裁决实验；③防守侧"共用反转效应"有内在矛盾（反转预测跌后反弹，实际 -1.94%）→ 补状态匹配回归；④重算器开工前三个必答（判据依赖/预注册三件套/成功失败标准先落盘）。
- **新增两个裁决模块**（`scripts/rhythm_profile.py` `--module noise-null` / `defense`，预注册判据写死在 docstring）：
  1. **Gate A null 模拟**（bootstrap ×1000，seed=42）：实际流入中位 4 日 vs 白噪声 95% 带 [2,7]，**p=0.473 → Gate A 正式 NO-GO**——episode 与随机切割不可区分，zigzag 节拍（开发计划 §4.A 主口径）在日尺度资金流上不成立。
  2. **防守侧状态匹配回归**（fwd20 ~ 流出确认哑变量 + 近20日收益 + 20日波动 + 20日新低，NW，n=87）：控制前 -1.94% → **控制后 -0.69%（t=-0.67）不显著** → 按预注册判据"防守层是价格状态的别名"（provisional：n=87 哑变量仅 9 个 1，功效极低，宽度序列扩样后同规格重估）。**教训固化：控制价格状态后表观效应塌缩 2/3 → 宽度重算器控制变量是生死项。**
- **产出 `主力节奏框架_Gate判定书与重算器预注册_20260916.md`**：
  - Gate A **NO-GO** / Gate B 进攻侧无信号（对照组 -236bp=确认流入是短期顶部选择器）/ Gate C 以对照组为准 / 防守侧别名（provisional）
  - **重算器预注册**：判据依赖调查（distribution 有纯价量弱口径可三年重算；**accum 依赖资金流无退化路径→三年只能重算价量代理=代理的代理，失败不反证原标签**）；自身验收=127 日重叠期与原标签一致率 ≥60%；主口径 W_t=accum占比−distribution占比（单占比/变化率只报不依）；null=block bootstrap 块长 20；前 2/3 探索后 1/3 确认；成功=控制价格状态后 top-bottom 未来 20 日差 ≥+100bp 且两段同向且 p<0.05；**失败条款=北极星 §2 时间序列半边正式 KILL，座基收缩为「横截面选股+价格状态防守」，重算器归档，不开第三条路**。
  - **三条净产出（待落地进教练预警）**：①确认流入=短期顶部选择器（-236bp）②短期反转（R_{t-1}=-0.169）昨日大涨不加仓/大跌不割肉 ③B3：涨的那天就是主力净卖的那天。
- **改动文件**：`scripts/rhythm_profile.py`（+175 行两模块）、`主力节奏框架_Gate判定书与重算器预注册_20260916.md`（新）。**未提交**。

## 🔔 三条净产出进教练预警（2026-09-16，未提交）
- **落地**：`backend/app/coach/rules.yaml` 新增 D 段三条市场级规则 + `rules.py` 三个求值器（数字由代码注入，符合红线）：
  1. `no_chase_rally`（大涨日不追，warn）：全市场等权涨幅 ≥1.5%（内存缓存推导，零网络）→ 文案带「确认流入=短期顶部选择器（-236bp）+ 涨日主力净卖 27 亿」
  2. `reversal_no_add`（昨日大涨今日不加仓，info）：沪深300 最近交易日涨幅 ≥1.5%（backtest_prices 最新两根，日期缓存）→ 文案带「R_(t-1)=-0.169」
  3. `reversal_no_panic`（昨日大跌不恐慌割肉，warn）：≤-1.5% → 「离场按剧本不情绪化追卖」
- **设计要点**：三条均 `push: false`（第一周仅 stop_loss_hit+hold_3d_review 推送的纪律不变），只落库攒样本；数据进 `build_context`（`idx_prev_move`/`market_avg_change_pct`）便于单测；`big_move_pct=1.5` 阈值标注「启发式起点，宽度序列扩样后校准」（参数表校准来源不空原则）。
- **验证**：`test_coach_rules.py` 38/38（+9 新场景，含缺失数据 fail-open 不误报）、`test_position_sizing.py` 16/16、`import app.main` 157 routes OK；真实 context 冒烟：昨日 -0.67% 不触发、市场缓存未就绪 fail-open 不误报。
- **改动文件**：`backend/app/coach/rules.py`、`backend/app/coach/rules.yaml`、`backend/test_coach_rules.py`。**未提交**。

## ⚖️ 判定书二审修订（2026-09-16，用户评审，全部接受，未提交）
- **用户点出三处「跑数时会背叛」的问题（全部成立并修）**：
  1. **附 B 动作列被判定书废止**：附 B 写于 09-15（跟随加仓）、Gate B 进攻侧 DOWNGRADE 落盘于 09-16 → 活冲突。修法：**信号照常判定落库、动作冻结为「观察，不加仓」**（判定书优先级 > 附 B），第 5 样本的价值在数据不在交易。
  2. **一致率 ≥60% 有基数率 bug**：稀有标签下原始一致率被「双方都无标签」格点主导（垃圾代理也能 ~81%）。修法：改为 sensitivity/precision/κ 条件化 + **accum 理论上限先算**（天花板=P(资金流入|低位筹码密集)，127 日可算；若天花板本身<阈值=阈值定错，会以工程失败错杀科学命题）。
  3. **#1 归因没过控制回归**：「确认流入=顶部选择器-236bp」是未控制的。**当场用已有 ref_fit 验证**：流入确认控制后 -1.08%（t=-1.14）不显著 → #1 与 #2 是同一现象（反转）两次表述，**#1 并入 #2**，净产出从三条收缩为两条半（反转纪律 + B3 注脚）。
- **另修三处**：① KILL 条款越权 → **三终局**（一致率不过=工程失败/暂不可检验可重开；主检验失败=真 KILL；通过=状态机 v2+前瞻期）② W 指标补**半边分解预注册**（accum-only/dist-only 分量，仅解释信号来源，非第二次通过机会，dist-only 可能是信息所在）③ 五条实现细节锁死（一致率用当前 overlay 重算原标签非存档/段内分位/推断对象=W 分位哑变量系数差 NW 滞后≥20 单侧/筹码燃烧期/regime 只描述不条件化）。
- **教练 no_chase_rally 文案改**：去未控制的 -236bp，改反转口径（R_(t-1)=-0.169）+ B3 注脚（涨日主力净卖 27 亿）。
- **制度沉淀（MEMORY.md 新增「工程方法论」节）**：形式达标是 null 的预言 / 代理前必查判据数据依赖 / 控制变量是生死项 / 稀有标签一致性指标基数率 bug / 工程失败≠科学证伪 / 未控制归因不可进结论 / 失败条款先落盘。
- **验证**：`py_compile` OK、`test_coach_rules.py` 38/38 通过。
- **改动文件**：`主力节奏框架_开发计划_v1.0.md`（附 B）、`主力节奏框架_Gate判定书与重算器预注册_20260916.md`（§5/§6/§7/§8）、`backend/app/coach/rules.py`、`MEMORY.md`。**未提交**。

## 🔍 「排行榜组合分消失」诊断 + 修复（2026-09-16）
- **用户报**：09-15 刚上线的 nb 市组合分排序，这几天在排行榜上看不到了。
- **实测线上（stock-scoring-v2.onrender.com）**：`GET /api/score/weights` → `regime_state="defensive"`（`regime_date=2026-09-15`，防御档权重 technical 0.12/quality 0.35）；`GET /api/score/batch/top?limit=10` → `rank_mode="total_score"`，但**每行 composite_score 仍有值**（002497 组合分 94.2 全场最高却排第 9）→ 实际排序键=total_score。（注：`limit` 参数下限 ge=10，传 8 会 422。）
- **根因（设计行为，非 bug）**：`_use_composite_rank()` 白名单只认 `state=="neutral_bearish"`；市况 09-15 从 nb 切到 `defensive`（`ma_trend=down` + 趋势强度≥moderate），nb 标签按设计作废（`market_regime.py:_apply_bearish_refine`「今日状态机已切档 → 尊重当日判定，不再保持 nb」）→ 开关关闭 → 前端 `rankMode!=='composite'` 不渲染 banner/组合分小字。
- **确认识别出的真 bug（已修）**：`routers/scoring.py` 并发防重分支（`_rank_result_cache["computing"]` 命中时）两个 return **漏 `rank_mode`** → 前端 `d.rank_mode` undefined → 回落 total_score → nb 市下组合分列在并发/自动刷新窗口「时有时无」（与用户推的长名分支 `fix-并发防重分支补上了漏掉的-rank_mode,...` 同一问题；该远程分支本机 fetch 多次失败，故直接在 main 等价修复）。
- **修法**：新增 `_rank_mode()`（`"composite" if _use_composite_rank() else "total_score"`），**4 处返回路径全部统一**（缓存命中 / 并发防重两个 return / 写缓存 / 正常返回）；缓存命中与防重的 fallback 由硬编码 `"total_score"` 改为 `entry.get("rank_mode") or _rank_mode()`（缓存缺字段时按当前市况实时推断）。
- **验证**：`py_compile` OK；`import app.main` 157 routes（与基线一致）；lint 0。
- **⚠️ 设计倒挂（待拍板，未动）**：nb（弱阴跌）用组合分，`defensive`（更强下跌）反而退出——因组合分只在 nb 快照回测过（IC +0.259 / 分离 +4.32），defensive 未验证，属保守设计。**建议不盲开**；若要开，先用 `scripts/rate_hike_weight_backtest.py` 口径扩到 defensive 快照验 IC/分离度，不达标就把「defensive 已测不达标」写进注释（防重复提问）。
- **改动文件**：`backend/app/routers/scoring.py`。**未提交**。

## ③ defensive 组合分回测：不可检验 + 分市况反直觉发现（2026-09-16）
- **结论一（硬）：defensive 组合分回测现在做不了——零样本**。三条硬约束：① `get_verified_records` 的 `min_age_days=2` 把 09-15/09-16 快照整体排除（cutoff=09-14）；② 固定 T+5 收益需 09-22 后才有；③ `backtest_prices` 覆盖在 09-14 后崩了（09-14 221 只 / 09-15 201 / 09-16 107，正常 ~700）。→ 属「暂不可检验」（工程性，可等样本积累后重开），非证伪。
- **结论二（反直觉，重要）**：给 `scripts/rate_hike_weight_backtest.py` 加 `--by-regime`（按 `market_regime_history` 分组）后，344 条样本（08-31~09-09）拆开：
  | 市况 | 样本 | 总分 IC / 分离 | 组合分 IC / 分离 |
  |---|---|---|---|
  | unknown | 165 | -0.069 / -2.86 | +0.231 / +4.60 |
  | neutral | 91 | +0.070 / +0.69 | **+0.237 / +2.68** |
  | neutral_bearish | 88 | -0.020 / +0.10 | **+0.030 / -0.10** |
  **组合分在它唯一被启用的 nb 组恰恰零信号，在 neutral 组反而最强**；全样本 +0.190 是混合撑起来的（典型 Simpson 混杂）→ **nb 白名单的正当性存疑**：原「nb 组合分 IC +0.259」实际是在混合市况样本上算的，却被标注为 nb。
- **⚠️ 严重局限（不可据此改白名单）**：每组只有 **2 个快照日**（nb=09-02/09-07，neutral=09-04/09-08）→ 名义 88/91 条实为 2 个日块，块内截面收益高度相关，**有效独立样本≈2**，IC 抽样误差极大。需各组 ≥5 个独立交易日（bootstrap/块内去相关）再重验。
- **顺带查出工程问题**：`market_regime_history` 仅 11 行，缺 8 个交易日（08-20/08-25/08-26/08-27/08-31/09-01/09-03/09-09）→ 165 条样本落 unknown 黑洞。日批 regime 任务没有每天落库，**待查**（否则分市况分析永远不完整）。
- **建议**：不开 defensive（零证据）；新增待办「nb 白名单正当性重验」（前置 = 补 regime 落库 + 每组样本够）；优先修 regime 落库缺失。
- **改动文件**：`scripts/rate_hike_weight_backtest.py`（+`--by-regime`，46 行）。**未提交**。

## 🔧 查 2：market_regime_history 缺日的根因与修复（2026-09-16）
- **现象**：`market_regime_history` 仅 11 行，缺 7 个交易日（08-25/26/27、08-31、09-01、09-03、09-09）→ 分市况分析里 165 条样本落 unknown。
- **先排除数据源**：`backtest_prices` 的 `sh000300` 日期**完整无缺**（08-13~09-16 每交易日都有）→ 不是数据缺口问题。
- **根因（调度逻辑缺陷，已实测坐实）**：
  1. `REGIME_CACHE_WINDOW=(940,1440)` → `regime_cache_loop` 最早 **15:40** 触发；而 `backtest_prices` 当日数据 **16:10 后**才回填。
  2. 15:40 时 `refresh_regime_cache` 取 `latest=load_regime_history()[-1]` = **昨天**；查 `market_regime_history WHERE date=昨天` → **有行** → 走「同日幂等跳过」分支并**返回昨日的 state（非空）**。实测确认：跑 `refresh_regime_cache()` 得 `date=2026-09-16, detail.idempotent_skip=True`（幂等跳过仍返回非空 state）。
  3. loop 的 `if cache and cache.get("state")` **误判为「数据就绪」** → `mark_schedule_done("regime_cache")` → **当天 loop 再也不会跑** → 当日行永远不会由 loop 写入。
  4. 于是当日行**完全依赖日批 `task_market_regime`**；而该任务 **09-11 才补进日批清单**（`daily_batch.py` 注释自述）→ 09-11 前缺日；有记录的那些天（08-24/08-28/09-02/09-04/09-07/09-08）是 loop 恰好在「当日数据到位后」才首次执行（后端当时休眠/晚醒）。
  - **本质**：代码违背了 loop 自己 docstring 的设计意图（「数据未就绪则不 mark_done，窗口内每 5 分钟重试」）——幂等分支返回非空 state 把守卫生生打穿。
- **修复（`scheduler.py:regime_cache_loop`）**：mark_done 条件加 **`cache.get("date") == now.strftime("%Y-%m-%d")`**——只有判定日期推进到当日才算成功；否则窗口内继续每 5 分钟重试（等回填），日志改为「市场状态未推进到当日（最新 X，今日 Y）」。
- **验证**：`py_compile` OK；`import app.main` 157 routes；lint 0。
- **未做的后续（待拍板）**：① 缺失 7 天可**补算**（sh000300 数据完整，按日期顺序重跑 `_apply_bearish_refine` 即可 upsert）→ 让 165 条 unknown 样本归位；② 可选把窗口起点 940→1005（16:45）减少空转（非必需，①修复已根治）。
- **改动文件**：`backend/app/flash/scheduler.py`。**未提交**。

## 🔧 补算 market_regime_history 缺失日（2026-09-16，已写库）
- **产出 `scripts/backfill_regime_history.py`**（默认 dry-run，`--apply` 才写；`--since` 可调起点）：自动发现缺失日 = 价格序列 date 集合 − 库中 date 集合。
- **自检（强验证）**：库中已有行「去 nb 化」vs 价格复算原始态 → **11/11 一致**，证明**原始态 100% 可复原**。
- **关键发现（决定了补算口径）**：库中 `bearish_refine_raw` 有 **7 行为 NULL**（08-24/08-28/09-02/09-04/09-07/09-08/09-10）——该列 **2026-09-13 才新增**。而 nb 的「连续 2 日确认」正是依赖该字段 → **09-13 之前的 two-day 链不可重建**；且 09-13 前的 nb 行本身是**单日口径**（两日确认尚未上线）。对已有行按两日确认复算只有 6/11 一致，差异全在 neutral↔nb（即此因）。
- **口径决策（保守）**：补算**只写原始态**（offensive/neutral/defensive，价格确定性可复原），**不做 nb 细化**。理由：`_nb_condition_raw` 的「上涨家数宽度」「外围恐慌」判据依赖**当前时点**实时数据、历史不可复原；且 prev raw 链断裂。因 **nb 与 neutral 权重/战法准入完全相同**（差异仅状态标签/仓位警示），保守标 neutral 不误导决策。（`--refine` 保留作对照，但结果不可信）
- **执行**：补 9 天（08-20/08-21、08-25/26/27、08-31、09-01、09-03、09-09），原始态**全部是 `neutral` + `ma_trend=down`**（adx 14.8~17.6 弱趋势）→ `market_regime_history` 11 行 → **20 行**。
- **效果（重跑 `--by-regime`）**：`unknown` **165 → 0**，分组收敛为两组：
  | 市况 | 样本 | 总分 IC / 分离 | 组合分 IC / 分离 |
  |---|---|---|---|
  | neutral | 256 | +0.020 / -1.25 | **+0.246 / +3.96** |
  | neutral_bearish | 88 | -0.020 / +0.10 | **+0.030 / -0.10** |
  → **结论不变且样本更干净**：组合分在它唯一被启用的 nb 组仍为零信号，在 neutral 组 IC +0.246 / 分离 +3.96。
- **⚠️ 残留局限（必须记住）**：① 补算的 4 个样本日（08-31/09-01/09-03/09-09）标 neutral 是**保守选择**——若当时实际为 nb，nb 组会变 6 天，结论可能不同；② 仍是「日块」结构（neutral 6 天 / nb 2 天），有效独立样本小；③ 口径真正可靠的样本（09-10 起 raw 完整）T+5 未到（09-10 的 T+5 = 09-17）→ **只能等**。
- **改动文件**：`scripts/backfill_regime_history.py`（新增）；库 `market_regime_history` +9 行。**未提交**。

## 🔍 查 4：backtest_prices 09-14 后只回填 ~200 只的根因（2026-09-16）
- **现象**：`backtest_prices` 09-14 为 221 只 / 09-15 为 201 / 09-16 为 107（正常 ~706）。→ **09-09 快照的 T+5（目标日 09-16）覆盖仅 25/50**。
- **排除两个"貌似故障"**：
  1. `DAILY_STOCK_QUOTA = 150`（`backfill_history.py:27`）是**设计**——防数据源限流打断长任务，分片推进（注释明说）。
  2. `schedule_state.backtest_backfill` 停在 **2026/9/8** 也**不是故障**——`backtest_prices_refresh_loop` 在 `scheduler.py:1697` 被 `*_heavy(...)` 包裹，`RENDER_READ_ONLY=1` 下**预期关闭**（与 mainflow/score_snapshot/strategy_scan/market_snapshot/calendar 等 20+ 重任务一起停；保留在 Render 的只有 regime_cache/快讯/复盘/日报/教练/模拟盘 等轻任务，与 schedule_state 两组时间戳完全吻合）。
- **真正根因（容量临界）**：回填**只剩 Actions 日批一天一次**（`task_backfill` → `backfill_daily()`，quota=150）。而 `_collect_rank_codes(7)` 近 7 天上榜股去重 = **157 只 > 配额 150** → 插队机制占满全部配额仍不够，普通股（~550 只）几乎补不到。实测：09-14/15 上榜股覆盖 153/157 ≈ 97%（勉强够）；**09-16 仅 83/157 ≈ 53%**（日批当日数据源限流或未完成；23:10 复查未再增长）。
- **影响面**：所有需要「未来收盘价」的功能——快照 T+N 收益、`bucket-stats`、模拟盘结算/绩效归因、教练 T+5 结果回填、组合分前瞻统计、`--by-regime` 样本。
- **建议（推荐 A）**：
  - **A**：Actions 日批 `task_backfill` 改调 **`backfill_lagging()`（quota=None 全量追平）**——全池 ~700 只 × 1s ≈ **12 分钟**（09-13 已用 499 只/10 分钟验证过），日批总时长 74→86min，离 Actions 180min 上限仍有大余量。
  - **B**：`DAILY_STOCK_QUOTA` 150 → 300，留余量（保留分片）。
  - **C**：把回填移回 Render 常驻——**不推荐**（当初就是为防 512MB OOM 崩溃循环才关的）。
- **未完成的验证**：09-16 仅 83 只是「日批失败」还是「数据源限流」未定（需 Actions 日志）。

## 🔧 修 4：日批回填改不限量 + 手动追平缺口（2026-09-16）
- **改动**：`scripts/daily_batch.py:task_backfill` 由 `backfill_daily()`（quota=150）改为 **`backfill_lagging()`（quota=None）**，理由见上（157 上榜股 > 150 配额，插队占满仍不够）。`backfill_daily(quota=None)` 会**跳过已同步到基准的股票** → 无积压时零开销、仅积压时一次性追平（幂等，可重复跑）。
- **手动追平**：本地跑 `backfill_daily(quota=200)`（上榜股优先），写入 **6008 行**个股。
- **效果（实测）**：
  | 日期 | 追平前 | 追平后 | 上榜股覆盖 |
  |---|---|---|---|
  | 09-14 | 221 | **350** | **157/157 = 100%** |
  | 09-15 | 201 | **328** | **157/157 = 100%** |
  | 09-16 | 107 | **267** | **153/157 = 97%** |
  | **09-09 快照 T+5 覆盖** | **25/50** | **48/50（96%）** | — |
- **研究侧效果**：`--by-regime` 样本 **344 → 369**；分组结论**不变**：
  | 市况 | 样本 | 总分 IC / 分离 | 组合分 IC / 分离 |
  |---|---|---|---|
  | neutral | 279 | -0.018 / -1.46 | **+0.245 / +4.15** |
  | neutral_bearish | 90 | +0.027 / +0.29 | **+0.023 / -0.12** |
- **遗留**：`stock_missing` 仍 **425 只**（本轮只补 200 只）→ **明天日批（已改 lagging）自动补完**；09-16 另有 4 只上榜股仍缺（停牌/数据源问题）。
- **★ 生效条件（重要）**：本轮所有改动**只在本地**，需 push 后才会生效——`scheduler.py`/`scoring.py` 要 Render 重新部署（`autoDeploy: yes`），`daily_batch.py` 要 Actions 下次跑。
- **改动文件**：`scripts/daily_batch.py`；库 `backtest_prices` +6008 行。**未提交**。

## 🐛 重大发现：筹码引擎换手率单位 bug（volume=手 vs float_shares=股）（2026-09-16）
- **起因**：用户拿 002452 前端「主力行为」与同花顺对比，数值差异大（获利盘 82% vs 67.80%、集中度 25% vs 10.09%）且 5 日资金流方向相反。
- **★ 我先犯的两个查询错误（教训，必须记住）**：
  1. `db.fetch_one("SELECT * FROM mainforce_state WHERE code=...")` **没加 ORDER BY** → 拿到 09-04 那行，**误判「该股停更 8 天」**。实际 09-16 行存在。（`fetch_one` 不保证返回最新行；**取最新必须 `DISTINCT ON`/`ORDER BY date DESC`**——`load_latest` 本身是对的。）
  2. 由 (1) 又误推「002452 不在数据包里」—— 实际 `pack_source.get_codes()` **含 002452**；当时 `get_klines` 返回空，是**本地 pack 陈旧（09-14）触发的「回退查 Supabase」**所致，不代表生产包。
- **数据链路（回答「怎么获取的」）**：新浪 MoneyFlow（`r0~r3` 分单净额）→ `mainflow_history`（日批 17:00）；K线（pack）→ `chips.chip_metrics`（CYQ 轮动筹码模型）→ `mainforce_overlay` 合成 → `mainforce_state`（日批 17:30）→ 前端。`flow5_amt` = **Σ(近5日 `main_pct`)**（占成交额%累加，**不是金额**）——前端显示成 `+23.91%` 容易误解。
- **★ 真 bug（对照实验确证）**：`chips.py:105` `tr = vol / float_shares`，而 **`volume` 单位=「手」、`float_shares`=「股」→ 换手率低估 100 倍** → `chips *= (1-tr)` 每天只衰减约 0.05%（真实约 3.8%）→ 260 根累计衰减仅约 6%，**老筹码几乎不换手**。
  - 单位交叉验证：`129710 手 × 11.81 元 ≈ 1.53 亿` ←→ mainflow_history 反算成交额 1.52 亿吻合 → volume 确为「手」。
  - **对照实验（002452，260 根）**：
    | 口径 | tr 中位 | 获利盘 | 集中度 | price_pos |
    |---|---|---|---|---|
    | A 现状 `vol/shares` | 0.0500% | **82.00%** | **25.40%** | **84.8%** |
    | B 修正 `vol*100/shares` | 3.77% | **65.25%** | **10.43%** | **55.5%** |
    | 同花顺参考 | — | 67.80% | 10.09% | — |
    → **A 精确复现系统当前值、B 与同花顺几乎重合** = bug 确证。
- **影响面（大）**：① `price_pos` 84.8%→55.5% 会**改变信号触发**：`distribution`（需 price_pos>0.75）大幅减少、`accumulation`（需 price_pos<0.35 且 concentration<0.25）增加；② **`mainforce_factor_backtest.py`（10744 样本「高位高获利×主力流出 5日 -4.3pt」）的回测背书建立在错误筹码上 → 结论作废，必须重跑**；③ 全池 `mainforce_state` 需重算；④ `MAINFORCE_MODE=auto` 的 ×0.85 降权依据随之失效（按「未验证不进决策链」应先关）。
- **修复**：`chips.py` `tr = vol * 100.0 / float_shares`（1 行）。**待用户拍板**（牵涉策略松紧与回测重跑）。
- **临时脚本** `scripts/_verify_chip_units.py`（验证用，已删）。

## 🔧 筹码单位 bug 修复 + 回测重跑（2026-09-16，用户批准执行）
- **修复**：`chips.py` → `tr = vol * 100.0 / float_shares`（1 行，含单位说明注释）。
- **重跑回测**（`mainforce_factor_backtest.py --hold 5 10 --step 5`，6906 样本 / 10 截面日）→ 报告 `backend/backtest_reports/mainforce_20260916_2338.md`。**对照（修复前 10744 样本，2026-09-05）**：
  | 指标 | 修复前 | 修复后 |
  |---|---|---|
  | winner_ratio IC(5日) | -0.170 | **-0.196** |
  | price_pos IC(5日) | -0.178 | **-0.204** |
  | cost_bias IC(5日) | -0.168 | **-0.204** |
  | concentration IC(5日) | -0.056 | **+0.050（符号翻转）** |
  | combo_accum 5日差 | 1086条 / +0.687pt | 1748条 / **+0.241pt** |
  | combo_distrib 5日差 | 440条 / **-4.272pt** | 107条 / **-1.625pt** |
  | phase=distribution 5日去超额 | -8.703（48条） | **-10.254（32条）** |
  → **核心筹码因子方向不变甚至更强**；两个组合信号的原数字**虚高**（修复后被挤掉一半以上）；`concentration` 是**弱因子（IC 会反号）不宜作阈值条件**；不依赖筹码的 `phase='distribution'` 依然最锋利。
- **更新**：`overlay.py` docstring 的回测背书改写为修复后数字，并注明旧数字作废。
- **★ 两条我建议「不做」的（与先前口头计划的偏差，理由充分）**：
  1. **不关 `MAINFORCE_MODE`**：P1-12 方案 A 后 `mult`（×0.85）**已不动分**（只作展示字段），关它≈空操作；且要跨环境改（Actions + Render 控制台 + `env_check` 期望值），成本高收益近零。**真正影响下游的是 `signal`**（`confluence.py:218-219` 个股层 `accum +2 / distribution -2`），关 MAINFORCE_MODE 管不到。
  2. **不在本地重算 `mainforce_state`**：本地 pack 陈旧（09-14），`DATA_SOURCE=db` 又只能覆盖 746 只，而生产 09-16 有 2028 只 → **会造成口径混杂（部分新 / 部分旧），比不重算更糟**。正确路径 = push 后由明天 Actions 日批用最新 pack 全池重写（写 09-17）；09-15/09-16 历史行保持旧口径一天。
- **修复的真实影响面**：`signal` 分布改变（accum 命中率 10%→25%、distribution 440→107）→ **confluence 个股层 ±2 会随之变化**；排序不受影响（mult 不动分）。
- **待决策**：`overlay.low_pos_dense` 的 `concentration < 0.25` 阈值——修复后该因子 IC 反号（+0.05），是否移出条件？
- **验证**：py_compile OK、`import app.main` 157 routes、lint 0。
- **改动文件**：`backend/app/mainforce/chips.py`、`backend/app/mainforce/overlay.py`。**已提交**。

## ✅ 提交与推送（2026-09-16）
- **用户已自行提交并推送** `68c348c fix(评分/调度/回填)`（含今日前三项：scoring `_rank_mode` / scheduler regime 守卫 / daily_batch 回填不限量 + `backfill_regime_history.py` + `rate_hike_weight_backtest --by-regime`）。
- **本次补提交并推送 2 个**：`ec0696e fix(mainforce): 筹码换手率单位` + `e98e8cd chore: 回测报告与工作记忆`。`68c348c..e98e8cd main -> main` ✓，工作区与 origin 同步。
- **★ 生效时间线（重要，避免误判"改了没变"）**：
  - **Render**（`branch: main`, `autoDeploy: yes`）→ push 后自动重新部署，后端代码（筹码修复）**即时生效**；
  - **前端** deploy-preview 同 push 重建 gh-pages；
  - **`mainforce_state` 数据仍是旧口径**——前端 002452 的筹码数值**要等明天 Actions 日批**用最新 pack 全池重写（写 09-17 的行）才会变；09-15/09-16 历史行保持旧口径。
  - Actions 明天日批同时使用新版 `daily_batch`（回填不限量，自动补完本轮剩余 425 只）。
  - ★ **修正**：用户澄清 **Render 已改为手动部署**（非 autoDeploy）→ 上一条"push 即生效"的判断作废；后端筹码修复**等用户手动部署**才生效（前端 deploy-preview 仍是 push 触发）。

## 🔧 本地环境：拉包 + 可立即重算 mainforce_state（2026-09-16 深夜）
- 用户要在本地启动 → 本地 `.env`：`DATA_SOURCE=local` + `ENABLE_HEAVY_JOBS=0`；旧包 `pack_date=20260914`（会回退 Supabase、egress 暴涨）→ **已拉新包**：`python scripts/sync_local.py --force`，44.3MB / 204s → 解压 179.1MB，`pack_date=20260916`、`is_stale=False`、`n_codes=2040`。
- **副产品**：新包覆盖 002452（751 根，至 09-16）—— 此前 `get_klines('002452')` 返回空，**确证是「包陈旧 → `_is_stale()` 直接 `return None`」**，与"不在包内"无关。
- **★ 关键机会**：包已是 09-16 → 本地跑 `python -m app.mainforce.state`（`DATA_SOURCE=local`）会用**新筹码口径**重算**全池 2040 只**、覆盖 09-16 的行（与生产同一天同覆盖 → **不混杂**）→ 可**立即**验证筹码修复，无需等明天日批。耗时约 2-4 分钟（外加一次 mainflow_history 全表读）。
- **启动**：`cd backend; python run.py`（**不要 `--reload`**）。注意本地 `READ_ONLY=False`，且现在 23:xx 仍处于多个盘后窗口（回填 16:10-23:59、资金流 17:00-23:59）内，而 `schedule_state` 里这些任务的日期是 09-07/09-08（旧）→ **启动会自动跑一遍 heavy 回填/资金流等**；只想看页面则先 `$env:RENDER_READ_ONLY='1'`。

## ✅ 本地重算 mainforce_state 生效 + 本地看页面配置（2026-09-17 凌晨）
- 用户自行跑完 `python -m app.mainforce.state`（本地 pack）：`{"codes": 2028, "saved": 2028, "date": "2026-09-16", "seconds": 377.9}` —— 与服务端 09-16 的 2028 只**同天同覆盖**，**无口径混杂** ✓（本地 pack 2040 个 code 中 2028 只满足 `len(b)>=120`）。
- **验证 002452（新口径已落库）**：09-16 行 `winner_ratio 0.6525 / concentration 0.1043 / price_pos 0.5548 / cost_bias 1.88`（旧口径为 0.82 / 0.254 / 0.848 / 14.6）→ 与对照实验预测**完全一致**（65.25% / 10.43% / 55.5%）✓；09-15 行仍是旧口径（本轮只重算了 09-16）。
- **本地看页面配置**：`frontend/vite.config.js` → port **3000**、`base=/stock-scoring-v2/`、proxy `/api → http://localhost:8000`；`backend/run.py` → `uvicorn ... port=8000, reload=True`（开发脚本；只监视 `.py`，写 `.db/.gz` 不触发）。
- **启动**：终端1 `cd backend; python run.py`；终端2 `cd frontend; npm run dev`；浏览器 `http://localhost:3000/stock-scoring-v2/`。00:xx 启动**不会**触发盘后重任务（16:10-23:59 等窗口均已过），无需 `RENDER_READ_ONLY`。

## ✨ 新增「5日主力净流入金额」字段（2026-09-17，方案2）
- **动机**：`flow5_amt` 是「5 日净占比累加（%·日）」，前端硬拼 `%` 易误读，且与行情软件的"净流入金额"**单位都不同**→ 追加**金额**字段作直观对照。
- **两口径并存、不可互换**：
  - `flow5_amt` = Σ(每日 `main_net` ÷ 当日成交额 × 100) —— 5 日**占额比例累加**；保留"持续性"信息；是评分因子 `scoreFlow5` 倒U锚点的输入（**改它必须重标定锚点**）；
  - `flow5_amt_yuan`（新）= Σ(每日 `main_net`) —— 5 日**净流入金额（元）**，仅展示用。
  - ⚠️ 因分母不同（比率 vs 绝对额），**两者可能异号**（实测 002452：09-15 `flow5_amt=+0.52` 而 `flow5_amt_yuan=-43.05 万`；09-16 同向 +23.91 / +3268 万）。这是口径固有差异，非 bug。
- **改动**：`overlay.py`（计算 + 返回）、`state.py`（PG/SQLite 建表加列 + 幂等 ALTER 迁移 + `_save` + `load_latest`）、`routers/scoring.py`（榜单标注透传）；前端 `StockDetail.vue`（主力卡片改为**主显金额、副显占额累计**；新增 `formatSignedAmt` 处理负数）、`ScoreRank.vue`（tooltip 去掉误导性 `%`）。详情页返回完整 dict，自动带上。
- **顺带修正**：`StockDetail.vue` 底部硬编码的旧回测数字 `-7.5pt / +1.1pt` → `-3.8pt / +0.7pt`（筹码单位修复后的重测值）。
- **迁移与补数**：`ensure_table()` 幂等 ALTER 已加列（`has_col=1`）；用一条 `UPDATE ... FROM (SUM(...) OVER (ROWS BETWEEN 4 PRECEDING AND CURRENT ROW))` 从 `mainflow_history` 回填**全部历史行**（4426/12678 行有对应数据），**无需重算筹码**。验证 002452：`flow5_amt=23.91`、`flow5_amt_yuan=+3268.09 万` ✓。
- **★ 遗留认知**：换成金额后**与同花顺方向仍相反**（我们 +3268 万 vs 同花顺 -1205 万）→ 这是**数据源口径差异**（我们=新浪 `r0_net+r1_net` 大单/超大单轧差；同花顺=自有算法），**不可互校验**，这条已写进项目认知（北极星「主力=可观测代理」）。
- **验证**：后端 py_compile + `load_latest` `keys_ok=True`；前端 `npm run build` ✓ 12.87s；lint 0。
- **改动文件**：`backend/app/mainforce/overlay.py`、`backend/app/mainforce/state.py`、`backend/app/routers/scoring.py`、`frontend/src/views/StockDetail.vue`、`frontend/src/views/ScoreRank.vue`。**未提交**。
