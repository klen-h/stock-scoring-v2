# 2026-09-07

## news_score 入包（轻量版，已完成）
- 决策：做。理由——成本近乎为零（1 次东财快讯请求 + 只存非 0 分 ≈ 3KB）、失败静默跳过不影响其它字段、可消除「本地模式该列永远空着」的不一致；代价是时效滞后（存的是打包时刻=收盘后快照）。
- 实现：`generate_backend_pack.py` 新增 `compute_news_scores(codes)`，与后端 `_batch_news_scores` 同口径（`get_global_news()` 1 次请求 → 按 `stocks` 分组 → `score_stock_news()` 带 decay_weight 时间衰减）；**只存非 0 的**（大多数股票无快讯，存 0 纯浪费；前端 `item.news_score != null` 判断，未存显示 '-'）。写入 `ind["news_score"]`（数值，与后端 item.news_score 形状一致）。
- 前端：`preciseScoreBatch` 与主线程降级两条路径都收集 `newsMap` → 新增 `attachNewsScore(results, newsMap)` 挂载。仅排行榜消费，详情页无此列。
- 语义已写进代码注释：这是「打包时刻（收盘后）」快照，快讯 24h 滚动 + 时间衰减，本地模式不会实时刷新；不参与综合评分。
- 验证：py_compile + AST OK、前端 lint 0 错误、`pnpm build` ✅。

## 当前 indicators-pack 三个新字段（均为打包时就地算，前端零现算）
| 字段 | 来源 | 依赖 | 大小 |
|---|---|---|---|
| `ind.mainforce` | `mainforce_overlay(bars, flow_rows, float_shares)` | K线 + mainflow_history(近10日) + quotes.float_cap | ~120B/只 |
| `ind.trend_health` | `ScoreEngine._calc_trend_health(_series)` | 纯指标，零外部 | ~300B/只 |
| `ind.news_score` | `score_stock_news(快讯)` | 东财 1 次请求 | 仅非 0 的几 KB |

## 待用户执行
- push（含 backend-pack.yml 新 workflow + kline-data.yml 拆分 + 前后端消费改动）→ 18:00 后手动 dispatch `backend-pack`（workers=5）→ 看日志：K线成功率 / 「趋势健康度 N 只」/「主力行为 N 只（含资金流强口径 z 只）」/「消息面: 快讯 X 条 → 非 0 分覆盖 Y 只」与总时长（目标 <30 分钟）。勿盘中触发。
